# P8-S6.11.COMPOSITE — a finite-parameter physical TT source-response screen

This child pins and replays S6.10, certificate SHA
`bfb0b74a2d50a508a5e5fc346b59a31958c9f9a8ccfad621d4caa34883e90eef`.
Ancestors remain immutable. The physical metric is the composite metric,
not the original CD/M1 metric. The internal matter is the shared canonical
scalar with the locally reconstructed potential of the pinned family.

## Fixed model and experiment

The constants are `G=F=M²>0`, `m4=M²m²`, `m>0`, `alpha=beta=1`, and
`beta_n=(0,0,1,0,0)`. Use the positive square root and positive lapses in
the repository's `+---` convention. Physical composite proper time is
`T`; `u=mT`. Set `Ae(0)=1`, `epsilon=1/Y>0`, `y(0)=Y`,
`rhobar(0)=9/Y²`, and the exact CD scale
`Ae=(1+11epsilon*u²/4)²`. The time interval is exactly `0<=u<=1`.
The comoving spatial band is `kbar=kcom/m in [1/16,1/8]`, so physical
effective momentum is `m*kbar/Ae(u)`. No temporal bandlimit is asserted.

Apply a separately background-conserved, external physical TT probe with
one real normalized polarization `eij eij=1` and pulse
`p(u)=u²(1-u)²`, zero outside the interval. This probe is not a TT
perturbation generated by the internal homogeneous canonical scalar.
The convention is
`delta Sprobe=(1/2) integral sqrt|g_eff| T^{ij} delta g_eff,ij`,
`T^{ij}=Pi eij/Ae²`, `T^{0mu}=0`, and
`delta g_eff,ij=-Ae² Gamma eij`. Define the literal u-action source
`J_u=m M² epsilon sigma p/4`. Equivalently `Jbar=J_u/m` is the source in
the physical action divided by `m`, and
`Pi=-2m J_u/Ae³=-M²m² epsilon sigma p/(2Ae³)`.
The sign defines this polarized probe; an energy condition on the
external anisotropic polarization is neither needed nor asserted.

Both metric tensor fields and their canonical fluxes have zero retarded
initial data. Compare the full two-tensor response with the exact
locked-field candidate obtained by restricting `h_g=h_f` in the action,
with the identical physical source and zero initial data. The compared
observable is `R=Gamma/(epsilon sigma)`; use `sigma>0` when taking this
ratio. The linear response operator is independent of sigma.

## Certified bounded result

For every `0<epsilon<=2^(-187116)` and every momentum in the band, the
pinned background exists regularly on `[0,1]`, its canonical scalar
null source is positive, and the full and locked flux problems exist
uniquely. Their normalized physical outputs differ from their respective
limits by at most `1/600`, uniformly on the interval and band. At `u=1`,

`R_epsilon-L_epsilon >= 1/75`,

`R_epsilon>0`, `L_epsilon>0`, and

`(R_epsilon-L_epsilon)/R_epsilon >= 1/3`.

These are sufficient, deliberately unoptimized ranges. They are exact
rational gates represented by binary exponents, not floating-point
integration results. With `0<=sigma<=2^(-67162)`, both individual metric
polarization amplitudes are at most `1/128<1/100` on this interval.
Absolute physical source and composite response vanish as
`epsilon*sigma`; the source-normalized discrepancy does not.

A separate positive control prepares zero-source common-metric data
with common normalized value and u-velocity bounded by one, and zero
relative canonical data. The full and locked physical outputs then
agree within `1/600` under the same parameter gate. This control concerns
prepared data, not arbitrary physical probes or arbitrary heavy data.

## Strict scope

The theorem rejects this locked-action prediction for this specified
physical conserved source, data, band, window, and relative-error
contract. It does not reject every second-order EFT or every
nonadiabatic reduction. A different source map, local/nonlocal operator,
memory prescription, or error topology needs its own matching test.

No finite-amplitude nonlinear theorem, uniform small-curvature claim,
full scalar/vector health, cutoff hierarchy, ultraviolet completion,
quantum state, global CD background, free old M1 matter match, or closure
of S6/P8 is certified. The epsilon-zero coefficient limit is not a
regular two-metric geometry. Some parent proper-time/curvature scales
increase as epsilon decreases; the tiny epsilon gate is not a cutoff
or EFT-validity proof. The arbitrary C1 pulse is spatially band-limited
here, not band-limited in temporal frequency.

The verification boundary is the written proof, exact symbolic and
independent rational identities, outward interval automatic
differentiation, source hashes, and immutable certificate replay. It is
not a proof-assistant formalization or an optimized validated ODE solve.
