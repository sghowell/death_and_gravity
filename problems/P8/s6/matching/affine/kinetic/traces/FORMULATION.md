# S6.39: the complete constant two-trace curl family

Status: the stated constant two-trace family is classified. Original P8 remains OPEN.
This is a new quadratic rolling-action audit; all ancestors remain frozen.

## Literal family

Keep the exact S6.37 CD/M1 affine action, original physical metric and
free chi. For unrestricted kappa=Gamma-LC(g), in source signature
(-+++), derivative index last, define the two projectively invariant
parity-even distortion traces

    V_mu=kappa^a_mu_a-kappa^a_a_mu/4,
    U_mu=g_mu_a*g^bc*kappa^a_bc-kappa^a_a_mu/4.

Let C=(V,U) and add only

    -(1/4) Z_IJ F(C^I)_mu_nu F(C^J)^mu_nu,
    Z a constant real symmetric 2-by-2 matrix.

No torsion or metric-compatibility restriction is imposed. No other
kinetic, mass or higher-order operator is tuned along with Z. This
includes mixed curl contractions and all ranks/signs of Z. It does
not include field-dependent matrices, axial traces, other tensor
kinetics, additional fields or changes to the matched action.

After extracting M^2*tau^2 from the action, Z=Z_physical/(M^2*tau^2),
and C_normalized=tau*C_physical. Thus q=(tau*k_physical)^2. The
original rolling solution has kappa=0 and remains a solution for
every constant Z. The target is its full coupled quadratic health,
not a fixed-metric or isolated-vector spectrum.

## Rank-complete obligations

Derive both full trace maps from the literal 64-component connection
and their 8-by-8 Schur matrix on the whole actual trajectory:

    D8 = D2 tensor diag(1,-1,-1,-1),
    D2 = (3/8)*[[1,-7],[-7,1]].

This is a quadratic rolling identity at p=1/2 for every finite time.
No general nonlinear open-tube complement inverse is asserted.
Retain coefficient/background-Hessian variations in both trace
sources; Ustar is not minus Vstar in its full rolling first variation.

For rank one, write Z=zeta*(A,B)^T*(A,B), with (A,B)!=0 and zeta!=0.
The single-vector Schur value is gamma=3(A^2-14AB+B^2)/8. Analyze
gamma>0, gamma<0 and gamma=0 separately. In particular gamma=0
must use every original connection equation, not an inverse of gamma.
The punctured lapse/shift reduction must retain chi and prove either
a negative configuration-velocity direction or a nondegenerate
higher-derivative Ostrogradsky obstruction after all constraints.

For rank two, distinguish an already negative transverse kinetic
direction from positive-definite Z. In the latter case eliminate both
temporal vectors and test the exact longitudinal velocity matrix,
including its embedding in the full scalar constraint action. Rank
zero is the unchanged auxiliary theory, not a kinetic completion.

## Domains and limits of the verdict

Use finite u!=0 where the original Theta is nonzero, q>0 and each
explicit nonzero algebraic divisor of the chart being used. The
original full rolling solution is retained throughout. Singular
Legendre, temporal-vector and zero-Schur charts must not be merged.
The zero kinetic matrix and zero vector combination are controls.

The exclusion is of every nonzero Z in this literal family
as an everywhere-positive-kinetic fundamental parent on the rolling
solution. No inequality in q alone identifies an unhealthy low-energy
frequency or a justified EFT cutoff. No additional V/G/B estimate,
UV completion, general metric-affine no-go, original P8 closure, or
new requirement on the completed scoped photon result is inferred.
The evidence consists of exact algebra with written constraint/rank
proofs, not proof-assistant formalization.
