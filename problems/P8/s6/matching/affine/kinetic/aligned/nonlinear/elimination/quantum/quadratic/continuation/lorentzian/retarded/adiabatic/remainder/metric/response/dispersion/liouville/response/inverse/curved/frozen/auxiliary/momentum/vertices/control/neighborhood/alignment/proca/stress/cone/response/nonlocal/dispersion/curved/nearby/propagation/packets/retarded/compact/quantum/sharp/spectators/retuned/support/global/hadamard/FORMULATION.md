# P8 S6.99: a global two-cone Hadamard scalar state

Keep the separately named total-margin epsilon=1/200 action and its
preserved ORIGINAL GLOBAL clock from S6.96--S6.98. Do not extend the
off-clock nearby solution or change any frozen state or report.

On the physical spacetime g=-du^2+a(u)^2 dx^2, a=(1+u^2)^2, let
Z=(v,chi,pi_v,pi_chi) be the regular reduced density-canonical scalar
phase, M its complete generator, and Omega=[0,I;-I,0]. The real
formally Hermitian differential operator is

    Q=a^-3 Omega (partial_u-M).

Its Green kernels are taken relative to physical source volume a^3.
Advanced minus retarded is E_Q=U_density Omega. The algebra has
commutator i*hbar*E_Q/kappa, with kappa>0. All four phase components,
not only the relational matter component, are included.

There exists a fresh positive quasifree state of this global reduced
quadratic scalar algebra whose two-point distribution satisfies

    WF(W) subset V+ x V-,
    V+={(u,x;tau,xi): xi!=0,
        tau=-c(u)*|xi|/a(u) or tau=-|xi|/a(u)},
    V-=-V+, c^2=J/(J+4*epsilon/h^2).

The sign uses W proportional to exp(-i*frequency*(t-s)).
The CCR then imply V+/V- decomposability of E_Q. This is the
generalized two-cone Hadamard condition, not the ordinary
single-metric free Klein--Gordon condition.

The construction is by an all-orders matrix Riccati symbol on the
initial gamma slab, positive graph covariance, an infrared completion,
and exact global evolution. Complete order-zero overlap maps and
frequency gaps on every finite strip control its wavefront set.
A finite adiabatic truncation or a positive oscillator prescription
alone would not prove this statement.

Native checks certify the literal operator/canonical bridges, matrix
induction identities, actual first nontrivial jets, positivity/CCR,
and gap/overlap inputs. The all-orders symbol, distribution and
wavefront proofs are written and pinned, not proof-assistant formalized.
No renormalized physical stress, semiclassical backreaction,
interacting control, UV completion or original P8 closure follows.
