# P8 S6.98: global retuned scalar support and quadratic microcausality

Keep the separately named S6.96 action with total epsilon=1/200. This
checkpoint uses its preserved ORIGINAL GLOBAL classical clock solution,
not a continuation of the S6.96 off-clock nearby solution:

    N=e=1, a(u)=(1+u^2)^2, u in R,
    chi_background'=1/[10*(1+u^2)^6], W_background=0.

For every pair of finite times s<=t, the COMPLETE retarded response of
the local relational scalar O=chi-chi_background(phi) to the normalized
probe kappa integral a^3 J O has spatial support in

    |x-y|<=S_m(t,s)=F(t)-F(s),
    F(u)=atan(u)/2+u/[2*(1+u^2)].

This is the actual physical matter cone since F'=1/a. The regular
canonical phase, two overlapping complete Euler charts, all finite
frequencies, and the complex spatial null cone are included. No
principal-only inference or all-q velocity-chart inverse is used.

A new positive tempered Gaussian state of the global reduced quadratic
scalar CCR algebra exists. Its exact state-independent Kubo commutator
is causal for compact matter-spacelike test pairs at arbitrary finite
times. This is not a Hadamard, interacting, semiclassical or UV theorem.
No old nearby action, state, nonzero commutator or certificate is changed.

Native identities and rational-degree/positivity checks support the
pinned written compact-strip growth and support proofs. Prefactors are
proved finite on every compact time strip, not uniformly bounded over
the whole history and not evaluated as an interacting energy budget.
Original P8 remains open.
