# Exact new canonical reduction and continuous domain

At fixed canonical trace momentum p, the old post-temporal Hamiltonian
has trace part

H_old/N=(p-b-delta*j)^2/(4*a)-f0+gamma_t*j^2/(2*U)-c*j.

The new literal action instead gives

K_new=(p-b)/(2*a), T_new=-gamma_t*j/U,
H_new/N=(p-b)^2/(4*a)-f0+gamma_t*j^2/(2*U).

Consequently H_new-H_old equals
N*((p-b)*delta*j/(2*a)-delta^2*j^2/(4*a)+c*j).
The temporal Hessian before elimination is -N*U/gamma_t, not the
old source-mixed pivot. Neither auxiliary acquires a time derivative.
The full spatial diffeomorphism generator has its previous universal
canonical form, in the new canonical momenta; there are no additional
spatial source or lapse-derivative terms. The source-free trace velocity
pivot 2*a is nonzero. With the nonzero temporal and lapse secondary
pivots, the local seven-mode constraint count persists on this domain.

Retain the old fourteen-row invariant inventory for comparison. The
linear j and mixed dp*j rows now vanish. The j^2 row is the actual
N*gamma_t/(2*U), differentiated through order four. All other rows are
unchanged, including the full original I, I_phi and fixed-basepoint Q
jets. There are twelve nonzero monomials and sixty derivatives in that
nonzero inventory. Its degree in the nine invariants is at most two.

The actual change in the j^2 coefficient is -N*delta^2/(4*a); its first
two lapse Taylor coefficients vanish on the clock. All fourteen
coefficient values at N=1 and all nine linear-invariant lapse-force
coefficients agree. The background function agrees for every N and u
when j=0. Therefore the old stationary elimination formula
H2=Q0-L1^2/(2*A2), all invariant-to-physical quadratic maps, the spatial
York solution and the two canonical boundary generators give the same
complete physical quadratic action. This includes every one of the
previous 105 upper-triangular phase pairs, not an isolated vector test.
The nonlinear canonical momenta themselves need not agree.

The S6.75 complex domain is unchanged. In its termwise triangle proof
the new trace square deletes delta*j, so its previous upper bound 162
still holds; the separate source-Gauss bound is replaced by zero. All
other pieces, especially the complete scalar potential, boundary and
retuned mass coefficients, are identical. Their summed Hamiltonian
bound is still strictly below 10000. The unchanged background lapse
Hessian is -2*J_e. The same Cauchy estimates hence give the same
Newton contraction below 1/10, lapse radius 10^-14, nine-invariant
radius 10^-24, root displacement at most 8*10^8*max|Y| and secondary
pivot magnitude above 1/25. Now |T|<4*max|Y| directly, since
|gamma_t/U|<4 on the outer coefficient domain. Gamma_t is bounded
away from zero on the real domain. No frequency cutoff follows.

For every actual new coefficient the universal continuous lapse-jet
bound is Bnew_n=10000*n!*100^n, n=0,...,4. The prior sharper rational
coefficient bounds need not hold for this new action. Comparing exact
ratios proves Bnew_n < 10^9*Bold_n for every n. The analyticity and
positive-branch claims here are the original local tube claims, not
an extension through branch singularities outside the tube.
