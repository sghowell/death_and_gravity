# P8 S6.68: finite prepared homogeneous physical-metric response

The original clock, selected all-order Gaussian state, mass functions,
fixed S6.60 scalar profiles and S6.67 dimensional subtraction remain
unchanged. This checkpoint does not alter the physical action.

## Statement

Take independent physical lapse and log-scale sources

    N=1+e n, a_hat=a exp(e zeta),

at fixed scalar clock and spatial K=0. The sources are smooth,
compactly supported in time strictly after u0=-1/2, and zero on an
initial neighborhood. The output interval is I=[-1/2,1/2]. The
source norm is the maximum of both fields and their time derivatives
zero through ten on I.

The full vector Gaussian physical energy/pressure response exists
as an integrable exact-minus-adiabatic remainder plus the S6.67
finite local part. Its calculation includes moving canonical
normalization, both metric vertices, second mass derivatives,
physical stress normalization and the nonzero initial-state
interference. The eighth-order comparison reference is neither a
new state nor a new subtraction.

At L=M tau=10^24,m=m0 tau=1000, continuous rational bounds give

    joint nonlocal vector C10-to-C0 norm < 10^-42,
    joint complete vector C10-to-C0 norm < 10^-38.

The already-fixed scalar tadpole has physical stress variation
(rho_sigma+p_sigma)n in both energy and pressure. Its profiles
are not recomputed under variation. Including this action term
gives the background-cancelled Gaussian correction a joint
C10-to-C0 bound below 10^-37 at these scales.

The exact mode response is retarded in time. The subtraction and
fixed scalar action add only local time operators. A written
parameter-dependence argument justifies the common finite
dimensional limit.

## Boundary

No arbitrary spatial-momentum or independent initial covariance
response is claimed. A C10-to-C0 bound is not a no-loss estimate
for the coupled metric equations, a feedback contraction or a
quantum stability/cone result. Other loops, interactions, cutoff,
finite Wilson matching, a common UV parent and V/G/B remain open.
Original P8 is not finished or closed.

The evidence is exact algebra with written continuous and
dimensional arguments, not proof-assistant formalization.
