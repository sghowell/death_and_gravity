# Retuned covariant clock margin and actual classical domains

This separately named action has total clock margin 1/200, replacing
neither the frozen 10^-6 candidate nor any old report. A weaker 10^-4
trial remains an explicit fast-clock negative control.

Five written proofs cover the literal action and global clock bridge,
the freshly constructed nearby solution, complete box/principal
estimates, the central positive-matter continuum, and scope. The new
nearby conserved charge, lapse flow and physical bounce acceleration
are recomputed. No old state, quantum profile or matching is reassigned.

The read-only verifier rebuilds the pinned S6.95 parent and its ancestry,
pins every source/proof/test file, and rejects all report-field changes.
Scientific arithmetic is native SymPy; complete regression has its
separately audited adapter boundary. Report checking never writes files.

These results establish classical background and principal-cone facts,
not full retarded support, nonlinear/EFT/quantum control, UV admissibility
or original P8 closure.
