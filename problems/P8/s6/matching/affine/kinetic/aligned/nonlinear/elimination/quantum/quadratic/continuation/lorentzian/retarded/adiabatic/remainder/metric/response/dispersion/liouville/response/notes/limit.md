# Dimensional normalization and unchanged finite response

The acoustic rates f_T=N/a and f_L=N sqrt(bm/am)/a do not
depend on spatial dimension D. Before taking D=3, the physical
kinetic coefficients give acoustic pump squares

    C_T,D^2=a^(D-3),
    C_L,D^2=m^2 a^(D-1) sqrt(am bm) k^2/(k^2+a^2m^2 am).

These are derived directly from g_D^2 f, not extrapolated from
the physical-dimensional simplification. At D=3 they reproduce
S6.70. Their first dimensional derivative is log(a) C_D^2.

At first metric order, the logarithmic pump variations are

    delta log C_T,D=(D-3)zeta/2,
    delta log C_L,D=(D-1)zeta/2+(alpha+beta)n/4
                         -(1-z)(2zeta+alpha n)/2.

Both have dimensional derivative zeta/2. The transverse pump
therefore cannot simply be set to one before the dimensional
finite limit. Exact checks match these expressions to the
original moving physical canonical normalization plus half
the acoustic-rate variation.

## No auxiliary renormalization change

For real physical metrics the positive f map is independent
of D and preserves the prepared initial data. At complex D use
the already-chosen analytic plus/minus mode pair, not complex
conjugation at that D. The same linear mode and covariance
transformation is holomorphic near D=3. The D-1 transverse
multiplicity and original spatial measure are unchanged.

Map the actual physical adiabatic readout subtraction through
the same transformation, including its source variation.
Do not independently renormalize a scalar-shaped auxiliary
potential, and do not drop its dimensional pump jets first.
The regulated exact-minus-subtraction integrand is then the
same expression at each momentum, merely in different mode
variables. The fixed local action and its finite evanescent
part are still S6.67, and the fixed scalar tadpole is unchanged.

S6.68's integrable prepared response and common-limit argument
therefore apply to this exact rewriting. Its C10-to-C0 stress
bound is neither weakened nor improved by it. The C2 bound in
S6.70 controls only delta R_k in the potential; the physical
readout differentiations and ultraviolet integral remain to
be estimated before a no-loss stress statement is available.

## Next analytic requirement

The exact equations now provide the source and readout maps
needed to separate the longitudinal logarithmic channel from
the curved state-dependent remainder. That separation must
include both clock shifts, the displayed physical contacts,
the original initial interference and the fixed local finite
complement. Its remainder must be bounded in a norm compatible
with the full classical constraint/evolution system.

Only after that step can a causal matrix inverse, compatible
data, continuation and quantum stability/cutoff be addressed.
Positive real-axis results or a scalar-shaped mode potential
alone cannot supply those conclusions. Original P8 and the
separate common-parent V/G/B obligations remain open.
