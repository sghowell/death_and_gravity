# Same massive contact with a different analytic switch

Use the full retuned Ftree, F0=-28/25 and the S6.108
explicit classical calibration

    gamma=1024/10^800, lambda=10^-600, kappa_n=n/gamma,
    Fv=X/2-u^2/2+(lambda kappa_n-nF0)X^2+(-n/3-F0)u^4,
    F=Ftree+exp(-u^4) S(Fv-Ftree).

The independent full-function expansion gives the same
canonical quadratic mass/residue and quartics as S6.108:

    L2=(Y_bar-Phi_bar^2)/2,
    L4_lower=lambda Y_bar^2-n Phi_bar^4/(3kappa),
    L4_DHOST=-2n(L3_bar-L4_bar)/kappa.

There is no scalar cubic at the vacuum. The ordinary
positive GR, free chi and standalone Proca quadratic
sectors are unchanged. The connection is algebraic and
adds no physical oscillator there.

For the co-scaled limit eps=kappa^-1/2, n=gamma/eps^2,
the rational step T at X=eps^2Y_bar vanishes faster than
every power of eps on compact canonical field/jet sets:
its power ratio is O(eps^2)^n. Each fixed derivative adds
only polynomial powers of n and eps^-1. Meanwhile

    w=gamma eps^2Y_bar^2 exp(-gamma eps^2Y_bar^2),
    S=1-gamma eps^2Y_bar^2+O(eps^4).

These are the leading terms required in the independent
S6.108 canonical limit. The fixed massive lambda and
nonzero gamma interactions therefore survive, including
the positive quartic potential and the suppressed
canonical-graviton mixing. The same on-shell tree contact
and exact heavy-exchange remainder result applies to this
limit. It is not a finite-M gravity subtraction or an
all-order interchange of a limit with a loop integral.

The regular affine parent here and the healthy massive
two-scalar vacuum exchange model are different constructions.
They have not been shown to be restrictions of one
propagating heavy theory. A positive algebraic determinant
does not bridge that outstanding requirement.
