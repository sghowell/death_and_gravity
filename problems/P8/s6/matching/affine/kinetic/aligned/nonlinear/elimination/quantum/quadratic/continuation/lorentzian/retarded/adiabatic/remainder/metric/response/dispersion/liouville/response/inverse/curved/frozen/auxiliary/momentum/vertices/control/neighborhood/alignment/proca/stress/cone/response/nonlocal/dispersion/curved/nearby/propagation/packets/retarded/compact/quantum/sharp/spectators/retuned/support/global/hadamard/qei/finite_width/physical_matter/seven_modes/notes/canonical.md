# The same scalar marginal and the physical-stress phase

At the anchor, take the finite product covariance

    C7=diag(C_scalar,
            U_T,b C_T,flat U_T,b^T,
            U_T,b C_T,flat U_T,b^T,
            U_P,b C_P,flat U_P,b^T).

Here C_scalar is literally the previously chosen S6.101
member, not a new instantaneous or finite-adiabatic state.
All blocks carry the same hbar/kappa. Their Gram positivity
and CCR survive real symplectic transport. The finite
direct sum is positive, has antisymmetric part
i(hbar/kappa)Omega7, and defines the zero-mean quasifree
state by Wick's rule. Its exact bisolution is

    W7(t,s,k)=U7,a(t,0,k) C7(0,k) U7,a(s,0,k)^T.

Smearing a finite vector of test functions reduces positivity
to the corresponding sum of the sector positive forms.
The infrared and polynomial estimates make all these forms
continuous distributions. Restricting the algebra to the
old scalar entries gives exactly its former two-point
function and every Wick n-point function.

The native normalization bridge independently compares fourteen
literal quadratic coefficients of S6.76's actual constrained
Hamiltonian, retaining symbolic clock time. It checks the two
tensor coordinate/momentum pairs, all six vector diagonal
entries and cross-sector zero controls. Restore comoving
coordinates with coordinate volume a^3:

    T_local=T, PT_local=PT/a^3,
    W_local=W/a, Pi_local=Pi/a^2, k_local=k/a.

This gives precisely the tensor and Cartesian-vector
Hamiltonians used here; it does not equate physical wave
number and comoving wave number.

At the center u=0, let D be the existing S6.102 scalar map
from old regular density variables to the fixed spatial
phase. It contains BOTH canonical matter/metric boundary
shifts. The full map is diag(D,I10), and the exact
14-dimensional identity maps Omega7 to S6.102's phase
symplectic form. Thus the new joint state acts on all
fourteen center physical-stress channels with their actual
normalizations. No uncomputed all-time physical-stress map
is asserted. Replacing D by a guessed identity would
not give the same observable.

This is a canonical free-state bridge, not a new tadpole
solution. In particular no old subtraction or background
mean is silently reassigned to the product state.
