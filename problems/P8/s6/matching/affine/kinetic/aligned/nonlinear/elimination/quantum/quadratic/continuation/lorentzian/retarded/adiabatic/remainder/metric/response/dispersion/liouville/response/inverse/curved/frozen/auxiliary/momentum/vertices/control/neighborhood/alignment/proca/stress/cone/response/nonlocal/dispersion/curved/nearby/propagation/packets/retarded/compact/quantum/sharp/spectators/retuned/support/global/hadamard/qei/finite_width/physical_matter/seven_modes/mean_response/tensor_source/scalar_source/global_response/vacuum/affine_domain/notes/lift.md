# Complete principal auxiliary-affine lift

Use source signature -+++ and x=-X only as a convention;
the physical matter metric is not changed. With f=R/2,
the exceptional target has source alpha3=-R_x/x.
The generic inverse equation is

    p_x/p=f_x/f+x alpha3/(4f)=R_x/(2R).

Thus p=C(u)sqrt(R). Regularity of c=2(p-f)/x at X=0,
where R=1, forces the positive normalization C=1/2.
The same value cancels the possible double pole in
F4=(p^2-f^2)/(f x^2). A different integration constant cannot
remove S6.108's rank defect while retaining a regular vacuum.

For the new R, take

    p=sqrt(R)/2,
    c=(sqrt(R)-R)/x,
    F4=(1-R)/(2x^2), Delta=1.

The literal unrestricted principal dictionary reproduces

    f=R/2, alpha1=alpha2=0,
    alpha3=-R_x/x,
    alpha4=R_x/x+7R_x^2/(4R),
    alpha5=-R_x^2/(Rx).

The last denominator is R times x. Conversion back gives
the actual exceptional Ia functions used in S6.108 with
the new R. These identities are checked independently,
not inferred from a claim that every positive R is healthy.

Since R-1=O(X^2), c and F4 have removable limits at X=0.
Their actual leading values are

    c/X -> -n/(2h), F4 -> n/(2h), p -> 1/2.

No algebraic coefficient is defined by multiplying a zero
with an undefined old pole. Positive R fixes the square-root
branch on the entire connected domain.

The full quotient determinant in dimensionless units is

    det M_q=-2^52 p^72 (8p^2-1)^3
           =-2^52 p^72 (2R-1)^3.

Both factors are nonzero by the new domain proof. The
four projective directions are the only auxiliary gauge
kernel; all 60 remaining connection equations determine
a unique local distortion. An algebraic inverse is not a
heavy propagator, spectral gap or kinetic health certificate.
