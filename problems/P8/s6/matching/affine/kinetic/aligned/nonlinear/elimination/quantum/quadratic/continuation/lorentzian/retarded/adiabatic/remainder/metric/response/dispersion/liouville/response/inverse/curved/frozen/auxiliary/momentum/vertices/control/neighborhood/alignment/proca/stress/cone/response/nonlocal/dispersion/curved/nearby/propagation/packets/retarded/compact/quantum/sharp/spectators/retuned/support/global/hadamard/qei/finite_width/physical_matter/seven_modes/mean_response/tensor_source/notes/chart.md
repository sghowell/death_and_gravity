# Independent nonlinear center chart comparison

S6.102 used an additive spatial metric I+2v I+t. This
calculation uses exp(2xi) exp(gamma), tr gamma=0.
Through second order their exact relation is

    v=xi+xi^2+tr(gamma^2)/12,
    t=gamma+2xi gamma+[gamma^2-I tr(gamma^2)/3]/2.

The source verifies the matrix equality, trace and determinant
coefficients directly. At xi=0 with two tensor polarizations,

    v_2=(T^2+S^2)/6.

The second-order traceless shift has no linear homogeneous
scalar-density contribution. For one polarization S6.102's
isolated additive-coordinate density coefficient is

    -P^2/135+(25q+203)T^2/40500.

The exponential-chart mean equations predict instead

    -P^2/135+q T^2/1620 = -2 F_T/405.

They do agree after including the required v_2 shift:
delta ell=-3v_2/10,
n_1=(-delta ell/20)/(243/80), and
delta rho_m=delta ell/10-3n_1/200.
The added density is exactly -203 T^2/40500 per polarization.
The source checks BOTH polarizations simultaneously.

The canonical trace momentum also changes by tensor
coordinate-momentum products. For the chosen anchor addition
these mixed covariances vanish; moreover alpha(0)=0 makes
this center density insensitive to that trace shift.
This proves the stated center observable bridge, not an
all-time additive canonical momentum transformation.
Deleting the nonlinear scalar shift is an explicit nonzero
negative control, not an allowed change of the state or action.
