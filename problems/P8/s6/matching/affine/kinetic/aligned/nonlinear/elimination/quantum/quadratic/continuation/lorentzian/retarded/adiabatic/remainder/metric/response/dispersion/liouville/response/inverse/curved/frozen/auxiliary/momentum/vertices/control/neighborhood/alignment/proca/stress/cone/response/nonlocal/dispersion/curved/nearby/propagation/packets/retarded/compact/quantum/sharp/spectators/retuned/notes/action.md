# Literal change, preserved clock and quantitative deformation budget

The existing candidate already contains epsilon_old*(X-1)^2/h^2 with
epsilon_old=10^-6. This child declares total epsilon_new=1/200, so the
actual added coefficient is delta=4999/10^6. The physical scalar and
its X derivative, explicit u derivative, and total derivative of its X
derivative vanish identically on X=1. Its metric and clock Euler first
variations therefore vanish on the entire original clock solution.
It is independent of the affine connection, matter and Proca vector.
The original complete classical scale a=(1+u^2)^2 and free-matter
history remain exact. This does not cancel a quantum tadpole.

With the unchanged derivative-dependent conformal chart e=e^omega,
sqrt(h_physical)=e^3 sqrt(h_hat), X=N^-2. The complete new density per
hat volume is N e^3 delta*(N^-2-1)^2/h^2. It has no velocity, spatial
derivative, shift or vector dependence. The canonical Hamiltonian
increment is its NEGATIVE. Its value and first lapse derivative vanish
at N=1; the fixed-phase lapse Hessian changes by -8 delta/h^2.

Put D=1+N^2(h-1), e^4=N^2 h/D. Rationalization gives

    Delta V0 = e*(N e^3 Delta P) = delta*(N^2-1)^2/(N D h).

All four conformal powers matter. V0 changes by this literal amount;
the constraint constant changes by -partial_N DeltaV0+Ln DeltaV0,
where Ln=partial_N log(e). The coefficients a0,m0,d0,I0,b0,A,M,B
and the curvature, shear and matter derivative structures do not change.
No derivative of a constraint-parametrized charge is substituted for a
fixed-canonical-phase lapse derivative.

## Original global clock and both principal charts

The earlier S6.56 proof covers 0<epsilon<=1/100, including 1/200. On the
original clock it adds 4 epsilon/h^2 to J, leaves the physical gradient
form unchanged, and gives squared scalar speeds

    1, J/(J+4 epsilon/h^2).

The present literal rational system independently reproduces the full
clock matter history, lapse and trace flow, fixed-phase Hessian
-2(J+4 epsilon_new/h^2), and this exact speed. Equality of rational
expressions away from a chart crossing is not used as a proof of
coverage: the unchanged unitary/gamma charts supply that coverage.
The moving Euler boundary is retained, including at the bounce.
Ordinary Proca has its own source-free constrained physical wave block;
its three polarizations remain on the physical metric cone.

The native positive-even-polynomial proof gives J*h^2>=1199/800 for all
real u, and J*h^2<=3817516721573/107374182400 on |u|<=1/2. Hence the new
original-clock squared speed is above 1199/1215>0.986 globally, and its
compact squared-speed margin exceeds 1/2000. Relative to the FROZEN
old kinetic form, the added rank-one kinetic form has fractional bound

    4 delta/(1199/800+4 epsilon_old) < 7/500.

This compares to the old nonzero margin, not an undeformed reference.
On all real u and |X-1|<=1/10, the added normalized scalar, its first X
derivative and second X derivative are bounded by delta/100<1/20000,
delta/5<1/1000 and 2delta<1/100. Higher X derivatives vanish. These are
literal operator and classical kinetic budgets, not estimates of all
canonically reduced interactions, loops or UV matching errors.
