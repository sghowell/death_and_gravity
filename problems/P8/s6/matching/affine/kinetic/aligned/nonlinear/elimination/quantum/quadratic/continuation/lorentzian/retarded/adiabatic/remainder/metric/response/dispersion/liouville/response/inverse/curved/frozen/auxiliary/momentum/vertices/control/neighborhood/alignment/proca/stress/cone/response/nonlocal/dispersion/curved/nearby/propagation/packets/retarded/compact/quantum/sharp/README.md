# Sharper actual nearby compact response

The previous compact response proof used a very conservative
low-frequency exponential. This child proves new exact
all-frequency bounds on the same actual quadratic system and
uses them for a separately named rational-width compact probe pair.

The five written proofs cover actual complex jets, the small
symplectic basis, complete transfer/normal-form estimates,
three-region L2 control and new compact probes, and scope.
Every exact inequality uses rational arithmetic, including the
new cutoff below 10^94. No old report or probe is overwritten.

The read-only verifier rebuilds the pinned S6.93 parent and its
ancestry. It checks every report field against independent replay
and pins its complete source/proof/test manifest. It never writes
a report; --check is read-only. Native scientific tests and
separate complete regression runs retain their declared boundary.

A finite mathematical cutoff is not an established interacting
EFT cutoff. The common-parent matching and its own high-band
tail, temporal-frequency control and original P8 remain open.
