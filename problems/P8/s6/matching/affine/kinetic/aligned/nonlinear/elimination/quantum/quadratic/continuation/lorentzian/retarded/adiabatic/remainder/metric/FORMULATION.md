# P8 S6.67: finite homogeneous local physical-metric response

Original P8 remains open. The original physical clock, all-order
selected Gaussian vector state, fixed tadpole profiles and dimensional
subtraction prescription remain unchanged. This checkpoint follows
S6.66 but does not infer its missing full metric state remainder.

## Statement

At spatial K=0, vary the independent physical lapse and logarithmic
scale factor,

    N=1+n, a_hat=a exp(zeta),

with the scalar clock held fixed. Both are smooth compactly supported
time variations. Use the existing mass functions, including their
second lapse derivatives. The physical vector Hamiltonians and moving
canonical normalization determine all four lapse/log-scale response
blocks, including their instantaneous contact terms.

Differentiate the WKB readouts through orders 0,2,4 in D spatial
dimensions. Every full homogeneous pole matches the twice-varied
fixed covariant subtraction action. Retain its D-dimensional measure
and first dimensional jet. At mu=m, the radial finite part plus this
evanescent term is an explicit weighted-self-adjoint two-current
local operator through fourth time derivatives.

The zero-order result independently equals the full frozen finite
potential Hessian. The ordinary-Proca limit independently equals the
finite heat-action Hessian. Constant spatial rescaling tests include
the physical output normalization. Omitting the finite counterterm
or second mass vertices fails explicit nonzero controls.

On I=[-1/2,1/2], for m=m0 tau>=1000 and L=M tau>0, the report
gives a continuous rational C4-to-C0 bound for the physical energy
and pressure responses. The input norm is the maximum of both
sources and all their time derivatives zero through four. At
L=10^24,m=1000 the joint local bound is below 10^-38.

## Deliberate boundary

The result is only the finite local component of the homogeneous
Gaussian physical-metric response. It does not add the missing full
metric state remainder, arbitrary spatial momentum or arbitrary
varied initial covariance. It is not a no-loss feedback estimate.

The local fourth-derivative symbol is indefinite. That fact is
recorded, not reinterpreted as a particle spectrum obtained by
resumming a finite local truncation. No quantum stability/cone,
interaction, cutoff, finite Wilson matching, common-parent or V/G/B
claim follows. Original P8 is not closed.

The evidence combines exact identities with written variational,
dimensional and continuous-bound arguments, not proof-assistant
formalization. No earlier source or physical action is overwritten.
