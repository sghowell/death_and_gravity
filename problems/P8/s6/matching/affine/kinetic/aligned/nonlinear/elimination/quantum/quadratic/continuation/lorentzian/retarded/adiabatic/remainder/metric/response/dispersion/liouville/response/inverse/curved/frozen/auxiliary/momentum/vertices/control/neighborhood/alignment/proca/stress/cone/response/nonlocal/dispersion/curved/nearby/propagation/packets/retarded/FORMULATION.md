# P8 S6.91: actual nearby classical retarded relational response

Claim identifier: P8-S6.91.CLASSICAL_NEARBY_RETARDED.

On the fixed actual S6.88 nearby source-free classical
bounce with central lapse 1+10^-6, and on the inner interval
I=[-1/(2*10^7),1/(2*10^7)], prove the exact scalar response
to the local probe sqrt(-g) J[chi-chi_background(phi)] has

    G(t,s;x)=A_c(t,s) delta(r-S_c)/(4pi r)
             +A_m(t,s) delta(r-S_m)/(4pi r)+R(t,s;x),
    A_c>10^-8, A_m>10^-4, R(t,s;.) in L2(R^3),

uniformly for s<t in I. Here S_j=integral_s^t omega_j and
S_c>S_m. Prove the clock front cannot be cancelled and
deduce some real smooth compact source/detector tests
with matter-spacelike separated supports have nonzero
retarded response.

Requirements:

- Use the literal original finite-frequency Hamiltonian,
  local relational source and exact output normalization.
- Keep the original polynomial low-frequency chart,
  both endpoint normal-form changes and all remainders.
- Derive the two signed mode-pair amplitudes from the
  full source inverse and target basis.
- Include the entire compact low-frequency ball and
  the exact three-dimensional Fourier/Plancherel factors.
- Give written distribution non-cancellation and product
  test separation proofs, with actual neighborhood margins.
- Fully rebuild the frozen S6.90 parent and pin all local
  proof/code/test bytes in a read-only certificate.

The theorem is classical and linear. It uses unbounded
classical frequencies and does not transfer the front
or compact probes to an interacting EFT cutoff, a UV
parent or quantum response. Original P8 remains open.
