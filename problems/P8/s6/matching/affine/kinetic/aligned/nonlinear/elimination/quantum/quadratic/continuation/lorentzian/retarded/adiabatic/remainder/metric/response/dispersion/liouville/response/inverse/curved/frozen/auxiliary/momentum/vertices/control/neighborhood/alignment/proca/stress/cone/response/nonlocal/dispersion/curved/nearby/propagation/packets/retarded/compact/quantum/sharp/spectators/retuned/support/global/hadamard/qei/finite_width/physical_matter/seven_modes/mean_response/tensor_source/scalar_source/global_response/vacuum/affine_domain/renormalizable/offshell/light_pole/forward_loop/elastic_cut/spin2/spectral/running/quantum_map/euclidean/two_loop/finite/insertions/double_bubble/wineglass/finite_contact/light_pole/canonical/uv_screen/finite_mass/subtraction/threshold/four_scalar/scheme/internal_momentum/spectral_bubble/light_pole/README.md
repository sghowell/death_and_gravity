# First spectral fermion insertion in the scalar quadratic sector

P8 S6.136 differentiates the full scalar quadratic half-trace once
in its light covariance, retains all local terms, and bounds the
nonlocal mixed H-Phi bubble after both inner and outer on-shell
reference subtractions.

The named family's finite slope is below 10^-616 and its uniform
quadratic remainder coefficient below 10^-1017. The complete
one-loop functional plus this sector retains its local pole and
finite-window Euclidean control. This is not a complete two-loop
inverse or original P8 closure.

Read FORMULATION.md and all six notes. Read-only native replay:

    .venv/bin/python -u scripts/p8_replay.py cli p8_vacuum_fermion_spectral_pole.verify

The report recursively rebuilds S6.135 and compares all source hashes
and fields. Native/ordinary science uses unmodified SymPy with the
documented interpreter-only runtime allowances.
