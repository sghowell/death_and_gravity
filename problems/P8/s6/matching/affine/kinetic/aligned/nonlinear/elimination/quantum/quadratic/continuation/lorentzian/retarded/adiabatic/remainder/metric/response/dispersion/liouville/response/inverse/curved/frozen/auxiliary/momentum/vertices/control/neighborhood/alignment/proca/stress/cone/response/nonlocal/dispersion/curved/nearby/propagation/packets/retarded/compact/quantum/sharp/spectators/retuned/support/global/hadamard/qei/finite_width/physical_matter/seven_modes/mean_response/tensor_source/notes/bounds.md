# Global mean bounds and a complete metric representative

For h>=1 the actual lapse source satisfies |F_T|<=3r/2.
The exact proof retains the positive kinetic and gradient
coefficients of 3r/2 +/- F_T. The S6.104 mean matrix is
literally unchanged, and its source estimate used the looser
envelope |F|<=5r/2. Its written weighted proof therefore
applies with the NEW tensor r_star=4, not the Proca energy.

For Y=(xi,100(1+u^2)^3 dp), the integrable generator bound
is still below two and the weighted source L1 bound below
170r(0). Thus |Y|<9*170*4eta<6400eta on the future half.
Reflection of the added state and uniqueness give the same
bound on the past half, with xi,n even and dp odd.

Writing t=1+u^2, direct constraint reconstruction gives

    |n(u)|/eta <=1920|u|/t^3+480/t^11+25/t^3.

The uniform bound is 1465eta<1500eta. Consequently

    |B|<7200eta, |delta psi|<1000eta,
    |delta rho_m|<=216eta/t^12,
    |delta rho_m|/rho_m,background<=43200eta.

The matter field bound follows by integrating
(|n|+3|xi|)/(10t^6) with zero anchor field. There is no
independent matter-charge reset. The lapse tends to zero.
The first mean equation gives a finite xi limit, with
the explicit tail error in the report obtained by setting
C=6400 and r_star=4 in the unchanged S6.104 tail estimate.
The limits on both tails agree by reflection.

For the leading profiles define, without truncating the frame,

    N_eta=1+n,  hat_a_eta=a exp(xi),
    a_eta=hat_a_eta[(h-1+N_eta^-2)/h]^(-1/4).

For eta<=10^-20, the actual frame logarithm obeys
|omega|<=|log N_eta|/2<=|n|, hence
|log(a_eta/a)|<8000eta. Both N_eta and a_eta exceed 1/2
globally. Smoothness holds at each finite u. The conserved
spatial geodesic momentum then makes both the timelike
proper-length integral and null integral of N_eta a_eta
diverge on either time tail, exactly as in S6.104.

Parity gives H_phys(0)=0. Importantly, the new n'' is
negative: the Proca positivity argument cannot be reused.
The independently checked tensor jets give 0<=n<eta,
|xi''|<2eta and |n''|<30eta. The exact center numerator is

    (log a_eta)''=
       4+xi''-(3/2)[(1+n)^2-1]+n''/[2(1+n)].

Using N_eta>1/2 bounds it below
4-35eta-(3/2)eta^2>3. Division by N_eta^2 leaves a
strictly positive proper bounce acceleration.

This is a complete bouncing GEOMETRIC REPRESENTATIVE of
the leading profile. No state has been co-evolved on this
new metric, and no nonlinear or higher-loop residual has
been bounded. It is not a new exact semiclassical solution.
