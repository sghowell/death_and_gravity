# Actual leading tensor-forced homogeneous response

Only the finite tensor covariance addition is changed.
Subtract the leading mean equations for the two states on the
S6.103 classical background. The common absolute source and
all common counterterms cancel in this DIFFERENCE; they are
not assumed zero. The quadratic tensor action in action.md
supplies the source, not a minimally coupled surrogate.

Use the literal S6.104 retuned-action coefficients J>0,
alpha, beta, ell, H and h without modifying them. For
xi=delta log A, dp=delta canonical trace density,

    F_T=(1-1/h)r+3s/(2h),
    n=(alpha dp-3ell beta xi+F_T)/(2J),
    xi'=-dp/2+alpha n/3,
    dp'=-3H dp+ell beta n-3ell^2 xi+s,
    delta psi'=beta n-3ell xi.

The source checks the complete two-component matrix against
S6.104; only the inhomogeneous forcing is changed.
Initial xi,dp,delta psi vanish. The initial lapse is instead
determined by the constraint and must not be reset to zero.
The physical linear log-scale response and actual induced
scalar matter density/pressure are

    B=xi+n/(2h),   delta rho_m=delta p_m=-3ell^2 B.

The induced matter variation obeys its own linear Ward identity
only when the background connection term 3ell^2 B' is retained.
Adding the actual tensor source gives

    (delta rho_m+rho_T)'
      +3H(2delta rho_m+rho_T+s)+3ell^2 B'=-E_phi.

Both this identity and the independent clock variation are
checked symbolically. This is the homogeneous reduced-source
identity, not an interacting covariant graviton Ward theorem.

At the anchor the covariance trace gives
r=(q+4)/2, s=(12-q)/6,
r''=-4(q+12), s''=4(q^2-3q-36)/3,
F_T=(12-q)/4 and F_T''=2q^2-3q/2-78.
These second stress jets are obtained by independently
transporting both Hessians with the actual tensor generator.

The retuned pivot has J=243/160, J''=-633/200 and alpha'=9.
The resulting anchor jets per unit band amplitude are

    n=-20(q-12)/243,
    xi''=-199(q-12)/1215,
    n''=2(54000q^2-135433q-966804)/164025.

The proper-time physical Hubble derivative variation is
xi''+n''/2-11n, namely

    2(27000q^2-6899q-1213212)/164025.

Both lapse factors and the second frame derivative are retained.
This polynomial increases on [1,4] and lies strictly between
-15 and -9. It is a small decrease for this state addition,
not the positive Proca result and not a bounce obstruction.
