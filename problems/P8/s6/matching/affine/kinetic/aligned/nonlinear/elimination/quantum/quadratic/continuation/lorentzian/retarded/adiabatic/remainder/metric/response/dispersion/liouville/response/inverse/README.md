# Exact isolated massive dispersion inverse

See [FORMULATION.md](FORMULATION.md). The new result is a causal
L1 regular kernel plus a nonzero instantaneous matrix for the
exact massive two-current block. The apparent low-frequency pole
of a leading-log surrogate is not used.

The certificate is a read-only replay with parent/source hashes,
exact matrix identities, strict input controls and written
complex-analytic and endpoint estimates. Run the local tests and
the p8_vector_dispersion_inverse.verify --check entry point with
the repository's P8 source paths. No claim of proof-assistant
formalization or of full P8 completion is made.
