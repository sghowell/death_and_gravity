# Actual global action, source and regular phase

S6.96's literal lower-scalar change has a double zero at X=1 and
preserves the full original global classical clock. Its rational
constraint, velocity and lapse-pivot bridges are pinned in that parent.
Here delta_J=4*epsilon/h^2, epsilon=1/200 and h=(1+u^2)^3; J_new=J+delta_J.
The exact positive-even-polynomial certificate gives J*h^2>=1199/800.
Thus J_new is nonzero at every finite time. The actual a is positive,
and every background coefficient and time derivative is smooth on R.

Replay the literal aligned scalar Lagrangian and its changed lapse
elimination before using either velocity chart. Its regular light
Hamiltonian is the pinned H=C0+R_lapse^2/J_new, with the aligned vector
block set to zero. The scalar shift remains a canonical variable until
the primary relation is used. Define density momenta

    pi_v=-2*a^3*q*b, pi_chi=a^3*p_m,
    Z=(v,chi,pi_v,pi_chi), q=k^2/a^2.

Substitution into a^3 H gives the original density Hamiltonian H_rho
with no inverse Theta or Lambda. Its generator M=Omega Hessian(H_rho)
is polynomial in q of degree two; exact reconstruction and
M Omega+Omega M^T=0 are checked. The original phase defines the regular
zero-frequency continuation and the retarded evolution. In particular
no pole of a subsequent velocity chart is a pole of this generator.

Varying the physical normalized probe action kappa*a^3*J*chi gives the
actual force a^3*e4*J in the density equations. The original output is
chi, not a spatially projected field, and

    G_hat(t,s,k)=a(s)^3*U_rho(t,s,k)[1,3].

The initial retarded phase is zero. The generator is spatially polynomial,
so its Peano--Baker evolution and the local scalar multiplier are entire
in zeta in C^3 with q=(zeta dot zeta)/a^2. Uniform convergence holds on
every compact time-and-momentum set. This includes complex null vectors
and zero momentum without extending a singular velocity formula.
