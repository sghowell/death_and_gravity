# P8 S6.58 — Actual Gaussian mode variation and Cauchy-state boundary

This step retains the S6.56 action and S6.57 fixed-source response.
It derives the actual vector's per-mode first variation before
UV integration. It does not yet supply a renormalized quantum
feedback map. Original P8 remains open.

## Mode and observable variation

The full homogeneous vector mass tensor is retained, including
a_m(N,u),b_m(N,u). Eliminating only the temporal component gives
the exact transverse and longitudinal canonical coefficients.
Their first variations under physical lapse n and log-scale
zeta are derived, not replaced by an ordinary scalar formula.

The original physical canonical phase system has a variation
depending algebraically on n,zeta. Its time-dependent oscillator
map produces the equivalent
delta Omega²=delta Omega_bare²-r''-2d*r', r=delta log(g).
The map variation and initial homogeneous solution are retained.

Direct physical metric differentiation of the same canonical
Hamiltonian gives per-mode energy and pressure matrices. Their
contact variations retain both second lapse mass jets and
the changing readout normalization. They replay S6.50's actual
clock readouts exactly. The covariance response contains both
the varied state and these contact matrices.

## Actual initial-preparation test

At L=M*tau=10^12,R=m0*tau=1000 the exact local coefficients
and bounded state term give F_n(-1/2)>5*10^-15. Consequently
S6.57's zero-independent-data response has a strictly negative
initial lapse correction. This is not an adjustable zero datum.

On the exact point-chart lift at that slice, simply copying
the old physical longitudinal coordinate/momentum covariance
does not retain even its leading positive-frequency condition.
Its limiting impedance ratio obeys

    r_L²=a_m*b_m/(4p_affine²)<1,

for the actual p_affine>1/2 in the small clock tube.
The resulting limiting Bogoliubov coefficient has
|B_infinity|²=(r_L-1)²/(4r_L)>0. A compatible all-order
preparation must therefore vary; its initial covariance term
cannot be set to zero by default.

The transverse leading physical impedance is instead k_com,
an exact conformal control. This does not prove its complete
higher-order state condition. Freezing the oscillator variables
is a different prescription and already fails at leading
transverse order when the lapse changes.

These conclusions concern the specified frozen-Cauchy-data
prescriptions, not the nonexistence of a compatible state.
Off the clock the vector mass tensor is anisotropic; no
ordinary-Proca propagation theorem is imported as if that
operator were unchanged. No full coupled physical-cone,
quantum feedback, cutoff or V/G/B verdict is asserted.
