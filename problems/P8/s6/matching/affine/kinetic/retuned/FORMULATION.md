# S6.41: a source-centered trace-mass candidate

Status: matching, primary-rank and physical matter-cone results certified.
The literal positive-curl candidate fails the original cone requirement.
Original P8 remains OPEN.
This is a separately named parent action, not a revision of any ancestor.

Keep the unrestricted S6.37 action, physical metric, clock and free chi.
In its source signature (-+++), let T=V+U be the sum of the two frozen
projectively invariant distortion traces. Let Tstar[g,phi] be that same
trace of the complete original stationary connection. Define the new
literal action by adding

    -(11 M²/18) g^(mu,nu) (T-Tstar)_mu (T-Tstar)_nu
        -(zeta_physical/4) F(T)_mu_nu F(T)^mu_nu,
    F(T)_mu_nu = partial_mu T_nu-partial_nu T_mu.

Both densities have the full physical metric volume. The source-centered
term includes its linear source and its Tstar² counterterm; adding only
a bare connection mass would be a different, unmatched action.
Tstar is a specified local covariant function of g, phi and their first
and second derivatives, not a prescribed rolling external source.
Its formula and all-64 derivation are part of this checkpoint.

The auxiliary mass coefficient is fixed to mu=11/9 after extracting M².
The normalized curl coefficient is zeta=zeta_physical/(M² tau²).
The matching tube is the unchanged original closed tube, with
9/40<=p²<=11/40, p>0. Zero curl is a separate exact auxiliary control.
The initial positive-curl principal test uses 0<zeta<=1/2000, every finite
real time, and q=(tau*k_physical)²>0 on the stated scalar charts.

Required checks are exact zero-curl matching and a full quotient rank,
the nonlinear primary lapse-velocity cancellation in the original
spatial chart, the actual rolling sources and all scalar constraints,
and continuous principal kinetic/gradient signs with free matter.
The regular center must use the first-order action, not divide by Theta.
The original matter-cone requirement K-G>=0 is also tested, not replaced
by the weaker requirement that K and G are separately positive. The
isotropic retuning fails that test in its physical high-frequency limit:
one coupled scalar characteristic is faster than the matter metric.
That rejects this literal action as an accepted witness; it is not a
claim that the violating regime lies below a justified EFT cutoff.

No complete nonlinear secondary-constraint theorem, all-frequency or
nonlinear stability, controlled heavy-field elimination, spectral gap
of the coupled theory, loop/cutoff bound, vacuum or finite-gravity
positivity verdict, UV completion or original P8 closure is presumed.
An isolated Proca mass parameter is not an EFT cutoff or a coupled gap.
