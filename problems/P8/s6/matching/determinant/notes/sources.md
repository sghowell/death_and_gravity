# Sources, convention and evidence boundary

Flinckman, [arXiv:2607.24347v1](https://arxiv.org/pdf/2607.24347), equations
(2.1)–(2.5), specifies the rank-one determinant-sum potential, nonzero
same-sign coefficients and invertible U. Matter is separate by EH metric;
the background example uses metric 1, not composite U. Equations (3.9)–(3.23)
give the two sums, stresses and unfactored Bianchi/regular velocity locking.
We derive our normalization directly from the action. Its `-+++` convention
maps to P8(b) by `g_B=-g_source`, `R_B=-R_source`, with identical physical
proper-time density and `3G H^2` lapse normalization.

The aligned Lorentz choice solves (3.5); it is not a uniqueness theorem.
Footnote 3 and section 4.1 discuss an adjugate extension at `S_a=0`, where
linear interaction terms vanish. They do not analyze the different
persistent mixed-sign `S_N=0,S_a!=0` branch. Section 3.5 gives asymptotic
metric-cone speeds; these are not used as light-EFT/UV verdicts here. The
scalar/vector stability analysis is explicitly unfinished in section 5.
Our regular-domain monotonicity proof and its mixed-sign mathematical
extension are derived in this child, not attributed as the paper's theorem.

The auxiliary identity in proof section 4 is derived exactly. Its relation
to the old matter-coupling construction should not hide a source change:
[Hassan and Schmidt-May, arXiv:1804.04671](https://arxiv.org/pdf/1804.04671),
section 2, places matter on the auxiliary/third metric before elimination;
its sourced solution is not obtained by setting the auxiliary field equal
to the unsourced linear sum. Here matter instead remains on the original
EH fields, as in the new determinant paper.

## Immutable local inputs

Paths below are relative to this child root, except `P8/...`.

- `../star/certificates/star-no-bounce.json`: S6.16, including the exact
  rational boundary and independently checked coframe/clock primitives.
- `../trimetric/certificates/auxiliary-parent.json`: S6.13 literal old
  auxiliary action and arithmetic ancestry. No old physical-u source
  placement is silently carried into the determinant model.
- `P8/s6/FORMULATION.md`: the adopted conditional V/G/B matching contract.

Both reports and the contract are SHA256-pinned. STAR's own ancestry is
replayed recursively. The separate independent engine imports only exact
arithmetic helpers from this frozen lineage, not new primary formulas.
No mutable TREE report, general graph theorem or original C/D matching
assertion is used. New sources and the reserved covariant audit are hashed
without changing either parent certificate.
