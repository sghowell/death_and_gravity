# Source and independence boundary

The source-aware metric equations and literal transverse-shift polynomial
come from the pinned S6.20 report `variable-beta-local.json`, SHA-256
`335cd52028baf56b30c75377aa7e6edd7ec86db2799f49b13f467242674fc4d0`.
The actual all-beta interaction coframe variation, rather than a scalar
health formula for a different background, supplies the f null stress.
The frozen `model.py` and `tensors.py` modules are read-only inputs.

Exploratory G1 motivated the question but is not a certificate dependency.
Its radical lapse, Taylor fixtures, clock-positivity proof and asymptotic
mass formula are not used to infer this theorem. The geometric argument
allows all five beta coefficients within its explicit source/sign class.

The written proof derives the integrating factor, tail primitive and
geodesic affine/proper-time integrands directly. No external singularity,
inextendibility or UV theorem is invoked. The exact calculations check
the source and differential identities; the continuous all-time conclusion
uses the written sign, integration and continuation argument. This is not
proof-assistant formalization or an exhaustive classification of bimetric
backgrounds.

The separately authored audit reconstructs the literal coframe and
geometric identities independently. Any finite parameter fixtures are
controls of those identities, not substitutes for the continuous proof.
