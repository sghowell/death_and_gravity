# S6.22: regular local constraints and complete vector reduction

This child addresses the quadratic constraints of the **same** actual
variable-coupling action and local solution frozen in S6.20. It does not
depend on the separate tensor-response or variable-lapse explorations.
The adopted S6 matching contract and original physical metric `g` are
unchanged. Original P8 remains open.

## Precise domain and convention

Use `+---`, `R_B=-6(DH+2H^2)`, two Einstein terms `-M^2 R_B/2`, and
the canonical interacting clock `phi` plus genuinely free canonical `chi`
on `g`. The potential is `-2 sum beta_n(phi)e_n(sqrt(g^-1 f))`; the
coefficients are dynamical-clock functions, not externally prescribed time
functions. The actual S6.20 branch has `N_g=1`, **constant** `N_f=c`,
`a=(1+u^2)^2`, `b=2/a`, `u=T/tau`, and `|u|<=1/10`. Admit `c=1` or
`2<c<=4`, `M,tau>0`. All scalar/vector results concern nonzero Fourier modes.

The code uses dimensionless coordinates `u=T/tau`, `xbar=x/tau`, normalized
scalars `phi/M,chi/M`, and `K=(tau*k_com)^2>0`. The full dimensionless
action has the positive overall factor `(M*tau)^2`. Frequencies in `u`
restore as `omega_T=omega_u/tau`, and physical spatial momentum is
`sqrt(K)/(tau*a)`. A coordinate shift potential restores as `B_T=tau*B`.

## Certified target

1. A literal quadratic scalar ADM action with all four lapse/shift
   equations retained, and its exact Hamiltonian Legendre bridge.
2. The regular gauge `delta_phi=0`, `E_g=0`, allowed by the positive clock
   speed, followed by a time-dependent canonical map that never divides
   `H_g`. Its remaining scalar constraints form one nondegenerate
   second-class pair: the secondary coefficient obeys `D<-1/8` throughout
   the stated background box, for every finite `K>0`. There are exactly
   three reduced scalar canonical pairs in this quadratic nonzero-mode
   chart. This is a rank statement, **not** scalar health.
3. Exact recovery of both lapses/shifts, physical metric observables, and
   finite-momentum Cauchy/symplectic maps. At the bounce the specified
   physical Cauchy map is invertible for every `K>0` on `2<c<=4`; it extends
   to a neighborhood of the bounce for each fixed compact momentum band.
   No all-momentum uniform neighborhood is inferred.
4. Literal transverse pullback/relative-root vector action, exact two-shift
   reduction, its canonical normalization, and formal principal speed
   `c_V^2=(c+y)/(2y)`, `y=b/a`. For `2<c<=4` its kinetic coefficient is
   positive but its cone is wider than `g`; `c=1` has a negative formal
   high-momentum kinetic coefficient. No below-cutoff growth or light-only
   matching exclusion follows just from these full-parent statements.

## Essential scalar limitation

The physical observable map contains time- and momentum-dependent phase
mixing. Its determinant's second time derivative grows linearly with `K`
at the bounce, while its value tends to a nonzero constant. Its physical
observable coordinates do not commute away from the bounce. Consequently
neither an instantaneous Hamiltonian block nor a frozen center polynomial
is promoted to a scalar no-ghost/characteristic theorem. Exact omission and
nonuniform-map controls are part of the replay.

A uniform scalar high-frequency reduction, positive energy/gradient
analysis, finite-band physical matching error, rolling mass gap, nonlinear
stability, cutoff, loops and UV completion remain unproved. No result here
is transferred to the different variable-lapse G1 action. The completed
scoped photon objective and frozen linear classification are unchanged.
