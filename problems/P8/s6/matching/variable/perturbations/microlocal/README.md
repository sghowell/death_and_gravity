# Uniform local scalar principal reduction

This isolated child continues the scalar problem left open by frozen
S6.22.CONSTRAINTS. It changes no parent action or certificate.

P8-S6.24.MICROLOCAL proves a uniform high-frequency reduction through the
bounce for each fixed positive lower bound on `c-2`. The literal scalar
action and the exact physical Cauchy chart `(pi=-e/K, xi, chi_B)` agree,
including every time-boundary and normalization derivative. Exact rational
identities, a constructive inverse threshold and 611 continuous Bernstein
coefficients establish positive principal kinetic and gradient forms and
a Fourier energy estimate uniform above that threshold.

The center squared scalar speeds relative to the actual matter metric
are `1,1,(9c²-22c+144)/120`; the last is greater than `17/15` for
`2<c<=4`, and equals `5/3` at c=4. This is a full-parent principal result,
not all-band or nonlinear stability. The fixed-positive-`c-2` limit is not
the joint shrinking-window/heavy-mass limit. No cutoff, light-only
matching, quantum or original-P8 closure statement follows.

The precise scope is in `FORMULATION.md`, the proof in `notes/review.md`,
and the primary-source comparison in `notes/sources.md`. The read-only
replay is `python -m p8_variable_microlocal.verify --check` with the P8
source directories on `PYTHONPATH`. The certificate inventories the
independent audit in `tests/test_microlocal_independent_audit.py`.
