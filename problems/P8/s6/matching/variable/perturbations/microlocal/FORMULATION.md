# Scalar principal reduction of the specified variable-beta family

Claim: P8-S6.24.MICROLOCAL. The exact replay and independently audited
written proof have the following scope.

The physical action, curvature convention, free chi and sourced canonical
clock are those of frozen S6.20.VARIABLE and S6.22.CONSTRAINTS. This child
adds no operator and makes no change to either certificate. It considers
only `2<c<=4`, `|u|<=1/10`, with the same constant f lapse and reciprocal
scale factors. The B-sector convention is +---, `R_B=-6(Hdot+2H²)`,
positive Einstein kinetic coefficients and EH `-M² R_B/2`.

For each fixed exact `delta_min in (0,2]`, on the continuous box
`2+delta_min<=c<=4`, the theorem is:

1. The exact three-scalar Cauchy chart `(pi=-e/K,xi,chi_B)` is regular
   uniformly through the bounce for `K>=2000000/delta_min²`.
2. Its exact generic rational symbol has uniformly bounded lower-order
   remainders and principal action `q'^T K_s q'/2-K q^T G_s q/2`, including
   all time-boundary and normalization derivatives.
3. With `Pi=(a²yP/2)pi`, both principal matrices are between `(1/4)I`
   and `64I`, proved on the continuous coefficient box by 611 positive
   Bernstein coefficients. The exact high-K evolution has a uniform
   Fourier energy bound at each fixed delta_min, with finite explicit-
   formula C_delta, not a reported numerical optimal energy exponent.
4. At the bounce the squared physical scalar speeds are
   `1,1,(9c²-22c+144)/120`; the last is strictly greater than `17/15`
   for `2<c<=4`. It equals `5/3` at c=4. Positive principal kinetic and
   gradient forms therefore do not imply full scalar subluminality.

The theorem is a formal full-parent high-frequency result on a specified
local background. It is not an EFT cutoff, a uniform joint c->2 theorem,
finite-band light-only matching, a rolling spectral-gap calculation, an
all-band/nonlinear/quantum stability proof, or a P8 closure result. The
singular c=2 edge is used only for extended principal-coefficient bounds.
All full equations and actual finite-c parent fields exclude that edge.

The completed scoped photon objective and frozen original linear
classification are unchanged; original P8 remains open.
