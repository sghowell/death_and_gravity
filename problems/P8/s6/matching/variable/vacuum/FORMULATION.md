# S6.28 source-pinned local-field-domain vacuum audit

Claim: P8-S6.28.LOCAL_VACUUM. The source-pinned certificate includes the
independently reviewed proof and separately authored audit. Frozen S6.20
and the adopted S6 contract remain unchanged.

Take precisely S6.20's action: equal positive constant Einstein coefficients
M², physical metric g, two positive canonical g scalars phi and chi, free
chi, and interaction `-2 sqrt|g| sum beta_n(phi) e_n(sqrt(g^-1 f))`.
The B-sector convention is +--- with EH `-M² R_B/2` and
`R_B=-6(Hdot+2H²)`. No separate scalar potential or cosmological offset is
added. The actual beta2 and beta3 vanish.

For each fixed action parameter `c=1` or `2<c<=4`, define the local field
interval by `phi=M integral_0^u sqrt(kbar(v,c)) dv`, `|u|<=1/10`, exactly
as in S6.20. The coefficients are `beta_n=M² b_n(u(phi/M),c)/tau²`.
This inverse is analytic and one-to-one on the specified field range.

Consider every positive proportional root r, independently of c and of the
rolling ratio y(u): `g=eta`, `f=r² eta`, constant phi in that field interval,
constant free chi and no additional source. Coordinate rescaling of the
constant g coframe does not change the equations. The vacuum parameter u
is only the label of the constant clock value, not vacuum time or a proposed
history from the bounce to the vacuum.

The conclusion is that no such regular proportional Lorentz-invariant
Minkowski vacuum exists in this local field domain. All four metric
components and the clock equation are retained. On the unique positive
root of the exact f-flat equation, the dimensionless g potential residual
obeys the continuous strict bounds

```
U_vac=b0+3r b1 < -123/25                  (c=1),
U_vac=b0+3r b1 < -459/800                 (2<c<=4).
```

The actual g density is `2M² U_vac/tau²`. These are conditional residual
gaps after the f equation has been imposed exactly, not a generic
multi-equation error bound for approximately flat configurations.

This fails the algebraic vacuum prerequisite of the adopted V test within
this action's stated field domain; it is not a positivity-amplitude or
spectrum verdict. A smooth off-interval extension is permitted as a distinct
candidate by the S6 contract. No global analytic continuation is assumed,
and no absence claim is made outside this interval, for changed coefficients
or sources, nonproportional configurations, or other parent actions. The B
matching obligation remains separate; no global scattering history between
the two backgrounds is required. Original P8 is not closed by this result.
