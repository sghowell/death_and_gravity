# Unchanged source, continuous domains and original-field interfaces

The parent is S6.35, report SHA-256
`c77d6d3e41d81c5d2bda33aeba201fe438620d872f3e9f2d49fde5469194641e`.
It recursively rebuilds the exact own-f background and the original
action/physical-frame ancestry. This child edits none of those inputs.
It does not import the earlier coefficient-80 matter loading or a pulse
whose duration scales with sqrt(delta).

`bridges.py` checks the positive b,N bounds against the actual inherited
Fraction enclosure, verifies the full loading and propagation domain
subsets, and equates the complete remainder and pump caps. It then derives
the source sqrt(k)*(V+pump) from the literal parent's spring/sqrt(k).
Thus neither the positive source coefficient nor the absence of forcing
after the load is a hidden choice of homogeneous data.

An independently authored Fraction calculation in `audit.py` reconstructs
the Green constants, the sharper loading floors, the new central and tail
integrals, both error losses and the scalar Q separation. Eighteen exact
interfaces compare these values and the literal source weights with the
primary modules. Fourteen domain/premise checks tie the written proof to
the actual branch and its finite coefficients.

The independent real phase algebra computes the matrix in two real
phase coordinates and verifies the first-component amplitude, norm and
constant-mixing cancellation. Separate tests bridge it to the frozen
time-frequency transfer and both exact endpoint wave frames. The actual
inverse map's first row is (sqrt(r/k),0), so the scalar theorem concerns
the original hidden amplitude. The k_u term is still required in the
full state, and physical velocities obey Q_u=tau Q_T.

Ordinary parameter convergence is used only on the fixed punctured
loading interval and at the fixed nonzero endpoints, where D has an
explicit positive lower bound. The central transfer uses a fixed error
neighborhood instead. Tests include admissible opposite and alternating
comparison errors: neither a one-error loss nor assumed vanishing error
is sufficient justification. The sharper fixed loading floor protects
strictness when passing to the punctured limit.

The exact parameter sequences use the positive-real asinh inverse. Their
domain guard n>=2 is proved by positive-series rational bounds; it does
not supply a rate for convergence of the loaded data or assert that the
asymptotic endpoint gap already holds at that index.

Physical calibration records T_initial=-tau L, T_load=-tau a,
T_observe=tau a, and total duration 3tau/200. The Q gap has the units of
the dimensionless anisotropy itself. Its limsup/liminf bound is not a
pointwise positive lower bound on Q for every delta. The canonical Y_T
loading floor is Y_u's floor divided by tau, not an asserted Q_T floor.

The report pins all sources and scientific tests, recursively replays the
parent, and rejects invalid scalar, state-domain and phase inputs. This
is a written continuous theorem with independent exact calculations, not
a proof-assistant formalization or a source-matched physical EFT verdict.
