# Source, normalization and claim boundary

The action and actual local background are exactly frozen
`../FORMULATION.md` and `../notes/proof.md` relative to this child root.
The pinned report is `../certificates/variable-beta-local.json`, SHA256
`335cd52028baf56b30c75377aa7e6edd7ec86db2799f49b13f467242674fc4d0`.
Its complete read-only verifier is replayed, including the adopted S6
contract, curvature dictionary, source-aware action and canonical operator.
No new assumption is inferred from its old constant-forcing `5/6` control.

The earlier model source, [Wood, arXiv:2501.16442v2, section 4.1 and
Appendix B](https://arxiv.org/pdf/2501.16442), motivates variable interaction
coefficients but does not prove this response theorem or a UV completion of
the specified fixture. The parent's source audit fixes its factor-two
interaction and actual-matter-frame dictionary. Here the physical metric
remains `g`; its signature is `+---`, `R_B=-6(DH+2H^2)`, and the Einstein
action is `-M^2 R_B/2`. No opposite photon-track convention is imported.

The inner special-function identities were checked directly against:

- [DLMF 15.10.21](https://dlmf.nist.gov/15.10.E21), the hypergeometric
  z=0/z=1 connection, with `a=-1/2,b=3/2,c=1-i mu`;
- [DLMF 14.3.1](https://dlmf.nist.gov/14.3.E1), the Ferrers representation
  for real arguments in `(-1,1)`, if the Legendre notation is used;
- [DLMF 5.5.1 and 5.5.3](https://dlmf.nist.gov/5.5), Gamma recurrence and
  reflection, fixing both the sign of `B=i/sinh(pi mu)` and the modulus
  `|A|^2=coth(pi mu)^2`.

All arguments and branch choices are stated in the proof. The power-basis
orientation and log-delta phases are rederived from the physical variable
`u`, not copied from a scattering-probability convention. The basis is a
classical ODE normalization, not an assertion about particle number or a
chosen quantum vacuum.

The exact coupled chart transformations, continuous coefficient bounds,
Volterra estimates, fixed physical-g loading operator and prepared-state
controls are derived here from the pinned action. Their scope is ordinary
linear ODE response on a specified local background. Neither DLMF nor the
model source supplies a low-temporal-band heavy-mode elimination theorem.
No novel-priority claim is made for matched asymptotics, Poeschl--Teller
scattering or source controllability. No rolling tensor gap, scalar/vector
health, finite-loop error, cutoff, original C/D physical operator match or
UV consistency verdict is asserted.
