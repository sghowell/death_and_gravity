# Actual selected-state remainder after four time primitives

The actual homogeneous current and its first variation are already defined by the S192/S193/S194 convergent fixed-prescription comparison; the S217/S219 branch-corrected spatial assembly agrees with that homogeneous anchor. Here we prove a stronger time-kernel normal form at zero transfer. We do not infer it from the C12-to-C0 or H13-to-weak norm estimate.

## Finite high-frequency expansion of the actual modes

The background is the unchanged smooth CD profile a=(1+t^2)^2 on a compact slab, with positive mass1000 and a bounded away from zero. The transverse and longitudinal canonical frequencies, pumps, normalization and constrained vertices are smooth rational functions of k^2 and the background jets. At fixed sufficiently largek their expansions are classical inverse-power expansions, with the original common leading phase k sigma(t).

For any chosen finite number of time derivatives, take a sufficiently deep finite WKB/Riccati expansion. Variation of constants bounds the exact evolved normalized modes by that expansion plus an arbitrarily inverse-power-small error at physicald3. The all-order Cauchy preparation supplies the matching initial error. It does not replace the actual state by W8, reset its initial interference, or evolve a new flat vacuum. The available finite residual estimates apply on the whole compact slab.

The product of two modes at k,-k, including a mixed T/L pair, has leading phase2k Delta_sigma. Expanding its residual phase and full physical vertices to the desired finite depth gives a finite sum of smooth time coefficients times integer powers ofk multiplying sine or cosine, with radial degree at most4. The degree4 coefficient is exactly the full leading pair calculated in notes/leading.md. Increasing the expansion depth makes the remaining radial integrand and every desired finite time derivative absolutely integrable. The bounded low-k interval is smooth, with the original regular constrained limit at k0. This argument uses time derivatives and finite mode expansions; it does not assume an unproved momentum-derivative bound on the cutoff-summed state.

In the original analytic-dimensional neighborhood, the unchanged W4 plus dimension-independent Borel correction has initial mixing O(k^-6). With the leading k^4 radial factor, the state-comparison remainder is O(k^(-2+|Re(d)-3|)), locally uniformly in a sufficiently small dimension neighborhood. It is integrable. At physicald3, deeper all-order matching supplies the arbitrary fixed time differentiability used above. The finite-dimensional limit is therefore taken in the original prescription, not by changing a finite local coefficient to fit a target.

A simultaneous time derivative Dt+Ds of the leading phase inserts
k[sigma'(t)-sigma'(s)], whose bracket has a factor t-s. A lag factor compensates the additional radial degree in the Abel distributions. Taking deeper finite expansions for each fixed derivative count preserves the same singular class for simultaneous kernel derivatives.

## Fix the highest contact before lowering the singular order

The actual degree4 singularity differs from its massive flat shear reference by a smooth coefficient vanishing on the diagonal, as shown in notes/leading.md. All other expanded radial terms have one or more lower singular orders. The reference is the COMPLETE original F2, not a bare logarithm: its finite coefficient, massive cut, subtraction polynomial and isolated inverse pole are unchanged.

Near the diagonal, a dimensional finite part is defined by subtracting the required Taylor polynomial of the test function in the same regulated radial/lag integral. Multiplication of the order-five causal singularity by a smooth diagonal-vanishing factor reduces it to order at mostfour. In particular, a possible delta'''' contact from the leading extension is reduced to at mostdelta'''. Matching the leading coefficient for every nearbyd, including its first jet, is what excludes an unspecified extra fourth-order finite contact. Equality only at physicald3 would be insufficient.

The original complete finite local action supplies exactly the fourth coefficient-1/30 in proper time. Its remaining curvature and mass terms have order at most3 after that coefficient is separated. The original second Hamiltonian vertex, normalization and metric-chart contacts remain in the fixed current; before subtraction they are local and of order zero in this homogeneous source. Their prescribed finite extensions and all lower adiabatic terms are included, not set to zero. No new delta'''' is available after the full-dimensional leading match and the original finite local coefficient have been fixed.

Thus the difference between the complete actual quantum operatorT_Gamma and F2(Dt^2)Dt^4 consists of fixed causal finite parts with off-diagonal powers tau^-n for n<=4, logarithms, an ordinary integrable remainder, and local derivatives of order at most3. All smooth amplitudes and their required diagonal derivatives are bounded on the compact triangle.

## Four zero-past primitives

Apply I4 in the OUTPUT time, keeping coefficient order. Four primitives of the fixed finite part tau^-n, n<=4, are multiples of tau^(4-n)log(tau) plus ordinary smooth polynomials. For a variable smooth amplitude, expand about the diagonal through the required finite order; its Taylor terms have exactly those primitives and the remaining vanishing factor makes the integral ordinary. The k^-1 radial term has at most a logarithm, as seen by splitting at1/Delta_sigma and integrating its oscillatory tail. The absolutely integrable mode remainder stays ordinary after I4.

Local derivatives through order3 produce bounded polynomial kernels with time-dependent coefficients differentiated on their actual side, as proved in notes/primitives.md. The common nonempty zero initial neighborhood removes lower preparation endpoints; no upper retarded endpoint or physical contact is dropped.

It follows for the ACTUAL complete homogeneous quantum response that

I4 T_Gamma=F2(Dt^2)+V_Gamma,

where, for every fixedj, there is a finite constantCj with

|(Dt+Ds)^j V_Gamma(t,s)|<=Cj(1+|log(t-s)|).

The constants are actual compact-triangle majorants of the constructed kernels, not freely chosen physical coefficients. No numerical Cj is claimed. Adding the actual classical tensor tree only adds the explicit lower local primitive kernels, so the same assertion holds for V_total in
I4 T_total=F2(Dt^2)+V_total.
The constants for the total operator retain the full kappa-dependent classical coefficients.

This is a homogeneous time-kernel proof. It supplies neither a uniform nonzero-transfer bound, a same-H^r estimate for the full spatial current, nor a nonlinear metric-neighborhood remainder.
