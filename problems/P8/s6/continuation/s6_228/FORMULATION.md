# Actual homogeneous tracefree quantum-plus-classical equation

Keep the original CD slab t in[-1/2,1/2], a=(1+t^2)^2, Proca mass1000, kappa1e800, canonical constrained measure, all-order Cauchy state, fixed physical mu=m prescription and common nonempty zero initial source neighborhood. Work in proper time with the per-unit-comoving-volume, zero-external-momentum homogeneous response.

For a unit-Frobenius exponential tracefree metric amplitude gamma, letT_Gamma be64pi^2/a^3 times the actual retarded metric-current derivative. Rotational invariance gives five identical tracefree time operators, with no linear scalar mixing at zero transfer. The original canonical chartgamma=2h/sqrt(kappa) gives quantum forceT_Gamma h/(16pi^2 kappa).

The full constrained leading pair in spatial dimensiond is

C(d)=(2d^2+d-8)/(2d(d+2)),
C(3)=13/30, Cprime(3)=91/450.

The correctly phased leading off-diagonal kernel has coefficient52/5 divided by a(t)^4 a(s) Delta_sigma^5, where sigma'=1/a. Its full-dimensional proper-time diagonal matching agrees with the original F2(Dt^2)Dt^4 reference, including the finite fourth coefficient-1/30. The actual unchanged selected-state remainder and all original contacts are retained.

LetL=Dt^2+3H Dt and

T_total=T_Gamma-16pi^2 kappa L.

For every fixedj the ACTUAL normal form is

I4 T_total=F2(Dt^2)+V_total,
|(Dt+Ds)^j V_total(t,s)|<=Cj(1+|log(t-s)|),

with finite compact-triangle constants. The completeC0 includes the classical kappa terms. The arbitrary fixed regularity uses the actual all-order state and simultaneous time derivatives; it is not inferred from a derivative-losing weak norm.

LetK2 be the original S223 pole-plus-cut inverse ofF2, andK=||K2||L1(0,1). K is finite, while the full kernel is not half-lineL1. ForC=C0 choose

lambda=(4KC+1)^2.

Then||K2 V_total|| in the weighted continuous space is below1/2. For every smooth prepared normalized sourceg there is exactly one smooth prepared solution

h=(I+K2V_total)^-1 K2 I4g,
||h||C0<=2exp(lambda)K||I4g||C0.

For canonical forcingf in-Lh+T_Gamma h/(16pi^2 kappa)+f=0, g=-16pi^2 kappa f and the bound retains32pi^2 kappa exp(lambda)K times||I4f||. Time-shift regularity and the complete causal initial identities prove both inverse identities for the original unintegrated total equation.

These are finite bounds in unevaluated actual majorants, not numerical constants, small feedback or stability. No pole, contact, initial state or higher-order mode is deleted. No final-time condition or physical momentum cutoff is introduced.

The theorem is homogeneous and linear. It does not provide a uniform nonzero-transfer inverse, the full S222 scalar/clock/matter graph, a nonlinear or finite-coupling common-parent remainder, physical stability/cutoff, original V/G/B or P8 closure. The continuum proof is written, not Lean-formalized.
