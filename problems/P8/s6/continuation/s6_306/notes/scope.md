# Scope and unchanged original obligations

This packet closes one explicitly identified estimate: the complete known
C/kappa plus g^2/kappa physical-region hard graph contribution, normalized
by the same full positive Born. It does not re-derive or relabel the
previous complete graph enumeration as new. Its new box identity and
physical continuation remove the need to assume a subthreshold bound
also controls a scattering cut.

The finite local, curvature and RH coefficients are unassigned, as are
higher-derivative physical matching data. The bound applies to the named
known graph representative only. An arbitrary matching addition can
change the full rate. Reference E is not the physical calorimeter cut;
the established finite conversion and leading-soft resolution factor
remain explicit in a later assembly. No uncomputed loop square or
all-order nonleading radiation bound is imported from this estimate.

All9 original primitive records,161 previous matching records and all6
historical qualifications are preserved; exactly one matching record is
appended. The rejected S279 archive remains rejected. No frozen ancestor
or original source is edited. Scoped P8(a) remains unchanged.

Original V requires an actual healthy common quantum state and its
analyticity/unitarity/error premises, not just finite-order identities.
G still requires the fixed-negative-transfer dispersion construction,
absolute Regge/IR remainder and correct combined pole/contour forward
limit. B still requires the same parent, state/domain, canonical maps,
heavy-spectrum/cutoff and omitted-term bounds. The bounded finite graph
piece does not close any of these missing premises.

Original V/G/B/P8 remain OPEN. This is symbolic and analytical research
with independent checks, not a machine-checked proof of a UV theory.
