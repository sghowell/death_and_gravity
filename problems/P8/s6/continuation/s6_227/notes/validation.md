# Validation boundary and independent checks

The package checks31 named residuals containing33 scalar entries,24 proof gates,9 controls and184 rejected unsupported inputs. There are83 matching obligations and the same9 original primitive rows. The manifest has17 immutable scientific inputs:6 package modules,2 tests,2 root documents and7 proof/scope notes. The native report has20 top-level fields, each covered by a rejection test for mutation; missing and additional fields are also rejected.

The independent science tests include the complete arbitrary-coefficient Euler-Hessian identity and its nonzero density boundary; direct matching to the original finite alpha/gamma coefficients; interval bounds on the unchanged background; source-time rather than output-time adjoint reciprocity; and all ordered terms of the bounded conjugate and finite local inverse. The initial derivative test has nonzero source value at the left endpoint and detects omission of its initial delta.

Reciprocity is diagnosed both with exactly solvable constant-coefficient matrix kernels and nonconstant sixth-order Taylor jets. Finite causal4x4 block solves at q=0,7/5 and10000 check noncommuting time-coefficient order, left and right inverse products, and failures caused by deleting J, deleting the full local remainder or reversing a Neumann product. These finite jets and block matrices are diagnostics, not certificates of convergence of a continuum numerical scheme.

The continuum conclusions require the written arguments: full density integration by parts; retarded/advanced Green reciprocity; the source-time derivative bound from reversed primal Volterra control; the complete initial-delta cancellation; Young bounds on weighted causal kernels; spatial Fourier and Sobolev completion; bounded-causal Neumann convergence; and the explicit composed-graph existence and uniqueness argument. Exact symbolic zeros alone do not establish these analytic statements. The composed graph is deliberately specified rather than inferred to be a maximal distributional domain.

Private drafts and independent probes are checked before the17 source files are frozen. The native rebuild must use original SymPy, replay the source-pinned predecessors, check the complete manifest, and emit the report without editing any input. A fresh direct science run, ordinary package replay, read-only command-line replay and complete P8 regression are required before publication. Only the full regression uses the previously audited exact-GCD adapter; native, direct, ordinary and CLI paths retain original SymPy. Actual outcomes and any private-draft corrections are recorded in the external publication assessment, not by modifying frozen inputs.
