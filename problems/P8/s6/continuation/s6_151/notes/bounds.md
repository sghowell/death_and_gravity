# Finite constant and all-radius converted-family bounds

In the sector variables 0<=r,t<=1, B00<=3 and

    0<=D=(1+t+t^2+t^3-r t^2)/[(1+t)^2(1+t+rt)^2]<=4.

Also log(1+t)<=1. If U=log v-2log w then

    |U-log(1+t)|/r
      <=t/(1+t)+2(1+t)<=9/2,

using log(1+x)<=x. Thus the absolute R'(0) integrand is
bounded by 4|log r|+4+27/2; its double integral is <=43/2.
The endpoint coefficient |P''(0)/2|<=1/2 follows from
0<log2<1, so |j|<=22. These estimates also give an explicit
dominating function at the finite coefficient.

For the full forward disc, the inherited routing has |z|<=3
and x(1-x)<=1/4. The logarithm series therefore gives
|I_R(P)|<=log4/Q<2/Q. Together with |C(P)|<2L, the sum of
three additional core scale terms is bounded by
12 ell L^3/Q^2.

The same full-propagator routing gives

    |H(q)|<20L^2 g/(y+M), Delta real part>=1/4.

The exact all-radius triangle integral is
M log(4M)/(M-1/4)^2-1/(M-1/4)<2 ellH/M,
where ellH=log(4M), at the actual M>32.
Using g/M<L/3 and all three assignments bounds the additional
heavy scale term by 40 ell ellH L^3/Q^2. No finite cutoff or
large-M expansion enters.

The already-proved old-family bound is
[240+40 ellH(ellH+4)]L^3/Q^2. The conversion's remaining
local term has EXACT relative b2 shift L^2 F (reference note).
With pi<4,

    |F|< [ell^2/2+ell+22+4/3]/Q^2.

Use ell=400log10<1200, ellH<600 and Q>144.
The added scale bound is about 1.66616720420620e-611;
the local-reference b2 bound is about 7.28832000512975e-1008.
Adding the old 8.38232261379801e-612 gives

    E_wineglass,MS<2.50439946558601e-611<3e-611,
    E_wineglass,MS/(4lambda)<6.26099866396502e-12<8e-12.

The second-coefficient bound uses Cauchy's radius-one estimate
on the COMPLETE added finite scale function, with no extra
factor 1/2. The local term instead uses its exact heavy-tree
coefficient. Every certified numerical comparison is rational;
the decimals are diagnostic displays only.
