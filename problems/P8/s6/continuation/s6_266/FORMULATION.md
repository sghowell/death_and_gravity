# Full local nonlinear constraints, unchanged parent

Fix u=0 and the complete original S257 canonical Hamiltonian, its
full current R,F, both profiles, primitive time contact, normalized
heavy source, all three vacuum constants, kappa=10^800, zeta=10^-6
and the original H/Proca and S251 preparations. No state is used to
solve this classical constraint and none is changed.

Use the twelve normalized density/spatial-jet coordinates
p,G,pm-1/10,ph,eta=10^100 H,shear,electric/zeta,zeta*magnetic,
vector_mass,grad_M1,grad_H,curvature, each of absolute value at most
10^-250. The actual mass ratio mH^2/10^200 is in[10^-4,10^-2].
These invariants are not independent canonical modes.
Their signed analytic box contains the physical invariant subset;
it does not solve the remaining spatial momentum constraints.

There is exactly one lapse root in |N-1|<10^-245. It is unique on
the larger verified strip |N-1|<=10^-6. The explicit iteration
N_(k+1)=N_k+C(N_k,z)/3, N0=1, contracts by1/30.
At the unchanged reference center z=0 its distance from1 is below
10^-375. Neither this shift nor its sign is assumed nonzero.
No external force or change of reference matter momentum is inserted.

The complete normal-vector root obeys |T|<3*10^-250.
The temporal auxiliary pivot is between-2 and-1/2, and the lapse
Schur pivot is between-31/10 and-3. The full auxiliary Hessian is
negative definite with determinant>3/2. The branch is uniformly
second class on this finite-jet domain.

Every individual first, second and third invariant derivative of
the full lapse is bounded by2,2000 and10^7 respectively.
All mixed implicit and nonlinear canonical-invariant chain terms
are retained. The full physical volume is exp(3vhat)R_full(Nstar)^(-3/4);
its second variation includes both nonlinear lapse and metric-chart
second contacts. It is not replaced by its linear-lapse composition.

The complete D=M/N,Q=N/U,U and physical C coefficients are positive
and bounded between1/2 and2 on this branch. This is not a quantum
operator-domain theorem. In particular it neither assigns the
original Gaussian to these nonlinear invariants, confines it to the
branch nor removes S265's distinct Gaussian-coefficient divergence.

No physical quantum ordering/gauge regulator, interacting mean,
covariant subtraction, loop-remainder bound, Wilsonian cutoff,
nonlinear inhomogeneous evolution or global completeness theorem
is established. Original V/G/B/P8 remain open.
