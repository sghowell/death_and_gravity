# Independent validation and immutable evidence

Native, direct, ordinary and CLI calculations use original SymPy.
Only a separately captured complete P8 regression may use the
independently audited exact-GCD adapter and its original-path
selfchecks. All source/report bytes are frozen before native
construction, and original ancestors are never repaired in place.

The exact packets bind the full original Hamiltonian before the
bounce-slice variation, retain the primitive second-time contact,
evaluate the full mass-adapted domain, and check all mixed implicit
and physical chain formulas. The27 named residuals contain331
scalar entries, including matrix entries; they are not331
independent scientific theorems. There are45 proof gates,
nine controls and291 rejected inputs. Nine primitive and122
matching frontier rows remain.

Independent finite fixtures keep both switch structures, all
spatial/vector/matter invariants and a visibly nonzero normalized
H force. Their modest order/localizer and deliberately finite
coefficients are diagnostics, not a retuning of the actual parent.
They solve the complete scalar constraint by an independent
bracket method, check the full temporal Euler equation, vary the
entire independently transcribed Hamiltonian including its
integrated primitive, and test every invariant root response.

Additional checks include the full auxiliary Schur Hessian,
nonlinear invariant-chart pullbacks and exact known nonlinear
implicit curves with every third contact. The source-only center
bound is explicitly distinguished from the inherited interval
engine's100-bit positive-root width.

The initial private pilot stopped when a direct subtraction of
independently enclosed O(1) root factors could not resolve the
10^-245 target. A complete centered mean-value bound fixed the
proof route without changing the source or frozen interval engine.
The corrected full-source pilot passed, then the extended
third-derivative pilot passed.

The first numerical subset passed114 tests in446.65 seconds.
Its repeated full-expression coefficient compilation was then
replaced by one shared source-jet calculation and small kernels
of the exact same abstract Hamiltonian/constraint expressions.
The numerical tests plus the added contract checks passed432 tests
in15.99 seconds, with five exact packets deselected.
The final all-science preflight must still include those packets.

One contract-only diagnostic omitted the standard explicit4000
recursion allowance and stopped during ancestry import.
The corrected runtime import passed all291 rejection checks.
This was not a scientific source correction.

Final private and fresh repository preflights, exact source hashes,
native raw report transfer, ordinary/CLI zero exits and the captured
full regression are recorded in the external assessment only after
they have completed. Provisional research files are outside the
scientific source manifest. No frozen source or raw JSON report
is edited to match a later desired claim.

Written interval, contraction, functional and chain arguments are
not FORMALIZED. Tests of finite examples do not prove an interacting
quantum state, a continuum regulator or original P8 closure.
