# Original physical volume on the nonlinear lapse branch

The physical spatial-volume density is exp(3vhat)U(Nstar),
U=R_full^(-3/4), not the refuted shifted-R factor.
For a variation with full first/second jets v1,v2,N1,N2,

    V'=exp(3v)[3U v1+U_N N1],
    V''=exp(3v)[9U v1^2+6U_N v1 N1+U_NN N1^2
                         +3U v2+U_N N2].

The packet differentiates the complete composition and checks
these contacts exactly. The full original R, R_N and R_NN are
retained; their values belong to the actual nonlinear branch.

In particular the U_N N2 term is absent if one replaces the
nonlinear auxiliary solution by only1+n_linear. The3U v2 term
also matters under nonlinear metric/canonical coordinate changes.
Both are retained here, together with the mixed v1 N1 contact.

To contract this Hessian against a separately specified ordered
canonical state, the full invariant-to-canonical map and every
source/ordering term must first be established. The twelve
invariants are not jointly Gaussian canonical variables.
Nothing here evaluates an interacting physical onepoint, proves
that a partial Wick contact is the full mean, or changes the
original S251,H or Proca preparations.

S265's entire reference observable on the LINEAR Gaussian lapse
is still precisely that reference observable, with its original
commuting-bounce interpretation and bounded mean. It is not
retroactively renamed as this nonlinear constrained observable.
The original source-only shift of Nstar at the center is bounded
but is not asserted zero.
