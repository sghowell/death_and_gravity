# Independent validation and immutable replay boundary

The science suite checks every exact residual, written proof gate and unsupported-input rejection. It retains the nine primitive rows and98 prior separate matching records. The package has67 named exact scalar checks,24 gates,nine controls and265 rejected unsupported input/scope cases.

Independent literal five-feature Hamiltonian algebra checks all trace, kinetic, gradient and mass terms and the physical a/r normalization. Direct Cartesian spherical moments test two noncommuting matrix pairs in spatial dimensions3,4,5,6, all four ordered products and five extra u powers:160 exact comparisons. The invariant formula is not used by that direct integrator.

Independent numerical time-Taylor arithmetic evaluates all SIX UNEXPANDED Riccati iterates before constructing complete physical frequencies, time derivatives, pair vertices and source endpoints. Three distinct fixtures include time dependence, unequal internal legs, trace/gradient cross terms and noninteger dimension. Two large radii give360 endpoint comparisons. An additional eight-radius Vandermonde calculation independently recovers all140 individual UV coefficients per fixture,420 coefficient checks in total, at100 decimal digits. It compares each coefficient to relative/absolute tolerance10^-25. These numerical fixtures are diagnostics, not the uniform proof, the actual state or an all-transfer remainder bound.

The complete Gamma-function sphere and MSbar measure are differentiated independently. Multiplying the full analytic radial expression by epsilon verifies cancellation of the actual pole before its derivative produces the finite part, including the entire covariant counteraction. Exact tests distinguish scalar/vector weights, nonzero dimension slopes, the complete finite counteraction and both ordered trace/gradient geometries. A missing time-jet H6 raises rather than being silently differentiated as zero.

All lower spatial UV rows, both high logarithmic source orders, all eighteen finite coefficient reconstructions, ordered Green identities and the complete rational coefficient sums are checked. The second Hamiltonian contact is explicitly nonzero but transfer-independent; only its P-minus0 difference is zero. Tests reject whole-kernel or inverse claims, dropped contacts, premature dimension freezing, new finite targets, double addition of the existing heat Hessian and original P8 closure.

Before freezing, require private science, lint/format, exact17-file/20-field preflight and the protected native rebuild. After freezing, independently run repository science, ordinary pytest, standalone CLI and the full P8 regression. Only the latter uses the audited exact-GCD adapter and S219 helper allowance. Native/direct/ordinary/CLI retain original SymPy. Frozen source, proof, test and report bytes are immutable. Publication uses only the exact21-file manifest and excludes unrelated work.

A private draft had a closing-parenthesis syntax error, corrected before any scientific freeze; the ensuing independent structural calculation exited0. Unused symbolic bindings, loop-variable captures, imports and formatting were resolved before final preflight. These exact algebraic and written arguments are not FORMALIZED.
