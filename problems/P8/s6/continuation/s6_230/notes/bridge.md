# Complete original vacuum tensor symbol, not an isolated factor

The background here is the separately specified Minkowski vacuum of the original CD-REG-AFFINE-ISO-QG1 retained mean construction. The state is its Minkowski Proca vacuum with mass1000 and the original canonical measure/prescription. It is NOT the nonstationary CD all-order state moved onto a flat metric. S182 cancels the specified vacuum constant while retaining R and the low scalar jets. At the constant scalar vacuum R=1, so the classical tensor Einstein coefficient is exactly kappa=10^800. Pure tensor metric variations do not activate the source-dependent vector/light terms at this reference. Scalar, graviton and mixed quantum loops are not included.

Use the original unit-Frobenius canonical chart gamma=2h/sqrt(kappa). The independent S177 Einstein calculation gives the ordinary TT equation -Box h+f=0. The original Proca cut and finite local density must be combined before locating poles. S223's isolated F2 zero alone is not a pole of the complete force response.

## The original fourth-order subtraction fixes the lower polynomial

At flat internal momentum k, all three physical Proca polarizations have common frequency omega=sqrt(k^2+m^2). A fixed canonical balancing makes G0=omega I6 and Sigma0=I6/2. A homogeneous spatial metric vertex is instantaneous, with arbitrary symmetric blocks deltaG=diag(X,Y). The exact vacuum covariance tangent satisfies

lambda deltaSigma=omega[J,deltaSigma]+(J deltaG-deltaG J)/2.

Writing its blocks U,V,C, the solution is

U=omega(Y-X)/(lambda^2+4omega^2), V=-U,
C=lambda(Y-X)/[2(lambda^2+4omega^2)].

The production check uses full arbitrary3x3 symmetric source and detector blocks. The detector current is

omega tr[(YD-XD)(YG-XG)]/[2(lambda^2+4omega^2)],

plus the complete original instantaneous second vertex. No longitudinal or mixed physical polarization is removed.

The derivative marker used in S192/S193 multiplies lambda. Its current coefficients of orders0,2,4 are therefore the Taylor coefficients in p=lambda^2 of this exact flat response. This also follows by uniqueness of the coefficient recurrence in the covariance equation. S193's full noncommuting variational identity identifies that marker current with the ORIGINAL J_ad4 and its original dimensional finite matching.

The instantaneous second vertex is contained in the zero-order term and is subtracted with that term. Pointwise in k,

1/(S+p)-1/S+p/S^2-p^2/S^3=-p^3/[S^3(S+p)], S=4omega^2.

Thus the convergent actual-minus-J_ad4 vacuum integral leaves precisely the original three-subtracted cut; it contributes no additional order-p or order-p^2 polynomial. This establishes the missing lower-polynomial bridge, rather than guessing it from the spectral cut.

The remaining original finite density before64pi^2 is

5m^4/2+5m^2 R_old/3-Cweyl^2/30+Euler/90-R_old^2/18.

At flat background, its TT quadratic action is 5m^2 gamma'^2/12-gamma''^2/60. Its Euler variation is -5m^2 gamma''/6-gamma''''/30. The constant has no unimodular TT variation, and the specified vacuum mean is already retuned. Compact Euler/box-R boundaries retain their original meaning.

With A2=-F2 from S223, the complete normalized tensor Euler expression is

-[p^2 A2(p)+(16pi^2 kappa+5m^2/6)p]h/(16pi^2 kappa)+f.

Define C0=16pi^2 kappa, C=C0+5m^2/6 and the physical FORCE operator

O_phys(p)=p[C+p A2(p)]/C0, O_phys h=f.

The sign is fixed by the original Einstein and finite-action variations. Its inverse is positive C0/[p(C+pA2)], not the reciprocal of the negative unsourced Euler expression.

Lorentz invariance of the original Minkowski vacuum, canonical Proca operator and covariant subtraction makes the spin2 form factor a function of p=lambda^2+P^2. Its homogeneous value therefore determines its full TT value; the S199 covariant projector/cut normalization is retained. No curved-state or physical cutoff conclusion is inferred from this flat bridge.
