# Actual full flat TT poles, low-energy bound and causal graph

Fix the separately specified retained Minkowski vacuum of CD-REG-AFFINE-ISO-QG1, canonical mass m=1000, kappa=10^800, original covariant Proca subtraction, original finite density and vacuum retuning. Do not transplant the CD all-order state onto Minkowski space. Quantum scalar, graviton and mixed loops are outside this retained sector.

Let

A2(p)=1/30+integral_0^1 p y^2(30-20y^2+3y^4)/[30(4m^2+p(1-y^2))]dy,
C0=16pi^2 kappa, C=C0+5m^2/6,
D_C(p)=C+pA2(p), p=lambda^2+P^2.

The original canonical physical force equation is

O_phys h=f, O_phys(p)=pD_C(p)/C0.

The unsourced Euler residual is -O_phys h. The physical inverse is C0/[pD_C], not the inverse of that negative residual. The full flat covariance marker calculation and original S193 matching prove that no additional p or p^2 polynomial is hidden in the actual-minus-J_ad4 vacuum remainder.

## Analytic and quantitative conclusions

For any real C>2m^2/15, D_C has exactly two first-sheet zeros: one simple upper-half-plane z and its lower conjugate. Both have negative real part. There are no real or cut-bank zeros. The real-boundary image and the ENTIRE large closing arc give this count; it is not a numerical winding inference.

For the actual constants,

10^796<|z|<kappa, Re sqrt(z)>10^393,
|1/[zD_C'(z)]|<=250/(249C),
|C0/D_C(p)-1|<197m^2/(30240 kappa)<10^-796 for |p|<=m^2.

The massless p0 pole remains. The isolated S223 A2 zero is not a complete response pole because D_C=C there. The low-p ratio estimate is not a temporal frequency restriction.

The complete reciprocal has its massless pole, both complex poles and a positive massive cut with density

rho_E=U/[(C-tau D_A)^2+pi^2 tau^2 U^2],

where A2(-tau+i0)=D_A+i pi U is the original cut. For R=1/[z D_C'(z)], its finite moments satisfy

1/C+2Re R+integral rho_E=0,
2Re(Rz)-integral tau rho_E=0.

No cut, contour, pole or finite counterterm is changed.

## Actual all-momentum causal realization

On every finite interval of length T and every real r, fix the original nonempty zero initial neighborhood. In Y_r=C_t H^r_TT use the domain of h in Y_r for which the complete original causal distribution O_phys h is in Y_r, with forward equality understood in D'_t H^(r-6)_TT.

The original Bromwich inverse, to the right of both growing lambda poles, is two-sided on this graph and satisfies

||G_phys||<=4T^2 exp(sqrt(kappa)T),
||D G_phys||<=8T exp(sqrt(kappa)T).

There is no spatial derivative loss. The explicit bounded paired forward kernel and Fourier cutoff limit prove both identities on this specified domain. The bounds also hold in L2-time, with its own correspondingly defined graph.

Smooth prepared real spatial L2 compact-time TT forcing can excite an exponentially growing response. This is an actual retained-equation obstruction to claiming absence of growing modes. It is not a statement about the validity of the conditional approximation at its very high pole scale.

## Not established

The poles are between one hundredth and one times sqrt(kappa), not above that normalization. No physical cutoff, full-parent UV inconsistency, curved CD instability, unrestricted curved S222 graph inverse, nonlinear stability, complete interacting quantum background or original V/G/B/P8 closure follows. All original primitive statuses remain unchanged.

Exact identities and arithmetic margins are replayed; the analytic matching, argument principle, contour moments, causal graph and packet construction are written proofs, not FORMALIZED.
