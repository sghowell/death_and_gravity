# Uniform zero-threshold remainder for the unchanged single-real tree

## Source and domain

Keep the original P8 source and parameters, in particularmu=nu=1 and
kappa=1e800. Use S295 recoil coordinates with hard COM energy5/4<=E<=2,
s=4E^2, t,u<0 ands+t+u=4. Include every emitted direction and both
unit-Frobenius physical TT polarizations,0<omega<=x<=1/8.
Letdelta=min(1,-t,-u). No forward angular window is discarded.

S297 supplies the positive full Born A_B=A_m+A_G. The selected tree is
the same26 matter plus21 minimal Einstein graphs established in
S295/S304, normalized with every interference before squaring.
Write its soft currentS0 at the corresponding Born kinematics.

## Results

Exact external-current regrouping gives, foromega<=sqrt(delta)/192,

|M5/A_B-S0|<2e8/sqrt(kappa).

The complete gravity subamplitude has the all-energy bound

|M_G5|/A_G<1e17*delta/[sqrt(kappa)*omega*sqrt(delta+omega^2)].

Both estimates keep all off-shell shifts and all internal emissions.
Their two-region energy integration proves that the total variation of
the normalized one-graviton real-minus-soft measure satisfies

TV(x)<(2e14*x+2e37*x^2)/kappa.

Combine this with the separately proved S304 bound1e32/kappa.
Both hold uniformly at every nonforward hard angle. In particular the
absolute error tends tozero uniformly asx->0.

For the elastic known leading-soft reference
P(x)=exp(Delta)F(alpha)x^alpha, F=exp(-gamma_E*alpha)/Gamma(1+alpha),
the inherited original bounds giveP(x)>x^alpha/2 and0<=alpha<1/2.
Splitting atx0=1/1000 proves

TV(x)/P(x)<4e32/kappa=4e-768,

and the separate uniformly vanishing majorant is

TV(x)/P(x)<[4e14*sqrt(x)+4e37*x^(3/2)]/kappa.

No expansion inalpha*lnx is made, even for exponentially smallx.

## The remaining boundary

The comparison is between the computed complete single-real finite
remainder and a named all-N leading-soft reference. It is not the
complete nonleading correction to the all-N calorimetric observable.
The latter still requires multi-real recoil and self-interaction control,
hard virtual terms, physical matching and an actual quantum limit.
There is no exchange of dimensional-regulator, forward or threshold
limits. The divergent forward Born cross section remains divergent.
All original V/G/B/P8 status entries and all six historical
qualifications remain unchanged.
