# Complete amplitude bounds after pairing

## Low-energy region

Letomega<=sqrt(delta)/192. S304's physical recoil proof gives
|D-D0|<=7sqrt(tau_j)omega and|D|>=tau_j/2 in each mixed channel.
For the common unshifted numerator, the old bounds
|N-N0|<=21696omega and|N0|<=3600 still apply: they allowed the larger
momentum shift of an individual emitting graph. Thus each quotient
difference is at most

43392omega/tau_j+50400omega/tau_j^(3/2).

Multiplying the two paired currents gives
600[43392/sqrt(tau_j)+50400/tau_j].
Since0<tau_j<=12<16 this is below134380800/tau_j.
The two mixed channels and the timelike64*100992 contribution total
at most275225088/delta.

When subtracting the Born soft amplitude, first sum the old hard
channels. Their common soft-current recoil multipliesG0 itself.
The two final scalar currents differ from their Born counterparts by
at most608 in total, so this term is608G0. No absolute separate-channel
bound is substituted for this exact common factor.

Every component ofT(k,q;0) is bounded by12omega:
the two products give4omega and the contraction at most8omega.
Since|Hnum|<=9 entrywise, the shift contraction is at most1728omega.
Use|J_i|<=16/omega and|D|>=delta/2 for all twelve mandatory shift terms;
their sum is bounded by663552/delta.

The unchanged full seagull bound is283392/delta.
S304's momentum-sensitive cubic bound is537477120/delta. This is a
bound on the entire three-cubic sum, with its derivative momenta
included, not the olderdelta^-2 estimate.
Dividing all terms byG0>8/delta gives

|M_G5/A_G-S0|
<[608+(275225088+663552+283392+537477120)/8]/sqrt(kappa)
=101706752/sqrt(kappa)<2e8/sqrt(kappa).

The matter remainder330000/sqrt(kappa) lies below the same constant.
Positive full-Born weighting proves the bound for all47 trees.

## Global direct estimate

At arbitrary0<omega<=1/8 use the exact gap
-D>(tau_j+omega^2)/30000.
The two mixed paired bases contribute at most

2*3600*1200*30000/[omega sqrt(delta+omega^2)]
=259200000000/[omega sqrt(delta+omega^2)].

The timelike base is at most46080/omega. Because
delta+omega^2<=65/64<4, it adds at most92160 in the common numerator.
All twelve exact shift terms add9953280000/(delta+omega^2).
The unchanged whole seagull and cubic bounds add respectively
4250880000/(delta+omega^2) and
201553920358318080/(delta+omega^2).

Use1/(delta+omega^2)<=1/[omega sqrt(delta+omega^2)], then divide
byG0>8/delta. The complete numerical coefficient is25194274220321280,
strictly belowD=1e17. Therefore

|M_G5|/A_G<D*delta/[sqrt(kappa)*omega*sqrt(delta+omega^2)].

This is a bound on the full physical amplitude, not a soft expansion
beyond its validity region. Joint forward-soft checks evaluate the
original21 gravity diagrams withomega proportional to the square root
of the Born transfer, on both sides of the energy split.
