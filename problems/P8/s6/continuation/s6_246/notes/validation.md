# Independent validation and immutable certificate boundary

The packet has 65 named exact identities with 200 scalar entries, 50 explicit proof gates, nine controls and 172 rejected unsupported inputs. All nine primitive rows and 101 earlier matching records are retained, with one new matching record.

The exact checks include all sixteen first and mixed metric-chart entries, both complete five-feature scalar Hamiltonian vertices, two-momentum CCR preservation, the full nonzero-transfer canonical gauge generator, every weight-one density Lie component, full tensor-density Leibniz and boundary identities, and both ordered local-action Ward controls. All three complete general-dimensional adiabatic mean Ward rows are rechecked.

The whole actual profile and parent clock functions are differentiated for their relevant clock jets. All source clock/value jets through degree two, both nonlinear clock contacts, the nonzero projected-mean contact and the exact frozen scalar specialization remain. The norm packet checks the full physical density bounds using \(a\le25/16\), every complete Cauchy-Leibniz sum, source/detector derivative weights and the positive ADM/clock/cutoff sums.

Independent scientific diagnostics include:

- Twelve genuinely noncommuting metric pairs. Finite differences of the ENTIRE matrix exponential check both full metric charts and scalar Hamiltonian first/second contacts, not just principal entries.
- Eighteen independent scalar canonical IVPs, each at 61 times, including zero and nonzero external transfers of both signs. The prepared gauge profile has an actual initial zero neighborhood. The complete mode and Hermitian-Gram tangents agree with direct scalar/canonical-density pullback within \(2\cdot10^{-10}\). Largest observed errors are approximately \(2.45\cdot10^{-11}\) and \(6.28\cdot10^{-11}\). Omitting the canonical-density gradient term gives error greater than 0.63 in a fixture.
- A nonzero density-Lie and nonlinear metric contact in a pure-shift local-action test; their full sum cancels, while either omission fails.
- A full differentiated boundary-flux test: the wrong retarded detector primitive leaves a nonzero final flux; the advanced choice removes it.
- A nonzero nonlinear clock contact at unequal projected/full means. The two full-reference contacts cancel only after being separately retained.
- Independent Fraction reconstruction of every one of 42 Cauchy-Leibniz constants and the complete positive norm/tail sums, plus direct original-frequency tail quadrature.

The toy scalar IVPs check the exact mode/Gram bridge, not a replacement physical reference state or a uniform renormalized stress bound. The written proofs and pinned actual-state estimates provide the latter. Numerical diagnostics do not establish the infinite-dimensional, dimensional-limit or operator-domain claims by themselves.

The initial private science suite passed 363 tests. Before freezing, import order, unused variables and loop-variable bindings were corrected. No physical inequality or required contact assertion was weakened or removed. Final preflight and subsequent replay outcomes are recorded separately from frozen scientific bytes.

The report has 17 source inputs and 20 top-level fields. Its certificate suite checks every field mutation, missing/extra fields, exact replay and source hashes. Native/direct/ordinary/CLI retain original SymPy. Only full regression uses the separately audited exact-GCD adapter with all 128 original-tuple self-checks.

The raw native report SHA, source manifest, private/repository bytes and field list must match before publication. Fresh CLI, ordinary, focused and complete snapshot replays are required. No frozen predecessor or unrelated P4/P9 work is edited. These written proofs are certificate-backed, not Lean or other FORMALIZED mathematics.
