# Complete actual scalar ADM/common-clock reference response

## Unchanged physical input

Keep S6.240's actual heavy SLE, fixed initial preparation and covariant minimal-scalar prescription at \(\mu=1\). Keep the complete S6.240 fixed heavy profile and S6.241 nonlinear clock, without a further retuning. In particular
\[
 n=10^{200}/512+2,\quad \kappa=10^{800},\quad
 a(t)=(1+t^2)^2,\quad I=[-1/2,1/2].
\]
The heavy coherent mean is zero. The complete H8A420 linear heavy source and its clock/value jets through total degree two vanish at the reference. This supplies the stated conditional Gaussian metric bridge, not disappearance of interacting light/mixed quantum effects.

Use physical ADM variables
\[
 N=1+n_{\rm lapse},\quad h=a^2e^Q,\quad
 g_{00}=N^2-h(\beta,\beta),\quad g_{0i}=-(h\beta)_i,\quad g_{ij}=-h_{ij}.
\]
The tuple \(D=(n_D,\beta_D,Q_D)\) contains one lapse, three shifts and all six symmetric spatial directions. The same applies to \(G\).

## Domain and norms

Begin with smooth spatially compact or Schwartz directions. Sources vanish on the same initial time neighborhood, with no final source cutoff. Detectors vanish on a final neighborhood. The bounded form extends to the closure of prepared sources and \(L^2\)-time detectors in
\[
 V_{02}(D)=\|(1-\Delta)D\|_{L^2(I\times\mathbb R^3)},\qquad
 U_{138}(G)^2=\sum_{j=0}^{13}\|(1-\Delta)^4\partial_t^jG\|_{L^2}^2.
\]
Tuple norms are Euclidean/Frobenius and the Fourier measure is \(d^3P/(2\pi)^3\). All external transfers are admitted. The weak complex Fourier extension is linear in the tensor coefficients; internal sharp ordering is not an antilinear operation on those external coefficients.

The source-retarded and detector-advanced synchronous maps use time primitives, not inverse \(H\), momentum or constraint symbols. They remain regular at the bounce and zero transfer.

## Claim and complete assembly

S6.245 supplies the actual full spatial response, including its profile. Remove precisely that profile's QQ term before applying the two ordered scalar Gaussian Ward identities. Use the full actual nonzero heavy mean in both Ward corrections and the full nonlinear ADM chart. Restore the complete fixed physical profile once.

The resulting entire first physical ADM response is below \(10^{-327}\), after division by \(\kappa\), on \(V_{02}\times U_{138}\). Pull it back by the actual common clock. Retain the separate Gaussian and profile nonlinear-clock contacts before their full-reference cancellation. The complete clock response is below \(10^{-300}\) on the same graph.

## Precisely limited projected statement

Let \(\Omega_k^2=n+|k|^2/16\) and \(K\ge2\sqrt n\). The spatial block uses the S6.245 complete unaveraged two-leg restored projection. The mean uses the SAME original one-leg \(\Omega_k\le K\) condition on the full actual-minus-ad4 integrand, with full finite local terms retained.

Insert that projected mean in BOTH ordered Ward corrections, restore the full fixed profile, and keep the total nonlinear clock contact
\[
 3a^3(P_K-P)(4\delta^2-3\delta)n_Dn_G,\qquad
 \delta=\frac1{2(1+t^2)^3}.
\]
This defines a Ward-completed computational approximant. Its complete same-graph error is below \(10^{-200}/K\). The mean tail and nonlinear contact are not reset to zero.

No equality with a bare full projected Gaussian response, no physical cutoff and no local/shape counteraction identification is claimed.

## Completion boundary

This completes the conditional actual heavy Gaussian first ADM/common-clock response on the fixed reference, not a second derivative on finite inhomogeneous histories. A compatible quantum inverse, nonlinear feedback, omitted light/mixed loops, quantum gravitational limit, physical UV/Regge estimates and original V/G/B/P8 remain open. The unchanged older sectors still require their own assembly in any total quantum operator.
