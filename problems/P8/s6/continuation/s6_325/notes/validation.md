# Scientific evidence and required immutable-source acceptance

The analytic argument uses the exact S300 recoil and S301 fixed-ball
formula. Nuclear-norm gradients and the two-state minimum majorant
prove uniformity;18 rational one/two/three-radiation radial/angular
samples only calibrate the implementation. The full additional-soft
dimensional trace and phase terms are retained. Nothing is claimed
to be Lean- or kernel-formalized.

The independent first research probe passed its arithmetic and all18
samples before a symbolic complex antiderivative gave a spurious
-2i*pi*x for a REAL endpoint-log integral. The v2 reflects v=1-u on
the real interval first. It passed24 exact checks and13 gates in
0.36527208308689296 seconds with original SymPy. Both diagnostics
are retained; no physical term was discarded.

The first package-level three-module diagnostic passed source234
checks/234 scalar entries/5 gates, continuity16/139/66 and
matching10/10/9, in85.52934124995954 seconds. Its four captured
source files remained unchanged. Audit, tests and written proofs
were added later; this is not a receipt for the full package.

An initial audit-file adaptation left the old packet names in its
unexecuted packet builder; Ruff rejected four undefined names. Those
references and the stale observable prose were corrected before
any audit execution. No scientific formula or failed physics test
was bypassed. Later Ruff and compile checks passed.

The first full17-file draft reconstructed260 named checks,383 scalar
entries and89 gates in99.27669979201164 seconds, with54824 captured
ASCII bytes. Its science suite passed478 tests and rejected one
incorrect exception type: None reached the frozen S300 exact-real
helper and raised AttributeError. The NEW energy entry point now
rejects nonscalar, string, Boolean and inexact inputs before that
helper. The frozen S300 source is unchanged. Additional malformed
scalar tests retain the strict boundary; no physics formula changed.
The failed run is retained. The corrected full draft then passed484
science tests in0.51 seconds after97.87270104198251 seconds of
reconstruction, with260 named checks,383 scalar entries,89 gates,
83 rejections,17 ASCII files totaling55843 bytes,20 report fields and
6 module exports. Its complete source snapshot was unchanged.
This receipt predates this paragraph, the explicit measure notation
clarification and the actual S324 native-parent hash substitution.
Final cold/fresh/native acceptance is required on the final bytes
and is recorded externally.

The independent fresh interpreter may first reconstruct every frozen
repository S324 parent packet with all20 source hashes pinned. No S325
module is imported during warmup. Only after the actual S324 native
report is validated, final private S325 science passes, and the exact
final source copy is checked may one START release its S325 preflight.
That preflight again forces all parent packets before its own residuals.
This is a disclosed parent-warm fresh check; no private/native S325
amplitude or own-result cache is reused.

Final acceptance requires the ACTUAL frozen S324 parent report,
plus direct complete S301 and S309 replays. All17 ASCII source
files must be newline-terminated, byte-identical across independent
private/fresh/repository copies and unchanged during their passes.
Every one of the20 report fields and the entire source manifest
must agree with native, ordinary and CLI read-only reconstruction.
FULL alone may use the audited exact-GCD adapter with all contracts
and captured-input invariance; all other science uses original SymPy.
The external assessment records final acceptance. A pending parent
digest, private draft pass or named soft insertion cannot close P8.
