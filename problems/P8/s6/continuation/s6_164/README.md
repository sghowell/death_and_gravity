# A polynomial source map transparent to the unit clock

The [formulation](FORMULATION.md) states the two-domain
map result. The [full-resolvent comparison](notes/norms.md)
keeps the changed Fourier support, and the
[clock dictionary](notes/clock.md) separates exact
kinematic alignment from a background or state solution.

The [read-only native report](certificates/polynomial-vacuum-clock-transparent-map.json)
pins all sources, written arguments and independent tests.
Replay with this directory as the ordinary target or
p8_vacuum_clock_transparent_map.verify as the CLI module.

The named vacuum coefficients and the restricted full
classical target match are controlled. The original
V/G/B and P8 closure questions remain open.
