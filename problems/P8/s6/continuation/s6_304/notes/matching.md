# Virtual pairing and unassigned hard data

The full47-graph tree supplies the missing gravity-Born real-radiation
partner. Its leading soft current is the common massive-scalar current
multiplying the full Born Am+AG. The finite real-minus-soft error is now
controlled uniformly at every nonforward hard angle for any fixed0<x<=1/8.

Use the inherited physical virtual-real prescription: retain the same
external state, dimensional projector, analytic reference and finite
conversion, subtract the local UV poles before combining IR pieces,
and pair the leading real pole with the corresponding full-Born virtual
soft pole before removing the regulator at fixed nonzero transfer and
fixed admitted resolution. S299's leading multiple-soft calorimetric factor
does not by itself bound nonleading radiation at arbitrary multiplicity.

Nothing assigns S302's finite curvature/local/Newton coordinates or the
earlier mixed gravity hard-matching coefficients. The known S303 real
virtual interference remains separately bounded; its imaginary Coulomb
phase is not removed from the complex amplitude or the Regge problem.
The present real remainder is not a computation of unknown hard terms.

The fixed-resolution forward boundary layer is bounded here using the
physical recoil gap and a low/high radiation-energy split. No angular
window or shrinking resolution is needed for the selected real correction.
This does not address higher hard loops or all-N nonleading real emission,
and it does not make the forward Born cross section finite.
