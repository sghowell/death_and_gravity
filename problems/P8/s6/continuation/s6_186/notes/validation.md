# Validation and proof boundary

Exact checks rederive the full physical Proca stress,
its ten-field quadratic matrices, every constrained
mode readout and the smearing volume cancellation.
Continuous complex coefficient majorants preserve
the complete W8 reference and exact all-order state
remainder. A literal third integration-by-parts
identity and its full Leibniz coefficients are
checked independently of the bound construction.

Independent complex-time, arbitrary-momentum,
polarization-rotation, Fock-pair and radial-integral
fixtures supplement the written proofs. Numerical
mode or oscillatory fixtures are not continuum
certificates; the analytic bounds provide coverage.

The certificate has18 scientific source files and
20 fields, explicit source hashes, full read-only
equality and per-field mutation rejection. Every
earlier primitive and matching obligation is
retained and unsupported promotions are rejected.

Scientific and proof bytes are frozen before native
report capture and transferred losslessly. Native,
direct science, ordinary and CLI use unmodified
SymPy; only the full P8 replay uses its audited
exact GCD adapter.

The written analytic, distributional and Gaussian
arguments are not FORMALIZED. No small-noise input
alone supplies a metric solution or original P8
closure.
