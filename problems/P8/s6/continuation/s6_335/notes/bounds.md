# Complete real-minus-soft bounds on both energy windows

## Positive normalization and integrable all-polarization kernel

Restore kappa^(-3/2) for gravity. Since A_G,D=G0_D/kappa>8/(kappa delta),
the gravity remainder relative to its positive Born amplitude is
<4000000000/(sqrt(kappa)delta).
The matter analogue is330000/sqrt(kappa) by S295/S296. Its nonmetric
dyadic proof and pure-gauge heavy cancellation also bound the canonical
Frobenius core; this is not multiplication by a noninteger helicity count.
Positive complete-Born weighting therefore gives

||M_D/A_D-S0||_F <= B/(sqrt(kappa)delta), B=4000000000,
||S0||_F<=64/(sqrt(kappa)omega).

The full-D sew is positive and bounded by the Frobenius form on this
rank-two core. Its exact phase ratio is
J_e=J0*(rprime/r0)^(2e), with0<J_e<=1 and1-J_e<=3omega,
as established in S296. Thus the complete, Born-normalized,
real-minus-soft squared kernel is bounded by

[128B/(omega delta)+B^2/delta^2+12288/omega]/kappa.

Use the actual S296 graviton phase
p(e) omega^(1+2e)/(4pi^2),p(e)<=1,
and normalized positive disk density
(1+2e)(1-|v|^2)^e dP0(v)<=5dP0(v)/4.
The pointwise bound is uniform in angle, so normalization alone gives

|R_real,e(x)| <=[128Bx/delta+B^2 x^2/(2delta^2)+12288x]
                /(4pi^2 kappa).

The whole expression has its original D-dependent Born normalization.
If the same D-dimensional two-body reference phase is retained but the
amplitude denominator is fixed to A0^2, multiply by(A_D/A0)^2<=361/324.
Even this larger bound is<10^13/kappa=10^(-787) at the original source
for x<=delta/192. Initial flux and identical-scalar factors cancel
between the matching reference measures; they are not dropped.

At fixed nonforward E,u,x the exact numerator, Born normalization and
S334 sew depend continuously on e; the S296 fixed disk and the above
omega-integrable majorant justify dominated convergence. Hence e->0
gives exactly the S304 selected physical real-minus-soft remainder.
This also justifies removing a lower IR energy cutoff jointly with e
for this SUBTRACTED integrand. It is not a limit of the separately
divergent real and soft integrals.

## Extension to every fixed nonforward0<x<=1/8

The preceding low-energy estimate is not extrapolated. Split at
a=delta/192. S304's exact physical recoil geometry already proves
|pair invariant|>=(tau_j+omega^2)/30000 for crossed pairs and fixed
positive gaps for the timelike pairs. Thus on the entire compact
radiation interval every propagator has absolute value>=m=delta/30000.

For omega>=a, derive a separate D6 absolute tree bound. The shifted
external scalar stress is bounded by40 per entry, the opposite
trace-reversed numerator by9. Three hard quotients are bounded by
38880/m. The four currents have total norm<=64/omega. Accordingly,

|G6:unit_TT| <=2488320/(omega*m)+441936/m+3265173504/m^2.

The seagull and cubic numbers are the earlier exact D6 contraction
budgets with field bound9/m replacing18/delta. Since
1/omega<=192/delta=4/(625m) and m<=1, this is<4000000000/m^2.
The auxiliary canonical core has
||S||_F<=192/(omega*m)+96/m^2<100/m^2.
The same trace-completing embedding and exact dimension identity give

||G_D||_F<9000000000/m^2.

Normalize by A_D>=G0_D/kappa>8/(kappa*delta) and restore the
gravity coupling. The whole matter tree, by its canonical S295 bound,
is at most(64/omega+330000)A_m/sqrt(kappa). Therefore

||M_D/A_D||_F<2000000000000000000/(sqrt(kappa)*delta),
||S0||_F<=12288/(sqrt(kappa)*delta).

Using J_e<=1, the absolute real-minus-soft squared kernel on this HIGH
interval is below5*10^36/(kappa*delta^2). Its radial integral, even
multiplied by the fixed-A0 factor361/324, is below

10^35*x^2/(kappa*delta^2).

Thus for every fixed nonforward E,u and every fixed0<x<=1/8,
the combined bound may be written

|R_real,e(x)| < [10^13+10^35*x^2/delta^2]/kappa,

in either the D-dependent Born normalization or the specified fixed-A0
amplitude normalization with the same D-dimensional reference phase.
For x<=delta/192 the sharper low-window expression above should be used.
The high bound is finite at each fixed delta>0 and is NOT uniform as
the forward transfer tends to zero. On the high interval omega>=a>0,
all denominator gaps are uniform and the integrand is continuous in e;
the positive fixed-disk representation provides dominated convergence.
Together with the low proof this gives the full fixed-resolution D4
real-minus-soft limit, and its joint limit with a lower IR cutoff.
