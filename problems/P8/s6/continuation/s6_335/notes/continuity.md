# Fixed-domain dimensional limit and joint lower-cutoff limit

The action identities alone do not justify integration through a soft pole.
First subtract the SAME-D complete Born times the SAME-D soft current.
Use the entire canonical rank-two polarization sew from S334, including
the trace term absent from the two D4 helicities.

S296's actual normalized angular measure has density
(1+2epsilon)(1-|v|^2)^epsilon relative to its D4 disk measure.
For0<=epsilon<=1/8 it is bounded by5/4 and tends pointwise almost
everywhere to1. The actual graviton radial normalization is
p(epsilon) omega^(1+2epsilon)/(4pi^2), withp<=1.
The recoil phase ratio isJ_e=J0(rprime/r0)^(2epsilon);
0<J_e<=1 and1-J_e<=3omega on the original compact domain.

Fix nonzero transfer delta and resolution x. On
omega<=min(x,delta/192), the bound in the bounds note yields an
integrable majorant of the form constant*(1+omega) after radial
weighting, uniformly in epsilon. On the remaining closed energy
interval omega>=delta/192, the physical propagator gap and absolute
tree bound give another finite majorant. The fixed-disk density
bound supplies a common finite dominating measure in both regions.

S334's rational tree and full sew, the exact positive Born denominator
and these phase factors are pointwise continuous in epsilon.
Dominated convergence therefore yields the physical full selected
S304 D4 real-minus-soft integral at fixed hard data and x.

For a lower energy cutoff lambda<=delta/192, the missing subtracted
tail is bounded by the explicit low-window expression evaluated at
lambda, independently ofepsilon. It tends to0 aslambda->0.
The triangle inequality with the full-integral dimensional limit
then proves any joint(lambda,epsilon)->(0,0) limit and both iterated
limits of this subtracted integral. There is no such claim for the
two separate divergent real/soft integrals.

When fixing the amplitude denominator toA0^2, retain the SAME-D
reference two-body phase and the exact ratio(A_D/A0)^2, bounded by
361/324 and tending to1. Matching initial flux and identical-particle
factors cancel between those same reference measures. No hard-angle
measure or finite virtual matching is silently replaced.

The high-window bound grows asdelta->0. No forward limit, quantum
probability construction or finite virtual hard amplitude is inferred.
