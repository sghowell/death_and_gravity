# Six-dimensional trace completion and exact action counts

## Control the missing canonical trace by a SIX-dimensional embedding

Do NOT infer a trace bound from the two D4 helicities.
For any complex symmetric2x2 matrix E with||E||_F=1, embed it into
the four-dimensional transverse space in D6 as
Ehat=diag(E,-tr(E)I2/2). It is TT and
||Ehat||_F^2=1+|tr E|^2/2<=2.
Since the momenta lie in the original four-dimensional Lorentz slice,
their canonical anisotropic core has zero extra entries.
Thus a unit-polarization D6 bound controls its canonical Frobenius norm
with factor sqrt2<2. The same statement holds for its soft remainder.

Derive a D6 unit-polarization gravity remainder bound directly from
the canonical action, keeping all six-dimensional sums. The physical
momenta still occupy the four-dimensional slice and each component
has absolute value<=2. Old scalar stress entries<=25,
trace-reversed numerator entries<=9, changes<=84omega and24omega.
A contracted hard numerator now has36, NOT16, possible entries:

|N-N0|<=36(84*9+25*24)omega=48816omega;
|N0|<=36*25*9=8100.

The quotient error is<1652832omega/delta^2 in each channel,
so its three-channel sum is<5000000omega/delta^2.
Also |G0_6|<=24300/delta. External-emission comparison therefore costs
64*5000000/delta^2+608*24300/delta.

For the following component bounds let F be a D6 TT polarization
normalized to unit Frobenius norm. Internal fields have entry
bound18/delta. In a scalar seagull,
|tr H|<=108/delta,|tr(F H)|<=648/delta and|d2|<=324/delta.
Each p F H q has at most6^3 products. Six seagulls cost at most

6*4[17*324+108*4/2+2*6^3*2*18*2]/delta=883872/delta.

With momentum component maximum4:
|C1(B)|<=6B, |C2(A;B)|<=36AB, |M1(A)|<=4A.
A general connection bilinear has2*6^4=2592 products; a diagonal
metric one has2*6^3=432. The cubic coefficient is

4*6[2592*4*6^2+2*432*36*6]=13436928.

All three cubic graphs cost at most
3*18^2*13436928/delta^2=13060694016/delta^2.
The complete unit-polarization D6 remainder is therefore bounded by
13396352288/delta^2.

Now use the exact identity G_D=G6+(2theta-1)S and its Born counterpart.
For0<=theta<=1/9, |2theta-1|<=1. The canonical full-D remainder obeys

||R_G,D||_F
 <=[2*13396352288+45216]/delta^2
 =26792749792/delta^2 <30000000000/delta^2.

This controls the otherwise missing trace response without supposing
that a four-dimensional amplitude bound supplies it.
