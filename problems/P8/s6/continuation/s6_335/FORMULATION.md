# Complete selected tree, fixed resolution and matched soft subtraction

Use the SAME original action, formal tree vacuum, parameters and
independent matching coordinates. The object is the complete47-graph
tree of S295/S304, continued by S334; all polarizations and interferences
are retained. S296 already proved the matter-only dimensional integral.
This result extends it to the gravity-exchange radiation sector.

In mass-one units,25/4<=s<=16,-1<z<1,
tau=(s-4)(1-|z|)/2,delta=min(1,tau)>0,0<x<=1/8 and0<=epsilon<=1/8.
Use the exact recoil and D=4+2epsilon measure from the actual S296
prescription. The SAME-D complete Born amplitude times its SAME-D
leading soft tensor is subtracted before either regulator is removed.

A trace-completing D6 embedding supplies a canonical Frobenius remainder
bound that D4 helicities alone cannot give. On the low-energy window
omega<=delta/192, the complete Born-normalized amplitude remainder is
<=4e9/(sqrt(kappa)*delta). A separate absolute-tree bound using the
full physical recoil gaps covers omega>=delta/192; the low Taylor
estimate is not extrapolated.

For fixed E,z,x this gives a uniform-in-epsilon integrable majorant
on a fixed positive angular domain. Dominated convergence yields the
actual S304 D4 real-minus-soft remainder, not an assumed finite
scheme-dependent constant. The low integral also vanishes uniformly
with a lower cutoff, giving their joint limit for this subtracted object.

The exact sharp low/high API bound includes the safe361/324 factor
for fixing the amplitude denominator to the D4 full Born A0^2. This
choice retains the SAME D-dimensional reference two-body phase.
No A0 replacement is made inside an uncancelled soft pole. The same
bound also controls the D-dependent Born-normalized remainder.

This is not the finite virtual hard amplitude, an IR-finite separate
real rate, a uniform forward statement, all-N hard summability,
interacting probability or original P8 closure.
