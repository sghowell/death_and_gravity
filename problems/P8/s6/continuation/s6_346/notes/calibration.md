# Independent original-vector and bubble checks

For each of the three original massive four-scalar/null-graviton states
and both exact transverse polarizations, evaluate all ten labeled
metric responses and all six jet responses directly from four-vectors.
All60 differences equal the prescribed tau*T; all six T are nonzero.
Mass shells and momentum conservation are separately checked.

Independently invoke the actual frozen S339 full channel kernel for
F(v)=v^3. Subtract its explicit four external emissions and sum all
three partitions. Its remaining internal response equals half the
labeled W_300 contact for all six original cases. This verifies the
otherwise easy-to-miss full bubble conversion factor.

These are diagnostics, not the generic proof: the latter compares
the complete symbolic polynomials. Direct S344/S345 report validation
also retains54 original triangle-kernel and72 box-kernel checks with
the unchanged heavy mass, all original orderings and the explicitly
off-shell scaling documented there.
