# Original source and unchanged subtraction

S235 fixes the full old polynomial one-loop normalization:
before1/(16pi^2), the bubble is(1/2)sum_channels A(v)^2 B(v),
triangle(g^2/4)sum24 A(v_ab)Cbar, and box(g^4/4)sum24 Dbar.
A(v)=C+g^2/(n-v), B_MS(v)=-int_0^1 log(1-v*x*(1-x))dx at mu1.
In particular B_MS(0)=0.

S339 includes all bubble insertions and its three old pole
counterterms. S344 includes the complete triangle and outer-heavy
product divided difference. S345 includes every ordered box.
No counterterm or class is counted twice.

H8A420-VAC-OS4 in S239 subtracts the full off-shell generated poles
and retains classical finite derivative couplings at mu1. Its finite
four-point OS4 condition changes only the constant potential quartic.
Mass/residue/source conditions remain fixed. They introduce no new
six-derivative finite retuning in the selected classes.

The actual tuned C/g^2=-3/(n-2)+2/(n-2)^2 is used exactly, not
replaced by-3/n. New local six-field source tadpoles and mixed-source
graphs remain distinct. Their on-shell physical reductions do not
automatically give their off-shell common-basis coefficients.
