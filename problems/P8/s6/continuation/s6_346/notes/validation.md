# Immutable scientific and operational verification

The21 sources contain9 Python modules, two test files, README,
FORMULATION and eight proof notes. The report has20 fields,
447 named exact checks,465 scalar entries,47 proof gates,
eight controls and795 rejected inputs.

The full generic word projections and radiative differences, four
moment primitives, complete original sum, finite rational tail budget,
positive exponential partial sum and original vector/kernel comparisons
are recomputed. No assumed zero residual table, floating fit or sampled
replacement for the generic proof enters.

All six direct raw reports and their source manifests are checked.
Every report field is mutation-tested. Missing/extra fields, changed
frontiers, invalid exact positive masses, invalid native word/order
labels and overbroad physical scopes are rejected.

Original SymPy is used for cold and fresh scientific checks. The fresh
process warms only its immutable preceding checkpoint before receiving
the identical new sources. Exact private/fresh/repository equality and
source/export/count checks precede the single native report build.
Raw bytes, fields, all inputs, counts and frontier are validated afterward.

Operational receipts stay outside the frozen manifest. Ordinary tests,
CLI replay and a covering whole-source snapshot are separate acceptance
requirements; a covering snapshot must actually include S346.
Not kernel-formalized and not a proof of omitted P8 obligations.
