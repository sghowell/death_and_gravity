# The canonical phase audit and exact repair

Let Z=u a+conjugate(u)a^dagger and H_M=Z^t M Z/2. For a single mode the pair part is (b a^2+conjugate(b) a^dagger^2)/2, b=u^t M u. Acting on the vacuum, <H_D H_G>=b_D conjugate(b_G)/2 up to the real disconnected part. Therefore i<[H_D,H_G]>=-Im(b_D conjugate(b_G))=+Im(conjugate(b_D)b_G). The same per-pair result with the appropriate identical-particle counting extends to all ordered physical pairs.

A three-level Fock fixture is exact for these vacuum quadratic products because only0,1,2 occupation levels occur. Nine position/momentum source/readout combinations independently match the real covariance tangent -tr(M_D U[J M_G C+C M_G J^t]U^t)/2. Mixed quadratures make stripped pair coefficients complex and expose the wrong convention. Tests of only real flat amplitudes would not suffice.

The WKB mode f is proportional to exp(-i theta). Its canonical momentum is(-iW-b)f/K with real b, so this is the annihilation branch. S207's full-reference tests and S212's jets.frequency use exactly that source branch and its sharp detector. S198's template instead has negative detector phase and minus imaginary part for creation-pair amplitudes. Its abstract IBP formula is correct; substituting the opposite branch without changing the phase/current rule is the error.

For annihilation-source b, set Kplus=exp(+iTheta_t) integral exp(-iTheta_s)b_s, Omega=Theta', g=1/Omega and Lb=(gb)'. Direct differentiation gives Kplus'-iOmega Kplus=b and

    Kplus=sum_(j=0)^(n-1) i(-i)^j g L^j b
          +(-i)^n exp(+iTheta_t) integral exp(-iTheta_s)L^n b.

All six orders are checked independently. The original initial source germ removes every lower endpoint; no changed state is needed. Polynomial fixtures whose first n-1 derivatives vanish at the initial endpoint independently check the exact integral identity.

Taking +Im after the sharp detector gives B_j(new)=(-1)^j B_j(old). The bulk must carry the corresponding positive phase and +Im too. Its absolute envelope is unchanged, but a full assembled physical current cannot be recovered by correcting the local UV term alone.

The private scalar pole discrepancy was found before applying the correction: Hpole-16 f4_old equals2a''p^2 Gamma in I/I,2a'p^2 Gamma' in I/S, and-2p^2(a''Gamma+a'Gamma') in S/I. The corrected coefficients match all three covariant poles exactly. The legacy branch is retained explicitly for these falsification controls, never presented as a physical endpoint.
