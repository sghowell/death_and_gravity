# Original finite prescription and ordered local norm

Let f4(d) be the full normalized-sphere logarithmic spatial coefficient after fixed six-invariant reconstruction. At d3,

    H_pole=m^2 H_R-H_R2/4+29 H_Ric2/30-2 H_Riem2/15 =16 f4(3).

This equality holds in all six independent invariants with the corrected phase. The original Fourier normalization restores1/(2pi^2), so H_pole/(32pi^2) equals the physical logarithmic density. The homogeneous part is subtracted at fixed tensor directions, not by discarding the full trace contact.

The full spatial QQ contact is independent of external transfer before the one-leg radial integral: S214's complete second vertex depends on the local tensor product and internal momentum, not derivatives of the external tensors. It therefore cancels in the spatial difference. It remains necessary in the unconstructed homogeneous trace anchor.

Keep the same original MSbar radial normalization and fixed comoving split m. With ell=log(m^2/mu^2), the actual local UV finite difference is

    Ffinite=-m^2 F2 Gamma/2+(1-log2-ell/2)F4 Gamma
      -(partial_d f4 at3)/(2pi^2)+(partial_d H_pole at3)/(32pi^2).

F2 is the complete physical quadratic coefficient; F4 is the complete physical logarithmic operator. Continue the original fixed scalar pole weights, Euler and volume. No extra local-action target is added. This is the finite part of this UV difference, not by itself a full dimensional-limit proof for every scalar response remainder.

The only odd tracefree spatial UV entry is j1/source0/degree3. Its physical value is zero but its invariant dimension slope is p^2(3t^2+1)(-31/420,1/14,0). Reversing its sign changes the finite part by

    p^2(3t^2+1)(-31T+30V)Gamma0/(420pi^2).

The physical tracefree pole and power coefficient remain unchanged. This is why the earlier physical pole test passed despite the finite error.

For the complete local operator, exchange UD and UG when interchanging detector/source directions. Its coefficients f0,f1,f2 obey f2=f2^transpose, f1=2(f2^transpose)'-f1^transpose, and f0=f0^transpose-(f1^transpose)'+(f2^transpose)''. This is the formal Green identity for the local finite operator, not symmetry of a forward retarded memory kernel.

For m1000,ell0 and |t|<=1/2, multiply each invariant coefficient by pi^2 a, expand the logarithm and use0<=log(1+t^2)<=1/4,0<log2<1,a>=1,pi^2>9. All resulting coefficients are rational polynomials of spatial degree at most4. The Frobenius bounds on(T,V,W,S,UD,UG) have weights(1,1,1,3,2,2). The exact weighted sum is66786194445073/464486400<1e6. Since |p|^j<=(1+p^2)^2 for0<=j<=4, Cauchy-Schwarz gives the stated L2 x Z24 bound, with no external momentum cutoff.
