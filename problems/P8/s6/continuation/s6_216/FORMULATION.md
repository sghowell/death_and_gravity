# S6.216 formulation

Retain the original conditional CD Gaussian Proca sector, mass1000, kappa10^800, unit proper-time slab, all-order prepared state, metric exponential and fixed finite covariant prescription. Source perturbations have the same zero initial germ. Spatial tensors now have arbitrary trace.

## Correct physical orientation

For annihilation-pair coefficients b_X=u^t M_X u, the S195 response is

    i<[H_D(t),H_G(s)]> = +Im(conjugate(b_D(t)) b_G(s)).

Writing b=a exp(-iTheta), use positive detector phase, negative source phase and +Im after sharp-detector contraction. Its endpoint coefficient is i(-i)^j. Thus every frozen annihilation-branch odd endpoint reverses sign. The negative-phase S198 identity remains correct when its amplitude is a creation amplitude.

This correction is independently fixed by exact Fock matrices and covariance tangents, before any comparison to a covariant counterterm.

## Full local spatial UV difference

Retain the trace-dependent temporal constraint and magnetic volume terms. The ten geometric products have350 retained radial coefficients and105 new ordered scalar entries. Reconstruct all six invariants T,V,W,S,UD,UG at symbolic spatial dimension d before taking a derivative at fixed physical tensors.

The full physical logarithmic coefficient equals the original prescribed curvature pole in every invariant. The original radial finite part retains the fixed comoving split, mode dimension derivative, continued Euler and full volume contribution. It does not select a new finite gravitational coefficient.

The corrected tracefree finite difference from S212 is

    delta Ffinite = p^2(3t^2+1)(-31T+30V)Gamma0/(420pi^2).

The complete corrected local spatial UV difference obeys

    |Ffinite[D,Gamma]| < 1e6 ||D||L2 Z24[Gamma],
    Z24^2 = sum_(r=0)^2 ||(1-Delta)^2 partial_t^r Gamma||L2^2,

for nonzero norm product, and is zero for zero norm product. This is only a local UV-difference estimate, not a full scalar-current bound. The two ordered cross entries obey the formal proper-time Green relation, not equality as forward retarded kernels.

## Explicit unresolved dependencies

S213's physical assembled current and anchored regulator formula require a consistent correction of their finite endpoints, sixth bulk and subtraction. S215's use of that known tracefree input is not restored here. The scalar homogeneous trace anchor and full state/time/contact remainders remain to be constructed and bounded.

Original P8(a)'s scoped closure, A.20-A.23 and all nine primitive statuses remain unchanged. Full reduced inverse, nonlinear quantum background, remaining parent/loop/heavy/cutoff and canonical vacuum/finite-gravity obligations remain open. This is neither original P8 completion nor a user-intervention blocker.
