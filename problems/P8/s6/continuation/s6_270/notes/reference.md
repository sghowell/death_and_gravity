# Entire original canonical flow and complex phase image

## Fixed canonical variables and actual generators

S251 supplies the complete light-scalar generator in fixed unit-CCR
canonical field/density coordinates. It contains the full original
Jc,A,Tcorr coefficients and all density time connections. The state
at u=0 already contains both canonical shears and the central swap;
these are not repeated as extra flow terms. The independent exact
symplectic identity G Omega+Omega G^T=0 is retained.

The unchanged S264 coefficient box has a in[1,2], physical wave-number
squared q in[P^2/4,P^2], |theta|,|Hclock|<=2,
|E|<=1/2, |ell|<=1/10, J in[1,100] and |A|,|Tcorr|<=10^-6.
The full original profile interval evaluation verifies J>1 and J<100
and the clock/theta bounds on its larger real slab. T is inside that
slab. Evaluating the summed absolute rational entries gives the actual
complete scalar bound<10^257.

For each tensor use the symplectic shape transformation
diag(-2,-1/2) on the original canonical tensor pair. The resulting
matrix is [[0,4/a^3],[-a^3 q/4,0]]. Both tensors are included.

The physical H,Pi_H unit-canonical pair has matrix
[[0,a^-3],[-a^3 n-a P^2,0]]. The fixed S240 balancing scale
Omega_star=sqrt(n+P^2/16) is NOT the physical dispersion
sqrt(n+P^2/a^2). At a=1 their squares differ by15P^2/16.
If one instead uses chi=a^(3/2)H, its oscillator equation also has
-3H'/2-9H^2/4. This clarification does not change the S269 state
bound, which used the original balanced S240 variables and equation.
The present physical generator never substitutes Omega_star for omega.

All three Proca pairs use
K=a^-1 I+kk^T/(a^3 m^2) and
V=a m^2 I+a^-1(|k|^2 I-kk^T).
The exact product KV=(m^2+|k|^2/a^2)I is checked, including the
longitudinal Gauss term. Unit W,Pi_W variables carry K/zeta
and -zeta V. Evaluate each of the three selected momentum axes.
The summed bound on all six real modes, two light channels, two tensors,
H and three Proca channels is<10^500 on all96 phase coordinates.

This FREE reference is not identified with the full nonlinear
Hamiltonian Hessian. Its entire generator and boundary conventions
remain precisely those of the chosen original preparation.

## Full covariance and entire flow

Take the same full pure covariance V0=S0 S0^T/2 with symplectic S0.
For any physical linear row l, ||l S0||^2=2 l V0 l^T, including
every original cross covariance. The S269 field bounds give bounds
on each row after restoring the exact sqrt(kappa). Extracting a real
Fourier coefficient costs sqrt((2pi)^3)<20, matrix conversion<2,
and the Euclidean sum over96 rows costs sqrt(96)<10.
The maximum original safe row bound gives ||S0||<10^120.
Since S0^-1=-Omega S0^T Omega and Omega is orthogonal,
||S0^-1||=||S0||. This is not an arbitrary square-root estimate for
a mixed or diagonalized substitute covariance.

Let M_w=S0^-1 S_u S0. Its generator has norm<10^1000.
The entire integral evolution, for positive or negative real time,
obeys ||M_w-I||<=exp(10^1000 |u|)-1. For 0<=x<=1/2 the full
positive series gives exp(x)-1<=2x, so this is<=2*10^-1000.
Consequently complex initial w ball4R maps strictly inside ball8R.
No frequency-times-time first-order truncation is used in this theorem.

## Enlarged full nonlinear image, with external means

Re-evaluate EVERY S269 field row on8R, rather than assuming its old
2R radius. The complete v_A2 and tau_A2 bounds remain below
10^-329 and10^-274. Every required momentum A1 bound, including
the tiny trace background, is<10^-200; W_A1<10^-275,
grad M1<10^-329 and grad H<10^-354. reference.py lists the full
actual rational bounds, not only their decimal powers.

The original external means satisfy
a=(1+u^2)^2, Hclock=4u/(1+u^2), ell=1/[10(1+u^2)^6],
Pi_M=a^3 ell=1/10, Pi_v=-6a^3 Hclock, M1'=ell.
H and W means vanish. The source's actual implicit lapse is NOT
substituted for the reference's N=1 in this flow.

For |u|<=T, full binomial identities and inverse inequalities give
|a^(+/-2)-1|<=16T^2, |a^(+/-3)-1|<=64T^2 and |Pi_v_bg|<100T.
Thus gamma=a^2 exp(2v) Q^-1 and its inverse still have A2 gap
<10^-272 from identity; their norms and c,volume,inverse-volume
are<2. The additional matter-density shift is<=13T^2 and fits the
spare (2/5)*10^-329 in the original dp budget.

The same A2 shape contraction, full derivative-Q spatial inverse,
DQ*, source, cotangent and curvature arguments therefore apply.
This extends the real analytic reconstruction to a complex phase
neighborhood using complex absolute norms, NOT positive definiteness
or real coercivity for a complex metric.

In particular the extended adjoint is the holomorphic continuation
of the REAL adjoint: its formal bilinear transpose. Conjugate
transpose at complex Q is antiholomorphic and would invalidate this
argument. At real Q the extension restricts to the genuine Hilbert
adjoint. The full complex Neumann inverse uses triangle/operator
norm bounds; all nonlinear harmonics and zero-mode conventions remain.
The independent complex Bessel/Galerkin diagnostic distinguishes
these two adjoints and fails if the derivative-Q term is removed.

The exact trace identity tr(gamma DQ*[B])=0 preserves Pi_v through
the full momentum lift. The shear is bounded separately by the
complete lifted momentum. All twelve ACTUAL complex invariants,
including the matter mean, electric/magnetic terms, both matter
gradients and full scalar curvature, have modulus<delta=10^-260.
The real restriction retains positive gamma and all three translations.

