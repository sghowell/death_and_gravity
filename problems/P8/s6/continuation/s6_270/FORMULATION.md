# Evaluated finite-time formulation

## Fixed objects

Retain the S268 explicitly declared finite quantization and the S269
exact family L=1, momenta {+/-10^64 e_i}, eight physical channels per
real mode, d=48 pairs. The homogeneous configuration is an external
original reference parameter, not a newly quantized homogeneous state.
The same pure Gaussian covariance is V0=S0 S0^T/2 in unit-CCR canonical
coordinates, with a real symplectic S0. No independent diagonal Gaussian,
state conditioning, compact support, new vacuum or nonlinear Gaussian
pushforward is introduced.

Retain the whole original action, its fixed smooth source profiles,
kappa=10^800, zeta=10^-6, Proca mass1000, heavy mass squared10^200/512+2,
all primitives, central canonical maps, spatial harmonics, and residual
translation generators. Only the declared finite regulator is new.
No physical meaning is assigned to the chosen Fourier or phase cutoff.

## Claim and domains

Let T=10^-2000, R=10^20, rho=10^-245 and delta=10^-260.
For each real |u|<=T, use the entire original free canonical propagator
S_u, not a frozen bounce matrix, to pull back the complete moving-chart
Hamiltonian and physical volume. The initial complex whitened ball4R
maps inside the S0-whitened ball8R. The full nonlinear spatial chart and
pointwise auxiliary branch exist on that image. Their twelve complete
density invariants have modulus<delta, and |N_star-1|<10^-257.

In the moving chart let h_red be the entire original constrained
Hamiltonian with its time connection, and h_ref the selected whole free
reference Hamiltonian. Define g(u,z)=(h_red-h_ref)(u,S_u z) and
F(u,z)=average_x exp(3v) R_full(u,N_star)^(-3/4), pulled back by the same
S_u. Their actual scalar centers g0,F0 remain; F0 is not reset to1.
On complex initial w=S0^-1 z ball4R, |g|<10^1000 and |F-1|<10^-255.

For b(y)=exp(-1/y) at y>0 and0 otherwise, put
theta(y)=b(1-y)/(b(y)+b(1-y)), with its smooth constant extensions.
Only the two cutoffs chi_c(w)=theta((||w||^2-R^2)/(c R^2)), c=1,2,
are claimed here. They equal1 on the core and have supports sqrt(2)R
and sqrt(3)R, strictly inside the real ball2R. Define
g_ext=g0+chi_c(g-g0), F_ext=F0+chi_c(F-F0).

Use the same normalized coherent map
Q_V(a)=(2pi)^(-48) integral a(z)|D(z)psi0><D(z)psi0| dz.
Let D=(1/2)V0_AB partial_A partial_B, equivalently Delta_w/4.
A_c=Q_V((1-D)g_ext) generates its exact bounded interaction-picture
Dyson unitary U_I,c. The physical-picture propagator is U_ref U_I,c.
The positive Q_V(F_ext) and separately calibrated
Q_V((1-D)F_ext) are distinct, both now proved positive.

## Result and exclusions

The quantified domain, positivity, ordering-symbol, state, leakage and
two-readout estimates in README follow from the exact bounds and written
proofs. The physical readout is conjugated by U_ref together with the
state; fixed-window metaplectic covariance is not presumed.

No comparison to an undefined unregularized singular Hamiltonian is
made. There is no arbitrary-smooth-cutoff theorem, uniform mode/volume
limit, original interacting volume mean, physical Wilsonian matching,
omitted-loop bound or nonlinear global completeness. Original V/G/B/P8
remain OPEN and completed scoped P8(a) is unchanged.

