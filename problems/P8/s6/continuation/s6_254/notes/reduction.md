# Whole four-mode constraint reduction and canonical chart

Use real Fourier parities with matter/metric cos(Px), spatial longitudinal
vector sin(Px) and temporal vector fluctuation -A0 cos(Px). Per kappa a^3,
the scalar ADM action is the S253 action with a two-component matter
coordinate sigma, positive kinetic matrix Z times identity, charges c,w,
and all lower lapse/volume/matter contacts d,vs,mass. The entire added
vector square is

    K(Adot-P A0)^2/2
      +Z[A0+r(3 vdot-b)+3 R_N(Hhat-Hclock)n]^2/2 -Yv A^2/2.

Here a=a_hat, r=R-1, K=zeta Cchi/(N a^2), Yv=N Cchi/a^2.
Wbar=Sbar makes its background normal square zero but does not remove
its full first-variation square. Homogeneous temporal Wbar has Fbar=0.

The four-velocity Hessian is diag(-6D gamma,Z,Z,K),
gamma=1-3Zr^2/(2D). The complete n,b,A0 Hamiltonian Hessian has the same
joint pivots as S253: -2D/3, -Z/gamma, and -2J after the vector-time Schur
step. Its determinant is -4DZJ/(3gamma). The full configuration Gaussian
density square acquires the second matter kinetic factor and is
8D^2Z^3JK. No separate vector or heavy constraint determinant is assumed.

All three exact auxiliary equations and the full Legendre reduction are
checked. Their solution has b=pv/(2D), n=Lnum/(2J), with

    Lnum=Theta[pv-3rPpA+3c.sigma]/D-w.ps/Z
         -(L0+L2 P^2/a^2)v-d.sigma-3R_N dH PpA.

The entire reduced Hamiltonian, including all two-matter potentials,
is stored. No channel or constraint is projected out for the symbol test.

The physical canonical normalization is sqrt(kappa) times
diag(1,1,1,sqrt(zeta),a^3,a^3,a^3,a^3/sqrt(zeta)). Its canonical density
and time dependence follow S253; the a^3 momentum damping cancels exactly.
The next symplectic chart uses

    Qb=-pv_phys/(2aP^2), Pb=2aP^2 qv_phys,
    QL=qA_phys/P, PL=P pA_phys,

and leaves both matter pairs intact. It is valid at P>0 and does not
divide by Theta. Its full connection is -Hhat Qb Pb. Direct substitution
checks the complete transformed eight-phase Hessian, not just its leading
entries. Coefficients can vary in time throughout.

This physical phase chart is used directly in the time-dependent proof.
An indefinite unclean frozen configuration Hamiltonian is not identified
with a ghost or an instability. In particular, omitting a momentum-dependent
boundary derivative can already give a false verdict at r=0; the exact
canonical equations and their full connections avoid that inference.
