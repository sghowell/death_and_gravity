# Actual clock and vacuum quadratic blocks

The full light target and original M1 are unchanged. The new
source satisfies S_background=S_first=0 and W_background=0.
Consequently the extra action has zero background equations
and its quadratic part is precisely physical-metric Proca.
The complete target's first metric equations are independently
replayed in S6.173; the current full-function clock jets give
the unchanged retuned CD/M1 light quadratic action.

All scalar/vector/tensor physical cones on this exact trajectory
are therefore those already checked for the retuned light action
and the physical Proca cone. This is not a cone theorem on the
whole analytic coefficient domain.

With A=sqrt(kappa*zeta)W the vector mass squared is1/zeta.
At fixed comoving k, q=k^2/a^2 and a=(1+t^2)^2.
Eliminating the longitudinal temporal component first gives
positive kinetic normalization Z=a*m^2/(q+m^2).
The transverse normalization is a. Since qdot=-2Hq, set
r=q/(q+m^2) in[0,1]; then

    (log Z)dot=H(1+2r), rdot=-2Hr(1-r),
    Omega_T^2=q+m^2-Hdot/2-H^2/4,
    Omega_L^2=q+m^2-(1/2+r)Hdot+H^2(-1/4+r-3r^2).

All normalization derivatives are retained. The identity
(-1/4+r-3r^2)+9/4=(1-r)(3r+2)>=0, together with
|H|<=2 and |Hdot|<=4, gives Omega_L^2>=q+m^2-15.
The transverse allowance is3. Thus0<zeta<=1/2000 gives
the uniform floor q+1985. At q=0 the two expressions agree;
the homogeneous vector chart does not lose a polarization.

At u=0,du=0,chi=constant,W=0, R=1 and its first X derivative
vanishes. The scalar quadratic potential/kinetic term is
(X-u^2)/2. The covariant vector source starts at four scalar
fields, so neither a vector/light quadratic mixing nor a vacuum
tadpole occurs. The seven physical quadratic modes are Einstein
gravity's two tensors, canonical mass-one Phi=sqrt(kappa)u,
the free massless matter scalar, and three positive mass1/sqrt(zeta)
Proca polarizations. This does not assert a nonlinear vacuum
neighborhood spectrum or a perturbative quantum completion.

The oscillator floor is not a stationary scattering gap or a
physical cutoff. The vector's nonlinear source, forced response,
vacuum determinant, quantum corrections and interaction domain
still require separate quantitative analysis.
