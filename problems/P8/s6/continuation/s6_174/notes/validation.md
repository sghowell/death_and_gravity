# Evidence boundary and immutable replay

The complete generic-R source/chart identities and new full
quotient update are derived independently of a vacuum-only
coefficient norm. The actual S6.109 functions supply clock
and vacuum jets; no old clock polynomial is used globally.

Written analytic proofs cover the full coefficient domain,
removable null-gradient limits, exact algebraic elimination,
source cancellation, local nonlinear constraint counting and
uniform time-dependent vector-frequency floor. Exact symbolic
checks cover all60 Euler-lift rows and every required boundary.
These are not Lean-formalized continuum theorems.

Independent science tests include dense unrestricted quotient
solves, literal source-centering variations, finite metric-map
Jacobian/inverse checks including a nonzero null gradient,
full-function covariant source variations and null limits,
direct longitudinal reduction and time-dependent normalization,
and full joint lapse/normal-vector controls. Numerical full-
function checks supplement the written bounds; they are not
validated interval integrations.

Initial private algebraic probes passed. A diagnostic probe
asked an older helper for an absent J key; the new clock J was
instead derived directly from the full target ADM action.
A script-wrapper syntax error occurred before any source write.
Neither affected a frozen file or changed a scientific bound.
The first independent dense tests caught a reciprocal in the draft
determinant-ratio formula. The matrix determinant lemma and all
three dense ratios give -tau_p*sigma_p^3, not its reciprocal;
the formula was corrected privately. The mass action, full Euler
lifts and nonzero-rank domain were unchanged. A field-degree test
also needed polynomial extraction rather than reading powers from
an expanded sum; the explicit quartic source coefficient is checked.

All own payloads must pass the unchanged exact serializer before
freeze. Decimal diagnostics, if present, are strings. Native,
direct science, ordinary and independent CLI retain unmodified
SymPy. Only full regression uses its separately audited exact
GCD adapter. Source/proof/test and native-report hashes are
checked independently, with lossless report transfer.

Fresh repository science, ordinary, CLI and full captured P8
replays are required before publishing the claim. Runtime
results belong in the later root assessment, outside frozen
scientific globs. No earlier scientific/report byte is edited.
