# Complete selected real-emission comparison, not original P8 closure

Keep the original action, exact parameter record, all9 primitive verdicts,
all150 previous matching records,6 historical qualifications and scoped
P8(a) unchanged. Add only the named S295 matching result.

Compute every tree at C/sqrt(kappa) and g^2/sqrt(kappa) with four on-shell
massive scalars and one physical graviton. Retain all26 labelled diagrams,
not only external-line emissions. Establish Ward identities before TT
projection, identify the exact heavy leading cancellation and use the
unchanged tuned Born source.

Specify a genuine on-shell recoil map and associated angular-bin
convention. Include its two-body phase-space Jacobian and both helicities.
Bound the complete selected squared-rate difference from leading soft,
including all C^2,Cg^2,g^4 interference, uniformly in a stated compact
physical domain and as the auxiliary lower soft cutoff is removed.

The bound is not a finite total Fock rate, virtual/dressed factorization,
a proof of S278 analytic division, pure-gravity radiation or a UV/Regge
completion. An upper bound on a finite real difference does not fix its
sign, select finite matching constants or prove forward positivity.
No missing physical/Regge coefficient is treated as a user-choice blocker.
