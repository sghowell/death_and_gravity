# Complete fixed-order reference and field-map invariance

The regulated scalar forest in forest.md defines one
unambiguous finite coefficient V2,scalar at mu=mF.
Adding the already paired two fermionic primitive
coefficients of S6.149 gives the full V2. No separately
repeated proper counterterm or first-source square is
added on top.

At the same reference, the complete first vacuum term is

    V1=[-log(mF^2)-3/2]/(4Q)
       +M^2[log(M/mF^2)-3/2]/(4Q)+63 mF^4/Q.

The second term is the heavy Gaussian determinant;
M is a mass squared. The last term includes all
fourteen Dirac flavors and three colors. Pure
massless gauge/ghost first determinants are scaleless.

The full first Phi field direction is
(L1,g1,Y1)=(-2k_D L,-2k_D g,-k_D Y), with M1=mF1=0.
V1,D depends only on the physical/free light mass one,
M and mF, not those shifted interaction coordinates.
A constant Phi rescaling changes its determinant by
(1/2)Tr log K, which is a scaleless constant trace.
The light kinetic/mass ratio remains one. These are
regulated identities, so no epsilon times a hidden
vacuum pole is discarded. The second coordinate map
acts on V0=0 and contributes nothing. Thus the same
assembled V2 is the matched vacuum coefficient at
the original MS interaction boundary.

The original physical condition is vacuum energy zero.
It fixes the finite local references rho1=-V1 and
rho2=-V2. These are prescribed values, not new fit
freedoms. The report gives an explicit rational
interval for rho1 and an absolute bound for rho2.
It does not set the large unrenormalized finite
vacuum contributions to zero or identify them with
small backreaction.

The full second H-source J2 is a different one-point
condition. Its inserted-fermion part is bounded in
S6.148, but its complete scalar and matching parts
are not evaluated here. It cannot change this
two-loop constant because J2 H vanishes at H=0
and its induced mass requires another loop.
