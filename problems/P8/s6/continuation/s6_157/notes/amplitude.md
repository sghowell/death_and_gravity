# Exact complete second-order forward coefficient

Write T=2g/(M-2)^3=4 lambda for the positive tree b2.
The complete finite-coordinate expansion gives

    b2^(2)=b2_H,raw^(2)
       -k0[4 b2_H,scalar^(1)+2 b2_H,fermion^(1)]
       +T[3k0^2-2t+(k1/Q)(2L-3g/(M-2))].

To obtain the last term differentiate T with respect
to g and M using the full S6.156 second map. The first
map has g1=-2k0 g and M1=0. T is linear in g, so the
fundamental G1 square is already in g2; no extra
Hessian term is missing. The induced M2 produces
-3k1 g/[Q(M-2)]. A local L2 has no direct b2, but
its relation to the bare map is still fixed.

The first term is exactly the disjoint sum of the
four scalar MS families and four fermionic quartic
families. Each proper counterterm, physical inner
condition and overall nonlocal-heavy reference is
already owned there. The two finite-coordinate
contributions on the second line and first-loop
variation are new and are each added once.

The complete first-order canonical error E1 is
inherited from S6.133. It already contains the first
tree field correction -2k0 T, so that term must not
be added once more.

The same physical Phi field and mass are those of
S6.156. Its complete unit-disc inverse satisfies
|Pi_R|<1e-18 |s-1|^2 and has only the mass-one
unit-residue pole there. The present amplitude map
does not redefine that free mass or external field.

This completes the Phi fixed-order pole and b2
obligation. It does not complete the vacuum/source
reference values, external-fermion canonical
dictionaries, physical truncation or cosmological
positivity assumptions.
