# Frozen scope: evaluated finite bounce-domain and volume comparison

## Original source and reference

The immediate parent is S6.268, raw report SHA256
410af7db450afb132e77a4e2c696ddd3a027a78c324becb3209c78509a4b1df0.
Every older frozen source and both historical errata remain.
In particular S261's wrong physical binding remains refuted.
The actual map is C=R^(-1/2), U=R^(-3/4). The original
parameters include kappa=10^800, zeta=10^-6, Proca mass1000,
the full heavy mass, fixed profiles, normalized source,
primitive contact and all matter/spatial channels.

Use the SAME full pure nonzero reference from S251/S264,
the actual H SLE and all-order three-mode Proca prescription.
Its exact covariance V defines z=S w, V=SS^T/2 after
the entire canonical/Fourier normalization is restored.
No covariance ceiling is substituted for V itself.

## Fixed finite family and assertion

Only u=0 is evaluated. The2pi torus has L=1.
Lambda={+-P e1,+-P e2,+-P e3}, P=10^64, gives six real
basis functions and eight configuration channels each:
d=48 canonical pairs,96 phase coordinates. Homogeneous
variables are external reference parameters, not an
additional quantum state. The full nonlinear spatial
reconstruction keeps all harmonics and generated means;
this is not an invariant finite-mode truncation of the PDE.

For all ||w||<=2*10^20, the complete source-pinned twelve
invariants are uniformly below10^-260 in absolute value,
strictly inside the S266 box of radius10^-250.
The full nonlinear lapse and temporal branch therefore
apply to every reconstructed point at the bounce.
Every mean-zero momentum constraint is solved by the full
adjoint inverse; the three global translations remain.

For any smooth invariant0<=chi<=1 equal1 on ||w||<=10^20
and compactly supported inside ||w||<2*10^20, use

    Fext=F0+chi(F-F0),

where F is the complete spatially averaged physical
exp(3v)R_full(0,Nstar)^(-3/4) and F0 its ACTUAL original
canonical-center value. This is the S268 named coherent
extension, with no state conditioning or background reset.

The same-seed coherent outside mass is strictly positive
and less than exp(-10^39). The positive infinite-Hilbert
operator satisfies ||Q_V(Fext)-I||<10^-255, also on the
common zero-translation-charge sector. Two such cutoffs
with the same core/F/F0 have seed vector difference
less than2*10^-255 exp(-10^39/2) and seed mean difference
less than2*10^-255 exp(-10^39).

## Required full derivations

The proof includes every original canonical row and
covariance block; full Fourier A2 rather than an
inapplicable high-P A8-smallness assertion; the variable
coefficient adjoint's derivative-Q term; all source
momentum, shear, Gauss, magnetic, mass, matter gradient,
density and intrinsic-curvature contributions; and
the whole implicit lapse and U_N strip bound.

The trace momentum is preserved exactly by the cotangent
lift, but the shear momentum is NOT. Initial coherent
mass is a POVM probability, not exact compact support
or a sharp joint law of noncommuting fields.
The two-volume comparison is for the uncalibrated
coherent readout; it does not omit ordering contacts
from a different Weyl-calibrated construction.

## Open boundary and evidence

No evaluated positive time interval, evolved-state
concentration, interaction norm, Hamiltonian ordering
error, calibrated-volume positivity, original quantum
mean, uniform volume/mode limit, matching cutoff,
omitted-loop bound or nonlinear global completeness
follows. Original V/G/B/P8 remain OPEN; the completed
scoped P8(a) objective is unchanged.

Exact source bindings, rational bounds and identities
support written Banach/operator proofs, not FORMALIZED
proofs. Independent numerical fixtures are diagnostics.
Frozen18-source and20-field manifests, complete prior
rebuild, native raw report hashes and observed separate
ordinary/CLI/full zero exits govern publication.
