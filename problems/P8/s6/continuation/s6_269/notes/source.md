# Entire source, density invariants and the fixed finite family

This is an evaluated member of the S268 finite coherent regulator,
not a new original action, state or physical UV cutoff. Work only
at u=0. Fix the2pi torus L=1 and the six nonzero wavevectors
+-P e1,+-P e2,+-P e3 with the exact integer P=10^64.
There are six real sine/cosine basis functions, eight physical
configuration channels per function, d=48 pairs and96 phase
coordinates. The basis is orthonormal in the FULL coordinate
torus integral. Homogeneous reduced variables are external
reference values, not an additionally selected quantum state.

The full S267 shape and cotangent reconstruction is used. All
nonlinear Fourier harmonics and generated homogeneous shape
terms remain, even though the canonical input has only these
six real harmonics. The mean-zero momentum constraints are
solved by the complete adjoint inverse. The three residual
global translations remain: they are imposed as the S268
quantum constraints, not discarded by declaring all classical
coherent displacements to have zero charge.

Every original R,F,B,Fhat,j, fixed profile, heavy mass, source,
primitive boundary and matter/vector channel is retained.
The original common kappa is factored out in the normalized
one-form and Hamiltonian and restored in the canonical state
dictionary. Write V=sqrt(det gamma) and g=gamma^-1. The reduced
trace momentum is Pi_v=2tr(pi gamma), with

    p=Pi_v/(3V).

The factor3 follows directly from the trace velocity:
K=tr(g gamma_dot)/(2N). A conformal velocity variation
delta gamma_dot=2N delta k gamma has delta K=3 delta k and
zero traceless velocity variation. Pairing it with pi gives
the same trace density in the entire Legendre transform.
It is not legitimate to identify p with a unit-CCR momentum.

Let pi_TF=pi-tr(pi gamma)g/3. The complete twelve S266
invariants, in its original order, are

    p=Pi_v/(3V),
    G=div(Pi_W)/V,
    dp=Pi_M/V-1/10,
    ph=Pi_H/V,
    eta=10^100 H,
    sh=tr(pi_TF gamma pi_TF gamma)/V^2,
    el=Pi_W^T gamma Pi_W/(zeta V^2),
    ma=zeta g^ik g^jl F_ij F_kl,
    wm=W^T g W,
    gm=grad(M1)^T g grad(M1),
    gh=grad(H)^T g grad(H),
    curv=R[gamma],  F=dW.

These expressions include the full metric, both density
factors in quadratic momenta, the complete antisymmetric
magnetic field and intrinsic scalar curvature. The direct
source checks retain the original five shear, three electric
and two matter Legendre blocks and the Gauss integration
by parts. The non-diagonal finite diagnostic does not restrict
these general formulas to a diagonal metric or a selected
channel.

The background density Pi_M=1/10 must be kept:

    dp=(Pi_M-1/10)/V+(1/10)(1/V-1).

The other reduced field/momentum backgrounds on this slice
are the original reference values. The actual positive lapse
at the canonical origin is the S266 full-source root, within
10^-375 of1, not a substituted bare root. The mass-adapted
H invariant is eta=10^100 H, with unchanged mass ratio in
the complete Hamiltonian.

A deterministic image bound for all these quantities on a
finite canonical phase ball is not a joint Gaussian law for
them. They are nonlinear functions of the original canonical
variables, not independent Gaussian oscillators.
