# Same full covariance and evaluated physical field ceilings

Let V be the full unit-CCR covariance of the SAME finite
original pure nonzero-mode reference at u=0. Define the
whitening by z=S w, V=SS^T/2. This does not require replacing
V by its norm bound. For any complete physical row l,

    ||l S||^2=2 l V l^T,
    |l z|<=sqrt(2 l V l^T)||w||.

The exact V remains the one defined by the original preparation
and flow. The inequality preserves every cross covariance;
it is not a diagonal or occupation-only state approximation.

The original S251/S264 coupled scalar balanced coordinates
r=(sqrt(P)y,Pi/sqrt(P)) have covariance<=10^21 I at this
actual momentum and time. Thus each complete coordinate row
has whitening norm below10^11. The entire central map and
original boundary data give at the bounce

    v=Pi_b/(2P^2 sqrt(kappa)),
    Pi_v_normalized=-2P^2 y_b/sqrt(kappa),
    M1_fluct=y_m/sqrt(kappa),
    delta_Pi_M=Pi_m/sqrt(kappa).

The symmetric central shear has instantaneous value0;
its derivative is not declared zero. No time-extension
claim is inferred from this bounce evaluation.

For each of both tensor polarizations, the unchanged balanced
covariance is<=10^5 I, giving row norm below1000. Use a
Frobenius-orthonormal TT polarization basis, so every matrix
polarization has operator norm<=1. The original action
normalization gives tau=-2 h_canonical/sqrt(kappa) and
Pi_tau=-p_h/(2sqrt(kappa)) in this basis. Both signs and
the complete dual normalization are retained.

For H, S240/S242 supply the exact comparison mode with
balanced norm<20 and the actual SLE relation
T=alpha S+beta conjugate(S), |beta|<1/2. Since
alpha=sqrt(1+|beta|^2), alpha+|beta|<2, including the
complex beta phase. Therefore the actual mode has balanced
norm<40 and full covariance<1600 I. At the bounce a=1,Hubble=0
these are canonical chi/chi' quadratures. The row ceiling100
is safe. The actual Omega=sqrt(n+P^2/16) lies in[10^98,10^100].
This is a bound for the original SLE, not a change to W6 or
its exact comparison state. Physical H=H_canonical/sqrt(kappa).

For Proca, the original S190 all-order state has graph norm
below1/10 at the actual high momentum: nu_minus>P/2>10^16.
The entire six-quadrature covariance obeys
Sigma<=(1+1/100)/(1-1/100) I<2I. At the bounce,

    K=I+kk^T/m^2, B=sqrt(K), omega=sqrt(m^2+P^2),
    A=B Q/sqrt(omega), pi_A=B^-1 sqrt(omega)Pquad,
    W=A/sqrt(kappa*zeta),
    Pi_W=sqrt(zeta/kappa)pi_A.

The coordinate and momentum factors are different and both
are required. All three polarizations and all coupled graph
covariances remain. Since P<=omega<2P, ||B||=omega/m and
||B^-1||<=1, the full vector row estimates do not mistake the
longitudinal coordinate for a transverse one.

For physical Fourier momenta use the Banach norm A_s with
weight(1+|k|)^s, absolute scalar, Euclidean vector and matrix
operator coefficient norms. A normalized real sine/cosine
basis function has A0 norm sqrt(2/(2pi)^3)<1.
Its A_s norm gains the exact factor(1+P)^s.
Summing ALL six functions, both tensor polarizations and the
whole three-vector rows gives the following conservative
ceilings per R=||w||:

    ||v||A2 <=10^-350 R,
    ||tau||A2 <=10^-295 R,
    ||Pi_v||A0 <=10^-288 R,
    ||Pi_tau||A0 <=10^-360 R,
    ||M1_fluct||A1 <=10^-350 R,
    ||delta Pi_M||A0 <=10^-350 R,
    ||eta||A0 <=10^-340 R,
    ||grad H||A0 <=10^-375 R,
    ||Pi_H||A0 <=10^-343 R,
    ||W||A0 <=10^-360 R,
    ||Pi_W||A0 <=10^-365 R.

The report retains every exact rational prefactor before
these ceilings. No floating representation of kappa=10^800
is used. On the support ball R<=2*10^20 they imply
||tau||A2<10^-274, ||v||A2<10^-329, and every one of the five
momentum fluctuation A1 norms below10^-200. Also
||W||A1<10^-275, ||grad M1||A0<10^-329 and
||grad H||A0<10^-354. These bound the full source of the
nonlinear cotangent lift, not just its metric part.

The evaluation uses the exact reference covariance defined
by its original integrals and all-order prescription. It
does not require a newly minimized numeric covariance table,
a new homogeneous vacuum, or equality with the original R3
state. The strong finite bound is not uniform under arbitrary
increases of mode count or torus volume.
