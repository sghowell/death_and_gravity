# Exact checks, independent diagnostics and publication boundary

The proof packets bind the complete source and canonical
normalization; exact full-field rational budgets; the generic
off-slice adjoint and all derivative-Q contacts; full
non-diagonal cotangent trace preservation; every nonlinear
invariant image; actual whole-strip lapse/volume bounds;
and the complete finite-dimensional coherent gamma tail.

There are44 named exact checks containing172 scalar
entries,76 proof gates, nine controls and167 rejected
input cases. Nine primitive and125 matching rows remain.
Matrix scalar entries are not separate scientific theorems.
The Banach, smoothness and operator proofs are written
arguments, not FORMALIZED proofs.

Independent tests include the full variable-coefficient
Galerkin ghost/adjoint on three increasing two-dimensional
mode sets and three amplitudes. The exact smooth Q has
Bessel Fourier coefficients and detQ=1, divQ=0. The
diagnostic compares M* with the transpose, detects the
wrong derivative-free adjoint, and checks the Neumann
inverse and complex dual pairing. It is not a discrete
diffeomorphism algebra or proof of the full inverse.

Twelve random full non-diagonal positive metrics test the
exact cotangent trace and nonzero shear correction.
Eight correlated squeezed pure-covariance fixtures test
whitening rows and physical kappa factors. Eight radial
smooth-cutoff examples in dimensions1,2,8,48 compare
independent quadrature against the complete gamma-mass
bounds. An arbitrary-exponent160-digit check tests the
actual extremely small tail without floating underflow.
All twelve invariant bounds and every rejected scope
input are tested separately.

The initial field and geometry probes passed. The first
complete private packet run passed all four packets.
An independent science subset then passed218 tests with
four complete packets deselected in2.10 seconds. These
are development observations before the final source
freeze, not replacements for the final release checks.

The scientific source manifest comprises seven package
modules, two test files, README, FORMULATION and these
seven notes:18 files. The report has20 AST-derived
fields. The certificate tests reject mutation of every
field, missing/extra fields and any mismatch of counts,
sources, complete report or retained original boundary.

Before publication: Ruff/format and sibling-export checks;
complete original-SymPy private preflight with RuntimeWarning
as error; exact-byte freeze; fresh repository preflight;
protected original-SymPy native rebuild and raw unreformatted
report transfer; independent ordinary package and CLI
replays; and one separately captured full P8 regression.
Only the full runner uses the independently audited
exact-GCD adapter and its original-path selfchecks.
An adapter/full success never substitutes for original-
SymPy native/ordinary/CLI evidence.

Actual timings, zero exits, snapshot hashes, source hashes,
raw report SHA and any failed diagnostic/correction history
belong in the mutable external assessment. Frozen science
files are never edited to add later evidence. Publication
excludes unrelated P4/P9 work and does not close original P8.
