# Complete flat continuum limit and canonical normalization

Both conversion and sine-bulk estimates use the same computational
squared-spectral-mass limit Lambda. Their exact finite-Lambda
identity is inherited from cubic division before integration.
Therefore their continuum limits have the same exact relation.

Let U^2=Z^2+||Gamma^(6)||L2^2. The triangle inequality gives

    |F(D,Gamma)| <=(1e10+1e-5)||D||L2 U
                  <2e10||D||L2 U.

For Lambda>=4m^2, 1/Lambda<=1/(2m sqrt(Lambda)).
The sum of the two tail bounds is at most

    (1e17/(2m)+1/100)||D||L2 U/sqrt(Lambda)
      <1e14||D||L2 U/sqrt(Lambda)

at m=1000. These are all-external-momentum, full spatial tensor
bounds on the actual specified improper nonlocal integrals.
They are exactly zero if either test is zero.

For the external-metric change h=sqrt(kappa)gamma/2, retain both
chain factors: the current response is multiplied by 4/kappa.
The complete F displays become 8e-790 and
4e-786/sqrt(Lambda). They are not a scalar canonical normalization
for an independently propagating spin0 degree of freedom or a
fully reduced mixed gravity/light-field norm.

Neither convergence nor smallness selects a physical finite local
polynomial. Lambda is not a physical cutoff. The original contact
one-leg and memory two-leg regulators have not been equated at a
finite value or assigned interchangeable finite counterterms.
No inverse poles, high-frequency modes or curved boundary terms
are deleted. A derivative-losing weak flat bound does not furnish
a same-space contraction, quantum background or stability theorem.
