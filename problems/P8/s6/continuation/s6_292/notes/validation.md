# Validation contract

The source manifest contains exactly eighteen ASCII files: seven
Python package modules, two Python test modules, README.md,
FORMULATION.md and seven notes. Its native JSON report contains
twenty top-level fields and is a separate nineteenth artifact.
The report is immutable raw evidence, not a place for external
validation summaries.

The source module copies the frozen parent's original checks before
adding its complete graph inventory. The proper module checks the
entire arbitrary-D Gram contraction, independent literal D4/5/6
components, the four-master pair reduction, contact trace, complete
LSZ collapse, separated UV accounting and whole B0 coefficient.
The masters module checks full parameter polynomials, all radial/
root/Cayley Jacobians, primitives and subtracted first derivatives.
The pairing module checks the full conserved current, every self and
cross contribution, invariant real/virtual pole cancellation,
evanescent finite coefficient, scale/unit cancellation and remainder
normalization.

The science tests include independent exact rational component
examples, complete numerical normal-sheet moments and derivatives,
and two whole-D real/virtual finite-window limits. Numerical cases are
identified as numerical evidence, not exact all-domain certificates.
Written graph and analytic arguments are necessary: Boolean gates and
finite test cases alone do not establish graph completeness, analytic
continuation or physical infrared existence.

Acceptance requires original-SymPy private cold and fresh-root
preflight with parent-cache warmup, exact eighteen-file source hashes,
all residuals/gates, recursively exact scientific payloads, all module
exports and the twenty-field AST contract. Only identical final source
bytes are copied into the repository and supplied to native replay.
The native helper must verify the source inventory and complete parent
ancestry and stream its raw report with exact chunk/hash checks.
No raw-report repair or frozen-source editing is permitted.

After source/raw freeze, separate original-SymPy ordinary and CLI
replays must both pass. The shared FULL gate must retain original test
collection, its exact source-file hash and the guarded exact-GCD
adapter contract; that adapter is FULL-only. A passing earlier FULL
does not cover this successor. External publication records actual
lane outcomes and exact source/raw hash equality before commit/push.

The rejected S6.279 packet remains archived outside accepted tests.
No source, raw report, scoped P8(a), P4/P9 work, matching coordinate,
physical prescription or historical qualification is changed here.
Original V/G/B/P8 remains OPEN regardless of this packet's test status.
