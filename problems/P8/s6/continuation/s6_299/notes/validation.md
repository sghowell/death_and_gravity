# Validation and immutable evidence contract

Seven modules, two test files and nine Markdown files form the exact
eighteen-source manifest. The native report has twenty top-level fields.
All scientific residuals are exact and contain no floating-point numbers.
Numerical probes are independent calibrations, not uniform-bound proofs.

The private research probe checks the eikonal sum through seven
permutation orders, Dirichlet recurrences, whole finite-regulator sums,
independent inverse Laplace transforms, the two-real excluded wedge,
whole Gamma inequalities and distinct ordered/joint limits. Tests
separately reject the power law above the reference-energy domain.

Cold and fresh preflights must agree after forcing complete immediate
parent packets. Source bytes are frozen before the native specification.
The native helper is protocol-only and its report is captured without
regeneration. Canonical REPORT path, source hashes, raw bytes, twenty
AST fields, every count and unchanged original frontier are checked.

Native, direct, ordinary and CLI use unmodified SymPy. Only the complete
P8 regression may use the separately audited exact-GCD adapter.
Native success is not a substitute for the ordinary/CLI and complete
captured regression. External assessments record acceptance and any
failed attempts; the frozen scientific sources are not edited afterward.

Original V/G/B/P8 remain OPEN and scoped P8(a) remains unchanged.
