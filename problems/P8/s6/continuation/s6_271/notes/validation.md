# Read-only validation and publication contract

This source packet comprises exactly 18 files: seven package modules,
two test files, README.md, FORMULATION.md and these seven notes.
The report has exactly 20 top-level fields and binds the immediate parent
SHA-256 5b6adbb4be256ec2df0051d781d43a2e94b8ab12d099c79295e87c821fb0fc01.

The exact packet has 28 named checks, 32 scalar residual entries, 43 proof
gates, nine controls, 235 rejected inputs, nine unchanged primitive rows
and 127 qualified matching rows. Every residual must be exactly zero,
every gate must pass and every rejected-input case must actually fail.

## Independent science tests

The science suite contains 872 parameterized cases, including:

- Whole source, Gaussian-kernel, derivative and operator-comparison packets.
- Independent defining-Weyl-kernel versus coherent-matrix-element Gaussian
  quadratures, off-diagonal phases and identity normalization controls.
- Entire-real-axis coherent-measure Schur integrals.
- Every one of the 193 total-order product-Schur coefficients.
- All 197 full radial-partition/Cauchy bounds and 196 decreasing jet ratios.
- The distinction between high phase and high real-time derivatives.
- Positive Gaussian symbols with negative Weyl excited-state eigenvalues.
- Independent Gaussian-radius Weyl and calibrated-coherent spectra,
  including large occupation samples and explicit one-pair error bounds.
- Noncommuting full-unitary Duhamel, changed-readout and reference-conjugation
  fixtures, exact actual rational margins, inherited scope and rejection.

These are diagnostics of the written full theorem, not a P8 Hilbert-space
truncation used as its definition or a simulation of its full evolution.
Runtime warnings are errors in the scientific checks.

The certificate suite adds 25 tests: complete read-only replay, manifest
equality, separate mutation of all 20 fields, missing/extra-field rejection
and exact count/scope controls. Expected ordinary total is therefore 897.

## Freeze and independent replay

Publication requires all of the following; the external assessment records
the actual observed outputs and times without editing these frozen sources.

1. Private original-SymPy preflight of all source hashes, AST field order,
   exact residuals/gates and full science suite.
2. Byte-identical repository copy, acceptance of its exact source manifest,
   and a fresh original-SymPy preflight of that accepted copy.
3. Native full-ancestry original-SymPy report construction, complete raw
   transport/hash validation and equality of all source and report fields.
4. Independent original-SymPy ordinary and CLI read-only replays.
5. A captured full-P8 regression with all captured source inputs unchanged.
   Only this FULL regression may use the separately audited exact-GCD adapter.

No ancestor source or certificate may be silently repaired or replaced.
Private staging source bytes are frozen with the accepted repository copy.
The report is added only after the native raw-output hash and complete
manifest validation. The publication assessment is kept external to that
frozen report source set, so observed replay evidence can be recorded
without regenerating an already certified source-bound report.

The count tests and mutation controls certify report integrity, not the
mathematical truth of every written analytic sentence. Original P8 remains
open even after every listed validation succeeds.
