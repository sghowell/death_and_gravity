# Unchanged full source and high phase domain

The immediate parent is S6.270 with report SHA-256
5b6adbb4be256ec2df0051d781d43a2e94b8ab12d099c79295e87c821fb0fc01.
Read-only ancestry replay and its source hashes are mandatory. No parent
report, source, state preparation or external correction is overwritten.

## Physical bindings

Use the same finite torus L = 1, six nonzero real spatial basis components
from the symmetric momenta +/- P e_i, P = 10^64, and eight physical channels.
There are 48 canonical pairs. The original physical kappa = 10^800 and
zeta = 10^-6, light/scalar, two tensor, heavy and full Proca sectors remain.
The pure covariance is the original V0 = S0 S0^T/2. The whitening is the
actual symplectic S0, not a diagonal replacement or a change in the CCR.

The full original reduced Hamiltonian retains the moving-chart subtraction,
clock and matter mean, all primitive terms once, background scalar terms,
and the reference subtraction. Write exactly g = (h_red - h_ref)(u,S_u z).
The normalized physical volume is the original spatial average of
exp(3v) R_full(u,N_star)^(-3/4), pulled through that same free flow.
Neither its center F0 nor the scalar g0 is reset to a convenient value.

All nonlinear spatial harmonics, generated means, the full mean-zero spatial
inverse, actual nonlinear auxiliary root and its mixed derivatives, complete
matter/vector/Gauss terms and the three residual translation charges remain.
Finite canonical input coordinates do not mean truncation of the nonlinear
spatial reconstruction to the initial input modes.

The original primitive frontiers, the S6.261 physical-binding refutation,
the S6.265 Gaussian integrability boundary, the earlier retarded-phase
erratum and the S6.269 Omega_star clarification are inherited unchanged.
Omega_star is the fixed heavy energy-balancing scale, not the physical
time-dependent heavy dispersion. Complex spatial adjoints mean holomorphic
continuations of the real formal transpose, not conjugate transposes
applied to complex field values.

## Uniform inherited domain

At real |u| <= T = 10^-2000, S6.270 established the complete reconstruction
on complex initial phase ball 4R, R = 10^20, with

    |g(u,w)| < H = 10^1000,     |F(u,w)-1| < E = 10^-255.

These are bounds for the complete implicit functions and spatial integrals.
They are not homogeneous-vertex or quadratic approximations. The free image
of this complex ball was controlled inside the parent field domain, and its
full lapse/geometry branch was selected there.

The radial cutoffs are the same c = 1 and c = 2 choices. Their supports have
radii sqrt(2)R and sqrt(3)R, respectively, both strictly below 2R. The complex
source need not be evaluated where the extended symbol is constant.

## Why 196 phase derivatives are legitimate

At a real point with |w| <= 2R, use the simultaneous coordinate polydisc of
radius R/8 in all at most 96 complex coordinates. Every point in it has norm
at most (2 + sqrt(96)/8)R < 4R. Cauchy's formula therefore bounds every
multiindex alpha of total order n by

    |partial^alpha g| <= alpha! (8/R)^n H <= n! (8/R)^n H,

and likewise F - 1 with E. The centered functions g-g0 and F-F0 have
amplitude bounds 2H and 2E; their positive-order derivatives are not changed
by centering.

This step requires arbitrarily high PHASE derivatives of holomorphic
finite-dimensional reconstructions at each real time, not higher real-time
derivatives of the fixed C5 profiles. The smooth real-time dependence used
for bounded propagators is inherited separately. In particular, the old
R/4 coordinate-polydisc argument valid for at most four varying coordinates
is not extended incorrectly to all 96 coordinates.
