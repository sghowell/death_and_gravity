# Sharper interference with the named logarithmic term

For either unit TT polarization the original current also obeys
|S|<=64/(sqrt(kappa)w). Let u=min(y/192,x) and L=ln(1/x).
Below u, |F|<=|S|+B/(sqrt(kappa)y). The elementary primitive
int_0^u w ln(1/w)dw=u^2*(ln(1/u)+1/2)/2, together with
u/y<=1/192, yields
int_0^u w|F|ln(1/w)dw
< [64+B/384]*x*(1+L)/sqrt(kappa).
Here z*(ln(1/z)+1/2) is increasing for0<z<=1/8, and
int_0^x ln(1/w)dw=x*(1+L).

Above u (only when u<x), apply the physical high-energy amplitude
bound, not a continuation of the low-energy Taylor estimate.
The soft, matter and gravity parts contribute at most
[64+Bm/16+D]*x*(1+L)/sqrt(kappa), respectively. For the matter
part use x<=1/8 and its w ln(1/w) primitive. For gravity use
y^2/(y^2+w^2)<=1. The two band budgets sum to
128+B/384+Bm/16+D=30000000000843509/3<2e16.

For the frozen S326 named L_lambda=C_Born,lambda ln(1/w)/kappa^(3/2)
with |C_Born,lambda|<320, bound the full signed difference by
rho sum(2|F_lambda||L_lambda|+|L_lambda|^2).
Both polarizations and4*pi^2>36 give
TV<1e18*x*(1+L)/kappa^2
+3000*x^2*(L^2+L+1/2)/kappa^3.
The square follows from int_0^x w ln(1/w)^2dw
=x^2*(L^2+L+1/2)/2 and320^2/36<3000.
At the original point the linear coefficient is1e-1582.
This strengthens the still-valid frozen S326 coarse coefficient
4e-1187 without rewriting it or changing the named amplitude.
The quadratic term is retained as an algebraic square, not assigned
one-loop perturbative order. This result is not a full radiative loop.
