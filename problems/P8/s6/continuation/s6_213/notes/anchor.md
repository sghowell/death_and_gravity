# Same prepared current, full contact and homogeneous anchor

Let gamma be spatial tracefree unimodular shear, with its common zero initial neighborhood. In the actual reduced Proca Hamiltonian,

    K_gamma=exp(gamma)/a-grad div/(a^3m^2),
    V_gamma=a m^2 exp(-gamma)+curl exp(gamma) curl.

The constraint A0=-div pi/(a^3m^2) is gamma independent. At transferP0 the S195 two-momentum generator and its first variation are precisely the homogeneous matrices of S194. The state is the same all-order prepared covariance, and its tangent is zero at the initial time because the source germ is unchanged, not because quantum metric data have been set to zero.

Differentiating the finite-mode matrix flow yields both tangent source terms,

    delta C'=A delta C+delta C A^t
                +J M_Gamma C+C M_Gamma J^t.

This is the P0 specialization of the off-diagonal S195 response. Both formulations have identical initial data; uniqueness of this linear finite-dimensional tangent ODE identifies them. The current is read out with -M_D/2 and the complete second metric vertex. The mixed exponential coefficient is (D Gamma+Gamma D)/2 and the mass contact has its positive second-variation sign. These generator and contact identities are independently checked using noncommuting matrices. The pair convention ell=-q only relabels the real-field Fourier algebra; it does not duplicate fields or omit reverse pairs.

For the original common finite mask the exact S208 decomposition is

    Jactual,K(P)=Known_K(P)+C_unit,K+U_pair,K(P),
    Known=R_actual-unit+F_unit+Qnew.

C_unit,K(P)-C_unit,K(0)=0 before integration, by the full second-vertex convolution. The contact itself is generally nonzero and remains in the homogeneous anchor. In the spatial difference the singular continuation is therefore entirely the difference of U. The uniform dimension-limit proof identifies its convergent complement; S212 evaluates its original fixed pole-subtracted finite part Ffinite(P).

Since S194 gives the same complete renormalized homogeneous first variation H0 in the unchanged prescription, the assembled response is

    Jren(P)=H0+[Known(P)-Known(0)]+Ffinite(P).

Here P0 always denotes an identity spatial multiplier evaluated from the explicit kernels, not a new physical constant history. This formula reproduces H0 exactly at P0 because Ffinite(0)=0. It includes the full state/time/endpoint pieces and contact. Neither S204's local action target nor the earlier Q203/Pfin pieces are added again. The fixed subtraction scale, finite coefficients, actual prepared state and original two-leg mask are unchanged.

The actual covariance tangent is retarded. Repartitioning it and adding the same local metric counterterms preserves its distributional time causality. The statement is a weak tracefree spatial Gaussian response; it is not strong differentiability of an unbounded stress operator.
