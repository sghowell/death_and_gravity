# Corrected raw rows and complete phase-domain proof

## Two changed rows, not an assumed old equality

Use the original prepared covariance and fixed prepared reference.
The earlier full generator bound maps initial phase ball4r into8r
for either r=R1e20 or the analysis radiusRstar1e150. This is a linear
reference-image estimate, so its relative bound is radius independent.

Keep the S269 prepared row ceilings, retaining every full covariance
entry. With Bv=4 old_v_A2/(1+P)^2 and
Bs=old_M1_A1/(1+P), the raw boundary increments satisfy

    extra_Piv <=144T Bv+(3/10)Bs <old_Piv_A0,
    extra_PiM <=(3/10)Bv <old_delta_PiM_A0.

Here a_bar^3<2, |Hbar|<=4T and3a_bar^3ellbar=3/10. Thus double the
trace and matter momentum rows and leave the other nine unchanged.
Evaluate the whole corrected rows at8r. The matrices/covariances are
not called unchanged in physical raw coordinates.

## Whole nonlinear reconstruction and source image

Recompute all four original momentum-source contributions: full
trace Pi_v grad v-gradPi_v/3, full shape/cotangent derivative, vector
Gauss source and both matter sources including heavy. The complete
mean-zero formal-transpose inverse bounds the longitudinal correction.
Keep all reconstructed harmonics and generated means.

The same full determinant/shape and inverse estimates apply because
the corrected metric/shape A2 norms are<1/1000 at BOTH radii. The
twelve pointwise invariant images and twelve average remainders are
displayed in full. Scalar curvature keeps its zero-mean linear
-4a^-2 Delta v and nonlinear remainder2e12P^2(v+tau)^2.
The source box includes absolute homogeneous data and all corrected
phase images, not just fluctuations about an off-shell N=1.

The full lapse self-map residual includes5e-400,
12e4 times the complex homogeneous radius andNz times the sum of
ALL corrected phase images. At both radii contraction is<1/20,
the full self-map lies inside1e-115 and |C_N|>2. First and mixed
second implicit rows are Nz1e4,Nzz1e18. For the actual uniform root
N0(Y), the complete mean response is bounded by

    average deltaN <= Nz sum average_deltaZ
                         +(Nzz/2)(sum point_deltaZ)^2.

This is Taylor's exact integral-remainder estimate of the FULL
function, not a truncated Hamiltonian or an independent lapse.

The centered volume amplitude includes20v^2, the72v deltaN contact,
12 average deltaN and5000(deltaN)^2. It is<1e-510 with corrected rows.
The full Hamiltonian spatial-average density variation retains the
scalar center, all quadratic integral remainders and every density
cross product, and is<1e-520 on the core image.

## Additional full spatial boundary energy

The pointwise F_t coefficient is bounded by

    18(4+48T^2) <1000.

For a completely conservative A0 bound use
Vol(T^3)=(2pi)^3<512<1000. Consequently the ENTIRE added boundary
energy is bounded by1e6 kappa v_A0^2. This includes the spatial
integral explicitly and does not rely on a tighter conversion from
real-mode coefficient bounds to Parseval norms. It is added at
both radii, not just at the bounce, and is independent of live Y.

## Complete quadratic energy and entire higher-order remainder

Start at the original literal raw H2 and C1 from S274, with the
complete C_N Schur correction. Its six positive row envelopes
retain trace/shear cancellation, both matter momenta, all TT
curvature, potential/heavy terms, longitudinal Gauss, transverse
curl and the full lapse correction. Every term has degree<=2 in
the two changed row bounds. Four times EACH old positive quadratic
row therefore bounds the corrected raw quadratic contribution.

Add the full F_t bound and a SEPARATE bound for href. No equality
between the actual-profile/live-root H2 and href is assumed. The
only such equality elsewhere is the expressly restricted tree
diagnostic used to locate the boundary error.

The spatially uniform shear preserves zero input means. At the
uniform root H_N=0, and all first input phase derivatives of the
complete spatial energy average vanish. Thus the centered full
interaction has zero constant/linear phase jets. The complete
nonlinear symbol is holomorphic in phase and the homogeneous
complex polydisc, with uniform bounds at each real source time.

For z in4R, scalar radial Cauchy on4Rstar bounds the entire degree
>=3 contribution by

    A_large (R/Rstar)^3/(1-R/Rstar).

With the corrected rows and full boundary integral A_large<1e533,
the entire tail is<1e143. Add the independently bounded full raw
quadratic, reference quadratic and boundary generator to get
AH<1e172. The exact evaluated sums, not these loose individual
decimal ceilings alone, check the final inequality.

Rstar changes the ANALYSIS domain only. Both physical cutoffs keep
R1e20 and the original prepared covariance. No term of the full
nonlinear Hamiltonian is deleted or defined by this Taylor estimate.
