# New corrected coupled paths, volume and regulator comparisons

## Homogeneous force chain and symmetry

Retain the original absolute homogeneous Hamiltonian, all six
canonical-density rates and their full lapse responses. The new
nonzero-mode shear is independent of live Y and has zero-mean
linear momentum increments. Therefore the homogeneous one-form,
initial data, cyclic M1 charge and canonical force formulas are
unchanged, with the corrected complete quantum K inserted.

The full scalar center g0(Y) remains in all background forces.
Only its projective global phase is factored from the Hilbert
comparison unknown. The heavy1e100 normalization, source, mass,
dilution terms and all scalar/matter contacts remain. The exact
reference residual includes the nonzero fixed pressure; the fixed
profiles are not replaced by the evolving means.

The original prepared covariance is radial with every internal
and q-p entry. Its actual scalar/tensor, heavy SLE and full Proca
prescriptions give the24 proper cubic actions checked previously.
The boundary now commutes with those actions and all3 translations,
so its transported physical raw covariance is invariant too.
No state averaging, fresh isotropic vacuum or projection is used.

Cubic equivariance of full source reconstruction and uniqueness of
all local roots gives the same symmetry of the corrected operators.
The invariant quantum path forces all three homogeneous vector
and all FIVE homogeneous traceless-shape coordinate/momentum pairs
to remain zero. Their Reynolds averages are checked in full.
The heavy homogeneous scalar is a live singlet and is not removed.

## Exact coupled fixed point

Let T=1e-180, real homogeneous radiusrY=1e-390 and weightw=1e-350.
The complete path space has the original Y Cauchy data and the
original PREPARED pure seed, with real continuous Y withinrY of
the prescribed reference and normalized continuous invariant
Hilbert paths. Its metric is the weighted MAXIMUM

    max{sup|deltaY|, w sup||delta_phi||}.

This is a closed complete product subset; the Hilbert unit sphere
need not be convex. The map integrates the full background-plus-
corrected-quantum force at input(Y,phi), and evolves the exact
bounded corrected quantum Hamiltonian at inputY. Unitarity
preserves norm and the actual invariant sector.

Write B1 for the first Y derivative operator bound,
Fq=1e104 B1/kappa and Lq=1e106(B1+B2)/kappa.
The original full homogeneous Lipschitz bound is Lh1e112.
The actual reference lapse displacement and fixed C5 profiles give
the exact checked rate error<E0=1e-280. The self-map displacement is

    T(E0+Lh rY+Fq)<rY/2.

The two contraction rows are

    T(Lh+Lq)+2T Fq/w,
    w T 5 B1.

Both are<1e-50 with corrected operators. The second uses unitary
Duhamel, not exp(T||K||). The contraction theorem yields a unique
NEW corrected coupled solution in the declared domain. The
norm-continuous bounded operators and complete source regularity
give C1 paths. Cyclic M1 is reconstructed by its full rate and
a^3pm remains conserved.

The physical raw state is Ub Uref exp[-i integral g0]phi. Ub is
configuration dependent; its full time generator is already in K.
Neither old/new solutions nor their initial raw covariances are
identified by uniqueness of a DIFFERENT archived equation.

## Corrected physical volume turnaround

The complete centered volume operator and the actual homogeneous
root/scale deviation give

    |nu(u)/(1+u^2)^6-1|<epsilon=1e-380.

All products of the small physical-scale and centered-readout
errors are included. The endpoint and location estimates evaluate

    (1+T^2)^6(1-epsilon)-(1+epsilon)>5T^2,
    [(1+rmin^2)^6-1](1-epsilon)-2epsilon>0,
    rmin=1e-188<T.

Continuity gives a global minimum; every minimum is strictly inside
|u|<rmin. The C1 mean has a critical minimum, and the endpoint gaps
give a negative and a positive derivative point on the respective
sides. This does NOT prove uniqueness, strict positive acceleration,
monotone half-intervals or a quantum homogeneous bounce.

## Positive core probability

The original prepared Gaussian has positive Husimi outside-core
tailtau<1e-4000. For every allowed corrected Y path,

    ||phi(u)-psi0||<=BH |u|.

The coherent outside-core effect is a positive contraction, so its
expectation on the evolved state is<=tau+2BH T<1e-6.
In physical raw coordinates conjugate BOTH effect and state by
Ub Uref. No exact joint phase support, sharp projection or zero
leakage is claimed.

## Fully coupled cutoff and ordering comparisons

For H use SH=finite coherent polynomial bound, EH=complete heat
remainder, BH=SH+EH, DH=ordering difference. Use analogous first-
parameter derivative rows S1,E1,B1,D1 and volume rows SF,EF,BF,DF.
Every remainder is retained, including the part without core support.

At fixedY the two-cutoff seed action and evolved displacement give

    ecut=2SH T sqrt(tau)+2EH T+BH^2 T^2,
    Fcut=1e104/kappa [2S1 tau+2E1+4B1 BH T].

For ordering use eord=T DH,Ford=1e104 D1/kappa. Actual homogeneous
histories are coupled, not externally identified. With

    a=T(Lh+Lq), b=2T Fq, c=5T B1,

the complete integral inequalities are y<=ay+bz+T Fdirect and
z<=cy+edirect. Since1-a-bc>99/100, solve BOTH rows:

    y=(b edirect+T Fdirect)/(1-a-bc), z=cy+edirect.

This yields cutoff y<1e-580,z<1e-12 and ordering y<1e-630,z<1e-65.
The independent tests solve the2x2 system afresh. These compare the
NEW corrected paths. Each state comparison factors its OWN scalar
g0 phase; the common fixed Ub/Uref cancels in the vector norm only
when retained on both physical states.

The physical-volume parameter row is<1e8. Its state difference
uses only the centered operator because scalarF0 I cancels.
The complete cutoff readout bound is

    1e8 y+4BF z+4SF tau+4EF+8BF BH T,

and ordering uses1e8 y+4BF z+2DF. The corresponding differences
are<1e-514 and<1e-566. All parameter, state and operator changes
are counted; none is equated by a false common-background assumption.

These are finite hybrid theorems on the declared tiny interval.
Original V/G/B/P8 remain OPEN, not resolved by finite regulator
comparisons or the repaired canonical bridge.
