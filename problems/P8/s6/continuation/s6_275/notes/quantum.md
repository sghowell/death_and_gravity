# Complete corrected quantum operators and exact heat remainder

Use the prepared coordinates and original seed/window throughout.
All physical raw source symbols compose with Sb; every state,
reference propagator and readout in the raw picture receives Ub.
The generating termF_t is retained in the full Hamiltonian before
reference subtraction or phase localization. The full volume only
composes with the map: it is not an energy.

Because Sb is a linear symplectic map in the finite system, Weyl
covariance is exact. The coherent quantization is likewise covariant
when its FULL window and symbol are transformed together. This
does not claim nonlinear canonical covariance or a new isotropic
window. Before multiplying a cutoff, a quadratic F_t is exactly
calibrated: D^2F_t=0 and exp(D)(1-D)F_t=F_t. After multiplying the
full cutoff all its derivative contacts must still be included.

With96 phase coordinates, the complete corrected holomorphic
domains and unchanged all-order radial bump estimates give

    J_n(A,j)=2 A 2056^n(n!)^3/R^n * j!/d^j,
    0<=n<=206, j=0,1,2, d=1e-124.

All207 full radial partition sums and206 successive growth ratios
are evaluated. An independent partial-Bell recurrence verifies
each partition. Orders0..196 agree with the earlier universal
coefficients, not with an old physical-state dictionary. All
ratios are<1e-9. No additional real-time profile regularity is used.

For D=Delta_w/4 and P6(theta D)=sum_j0..6(-theta D)^j/j!,

    d_theta[exp(theta D)P6(theta D)]
      =theta^6 exp(theta D)D^7/6!.

Integrate0..1 to obtain the EXACT operator identity

    OpW(b)=Q(P6 b)+OpW(r6),
    r6=-integral_0^1 theta^6 exp(theta D)D^7 b dtheta/6!.

This is positive heat with its complete remainder, not backward
heat evolution or an approximate Hamiltonian. The calibrated
operator remains Q[(1-D)b], not Q(P6b).

Let t_j=96^j J_(2j)/(4^j j!). Then both complete orderings are
bounded by B(A,j)=sum_l0..6 t_l+1e112 t_7. Their difference is bounded
by Delta(A,j)=sum_l2..6 t_l+1e112 t_7. The unchanged normalized
coherent-kernel Schur theorem uses derivatives through192+14=206;
positive heat commutes with those derivatives and contracts their
supremum. The full remainder norm remains explicit.

The difference of the two compact symbols and all its derivatives
vanishes on the common core. Its finite coherent polynomial shares
that support property, but its full positive-heat remainder does NOT.
The remainder operator norm is retained in every cutoff comparison,
force estimate and physical-volume readout. Positive core probability
is not exact phase support or a sharp joint projection.

For the corrected amplitudesAH1e172,AF1e-510 the complete operators
are<3A and their ordering differences<A/1e55. Every first/second
homogeneous derivative uses the same formula with j!/d^j. Sb,F_t,
Uref, S0 and chi are fixed-reference and independent of LIVE Y, so
the force chain differentiates the complete raw source and root but
does not introduce an unproved history-dependent vacuum derivative.

The full canonical quantum force is bounded by
1e104 B(AH,1)/kappa, and its parameter Lipschitz row by
1e106[B(AH,1)+B(AH,2)]/kappa. Scalar g0 retains every original
classical force. It is factored only as each solution's own global
phase for vector comparison. The distinct configuration-dependent
boundary phase Ub is restored along with Uref on both state and
readout; it cannot be discarded by this scalar convention.
