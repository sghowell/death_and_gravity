# Independent original ADM boundary and physical-state correction

## Derivation from the unintegrated original action

Start at the literal S180/S220 ADM action and its original coefficient
N jets, not a guessed free oscillator. With N=1+epsilon n, trace rate
H+epsilon vdot, shift-divergence epsilon b, volume exp(3epsilon v),
and matter velocity ell+epsilon sigmadot, the raw density is

    exp(3epsilon v)[
       -3 M2(H+epsilon vdot)^2/N
       +2 M2(H+epsilon vdot)epsilon b/N
       +B(3(H+epsilon vdot)-epsilon b)+N F
       +U(ell+epsilon sigmadot)^2/(2N)].

boundary.py takes its complete epsilon^2 coefficient. Differentiate
this raw coefficient and the ORIGINAL prepared S220 Lagrangian with
respect to BOTH vdot and sigmadot. Their exact difference is

    (-18H v+3ell sigma, 3ell v).

The two contributions are distinct. The weighted vv_dot boundary is
-9H v^2, while the original3ell v sigmadot is integrated into
-3ell vdot sigma by the cross boundary3ell v sigma. Since
ell_dot+3H ell=0, there is no leftover cross potential from this
weighted derivative. The complete actual clock has R=U=1 andB=0;
its fixed retuning terms have no velocity derivatives. The check
retains arbitrary A n^2+B n v+C v^2 potentials. No actual off-clock
source is replaced by its clock value in the nonlinear calculation.

At the bounce the two shifts are(3sigma/10,3v/10). S269 inverted the
later S251 central map but omitted this EARLIER S220 boundary.
S251's outer/central symmetric boundary shears are not substitutes for
it. Its canonical normalization includes a_bar^3 on both momenta.

## Independent full-Hessian binding diagnostic

Independently start at S274's literal expansion of the ENTIRE raw
Hamiltonian and constraint. Keep the complete constrained quadratic

    H2raw - C1^2/(2 C_N),

including scalar trace/shear cancellation, the complete scalar
curvature and matter momentum. Restrict this diagnostic ONLY to the
exact zero-profile tree clock, where the background is an exact root.
The result is not the original prepared free Hamiltonian in unshifted
variables: the full nonzero defect is retained in the packet.

Substitute the two momentum shifts and add the weighted time
generator f_dot+3Hf. The residual against the entire original
prepared Hamiltonian is identically zero for all u and positive q.
This independently fixes both momentum shifts and the time sign.

This fixture is not the actual nonzero-profile live root. For that
root the full original raw H2, reference H2 and nonlinear remainder
are independently retained and bounded. No physical equality with
a prescribed free Hessian is assumed there.

## Canonical one-form and Schrodinger checks

C=a_bar^3 Hessian(f) is symmetric. Therefore Sb=[I,0;C,I] is symplectic
and F_b=Q^TCQ/2 generates rawP=preparedP+F_Q. The full one-form identity is

    Praw dQ-Hraw dt
      =Pprepared dQ-[Hraw(Q,Pprepared+F_Q)+F_t]dt+dF.

For Ub=exp(iF), direct differentiation gives
Ub^-1(-i partial_Q)Ub=-i partial_Q+F_Q and
i partial_t(Ub psi)=Ub(i psi_t-F_t psi).
Thus the prepared Hamiltonian is Hraw composed with Sb PLUS F_t.

The arbitrary symmetric4x4 raw-Hessian test verifies the resulting
time-dependent vector-field identity, not just static symplecticity.
Independent differential-operator fixtures retain mixed momentum
powers through total degree3, at zero and nonzero times. Omission
or reversal of F_t fails independent vector-field tests.

F_t=-9a_bar^3(Hdot+3H^2)Q_v^2 in unit CCR variables. At the bounce this
is -36Q_v^2; H(0)=0 does not make its time derivative vanish. The
matter cross coefficient is exactly3/10 for all reference times.

## Why the archived physical Gaussian is different

At the bounce let Sb0 have lower block[0,3/10;3/10,0]. For ANY full
positive covariance, including arbitrary internal and q-p blocks,

    (Sb0 V Sb0^T-V)[q_v,p_sigma] = (3/10)V[q_v,q_v] > 0.

No special ignored covariance can cancel this difference. A global
scalar phase would leave all such moments unchanged. The actual
required wavefunction change exp(i3Q_vQ_sigma/10) is configuration
dependent. The literal old balanced-to-raw dictionary lacks

    delta r=3 r1/(10 sqrt(P) sqrt(kappa)),
    delta m=3 r2/(20 P^(3/2) sqrt(kappa)).

The corrected raw seed is Ub0 psi0, where psi0 is the SAME original
prepared Gaussian. Do not re-minimize, choose a new isotropic state
or erase q-p correlations. Work in the prepared picture with every
symbol pulled back, or equivalently transport state, reference,
window and readout to the physical raw picture together.

## Exact finite extension and symmetry

The displayed fixed-reference linear shear is used as an exact
canonical coordinate change on the whole finite nonlinear reduced
chart. This is explicitly stipulated; it does NOT assert that an
unproved nonlinear ADM generating function has already been derived.
No raw nonlinear source is removed by this coordinate convention.

Every scalar real wave gets the same C block. All24 actual proper
cubic actions commute with it, as do all3 continuous translations.
For each translation generator L, C L is antisymmetric, so
Q^T C L Q=0 and the translation charge does not change. This preserves
the original invariant sector without state averaging. It also
preserves zero means of the input momentum shifts; the homogeneous
data and live canonical force chain remain unchanged.

S269-S274 frozen records remain auditable as written abstract models.
H8A433-H8A438 receive an explicit physical qualification. Native or
regression C0 alone does not reestablish their incorrect dictionary.
