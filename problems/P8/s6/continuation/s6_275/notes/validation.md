# Immutable-source validation and evidentiary boundary

The nineteen ASCII sources consist of eight Python package modules,
two tests, README and FORMULATION, and seven written proof notes.
The native report has twenty exact top-level fields. Its source map
and every report field are compared in full, not as a selected subset.

Private science development must complete exact residual/gate,
independent test, source-hash, AST-field and module-export checks.
Copy the exact accepted bytes to the repository, freeze them and
rerun that preflight in a fresh original-SymPy process. The private
original and repository copy are then immutable; any correction
after freeze belongs in a successor or external erratum.

Native certification uses original SymPy and rebuilds the full
unchanged ancestral report chain. Capture its exact raw bytes,
validate private/repository equality and all previous frontiers,
then freeze the report. Independent ordinary, read-only CLI and
FULL regressions remain separate requirements. Native/direct/
ordinary/CLI may not use the exact-GCD adapter; only the audited
captured FULL run may use it after its original-versus-adapter
contract checks. Collection is captured before accepting a verdict.

No frozen source or old native report is edited to disguise this
physical correction. S269-S274 archive matching statuses remain
exactly as stored and all six affected physical identifications
receive an explicit new qualification. Reproducing the old report
does not endorse the omitted canonical boundary. The new packet
defines and validates corrected solutions instead.

The new exact audit has111 named checks,1493 scalar entries,
94 proof gates and13 controls including623 rejected inputs, with
9 primitive rows and131 matching rows. The existing130 matching
rows remain byte-for-value equal, with the newH8A439 record appended.

Independent science diagnostics include both velocity momenta,
arbitrary fixed-potential retuning, the separately restricted full
tree-H2 bridge, complete canonical time signs, mixed differential
operator conjugation, arbitrary full positive covariances, all24
cubic actions, both corrected source domains, every raw quadratic
budget, the entire radial Cauchy tail, all207 Bell partitions,
all206 ratios, all197 earlier universal coefficients, nonzero full
heat remainder, complete parameter derivatives, coupled maximum-
metric rows, independent2x2 stability solutions and every rejected
scope/type. Warm caches are tested against bool/float aliases.

The scientific claims additionally depend on the complete written
analytic/source/operator arguments. These are not Lean/formal proof
certificates. Numerical or finite symbolic fixtures test bindings
and signs but do not replace infinite-dimensional analytic proofs
or establish an original continuum completion.

Exact run times, source/report SHA256 values, actual collection and
exit verdicts belong in the external assessment after capture, not
in mutable edits to frozen science sources. A pending run is not
a passing run.
