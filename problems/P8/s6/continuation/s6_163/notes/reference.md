# Same dimensional prescription, named coefficients and classical target

The scalar has dimension 1-epsilon in D=4-2epsilon.
Use y_D=mu^epsilon y and R_D=mu^-epsilon R. The k-th
new coefficient is

    y_D/R_D^(8k)=mu^((8k+1)epsilon)y/R^(8k).

Its mass dimension is -8k+(8k+1)epsilon, precisely
D minus the dimension of Phi^(8k+1) times a fermion
bilinear. Retain this common lift before MS subtraction
and finite products. No lower-field finite retuning
is introduced to claim invariance.

The same named GY14 lower-field MS interaction values
and physical Phi mass/residue, vacuum and H references
are used. New EFT counterterms carry their inherited
loop grades. The complete forests contributing to the
named low-source-count observables through loop two
therefore coincide with the previous ones.

Consequently the existing bounds transfer exactly
for these specified coefficients:

    total b2 relative allowance                <1e-6,
    second b2 relative allowance               <1e-7,
    unit-disc Phi pole coefficient             <1e-18,
    complete second vacuum reference           <1e595,
    complete second H source                   <1e189,
    complete second elastic-cut/tree allowance <1e-407,
    complete cut-subtracted formal allowance   <1e-6.

The complete first physical four-Phi amplitude entering
the second cut is also unchanged. This is not a claim
about an exact positive spectrum or physical remainder.

Use the same full action map and physical source
J F(Psi), not an independently normalized ordinary
J Psi. No global inverse or rolling continuation
is needed or proved by this formal local statement.

With classical fermions set to zero, the mass-profile
change vanishes identically for every scalar argument.
The S6.162 full analytic-target stationary-action
match on its restricted flat class is therefore
unchanged, with coefficient below 1e-800.
This does not convert it into quantum target matching.
