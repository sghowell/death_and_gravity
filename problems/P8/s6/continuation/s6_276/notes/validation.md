# Validation contract

The scientific claims are exact full-source identities plus written
analytic arguments with the original fixed-profile enclosures. Independent
tests reconstruct Hamiltonian jets, raw auxiliary contacts, whole metric
curvature and momentum constraints, rotated field inputs and the connected
branch obstruction. They include off-diagonal density contacts, retained
nonzero primitives, finite Dirac blocks and strict warm-cache rejection.

The complete audit has97 named checks,158 scalar entries,48 proof gates,
8 controls including72 actual rejected inputs,9 unchanged primitive rows
and132 matching rows. All6 prior physical qualifications are preserved.

These scalar-entry counts are not counts of independent physical theorems.
The written proofs are not formally verified. Diagnostic field/profile
fixtures do not replace the actual original fixed coefficients or state.

Accepted ASCII source bytes and the native raw report are immutable.
The verifier hashes every module, test, formulation and written note,
pins the entire preceding raw report, rebuilds the complete ancestor
chain and rejects a mutation of ANY report field. Native, direct,
ordinary and CLI replays require original SymPy. Only an independently
captured complete P8 regression may use the audited exact-GCD adapter.

Private/fresh/native/ordinary/CLI/full completion evidence, including
any development failures and exact snapshot boundaries, is recorded in
an external assessment after observed results. New finite-stage
certification is not original P8 completion.
