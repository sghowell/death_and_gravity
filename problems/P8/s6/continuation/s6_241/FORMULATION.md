# S6.241 formulation — new clock hypotheses and complete finite local response

## Unchanged candidate and current inputs

Keep **CD-REG-AFFINE-ISO-QG2-H8A420**, the exact S240 heavy SLE, its covariant mu1 scalar prescription and fixed reference profile, all S239 finite vacuum coefficients, and the full S238 covariant parent. No new retuning or candidate is introduced. The physical CD metric is a=(1+t²)², u=t,X1, with the original Proca/M1 choices and S238 recomputed affine mean. The heavy mass squared is n=10^200/512+2 and kappa0=10^800.

The new F clock jets differ from QG1. They must satisfy the generic classical hypotheses explicitly; older stability or inverse claims are not transferred automatically. The full S240 reference stress is source-defined and fixed under later variations.

## Deliverables and graphs

First derive the entire added profile quadratic from N exp(3v)R(t,N^-2)^(-3/4)DeltaF. Retain all lapse, mixed and volume terms. Use the proved old-plus-heavy reference stress bounds to verify every finite-chart, denominator, coefficient and time-jet hypothesis. Apply the source-pinned generic S221 algebra and full energy proof only after those checks. The result is the stated classical COEFFICIENT-SECTOR comparison on[-1/2,1/2], not the quantum constraint inverse. The added classical H mode is a separate exact massive KG block with its own energy-scaled phase bound on[-1,1].

Second compute the ENTIRE finite scalar heat Hessian in physical lapse, spatial metric and shift variables, including the full second-metric variation. Combine its reference-mean profile part without discarding any curvature or volume term. The constant heavy vacuum action cancels exactly for every metric before expansion. Retain the remaining fixed state-profile part separately.

For each Fourier leg define
J=sum over fields n_lapse,zeta,B and j0..2 of (1+|P|²)|partial_t^j field|.
The weak norm is ||J||L2(dt dP). On compact prepared variations in[-1/2,1/2], the complete normalized finite-local-plus-fixed-profile bilinear form is below10^-580 times the two leg norms, uniformly over all momentum.

Transfer this complete local bound to the actual common clock variables(n_lapse,v,B). The matched heat block has zero first Euler variation, so its second metric-chart contact vanishes as an integrated identity. The fixed STATE-PROFILE piece alone is not on shell: its nonzero second-chart contact is explicitly retained. The full same-clock bound remains10^-580 in the corresponding graph. No lapse or shift quantum constraint is eliminated.

Finally derive the whole finite tensor Euler term using the actual a³ dt dx pairing. For any real r define the order-four graph norm as the sum of ||(-Delta)^l partial_t^j h||Linf(I,H^r) for j+2l<=4. The full normalized finite tensor operator maps this graph into Linf(I,H^r) with bound10^-597. All fourth-order contacts remain.

## Boundary

The remaining exact heavy SLE/subtraction-dependent determinant response is not included in this local block. A local graph norm is not a same-space inverse or a statement about physical roots of an isolated polynomial operator. The classical heavy mode's decoupling does not make its Gaussian metric response zero.

Full quantum constraints/inverse, interacting light/mixed clock state and loops, quantum gravitational decoupling, same-state nonlinear bounce, physical UV, finite-gravity Regge and original V/G/B/P8 remain open. The nine original primitive rows and all previous matching records remain unchanged. Seventeen source/proof/test inputs and twenty report fields are immutable after native freezing.
