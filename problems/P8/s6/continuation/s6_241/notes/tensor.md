# Entire finite tensor Euler operator in the physical pairing

For transverse-traceless gamma, use the unimodular spatial metric a² exp(gamma). Its determinant and X=1 are unchanged at every order. Hence the whole fixed scalar profile and the vacuum constant have zero tensor variations. The first scalar-curvature variation is zero.

The full scalar heat action gives the compact tensor quadratic, before 64 pi²,

integral a³ { A_H tr[gamma_t²-a^-2(grad gamma)²]
-(ell/120)tr[(D gamma)²] },

A_H=n(ell-1)/12-ell R0/72,
D=partial_t²+H partial_t-a^-2 Delta.

The Einstein term contributes one quarter of its coefficient and R_old² contributes R0/2. A separate literal unimodular-metric Christoffel calculation checks these kinetic and spatial-gradient factors. The scalar coefficient is used; the older vector coefficient is different.

## Weyl factor and full boundary

For a flat unit-Frobenius polarization h_yy=-gamma/sqrt(2), h_zz=gamma/sqrt(2), a separate full 256-index contraction gives the opposite-Fourier-leg mixed Weyl expression

f''g''-k²(fg''+gf'')-4k²f'g'+k^4 fg
=(f''+k²f)(g''+k²g)-2k²(fg)''.

This retains the exact compact boundary instead of equating the pointwise density to its integrated expression. Weyl conformal invariance and d_eta= a d_t give D_eta=a²D and d_eta_time=dt/a; thus the proper-time quadratic has the full a³(D gamma)² measure.

## Weighted adjoint and all contacts

In the physical a³ dt dx pairing,

Ddag=partial_t²+5H partial_t+2H'+6H²-a^-2 Delta,
L=partial_t²+3H partial_t-a^-2 Delta,
L_A=A_H L+A_H' partial_t.

The independently differentiated whole quadratic gives the canonical gamma=2h/sqrt(kappa0) Euler contribution -Qloc h, where

Qloc=[L_A+(ell/120)Ddag D]/(8 pi² kappa0).

In particular,

Ddag D=partial_t^4+6H partial_t^3+(4H'+11H²)partial_t²
+(H''+7HH'+6H³)partial_t
-2a^-2 Delta partial_t²-2H a^-2 Delta partial_t+a^-4 Delta².

All coefficient derivatives have been included. The zero-time single-Delta term cancels only after those derivatives are retained. Neither a flat adjoint nor a frozen kinetic coefficient gives this operator.

For a=(1+t²)² on I=[-1/2,1/2], expand this ENTIRE operator in (-Delta)^l partial_t^j with j+2l<=4. Each rational time coefficient has a positive even denominator. The report sums exact absolute coefficient enclosures, using 394<ell<462, n=10^200/512+2 and kappa0=10^800. Since pi²>9, the resulting operator bound is below10^-597 from the sum of Linf(I,H^r) norms of those input derivatives to Linf(I,H^r), for every real r and all spatial momentum.

The local finite kinetic coefficient is positive: A_H>=393n/12-462*78/72>0. This is a property of the finite local term, not a full quantum no-ghost or stability theorem. No causal root is removed or assigned a physical UV meaning from an isolated polynomial operator.
