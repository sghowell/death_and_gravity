# Every hypothesis of the all-momentum comparison

A transfer of the old estimates requires new trace-sector hypotheses, not merely an unchanged numerical constant. Here each required generator and current bound is verified.

First,
\[
 \omega^2=m^2+a^{-2}e^{-2\epsilon\Gamma/3}k^2.
\]
The S191 exponential mixed-jet argument used the direction's operator norm and its twelve time jets, not tracelessness. The scalar direction \(2\Gamma/3\) and every time jet have norm at most one. The relative exponential and scale-power2 convolution are therefore dominated by the same S191 frequency-square table. The positive square-root and inverse recursions then dominate every mixed \(\omega\) and reciprocal jet through the required orders. Directly \(2|H_A|\le2(8/5+1/300)<4\), so the sharper first-time-frequency bound also remains valid.

Second, \(H_A=H+\epsilon\Gamma'/3\), where \(H=4t/(1+t^2)\). For \(j=0,\ldots,11\), exact rational differentiation gives the following absolute base-jet bounds:
\[
 (8/5,\ 5,\ 13,\ 123/2,\ 363,\ 5475/2,\ 49185/2,\
 1033515/4,\ 3099915,\ 83703375/2,\
 1255522275/2,\ 41432546925/4).
\]
For \(j>0\), the denominator is a polynomial with nonnegative coefficients and constant term at least one. The numerator's absolute coefficient sum at \(|t|=1/2\) bounds it. The \(j=0\) value uses the exact monotonicity of \(4t/(1+t^2)\) on the slab.

For mixed amplitude order \(b=0,1,2\), a raw \(H_A^{(j)}\) bound is respectively
\[
 h_j+1/300,\qquad1/3,\qquad0.
\]
Half each bound is strictly below the old S191 \(L[j,b]\), in all36 cases; the smallest displayed margin is \(5519/600\). Since \(p/2=\rho\) is bounded by the already dominated frequency table,
\[
 |\partial_t^j\partial_\epsilon^b S_T|,
 |\partial_t^j\partial_\epsilon^b S_L|
 \le L[j,b]+\rho[j,b]=S_{\rm old}[j,b].
\]
The new rotation is zero and is bounded by the old \(R\) table. This proof needs at most \(\Gamma^{(12)}\), with no silent extra time regularity.

Third, the complete physical current vertices, including their first two amplitude derivatives, are bounded by the old \((2,25,650)\), as derived in the Hamiltonian note. The two transverse and one constrained longitudinal modes give the same six-quadrature trace factor. Real block-diagonal current parity is unchanged.

Finally,
\[
 e^{-2\epsilon\Gamma/3}\ge1-1/150>99/100.
\]
Thus the same lower frequency \(\nu_-^2=m^2+b k^2\), \(b=(99/100)/(25/16)^2\), and upper \(\omega<3\nu_-\) hold. The proof partition \(\nu_-=10^{16}\) stays independent of amplitude.

On the common zero initial neighborhood the scale, every time jet, canonical frame and chosen all-order frequency/slope coincide with the original state. The finite reference's initial graph and its nonzero actual-state mismatch are exactly the old ones. Initial parameter error jets are zero for this reason. No independently reset quantum state or metric datum has been introduced.
