# Independent validation and reproducibility boundary

Independent full ADM matrices are constructed from the positive spatial metric, determinant, inverse and curl/constraint terms. Their normalized current vertices and first two amplitude derivatives agree with the scalar-projector formulas at zero, intermediate and very large momentum and both amplitude endpoints.

A finite fixed-canonical-data IVP compares direct matrix-exponential derivatives with the full covariance tangent and second response, retaining the first-response metric contact. This checks finite retarded/contact algebra, not an alternate all-order prepared state or an admitted finite-amplitude history.

Actual frequency and both squeeze functions are differentiated through mixed time orders0,1,4,8,11 and amplitude orders0,1,2. Their values obey the original all-momentum majorants. Separate rational checks cover all twelve base H bounds and both slab endpoints. The pointwise polynomial jet fixture is not represented as the common all-order initial-germ source.

The local current is independently derived by varying the density in raw scale-factor jets and applying the alternating Euler time derivative. Two compact-endpoint polynomial source/readout pairs check the integrated full-chart Hessian, including the mixed second variation of the scale factor. A separate derivation differentiates the complete scalar K and omega in arbitrary dimension and evaluates rational radial moments; it does not import the local matching symbols it tests.

Negative controls retain a nonzero temporal-constraint change in the longitudinal vertex, the nonzero cosmological trace current and Hessian, and the finite covariance metric contact. Full density Green relations are checked as local formal transpose identities, not retarded kernel symmetry.

The initial independent run passed163 tests and failed a text-scope assertion because its expected substring differed from the actual qualified wording. That assertion was corrected before freezing; no mathematical formula changed. The strengthened independent run passed164 tests. Preflight then caught Python zero division converting twelve exact bound margins into floating-point metadata. An exact symbolic zero fixes that conversion before freezing, and each science packet is now independently checked by the exact serializer. No physical coefficient changed. Exact identities, all proof gates, malformed inputs and frontier mutations are separately exercised by the integrated suite.

There are56 named identities,110 scalar entries,46 proof gates,nine controls and167 rejected inputs. The source manifest contains19 scientific/proof/test files and the report has20 fields. Every report field, missing/extra fields, unsupported family input and false original-problem closure is rejected.

Native, direct science, ordinary and CLI replay use original SymPy. Only the independently audited complete-regression route uses the exact-GCD adapter. Continuous all-momentum, dimensional, dominated-C2, rotation and Hilbert arguments are written proofs, not FORMALIZED. Fresh run outcomes are recorded in the external audit, not by changing frozen certificate inputs.
