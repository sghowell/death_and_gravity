# Source ownership and the remaining hard problem

S317 owns the complete temporal gauge policy, finite Noether induction,
physical tree invariance and the conserved temporal propagator identity.
S315 owns the literal all-order Einstein vertices and their norm bound.
Both inputs and every frozen ancestor remain byte-for-byte unchanged.

This successor adds a proof and explicit numerical majorant. It does
not change the source evaluator or drop any root partition. On a purely
graviton-seeded tree subset, no matter line can occur: following any
matter branch would require an external matter seed or a closed loop.
Thus the pure-soft recursion uses precisely the full Einstein vertices.

The prior obstruction was not a wrong isolated calculation. A complete
cluster in the old inverse gauge could retain a pair-collinear pole;
its off-shell descendant need not be conserved. The temporal nonlinear
reorganization addressed the representative, and the new weighted norm
now controls the recursive angular behavior. It does not retroactively
declare old isolated tensors bounded.

The norm includes the velocity and double-velocity contractions, with
their angular weights. Bounding only a spatial Frobenius norm would
lose the information needed at the next root. The exact wrong-weight
control leaves a nonzero constant transverse source, so the additional
weights are essential, not decorative strengthening.

Original parameters, source ownership, the low-energy action, all
matching obligations and the scoped P8(a) result are unchanged.
No unknown finite matching constant has been chosen to make the
inequality hold. The positive cap and counting radius are derived
auxiliary majorant parameters, not physical coefficients.

A pure-soft current bound is not yet a complete hard-source amplitude
or detector theorem. The remaining hard/mixed propagation, full
energy-overlap subtraction and real-virtual assembly must be controlled
without promoting a named soft reference into the full interacting rate.
