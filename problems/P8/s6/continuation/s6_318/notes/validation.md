# Independent replay and immutable publication boundary

The packet comprises18 ASCII newline-terminated sources: seven modules,
two test files and nine Markdown files. Its native report has20 fields.
The corrected exact count pass found 345 named residuals covering
414 scalar entries, 96 proof gates, 8 controls and 73 rejected inputs.
All 174 matching records, 9 primitive obligations and 6 historical
qualifications are retained and checked before source freeze.

The initial count pass stopped on an unreduced symbolic zero in the
inverse-metric identity. Its rational diagonal expression reduces
exactly to zero; applying exact factorization before recording that
residual fixes its representation without changing the identity,
assertion, backend or tolerance. The complete count pass was restarted
after this unfrozen correction. One import-format issue was also
corrected before preflight.

Independent routes include arbitrary-function geometry, exact graded
low-valence vertices, a failed wrong-weight control, complete four-ray
weighted limits and two coefficient-counting methods. The all-order
proof is written explicitly; finite checks are not used to extrapolate
unknown angular behavior or multiplicity growth.

The public coefficient selector validates positive exact multiplicity
before entering its private cache. Energy-list and original-domain
checks reject floats, invalid couplings, unsupported types and excess
total energy. All scientific payloads remain exact and float-free.

Cold and distinct fresh-root preflights check every source export,
all residuals and proof gates, the report AST, exact serialization
and every science test. The fresh run forces the full S317 parent
packets before its own residuals. Byte-identical private/fresh/repository
sources are required before freezing.

The native helper receives one specification and only complete-chunk
acknowledgments. Both raw copies, source hashes, the report path and
twenty-field AST, exact counts and unchanged frontiers must validate
before native raw freeze.

Original-SymPy own ordinary tests, independent CLI and the complete
captured P8 regression remain separate publication requirements.
Their outcomes belong in the external assessment, not retroactively
in frozen sources. The exact-GCD adapter is FULL-only. At most one
FULL and one own pair run at a time; no resource diagnostics, blind
retries or changed tolerances are used. Original P8 remains OPEN.
