# Validation boundary

The explicit manifest contains seven package sources, two test modules, README.md, FORMULATION.md and seven notes:18 sources total. The report has20 top-level fields. The package supplies24 named exact checks covering53 scalar entries,46 gates,9 controls and124 rejected inputs/scope mutations. Every report field and missing/extra fields are independently mutation-tested.

Independent scientific fixtures evaluate the actual full W8 polynomial series and its total time derivative in joint complex domains at low, intermediate and very high internal momentum. They check both polarization kinds, genuine complex Euclidean frame norms, the temporal longitudinal constraint, both analytic Schwarz signs, all nine full stress pairs and the inverse phase.

Further fixtures evaluate the actual full leading endpoint's nonzero fifth-order spatial Taylor remainder, distinguish Schwarz continuation from naive in-place conjugation, and integrate both the exact fourth radial weight and the actual removed two-leg angular union restricted to FAR. Exact residuals, full gate counts and every invalid scope are tested separately.

The source-pinned S6.201 report is transitively rebuilt and the S6.176 fixed actual-state Gaussian anchor is checked. The native report must be rebuilt with original SymPy after all source bytes are frozen. Fresh direct-science, ordinary and CLI replays also use original SymPy. Only the separate full-regression path uses the audited exact-GCD adapter, with snapshot and adapter controls.

No report-generation action writes scientific inputs. Publication evidence is recorded only after the relevant processes actually complete. Continuous complex analysis, operator estimates and dominated convergence are written arguments, not FORMALIZED proofs. Passing numerical fixtures cannot by themselves close the remaining near region or fixed covariant matching.
