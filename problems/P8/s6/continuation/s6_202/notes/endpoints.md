# All first-five endpoint coefficient rows

Keep the complete S6.198 identity, including its noncommuting contact, prepared lower endpoints and all phase signs. Let Lb=(gb)' and b=a_Gamma Gamma. The unresolved upper endpoint terms have j=0,...,4 and coefficient(-i)i^j g L^j b, multiplied by the analytic Schwarz detector amplitude. Taking the final imaginary part is delayed until after this complete pre-current coefficient has been formed.

The product-rule/Cauchy recurrence gives the five coefficient rows

c0=(1),
c1=(2,1),
c2=(8,5,1),
c3=(48,33,9,1),
c4=(384,279,87,14,1).

Their sums are1,3,14,91,765. With the joint readout disc r=1/20000, |a|<=Cref nu and |g|<=nu^-1, the coefficient of the source time jet Gamma^(q), 0<=q<=j, in the jth endpoint is bounded by

9 Cref^2 c_jq r^(-(j-q)) nu^(1-j).

The factor9 sums all physical pairs. Cauchy is used in time only on the coefficient readouts and inverse phase. At the endpoint the detector factor is undifferentiated; the full source jet product rule remains.

View the combined row as an operator from the five tensor time jets to the detector tensor. Since r<1 and nu>=m, its norm is bounded by

9 Cref^2 nu sum_(j=0)^4 [sum_q c_jq/(r m)^j]
=1.108202949e27 nu <2e27nu.

This estimate is uniform in the complex momentum ball ||p||<=delta nu. For a fixed real direction v, apply the one-variable Cauchy estimate to E(lambda v). The Taylor polynomial through lambda^4 is the restriction of the full degree-four spatial Taylor polynomial. At |P|<=delta nu/2, the geometric remainder gives

||E(P)-T4E(P)|| <=2(2e27nu)(|P|/(delta nu))^5
=4e57|P|^5nu^-4.

This is an operator-norm estimate on the pre-current row, hence also bounds the current after the final imaginary part. It is not an assertion about holomorphy of that real-linear operation.

The independent tests evaluate the actual complete leading endpoint with all nine pairs and noncommuting detector/source tensors, not a scalar majorant alone. At three internal scales they compute its degree-four Taylor polynomial and verify a nonzero fifth-order remainder with the expected factor32 on doubling the evaluation radius. These fixtures supplement, not replace, the uniform written estimate for all five rows.
