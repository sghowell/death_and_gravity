# Full constrained readouts and pre-current holomorphy

Use the ten physical energy features(E1,E2,E3,B1,B2,B3,mA0,mA1,mA2,mA3). The common a^-3/2 factor lies outside each phase-stripped vector and cancels the a^3 stress-smearing volume exactly.

For a transverse vector e and the analytic l,rho,n of notes/frame.md,

T=(p e, i(l cross e)f/a, 0, m f e),
L=((m/omega)p n, 0, -i rho p/(a omega), omega f n).

The temporal longitudinal constraint is retained. The formulas reduce exactly to the earlier complete axis-aligned S6.186 readouts; complex evaluation is applied to these same formulas, not to a guessed transverse surrogate.

The joint frequency bounds give |m/omega|<2, ||l/a||<2nu, |rho/(a omega)|<3 and |omega|<2nu. Together with |f|<=nu^-1/2, |p|<64sqrt(nu) and each polarization norm<2, every component is below1000sqrt(nu). The complete ten-field norm is below4000sqrt(nu).

Every one of the sixteen full stress matrices has operator norm<=1/2. Consequently the complete sixteen-component pair has Frobenius norm at most2||u_k||||u_l||<Cref nu, Cref=10^9. This holds for every one of the nine physical polarization pairs, including mixed pairs. The source and detector need not commute and are not restricted to one tensor projection.

Both frequencies have Re W>.5nu, so g=1/(W_k+W_l) is holomorphic and |g|<nu^-1. The conjugate-on-real-parameters readouts have analytic Schwarz continuations with identical bounds. Time differentiation preserves holomorphy in p.

Apply complex analysis to the full coefficients BEFORE taking the final -Im that defines the current. Im itself is not a holomorphic operation. Test Fourier transforms are independent external factors and are not analytically continued or Taylor-differentiated here. Nor is the original Borel preparation.
