# Exact replay and independent checks

Four scientific modules retain the full original source, complete
conditional background Ward variation, literal massive scalar insertions
and the generated pole contribution with all analytic bounds. Independent
tests use different simplex beta variables, literal on-shell four-vectors
and harmonic contractions, exact complex Feynman points, polynomial
inverse Ward examples and both signs of an analytic transverse addition.

The primary scalar-positivity PDF was read and the relevant whole pages
were rendered and inspected to verify equations, symmetry factors and
scope. The result here is an independently derived unequal-mass formula;
the paper does not establish this parent's Regge or omitted-loop bounds.
Background-field Ward context was checked in
[Armendariz-Picon and Penco](https://arxiv.org/html/1108.6028), sectionsII.3
andIII.1. That paper's model-specific equivalence-principle conclusions
are not imported as a DHOST theorem.

Cold and independent fresh preflights certify every exact residual,
asserted gate, interface export, original parameter/frontier and source
hash. The fresh run warms the immediate parent packet before the new
checks to reject call-order-dependent dictionary mutation. The raw native
twenty-field report is transferred, independently hashed and frozen;
ordinary, CLI and a captured FULL regression are separate later gates.
Only an external assessment records their actual completion.

Native/direct/ordinary/CLI retain original SymPy. The exact-GCD adapter
is restricted to FULL. No research probe or DONE is sent to a native
helper. Accepted ancestors, native raws and private originals are never
edited. Written analytic/covariance/domain arguments are not FORMALIZED.
