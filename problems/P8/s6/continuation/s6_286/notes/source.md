# Whole unchanged source and graph scope

The imported S285 source preserves the entire fixed R,F and heavy/Proca
sources, all three Gaussian constants and the S240/S176 finite matching
boundary. Additional literal derivatives check j(0)=j_u(0)=j_X(0)=0,
j_uu(0)/sqrt(kappa)=g, R_u(0)=R_uu(0)=0, and the actual unrounded n.

S281 already checks the absence of F_XX, R_XX, F_uuX, F_uuu and F_uX
vacuum vertices and retains the complete C and g. Thus the leading
one-matter-loop two-Phi/one-metric calculation has the two scalar stress
triangles, the two metric-cubic contact bubbles and the quartic
bubble/tadpole. Vertices of higher matter-field degree require more
contractions/loops at this arity. Internal gravitational loops are a
different coupling order, not deleted from the full source.

With signature+--- and g_mn=eta_mn+2h_mn/sqrt(kappa), the scalar vertex
is-iT_mn/sqrt(kappa), T=pr+rp-eta(p.r-mu). Expanding the whole invariant
potential density gives+ig eta_mn/sqrt(kappa) for H Phi Phi h.

S234 gives Pi_MS=-C A0(mu)/(32pi^2)+g^2 B0(s;mu,n)/(16pi^2) and the
mass1 OS subtraction. S239 extends its finite counterterms with the
whole V(X)=1-T(X) switch, not the heavy source switch. At the vacuum this
fixes deltaZ=-Pi_MSprime(1) and delta_m^2=Pi_MS(1)-Pi_MSprime(1).
The full covariant kinetic and mass metric vertices must be included.

A heavy-metric two-point insertion joined to H Phi Phi by a heavy
propagator is reducible in H but is part of a full physical metric
endpoint. Its scalar background loop has only eta and qq tensors.
It cannot affect F1, but it is not omitted from any claimed full F2.
Its curved R H matching and the quartic R Phi^2 subtraction are not
fixed here. The source's flat conditions do not settle S285's light
pure-curvature polynomial either.
