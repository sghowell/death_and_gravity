# Frozen scoped objective: evaluate the actual curved linear conversion

Keep the unchanged actual CD Proca parent, mass m=1000, unit proper-time slab -1/2<=t<=1/2, full unit-W8 comparison, actual prepared-state correction and fixed covariant prescription. The scale factor is a=(1+t^2)^2 and H=a_prime/a. Every prime in this checkpoint denotes the original proper-time derivative.

S6.209 establishes the exact comparison

U_K-U_ball,K=K^3 A3+K A1+E_K,

with no complete quadratic or finite term and all-P remainder below1e40 ||D||L2 X46[Gamma]/K for K>=2m. The original two-leg mask and complete one-mode contact are unchanged. This checkpoint evaluates its remaining actual A1 functional.

Work first with real symmetric tracefree tensors. Extend to complex Fourier tests by the unchanged sesquilinear pairing and Fourier measure. For p=|P|>0 and phat=P/p, put

T(D,G)=tr(DG),
V(D,G)=(D phat).(G phat),
W(D,G)=(phat.D.phat)(phat.G.phat),

L=18T-12V-W,
M=42T+4V+3W,
S=298T-420V+63W,
C=2T+4V-W.

The cubic coefficient remains

A3[D,Gamma]=-p L(D,Gamma)/(512pi^2 a).

The complete actual curved linear coefficient is

A1[D,Gamma]=p/pi^2 [
 -m^2 a M(D,Gamma)/1024
 +p^2 S(D,Gamma)/(24576a)
 -a(H_prime+2H^2)C(D,Gamma)/1024
 +a L(D,Gamma_second+H Gamma_prime)/2048 ].

Detector time is held independent during source differentiation. The first source-time derivative is essential. The actual j1/d1 slot evaluates to zero; this does not delete the j1 endpoint or its nonzero higher slot.

The multipliers extend continuously to zero at P0, where the exact original conversion is zero. They remain spatially nonlocal. No direction, regulator or finite counterterm is selected as a new parameter.

Using the unchanged Fourier normalization, define

Z23[Gamma]^2=sum_(r=0)^2 integral dt d^3P/(2pi)^3
(1+p^2)^3 |partial_t^r Gamma_hat(t,P)|_F^2.

Then

|A3[D,Gamma]|<1/100 ||D||L2 ||grad Gamma||L2,
|A1[D,Gamma]|<1e4 ||D||L2 Z23[Gamma].

The second inequality controls two source time derivatives and three spatial Sobolev derivatives. Both canonical metric factors give coefficient displays4e-802 and4e-796, not cutoff-uniform response bounds.

The full original one-ball UV/contact finite and divergent coefficients and their fixed covariant matching remain required. This result does not add the unmatched local target, alter the prepared state, establish a full response/inverse/background, or close original V/G/B and P8.
