# Exact representation, preflight and immutable publication

The packet has18 ASCII newline-terminated sources: seven modules,
two test files and nine Markdown files. The native report has20 fields.
The expected structure is289 named residuals covering343 scalar entries,
108 proof gates,8 controls and73 rejected inputs. The complete cold
preflight checks these counts directly together with every residual.
All176 matching records,9 primitive obligations and6 historical
qualifications are retained.

An initial metadata prediction counted107 gates, omitting the new
source-scope gate added to the inherited61. The exact key-set union
has62 source gates and108 total. This was corrected before freeze,
with every scientific residual and gate assertion unchanged. The
initial cold pass still carried its loaded107 expectation; its result
is retained separately and is not used as an accepted preflight.

The corrected-count cold pass computed all289 residuals and108 gates
successfully, then passed556 science tests and failed the final scope
wording check: it required the literal word soft while the description
said zero-energy faces. The description now explicitly says zero-energy
soft faces. The scope and unchanged test agree; no mathematical
assertion, type guard, tolerance or exclusion was relaxed. That failed
pass is retained, and a clean complete pass is still required.

The standalone complex-current precursor initially failed a raw
zero-equality check: factor() had retained unreduced Gaussian-rational
temporal components. An independent diagnostic found all four reduce
EXACTLY to zero both by complex expansion and by simplify().
The failed output and diagnostic are retained in private research.
A separate canonical precursor passed the unchanged Ward, temporal
and weighted-norm assertions. No tolerance or assertion was weakened.

The full original complex5116-tree precursor also passed exact gauge
equivalence and the derived bound, with a nonzero imaginary amplitude.
The packet recomputes its own values; no success flag replaces that
calculation. Import/style warnings were corrected before preflight.

Public exact-order selectors validate BEFORE entering private caches.
Gaussian arrays reject floats, unsupported symbolic/algebraic entries,
nonfinite data, wrong shapes and nonsymmetric tensors. The complex
projection requires nonzero Q0. Its broader algebraic role does not
weaken the frozen positive-real physical point API.
Relative-energy and multiindex checks enforce their separate domains.

Cold and distinct fresh-root preflights check all exports, residuals,
gates, float-free exact serialization, the report AST and every science
test. Fresh forces the complete S319 parent packets first. All18
private/fresh/repository sources must be byte-identical before freeze.

The native helper receives one specification and complete-chunk ACKs
only. Both raw copies, source hashes, counts, frontiers and twenty-field
REPORT AST must pass before native raw freeze. Own original-SymPy
ordinary tests, independent CLI and complete captured regression
remain separate publication requirements, recorded externally.
The exact-GCD adapter is FULL-only. At most one FULL and one own pair
run at a time. No resource diagnostics or blind retries are used.

No acceptance outcome is retroactively added to a frozen source.
Original V/G/B/P8 remain OPEN.
