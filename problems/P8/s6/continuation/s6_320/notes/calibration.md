# Complete complex calibrations and the invalid global disc

## Why the two-real total-energy disc cannot simply be reused

S313 used two energy radii proportional to the total W. For larger N,
a proper cluster may be arbitrarily softer than the full radiative state.
The complete three-ray pure-gravity current has a genuine complex-energy
root pole which a global-W disc can then reach.

Take directions (1,0,0), (0,1,0), (-3/5,-4/5,0), with spatial plus
tensors tangent*tangent^T-z*z^T. At energies (1,1,-5/17), the root is
Q=(29,20,21,0)/17, which is null. All pair invariants are nonzero.
The COMPLETE amputated four-tree current is conserved and its
contraction with the root plus tensor is47089/2601, not zero; the
cross contraction vanishes. Trace reversal and temporal projection
do not remove a transverse root residue.

Scale those energies by lambda. From the positive center
(lambda,lambda,lambda), the pole requires only dc=-22lambda/17.
With a spectator energy1/16, lambda=1e-20 and c=1e-12, this shift
lies strictly inside a disc of radius c*W_total. Multiplying by
all leaf energies cannot remove the pole because none vanishes there.

This is a precise obstruction to a total-W holomorphy claim for
the complete block currents. It is not a divergence of the positive
real physical amplitude, nor by itself a proof about every full
hard-amplitude residue. A cluster-sensitive domain is required.

## Exact complex source checks

The conserved inverse and weighted factorization are checked as
matrix identities for arbitrary complex-coordinate symbols and an
arbitrary symmetric source. Nonnegative coefficient recurrences are
compared with independent EGF coefficients through N8. Exact rational
barriers control all N; finite coefficients do not extrapolate the proof.

A three-ray/four-tree calibration uses three different genuinely
complex relative energy shifts. All temporal components and the
complete source Ward identity vanish exactly, and all three weighted
norm components satisfy the derived estimate.

The full original5116-tree calibration uses v=31/16+i*1e-16 in
Eprime=(v+1/v)/2 and rprime=(v-1/v)/2, with zero total spatial
radiation. Every momentum is Gaussian rational, both massive shells
and total conservation are exact, and the energy shifts lie inside
the1e-12 relative tube. The original amplitude has a nonzero imaginary
part. Baseline and complete temporal policies agree exactly and obey
the complex bound relative to the same real positive Born pieces.
Each of three leaf tensors has squared norm2; the absolute amplitude
square is divided by8. The continued phase also obeys its stated bound.

Standalone probes supplement the packet's independent replays.
They do not replace the general holomorphy or coefficient proof.
