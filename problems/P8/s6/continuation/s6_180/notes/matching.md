# The actual scale kernel and the shared dimensional prescription

The current homogeneous connected vector operator and all physical
source/readout vertices are exactly those matched in S6.179.
The full-dimensional coordinate factors are
a^(D-2)/N and a^(D-2)m^2/[N(q+m^2)], respectively.
Their literal current differentiation gives the high-momentum
lapse/scale pairs

    b_T,D=(0,2(D-3)), b_L,D=(0,2(D-1)).

The transverse multiplicity is D-1. Its high square and first
dimensional derivative vanish at D=3; the longitudinal square's
first derivative is16. These evanescent data are not discarded.

At D=3 the complete finite-momentum pairs are
(0,2(1-z)) and (0,2(1+z)), with summed square
diag(0,4(3-2z+3z^2)). The exact massive normalized scale reference is

    F_m(p)=-4-integral_0^1 y^2(3-2y^2+3y^4)
                    p/[4m^2+p(1-y^2)] dy.

The fixed -4 finite fourth coefficient is unchanged; no new
counterterm is tuned. The independent radial identities give
-F_m(0)=4, -F_m(-4m^2)=16/15 and
(-F_m)'(0)=9/(35m^2).

The source-pinned S6.86 proof supplies the first-sheet gap, positive
inverse cut density, its moments1/4 and9/(560m^2), and the ordinary
L1 retarded scalar inverse K_m with no instantaneous part.
This is only the active range inverse of the isolated normalized
rank-one loop, not a full metric propagator or physical spectrum.

S6.87's matching proof uses the same selected all-order state,
all-dimensional vertices and fixed finite prescription. At frozen
scale, a^(-D-2)k^(D+1)dk becomes p^(D+1)dp identically in nearby
complex D. Hence no evanescent log(a) fourth contact is left.
The leading off-diagonal kernel is
96/[a(t)^4 a(s) Delta_sigma^5], sigma'=1/a.
It has the same diagonal fourth-primitive normalization as
96/(t-s)^5. The difference loses one singular order.

Its finite WKB/exact-state, dimensional subtraction and causal
extension argument therefore applies to the identical current
scale operator here. The new uncancelled term9P*w is local of
order zero. No old scalar profile or total old inverse is copied.
After four zero-past primitives, the remaining kernel is bounded
by C_j(1+|log(t-s)|) for every fixed diagonal derivative, with
finite constants on the compact triangle. There is no unspecified
fourth-order local difference.

The old kernel and curved report SHAs are, respectively,

    0f04fd277040ad21525eb1d9102e6af45c4d695fac3f69d0d37710a7ab8a0db6
    25eabbd3c7847a746ad7d9b015f27cf63cb3e23aa6d6acfb9b8697fc720bf8fc.

Both proof chains are rebuilt during certification. Their scalar
kernel and continuum matching transfer through explicit operator
equalities; the changed classical tree and uncancelled currents
are rederived in this checkpoint.
