# Uncancelled vector currents and the exact local time channel

The conditional homogeneous mean source/contact action vanishes
exactly by S6.179, including all first metric and light variations.
Its full connected vector response is ordinary Proca on the SAME
metric, measure, all-order Cauchy state and covariant prescription.

For a prepared time change psi=u+epsilon eta, identity near the
initial slice, the literal transverse/longitudinal coordinate
action pulls back with N=psi' and a=a0(psi). The canonical fields
obey f_N=f0(psi)/sqrt(psi') and p_N=sqrt(psi')p0(psi).
Recomputed exact covariance and physical readout identities give

    delta rho_time=rho' eta, delta P_time=P' eta.

The existing ordinary-Proca adiabatic and finite local Ward/time
identities use the same state and prescription. They transfer
through the S6.179 whole-operator bridge, not through the old
profile-total formula. Initial interference is not reset.

In this note rho,P are 64pi^2 times physical unscaled stresses.
Write S=rho+P and let R[w],Q[w] be the actual pure-scale energy
and pressure responses. For n=eta', v=w+H eta,

    delta rho=R[w]+rho' eta,
    delta P=Q[w]+P' eta,
    rho'=-3H S,
    R'+3H(R+Q)+3S w'=0.

Unlike the old profile-cancelled action, the physical mean currents
per FIXED reference volume have variations

    delta e_N=-delta rho-3rho v,
    delta e_v=3delta P+3P(n+3v).

Apply E_eta=-(partial_u+3H)E_N+H E_v and E_w=E_v.
Exact Ward algebra gives the actual current-parent result

    Q_eta=-3P w'-3H'P eta,
    Q_w=3Q[w]+3P eta'+3(P'+3HP)eta+9P w.

Thus the time channel is local and only the scale channel is
nonlocal. There is NO old (rho+P)eta'' profile term.
The local additions are the weighted variations of

    -(3/2)H'P eta^2-3P eta w'+(9/2)P w^2.

Both variations are checked independently. The nonlocal response
remains retarded; no symmetric single-branch Hessian is substituted.

The normalized retained equations are T+gamma Q, with the
classical kappa divided out and gamma=1/(64pi^2 kappa).
In particular gamma P^(j) equals the physical conditional
pressure derivative divided by kappa. S6.176 bounds it by
1e-770 for j=0,1 on the unchanged slab.
