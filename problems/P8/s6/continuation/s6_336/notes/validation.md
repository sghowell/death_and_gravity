# Independent exact checks and immutable acceptance

The private contact probe checks an unrestricted linear-curvature
contraction, all256 pure-gauge curvature components, exact quadratic
soft scaling, all24 identical-scalar assignments and both physical
polarizations at three original-source recoil states. All402 residual
checks passed, with6743 protected P8 inputs unchanged. The full original
47-graph tree interference is nonzero at all three states. No altered
rational-coupling fixture is labelled as original physical data.

The package groups tensor identities as matrices while retaining every
scalar component. Written loop counting and uniform estimates are
separate from finite numerical or exact-state calibration. The result
is not kernel formalization.

Cold and independently started fresh preflights use original SymPy.
The fresh process first rebuilds the complete frozen immediate-parent
packets and waits for one START after byte-identical final source copy.
Completed ancestor caches are explicitly disclosed. Exact source-set,
AST20-field/export checks, parent hashes and all scientific guards
must pass before source freezing.

Three-way private/fresh/repository source equality, one native report
specification, complete raw provenance validation, own ordinary tests,
a separate read-only CLI and a covering FULL whole-wrapper exit0 are
required before publication. Only FULL uses the audited exact-GCD
adapter, with original backend restoration required. Frozen source
and raw evidence are never rewritten to conceal failures.
