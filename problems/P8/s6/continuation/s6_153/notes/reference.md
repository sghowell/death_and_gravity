# Assembly of the four raw scalar interaction families

The original 192 raw refinements have the disjoint partition

    88 UV finite + 64 OS insertions + 24 double bubbles
      + 16 wineglass refinements.

The 88 entirely UV-finite graph coefficients have no local
interaction-reference subtraction to change. At fixed
coordinates their order-two coefficient is unchanged.
A finite coordinate change p->p+h v changes h^2 A2(p)
only at order three; the separate lower-order re-expansion
is not thereby eliminated.

Use the present converted insertion bound and the complete
S6.152 double-bubble and S6.151 wineglass bounds. Together
with the unchanged 88 finite graphs they give

    E_four_raw_scalar_families<2.69375534479472e-607<3e-607,
    E_four_raw_scalar_families/b2_tree
      <6.73438836198679e-8<1e-7.

The original finite sigma insertion is not added to this
raw-family sum: S6.150 handles its isolated nonlocal matching
cancellation. Other finite field/coupling-map contributions
and their cross terms are still required for the full
boundary-MS/canonical answer.

Thus the raw interaction-family ledger advances completely
in its stated intermediate scheme. It is not a claim that
all two-loop matching, canonical field conversion, vacuum
references or physical higher-order errors have been bounded.
