# Read-only verification and proof boundary

The explicit source manifest has eighteen files: seven modules, seven notes, two root documents and two tests. The report has twenty top-level fields, 37 named exact identities, 119 scalar entries, 39 gates, nine controls and 129 rejected scope inputs.

The scientific file contains 350 tests. Exact checks cover all four two-time projector identities, inverse-radius normalization, low scaled adiabatic jets, all retained powers and source time-jet counts, source-only differentiation and the complete massive flat coefficient recurrence.

Independent physical checks include the original unscaled W8 modes, full ten-field vs projector contractions, all source time jets, all retained curved coefficients at two Cauchy resolutions, independence of those jets from the sixth/eighth W8 terms, 90 flat field-level coefficient comparisons, nonzero fixed-P remainder checks and the corrected odd-endpoint distinction.

Exact order guards reject booleans, floating lookalikes, symbolic/nonfinite and out-of-range endpoint orders. Bilinear projector guards reject zero and complex-null momenta and wrong shapes. Inverse and square-root branches are checked. Scope mutations cannot turn this finite extraction into original P8 closure.

Formatting/lint, final preflight and a fresh private scientific process precede freezing. All frozen bytes are copied exactly, checked by hash and replayed in a fresh repository process. The native report rebuild is read-only and transferred losslessly. Native, direct science, ordinary and CLI retain original SymPy; only the full regression uses the separately audited exact GCD adapter.

Only final successful process completion qualifies as a passed replay. The publication audit records final counts, timings and hashes without editing frozen scientific files.

The analytic inverse-radius domain, polynomial degree filtration and fixed-P remainder estimates are written mathematics, not FORMALIZED proofs. Numerical samples, exact counts and scope gates do not replace those continuous arguments.
