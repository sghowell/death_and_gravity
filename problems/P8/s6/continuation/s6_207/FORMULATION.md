# Complete spatial symbol and finite ultraviolet extraction

Keep the same actual massive CD Proca sector, m = 1000, kappa = 10^800, compact unit time slab, unchanged prepared state and fixed finite covariant prescription. The actual-state/unit-W8 correction and original one-leg/two-leg regulators remain.

For a source at time s and detector at independent time t, sum the full tracefree spatial stress pair over four sectors TT, TL, LT and LL, with multiplicities 4, 2, 2 and 1. Each sector retains its own inverse phase

g_sigma,tau(s) = 1/(W_k,sigma(s)+W_l,tau(s)).

The detector uses analytic Schwarz amplitudes and is held fixed during source differentiation. It must not be differentiated by first setting s = t.

Set x = 1/r, k = n/x and l = (-n+xP)/x with n.n = 1. Write

W = What/x, omega = Omega/x,
f = sqrt(x) F, p = Pi/sqrt(x).

The exact normalized pair product x^2 F_pair is the four-sector formula evaluated at scaled momenta n and -n+xP with algebraic mass m*x and normalized amplitudes. This is an algebraic identity, not a change of physical mass. All normalized quantities are analytic near x = 0 for fixed bounded external P sets and compact source/detector times.

Let Fhat(s,t,x) be a complete normalized sector pair product, including a source tensor time jet. Define

Lhat F = partial_s(ghat(s,x) F),
H_j(x) = (-i)i^j ghat(t,x) [Lhat^j Fhat(s,t,x)]_(s=t).

The j-th pre-current endpoint is x^(j-1) H_j(x). Retain its Taylor coefficients of degrees d = 0,...,4-j for j = 0,...,4, and take the final current imaginary part only after constructing the complete row. These fifteen endpoint/power slots include thirty-five source time-jet entries.

External momentum enters only as xP. Thus a retained inverse-radius coefficient of degree d has spatial degree at most d. The third and fourth W8 frequency coefficients begin at x^6 and x^8, so they cannot change any retained degree at most four. This does not replace the full finite-momentum W8 reference by a lower-order state.

Each first omitted pre-current power is x^4 = k^-4. Analyticity therefore gives an absolutely integrable high-band remainder and O(1/K) tail at fixed bounded P. No explicit uniform all-P constant or full response norm is provided here.

The original sharp-band integral is not replaced by its one-ball comparator. Even polynomial coefficient integrands can produce non-polynomial conversion terms; S6.206 supplies the actual leading artifact. Full subleading conversion, quantum contact and finite/divergent matching in the original covariant prescription remain required. No full response, mixed inverse, finite-coupling background, stability, physical cutoff or original V/G/B closure is established.
