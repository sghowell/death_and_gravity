# Whole generic box curvature identity

In each of24 assignments cyclically split each of the four actual
scalar lines. Rotate both vertices and their ORIGINAL mass/weight
assignments. With Q0=0 and Qj minus the preceding vertex sum, replace
Q0 by gamma*k for the effective split denominator. The original
normalized S342 kernel is
-4*w_split*epsilon(Q0-barQ,Q0-barQ)/Delta_gamma^3.

At degree6 in a common external momentum scaling, this becomes

    -24*w_split*epsilon(Q0-barQ,Q0-barQ)
      integral_0^1 U_gamma^2 dgamma / M^5.

Multiplying the originalg^4/4 leaves-6 times all96 insertions.
Since k^2=0, U_gamma=U0+gamma*U1 and its square integrates to
U0^2+U0*U1+U1^2/3. No insertion or split weight is omitted.

For a full generic proof express the four hard vectors in the basis
p0,p1,p2,k, with p3=-p0-p1-p2-k. Retain six independent hard Gram
entries, three independent a_i=k.p_i, and six independent transverse
polarization entries. All vector dot products are formed algebraically,
not evaluated at physical sample momenta.

Exact sparse rational convolution computes the whole degree7 weight
polynomial in each line, integrates both angular groups, and retains
1866 radial coefficients before cancellation. Subtract the complete
covariant-jet response of the full off-shell flat projection.
Each of the2430 coefficients of z^j*H_ab*G_cd*G_ef is compared
with the proposed curvature term. Every residual vanishes. This is
not interpolation or an inference from finitely many kinematic samples.

The resulting weight is

    W(z)=-4/315*t*(18+18t+92t^2+363t^3), t=z(1-z),

and the whole difference is W(z)*T/M^5, where
T=sum_(i<j) epsilon((k.p_i)p_j-(k.p_j)p_i,
                  (k.p_i)p_j-(k.p_j)p_i).

The S343 generic curvature basis and original nonzero massive contacts
fix the same T normalization. Restoring the original loop and graviton
factors gives chi_box=g^4*integral W(z)/M^5 dz/(16*pi^2).

The flat degree6 polynomials agree identically off shell, so all four
external scalar-emission contributions agree and cancel in the
comparison. The identical fixed OS4 constant does not modify this
degree6 contact. The physical nonlocal box itself is not truncated
or counted a second time.

