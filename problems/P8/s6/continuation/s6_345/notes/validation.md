# Complete immutable verification contract

The21 source files contain9 Python modules, two tests, README,
FORMULATION and eight proof notes. The native report has20 fields,
753 named exact checks,3206 scalar entries,43 proof gates,
eight controls and727 rejected inputs.

The generic radiative identity is represented as all2430 scalar
coefficient residuals in a single exact matrix. It is computed from
the full sparse rational convolution, not populated with assumed
zeros. The six exact flat projections, complete third-jet response,
original Galileon crosscheck, mass-integral primitive, limits and
actual original line kernels are recomputed.

All six direct raw reports and complete source manifests are
checked. Every report field is mutation-tested; missing/extra
fields, changed frontiers, invalid exact masses, unsupported
operator labels and overbroad physical scopes are rejected.
Module exports and imports are checked before the source freeze.

Cold verification uses original SymPy. A separate fresh process
first verifies only the frozen preceding checkpoint; its completed
parent cache is disclosed before checking the byte-identical new
source copy. Private/fresh/repository source equality precedes
one native build, after which complete raw bytes, all fields,
source manifests, AST paths, counts and frontiers are validated.

Operational cold/fresh timings and later ordinary/CLI/covering
full-suite acceptance receipts remain outside this immutable
manifest. A covering snapshot must actually include this checkpoint.
Not kernel-formalized; no omitted physical P8 condition follows
from successful software verification.

