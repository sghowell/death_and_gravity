# Complete off-shell Bose flat projection

A parity-even quartic flat local six-derivative vertex contracts six
derivative indices into three metric edges. Self-edges are allowed;
multiple edges and undifferentiated scalar factors are retained.
There are220 three-edge monomials on the ten unordered pairs of four
labels, partitioning into20 full Bose graph orbits.

Impose ONLY p3=-p0-p1-p2 in the flat vertex. The six independent entries
of the Gram matrix of p0,p1,p2 remain symbolic. The coefficient matrix
of the six stated full-Bose words has rank6. Every one of the220
Bose-symmetrized monomials is recovered by exact ENTIRE polynomial
equality from these columns. This proves completeness at this order
without scalar EOM or on-shell fitting. The five at-most-second-jet
pivot words alone have rank5; the explicitly symmetrized third-jet
word supplies the needed sixth direction.

For an ordered box use
w0=(1-z)xi,w1=z*eta,w2=(1-z)(1-xi),w3=z(1-eta).
The flat kinematic numerator is

    U=w0w1*p_a^2+w1w2*p_b^2+w2w3*p_c^2+w0w3*p_d^2
       +w0w2*(p_a+p_b)^2+w1w3*(p_b+p_c)^2.

Every angular monomial has the exact integral

    <w0^a w1^b w2^c w3^d>
      =(1-z)^(a+c)*z^(b+d)
       *a!c!/(a+c+1)! *b!d!/(b+d+1)!.

The full simplex measure z(1-z) remains explicit.
Expanding U^3 keeps all56 multiset terms and all24 assignments.
Every coefficient at heavy-weight degree0,...,6 is separately projected
by complete polynomial equality. The six resulting coefficient
polynomials, including angular measures, are recorded in the report.

The scalar box expansion gives4*U^3/M^5, M=1+(n-1)z.
The originalg^4/4 cancels that4, leaving g^4 times the displayed
flat coefficient, with the common1/(16*pi^2) still outside.

This chooses a literal covariant representative; it does not assert
that all covariant completions of the same flat polynomial agree.
The difference between such completions is exactly why curvature
matching must be computed.

