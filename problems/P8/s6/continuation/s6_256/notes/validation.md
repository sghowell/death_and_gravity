# Immutable source and read-only replay contract

Freeze only after the complete private source set, payload, scope
checks and independent science diagnostics pass with original SymPy.
The source manifest has six package modules, two test modules, two
root Markdown files and seven proof notes: 17 files in total.
The certificate has 20 top-level fields and pins the exact unchanged
S6.255 report SHA256

    b8fdddc40a30e37e4c77b509a9bb7442d139127b00f4b7cb9bb7f6f068165607.

The package records 62 named exact identities, 297 scalar entries,
109 proof gates, nine controls and 277 rejected inputs. Fourteen
persistent initial margins are evaluated against the actual complete
derivative displacement. Scope validators reject changed mass,
coupling, lapse, interval, momentum band, quantum state and promoted
primitive or matching claims.

Independent science tests exercise exact root enclosure at tiny and
giant rational scales, interval edge cases and unsupported inputs,
a separate complete four-by-four Euler/constraint solve, the actual
fixed-mass skew energy identity, genuinely nonzero chart time
connections, retained heavy source and mass dependencies, and a
nonautonomous full sixteen-phase finite-parameter diagnostic.
The diagnostic is not numerical integration at the giant physical mass.

After freezing, copy the exact source bytes to the new repository
checkpoint and re-run fresh direct science and payload checks.
Native report construction rebuilds the complete ancestry using
original SymPy. Transfer raw ASCII JSON with exact chunk offsets,
lengths and CONTINUE acknowledgments; check the un-reserialized raw
SHA256, all 17 native/private/repository source hashes, all 20
AST-derived fields and unchanged previously protected scientific
inputs. Never edit a frozen source or report to repair a test.

Ordinary package tests and CLI replay also use original SymPy. Only
the separately captured full regression uses the audited exact-GCD
adapter, with its self-checks and immutable input manifest.
The full command must include S6.219's existing helper import path:

    PYTHONPATH=problems/P8/s6/continuation/s6_219/tests \
      .venv/bin/python scripts/p8_replay.py full

This is a launch environment requirement, not a scientific source
change. Collection failures or incomplete runs do not count as passes.

External publication notes, rather than these frozen source notes,
record actual completed run counts, timings, hashes, retries and
publication evidence. Only after all required validations pass may
the root ledger and README advertise the new checkpoint. Stage an
explicit manifest and independently verify the pushed remote hash,
preserving unrelated P4/P9 edits.

Exact computation supports the written complex-function bounds,
ODE continuation, nonautonomous cone and Fourier packet arguments.
It does not formalize those analytic arguments, establish a quantum
loop norm or cutoff, or license original P8 closure.
