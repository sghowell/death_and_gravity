# Full dimensional dominated limit and fixed-invariant matching

The continued auxiliary family is the complete minimal-scalar W6 at \(|d-3|\le1/4\), not the old vector W8. At real time the same full iteration gives
\[
 |W_6(d)-\omega|<10^{10}/\Omega.
\]
The two phase branches therefore have modulus below 2 on the unit slab. Both use the SAME complex dimension; sharp changes \(i\) to \(-i\), not \(d\) to its conjugate. The physical ordered current is continued by its sharp difference, not by an antilinear imaginary-part map.

Only the auxiliary endpoint/subtraction family requires this dimensional continuation. The absolutely convergent actual-state difference and finite sixth-time remainder may be evaluated at physical \(d=3\). No physical SLE at noninteger dimension is asserted.

## Near geometry without a zero-momentum singularity

Normalize the two real internal directions separately:
\[
 n=(c\,n_\perp,u),\qquad w=(h\,n_\perp,v),
 \qquad |c|,|h|,|u|,|v|\le1.
\]
The transverse average is computed algebraically at \(b=d-1\). With the six fixed physical invariants \(T,V,W,S,U_D,U_G\) in S6.243's order, the four ordered products are
\[
\begin{aligned}
 \mathcal G_{00}&=S,\\
 \mathcal G_{01}&=chS/b+(uv-ch/b)U_D,\\
 \mathcal G_{10}&=chS/b+(uv-ch/b)U_G,\\
 \mathcal G_{11}&=\frac{(ch)^2(2T-4V+S-U_D-U_G+3W)}{b(b+2)}
  +\frac{(cv+hu)^2(V-W)}b\\
 &\quad+\frac{chuv(U_D+U_G-2W)}b+u^2v^2W.
\end{aligned}
\]
The two trace-gradient orders are different and both remain.

The scalar radial gradient amplitude contains
\(|k||\ell|/(a^2\sqrt{W_kW_\ell})\); it has no inverse bare momentum. At a zero leg one may choose any unit direction, because its gradient amplitude vanishes and the full massive features remain bounded. This is why the near/low representation does not create a spurious singularity.

## Exact recovery of the far geometries

Put \(y=P/r,\ R_\ell=\sqrt{1-2uy+y^2}\). Substitute
\[
 c^2=1-u^2,\qquad h=-c/R_\ell,\qquad v=(y-u)/R_\ell.
\]
Multiply the 01 and 10 rows by \(R_\ell\), and the 11 row by \(R_\ell^2\). Each full expression is exactly the corresponding frozen S6.243 far geometry. No truncated angular expansion is used.

For fixed physical tensors the six invariant Frobenius weights are \((1,1,1,3,2,2)\). Expand every rational coefficient at \(d=3+\eta\) and bound its polynomial numerator at \(|\eta|\le1/4\). Subtract the full nonconstant denominator majorant from its constant part. All 48 denominator floors are strictly positive. The resulting whole-channel upper bounds are
\[
\begin{array}{c|rrrr}
 &00&01&10&11\\ \hline
 {\rm far}&3&2909/350&2909/350&1241257/41200\\
 {\rm near}&3&38/7&38/7&1361/103
\end{array}
\]
and all are below 100. Far bounds use \(|y|\le1/100,|u|\le1\); near bounds allow all four separately normalized direction variables of modulus at most 1.

These are coefficient-wise rational estimates at fixed invariants. They are not a positive Hilbert-space norm in noninteger dimension. The off-physical-dimension domination needs only a finite uniform majorant, not the sharp physical \(d=3\) response constant.

## Complete dominating envelopes

The joint-domain proof controls all complete scalar frequencies, amplitudes and derivatives uniformly in the dimension disc. For general dimension the normalized volume/pair cancellation uses \(a^d\); its physical restriction is \(a^3\). The angular measure contributes at worst
\((1-u^2)^{-1/8}\), an integrable function.

After subtracting every full Taylor coefficient, the far amplitude has the entire \(r^{-4}\) remainder. Together with radial measure \(r^{d-1}\), it is dominated by \(r^{-7/4}\) at large \(r\). The unexpanded massive low ball is dominated near zero by \(r^{7/4}\). Both are integrable.

The near band is finite but transfer dependent. Its full raw and power terms, together with the retained logarithmic inverse mass, admit a bound growing no faster than \(U^{21/4}\). This deliberately includes the extra \(1/4\) from the radial dimension disc and the full log-to-power allowance. Since \(21/4<6\), the stated source spatial graph dominates it. Cauchy-Schwarz with detector \(L^2\) gives an integrable transfer envelope. The same conclusion holds under a bounded two-leg mask without using angular cancellations.

Dominated convergence therefore identifies the full physical endpoint remainder at \(d=3\). This is a limit of the actual full auxiliary integrands, not solely a formal Laurent-coefficient comparison.

## Complete finite prescription

The direct full-frequency coefficient identity gives precisely S6.243's scalar UV rows. Its full angular and radial continuation therefore supplies the same pole, full volume/Euler terms and finite transfer difference. Reconstruct all six invariants before differentiating in \(d\); hold those physical invariants fixed through the limit.

The counteraction is varied in the continued dimension before taking the physical limit. Its evanescent contribution and the full scalar finite local piece are already present in S6.243 and S6.244. No old vector multiplicity or finite weight is imported, no curvature coefficient is fitted, and no additional S6.241 heat action is added.
