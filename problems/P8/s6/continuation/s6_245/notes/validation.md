# Independent checks and immutable scientific boundary

The packet contains 97 named exact identities with 126 scalar entries, 66 explicit proof gates, nine controls and 292 rejected unsupported inputs. All nine primitive rows and 100 earlier matching records are preserved; one record is appended.

The exact checks include every one of the two full six-iterate frequency four-jets; all four normalized near/far geometric reconstructions; all 48 dimensional coefficient denominator floors; every literal Cauchy-Leibniz row through order six; all six ordered integration identities; the full four-factor error telescope; every near power/log integral; both full endpoint positive sums; and the complete response/contact repartition.

The independent scientific suite also checks:

- The complete retarded time integral at 70-digit precision, with nonzero fifth endpoint and sixth bulk. Either incorrect sign fails independently.
- Full, unexpanded six-iterate complex frequencies, derivative features and pair amplitudes in 39 fixtures. Thirty-six use mass \(10^6\) at 90 digits; three use the ACTUAL physical mass at 1600 digits. Time centers span the slab, transfers include \(P/m=0,1,4,100\), and both time, inverse radius and dimension are complex. The largest observed normalized pair magnitude is approximately 0.206937, well inside the whole analytic bound.
- All 252 original two-leg removed-union quadrature cases, including near powers and logs, the far inverse-power tail, the unexpanded low band and angular breakpoints. The largest near actual/bound ratio is approximately 0.387906.
- An admitted angular-zero UV row whose masked value is \(1/4\), although its full angular average is zero.
- Every whole mass/spatial-weight inequality, complete retained source graph, source-only spatial loss and rejection of unsupported inverse, bare Ward or closure claims.

These numerical fixtures are diagnostics, not proofs of the uniform infinite-momentum or dimensional estimates. The other notes provide those estimates on full integrands.

The initial complete private science run passed 514 tests. Private development corrected a float-producing identity literal, a mistyped cutoff-radius identity, a logarithm requiring explicit expansion, and a numerical constructor that initially rejected complex values. Import order, unused variables and captured quadrature loop variables were corrected before freezing. Assertions and complete terms were retained; no failed physical inequality was removed.

The read-only report pins 17 source files and has 20 top-level fields. The certificate suite checks each field mutation, missing/extra fields, exact replay and the source manifest. Native/direct/ordinary/CLI runs use original SymPy. Only full regression uses the separately audited exact-GCD adapter and existing frozen helper allowance; its 128 original-tuple self-checks must pass.

Native raw report chunks and SHA, private/repository source hashes and the exact report-field manifest are checked before publication. Subsequent replay results are recorded outside the frozen scientific bundle. No frozen predecessor or completed checkpoint is edited.

The written arguments are certificate-backed mathematics, not a Lean or other FORMALIZED development. The checks do not upgrade the stated response theorem to full ADM/clock, nonlinear, inverse, UV/Regge or original P8 closure.
