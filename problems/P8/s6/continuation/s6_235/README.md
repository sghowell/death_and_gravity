# V2S-T1-OS4: complete first four-point loop and one value-matching condition

This checkpoint derives the separate two-scalar model's complete renormalized first-loop four-light integral representation. It retains all channel bubbles, all triangle terms, six mass-ordered boxes, the full UV counterterms and the S234 external normalization.

Its forward imaginary part exactly reproduces the complete S233 first elastic coefficient. A separate full constant-background Hessian independently verifies the distinct off-shell zero-momentum jet.

At the on-shell crossing-symmetric SUBTHRESHOLD point s=t=u=4/3, the unadjusted S234 loop is negative and more than10^190 times the positive, finely cancelled tree value in magnitude. This does not prove strong coupling or exclude the model; it shows that this value cannot be matched by the unadjusted tree prescription.

The separately named V2S-T1-OS4 prescription adds the explicit first-loop finite contact deltaC_fin=-A1_base(4/3,4/3,4/3). It fixes that one value through first loop order, without changing any frozen S233/S234 prescription.

Complete parameter bounds prove

0<deltaC_fin<10^-6 times24q,
q=g²(D-1)/[6D²(D+2)].

The contact-only classical comparison therefore retains a positive, coercive heavy-square potential. This is not a theorem about the quantum effective potential or every finite source/mass counterterm.

The physical-angle remainder, higher quantum coefficient matching, omitted loops, continuum UV construction, original common-parent bounce and finite-gravity Regge control remain open. Original P8 is not closed.

The17 source inputs and20 report fields freeze before native replay. See FORMULATION and all seven notes for the full integral, counterterm and scope arguments.
