# Immutable source and independent acceptance

There are18 ASCII newline-terminated sources: seven modules, two
test files and nine Markdown files. The native report has20 fields.
There are336 named exact residuals covering594 scalar entries and
120 proof gates, verified by the complete private count pass.
All8 controls,73 rejected inputs,175 matching records,9 primitive
obligations and6 historical qualifications are retained.

The standalone hard-core barrier and independent core/full-amplitude
calibration probes passed before packet assembly. The latter checked
all six core inventories and complete original5116-tree equality and
the bound. Initial import/style warnings were corrected before
preflight without changing a scientific assertion or tolerance.

Every public multiplicity selector validates the exact input BEFORE
entering a private cache. Energy validation rejects floats, nonpositive
energies, unsupported nested inputs and excess total radiated energy.
The new complete coefficient estimate uses only original parameters.

Cold and distinct fresh-root preflights check source exports,
all residuals and gates, exact float-free serialization, the report
AST and every science test. The fresh run forces the complete S318
parent packets before its own residuals. All18 private/fresh/repository
files must agree byte for byte before source freeze.

The native helper receives one specification and complete-chunk ACKs
only. Both raw copies, all source hashes, counts, frontiers and
twenty-field REPORT AST must validate before native raw freeze.
Own original-SymPy ordinary tests, independent CLI and the complete
captured P8 regression remain separate publication requirements.
Their outcomes go in an external assessment, never retroactively
into frozen sources. The exact-GCD adapter is FULL-only.

At most one FULL and one ordinary/CLI pair run at a time. No resource
diagnostics, blind retries, changed parameters or unknown matching
choices are used. Original V/G/B/P8 remain OPEN.
