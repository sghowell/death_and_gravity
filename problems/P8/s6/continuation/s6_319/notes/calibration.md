# Independent checks and controls

Literal fixed-root enumeration disables ONLY non-singleton purely
graviton-seeded currents to count the hard block cores independently
of the closed EGF. Through block multiplicity0..5, the contact and
each exchange-sector counts are:
(1,3),(5,21),(33,177),(269,1749),(2601,19833),(29045,253821).
Restoring complete pure-soft rooted graph coefficients by composition
recovers the full S315 counts. Dropping them would leave387 instead
of434 at N2, and3767 instead of5116 at N3.

Weighted core coefficients are computed twice: direct nonnegative
set/sequence convolutions and the rational denominator recurrence.
These agree through degree8. Composed matter and gravity coefficients
are independently compared through N6 and checked against the proved
barrier, not used to extrapolate its all-N conclusion.

The complete original5116-tree amplitude is reevaluated with both
the old inverse policy and the gauge-complete temporal policy.
Their difference is exactly zero and the physical amplitude is
nonzero. The unit-polarization normalization is done in its square:
each of three exact TT tensors has squared Frobenius norm2, so the
squared amplitude is divided by8. It obeys the derived coefficient
bound relative to the same positive unexpanded Born parts.

Exact trace-reversal isometry and ordered cumulative-energy checks
supplement the general vertex and disjoint-charge proofs.
All calculations use exact arithmetic. Finite graph, coefficient
and kinematic tests do not replace the written all-order arguments.
