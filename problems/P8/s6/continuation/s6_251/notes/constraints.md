# Constraints, physical fields and the infrared boundary

The full scalar phase derives from the original lapse and nonzero-transfer shift constraints, not a homogeneous fixed-charge reduction. Its canonical flow has no inverse Theta or inverse momentum singularity. The central b chart is used only at certified large q; the low-frequency evolution always uses the original full first-order system. A finite-q zero of the auxiliary central Hessian is not a singularity of that original flow.

The reduced state quantizes the two physical scalar modes, including the M1 fluctuation with its original classical mean retained. Lapse and shift are recovered with the existing complete linear constraints. This is not an independent Gaussian oscillator assignment to each ADM variable. The physical shift-vector1/P multiplier remains locally square integrable against the bounded initial covariance in three spatial dimensions. Its scalar potential would have a stronger infrared singularity and requires a separate test-function domain.

The TT state uses the existing bounded coordinate-free orthogonal projector. Its almost-everywhere Fourier definition does not set the literal homogeneous constraint sector. No unrestricted nonlinear reconstruction, covariant BRST complex or constraint/ghost measure is supplied by these reduced canonical facts.
