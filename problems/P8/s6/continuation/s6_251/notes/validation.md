# Immutable validation and independent controls

The proposed manifest has six source modules, two tests, README, FORMULATION and seven notes: seventeen scientific inputs, with twenty native report fields. All sources remain private until exact packet checks, independent science, report-field preflight and formatting pass.

Independent checks include separately derived weighted physical phase equations at and away from the crossing, both canonical time-boundary maps and their full ultraviolet transition, exact noncommuting symplectic covariance fixtures, complex uncertainty on arbitrary vectors, independent matrix-square-root covariance, tensor normalization, integrated full preparation along actual diagnostic bounce-crossing trajectories, variable-frequency matrix Riccati coefficients with lower kinetic corrections, and full smooth-sampling boundary and integration-by-parts identities. Finite nonzero diagnostic stress profiles are algebra/numerical fixtures, not approximations replacing the actual tiny fixed physical profiles.

Private representation checks use Python Booleans for exact gates and full symbolic normalization. No failed proof is repaired by editing frozen bytes. The written all-order state argument remains distinct from its finite diagnostics and is not FORMALIZED.

The first complete private science run passed405 tests and stopped six variable-frequency tests while converting rational expressions to polynomials: inverse momentum powers remained hidden inside unfactored denominators. Full exact cancellation resolved the conversion but was unnecessarily expensive. Independent common-denominator Laurent extraction passed all six cases. The final tests extract polynomial coefficients of the same complete matrix equation with symbolic time jets, then substitute the full rational coefficient functions and their derivatives; this second exact route also passed all six cases and avoids the costly multivariate GCD. The equation, requested coefficient orders and zero assertions are unchanged. These corrections precede freezing and are followed by a complete private rerun.

After freezing, build the native report with original SymPy and every earlier P8 input protected. Preserve its raw JSON through acknowledged length-checked chunks and verify all private/repository/native hashes and AST report fields. Run fresh repository science/preflight, ordinary and CLI replays, and a complete captured regression including this checkpoint. Only full regression may use the audited exact-GCD adapter and frozen S219 helper allowance.

Publish only after every gate passes, using exactly twenty-one files: seventeen inputs, native report, external assessment, root claim ledger and root README. Exclude later checkpoints and unrelated P4/P9 work. Keep all prior primitive and matching statuses unchanged.
