# Validation and immutable replay boundary

The executable packet checks the exact full-parent tensor
restriction, both tensor coefficients, physical source
normalization, polynomial and orthogonal TT projectors,
actual weighted Green/Wronskian identities and the
complete energy and time-derivative majorants.

Independent fixtures exercise exponential unimodular
matrices, arbitrarily rotated TT projection, a separately
differentiated local polynomial TT construction, actual
expanding-background forced wave equations from zero to
large spatial momentum, both homogeneous moments and
initial-data inversion, compact compatible bump detectors,
a genuinely incompatible generic detector, the full
adjoint norm and both gravitational normalizations.

These numeric fixtures supplement the continuous
all-momentum proofs; neither their sampled momenta
nor their finite grids constitute a physical cutoff.
The polynomial differential fixture compares expanded
entries, so structurally different but equal polynomial
expressions are not mistaken for a symmetry failure.

The18-source manifest contains seven package modules,
two test modules, two root notes and seven proof notes.
The report has20 explicit top-level fields. Every
field mutation, missing/extra field and unauthorized
frontier promotion is rejected.

Native report generation, private/direct science,
ordinary replay and CLI use unmodified SymPy.
Only the separately audited full regression uses
the exact GCD adapter. Its full captured test snapshot
and final process success are required before publication.
The analytic, PDE and distributional proofs are not
FORMALIZED. Frozen scientific bytes must not be edited.
