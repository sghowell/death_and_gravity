# Actual complete-parent tensor action

Use signature +--- and the original fixed full parent,
not a minimally coupled scalar surrogate. Set N=1,
shift=0,u=t and g_ij=-a^2(exp gamma)_ij with gamma
symmetric and transverse traceless. In this unimodular
chart det(exp gamma)=1 exactly. Thus X=1, R=1,
trace K=3H and uHu=0 throughout each pure tensor history,
not only at its tangent.

The complete affine source
S_mu=u_mu(R-1)[Box u/X-3H+3R_u/(4R)
+(-X^-2+3R_X/(2RX))uHu]
therefore vanishes exactly. A1=A2=0 and the remaining
DHOST L3,L4,L5 contain a normal Hessian contraction
that vanishes here. F, including the fixed QG1 scalar
retuning, depends only on u and X and has unchanged
volume. The original homogeneous M1 scalar has the
same property. Neither supplies a tensor potential
in this exact unimodular chart.

The surviving gravitational restriction is the original
F2=-R/2=-1/2 tensor action. Direct ADM expansion gives

    S_T^(2)=kappa/8 integral a^3[
        tr(gamma_t^2)-a^-2 sum_i tr((partial_i gamma)^2)].

For general noncommuting gamma and gamma_t, the quadratic
term in K contains their commutator; its trace is zero.
The independent original spatial-curvature calculation
for diag(a^2 exp h,a^2 exp(-h),a^2) gives
R3=-h_z^2/(2a^2). Rotational invariance and the two
TT polarizations give G_T=F_T=kappa and speed1.
No use is made of an unverified reduced scalar-metric norm.

Use unit-Frobenius TT polarizations and define
h=sqrt(kappa)gamma/2. The kinetic action is then
one half of the displayed unit wave form, summed
in the Frobenius norm. In the physical stress convention

    delta S_m=(1/2) integral sqrt(-g) T_ab delta g^ab.

Since delta g^ij=gamma_ij/a^2, the linear physical-frame
tensor coupling is + integral a^3 T_ij h_ij/sqrt(kappa).
Varying the canonical action gives the source-only equation

    Lh=Pi_TT T_centered/sqrt(kappa),
    L=partial_t^2+3H partial_t-a^-2 Delta.

L is formally self-adjoint with weight a^3 dt dx.
This defines the tree-propagated vector-noise component.
It does not assert that this is the complete quantum
tensor equation. In particular the full determinant
self-energy, gravitational counterterm response, and
finite-coupling remainder are not bounded here.
