# Boundary-compatible actual tensor response

The full clock parent gives the canonical tree tensor
equation Lh=T_TT/sqrt(kappa), with
h=sqrt(kappa)gamma/2 and
L=partial_t^2+3H partial_t-a^-2 Delta.

A nonempty restricted compact TT detector class obeys
two exact weighted homogeneous-mode moment conditions.
Its advanced stress test vanishes near both endpoints,
and the detector annihilates all free tensor initial data.

An explicit full-momentum energy estimate gives
N(Gadv q)<5000 D(q), including zero momentum.
The unchanged actual CD stress covariance then gives
source-only leading tensor STD below5e-372 D(q),
or dimensionless metric STD below1e-771 D(q).

No complete quantum metric state, finite-coupling response
remainder, scalar/mixed solution or V/G/B closure follows.
There is no initial-state factorization or gravity switch.
See FORMULATION.md and the seven written proof notes.
The read-only report pins18 scientific source files.
