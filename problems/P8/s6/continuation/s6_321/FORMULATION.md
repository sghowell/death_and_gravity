# P8 S6.321: complete three-current and one-block subtraction

This successor uses the unchanged original canonical action and parameters.
Original V/G/B/P8 remain OPEN. Scoped P8(a) is unchanged.

Let H3 be the COMPLETE four-tree pure-soft current in the gauge-complete
temporal reorganization of S317, with positive energies a,b,c, fixed real
directions and arbitrary complex TT leaves of unit spatial Frobenius norm.
On the generic domain with no exact internal propagator poles, put

 W=a+b+c, F=(abc/W)*H3,
 S=1/(a+b)+1/(a+c)+1/(b+c).

The theorem is uniform in all energy hierarchies and angular approaches.
The energy derivatives hold the directions and leaf tensors fixed:

 ||F|| <= 4800000000000000000000*W^2/kappa,
 ||F_i|| <= 1000000*W/kappa,
 ||F_ij|| <= 1000000*W*S/kappa for distinct i,j,
 ||F_abc|| <= 721*S/kappa.

The current has compatible continuous energy faces, axes and a zero
origin limit for each fixed generic angular configuration. This is not
an assertion of holomorphy on a global total-energy disc.

Let I(a,b)=(a+b)log(a+b)-a log(a)-b log(b), using its continuous
zero-axis extension, and J=c I(a,b)+b I(a,c)+a I(b,c).
The alternating eight-corner energy rectangle therefore satisfies

 ||Delta_a Delta_b Delta_c F|| <= 721*J/kappa.

In the original hard collision domain 5/4<=E<=2, W<=1/8 and positive
associated elastic transfers, let G188=abc*sqrt(rho)*M188/A0.
Here M188 is the term whose maximal COMPLETE pure-soft block contains
all three leaves, in the complete temporal tree reorganization.
It has47 hard-core trees times4 internal soft trees, not188 isolated
diagrams declared gauge-invariant in a different representation.
A0=Am+AG is the unchanged positive unexpanded real Born normalization.
Then

 ||Delta_a Delta_b Delta_c G188|| < 10^-738*J.

On the energy cube (0,x)^3,

 integral J/(abc) <= 24*x^2,
 integral J^2/(abc) <= 18*x^4.

These are integrable current/class kernels, NOT a complete probability
or an inclusive real-virtual observable. The other4928 temporal-tree
terms, all-N overlap subtraction, hard/evanescent matching, quantum
state, absolute complex Regge, common-parent bounce and original
V/G/B/P8 closure remain separate. Excluded exact internal poles
are not assigned values. The proof is exact algebra plus written
analysis, not a Lean/kernel formalization.
