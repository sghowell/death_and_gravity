# Validation boundary and independent controls

Check the original full bubble coefficient, general two-endpoint Ward
identities, three literal local covariant quartic normalizations,
eight coincident operator-power limits, both exact-D light triangles,
ordered A-B-A product rule, all three fixed UV counterterms, physical
cut/derivative identities and every rational original bound.

Thirty independent tensor-component comparisons use a specified
diagnostic mass17. Four exact ORIGINAL absorptive recoil states use
both physical polarizations and include actual coincident invariants.
Finite exact states are not substituted for a uniform proof.

Negative controls reject floats, unphysical invariant intervals,
cross-threshold channel quotients, invalid resolution/forward endpoints,
unsupported sectors, all report-field mutations and promotions of
a selected finite interference into a full physical probability.

A draft packaging check exposed accidental mutation of a cached
component-check dictionary; the final code copies it before adding
absorptive checks. A first warmup invocation had closed stdin after
successfully rebuilding its parent, so no fresh successor ran in that
invocation. Neither failed wrapper is accepted evidence. The corrected
interactive warmup requires exactly one START after final source equality.

All ordinary scientific algebra uses original SymPy. Cold source,
independent fresh-copy and complete native checks precede external
publication acceptance. The full native report has20 fields, including
all five direct input hashes and the complete source manifest.
The exact algebra and written derivations are not kernel formalization.
Later timings and acceptance receipts belong outside this immutable
scientific manifest.
