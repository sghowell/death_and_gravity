# Original uniform remainder and finite full-tree interference

## Uniform finite source remainder and original normalization

Let R_B=M5_B-A1_B*S0 with the complete Born leading soft current.
Each hard Euclidean norm is<3, each pair norm<6, and a pair changes
by at most12omega under original recoil. Therefore an invariant
changes by at most144omega and F changes by at most11520omega.

Using the complete canonical current norm64/omega and its variation608,
the external, coefficient-change and inner-insertion budgets are

3*64*11520 +608*(3*216+3*36) +3*2*36*80 =2688768.

This is a canonical Frobenius bound and thus controls both physical
TT polarizations. The actual full Born value obeys
A0>A_m>4g^2/n^3. Since32pi^2>288,

||R_B||_TT/A0
 <2334 g^2 n/sqrt(kappa)
 <10^194/sqrt(kappa).

The last step uses the ORIGINAL g=1/8192, n<10^200/256 and
kappa=10^800; the physical normalized bound is<10^(-206).
The isolated hard coefficient has only the deliberately loose bound
|A1_B|/A0<10^190. This is NOT a small perturbative hard correction.
Cancellations with the remaining triangle and box sectors remain
important and are not inferred from this selected-sector estimate.

The unrounded2334*g^2*n bound has enough margin that adding the
already known S337/S338 remainders still fits below10^194/sqrt(kappa).
That combined statement includes only these three known sectors.

## Finite full-tree interference

With r=10^194, delta=min(1,-t,-u), a=delta/192 and ell=min(x,a),
the actual-phase integral of|2Re<Mtree,R_B>|/A0^2 is bounded by

r[128ell+4e9 ell^2/delta]/(4pi^2 kappa)
 +1_(x>a)1e18 r(x^2-a^2)/(kappa delta).

The full original47-tree low/high bounds are used. The low window
x<=a is<10^(-602), and its lower-cutoff tail vanishes.
The high bound is finite at each fixed nonforward Born angle, not
uniform in a forward limit. The separately divergent leading-soft
loop extension is excluded. No full inclusive probability follows.

The current and recoil estimates are inherited from S335, with the
normalization inherited from S297. Specifically|C|<4g^2/n follows
from the positive exact margin(n-4)^2/[n(n-2)^2]. Original
A_sym=4g^2/[3(n-2)^2(n-4/3)] and strict convexity give
A0>A_m>4g^2/n^3. The selected symmetric loop subtraction never
replaces or retunes this original Born contact.
