# Independent checks and read-only replay boundary

Independent original-shell integration uses both the exact angular grazing boundary and a separately derived radial cap formula. Every radial UV degree and angular monomial through degree eight is checked at several separated cutoff ratios. Controls show that dropping the positive strip changes the relevant shape coefficients.

The full massive flat check uses all ten constrained physical readouts and all nine pairs, with analytic complex polarization frames, eight azimuthal samples, several polar angles and both signs of a transfer component. It checks all forty-five exact ultraviolet coefficients against a separate Cauchy extraction. The complete flat conversion is also recovered using centered translation.

Actual curved checks keep all four W8 coefficients, independent source/detector times, distinct inverse phases and all source jets. Full physical fields and projector routes agree. Exchanging centered created momenta and reflecting inverse radius check the exact symmetries. Twenty-four- and thirty-two-point inverse-radius extractions independently check every centered UV slot.

A private test initially expected a nonzero j1/d1 coefficient in the chosen CD fixture. The coefficient cancels. The test was corrected only after inspecting every retained slot: j1/d3 is nonzero, while full finite-momentum j1 and j3 endpoints are also nonzero. This is an allowed-slot cancellation, not permission to delete endpoint labels. All 653 expanded independent tests then passed in42.68 seconds.

The all-P tail tests integrate every original UV slot directly with the exact two-leg cap, including both small and large external transfer, logarithmic powers, changing radial breakpoints and a cutoff at twice the mass. Exact Chebyshev derivative expansions and all source-jet/weight counts supplement the written estimates. Generic amplitude exchange/Schwarz identities and hardcoded flat coefficient benchmarks are independent of those numerical quadratures.

The integrated scientific suite also checks every named residual, scope gate and unsupported closure mutation. The report includes an explicit eighteen-source manifest and twenty fields; independent tests reject mutation, omission and addition of every report field.

Native, direct science, ordinary and CLI replay use original SymPy. Only full regression uses the separately audited exact GCD adapter. Final private/repository scientific, lint, native report and all independent publication replay outcomes are recorded in the continuation audit after completion. No report is advertised complete before its replays finish, and no frozen scientific byte is edited.
