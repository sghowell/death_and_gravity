# Frozen scoped objective: complete original UV-symbol conversion

Use the unchanged actual CD Proca parent, mass m=1000, unit time slab, full unit-W8 reference, actual-state correction and original fixed covariant prescription. S6.207 supplies all fifteen j/d UV slots with all thirty-five source time-jet entries. S6.208 supplies the exact same-current repartition and all-P integrable remainder. No previous finite piece or unmatched local target is added again.

Let U_K be the retained UV symbol integrated over |k|>=m with the original mask |k|<=K and |P-k|<=K. Let U_ball,K use the explicitly named |k|<=K comparator on the same high band. Write Delta U_K=U_K-U_ball,K. The full one-mode contact has the same mask on both sides of this comparison, hence zero difference but not zero value.

For q=j+d<=4, the current coefficient F_q(n) multiplies r^(1-q). Put p=|P| and u=n.phat. For K>p+m, the exact lost radial moment is

K^(4-q) [1-R^(4-q)]/(4-q), q<4,
log(1/R), q=4,

where R=(p/K)u+sqrt(1-(p/K)^2(1-u^2)), and -1<=u<=p/(2K). Pair-band minus comparator has the negative sign.

For q<4, retain every shape h=1,...,4-q, including finite K^0 terms. The azimuth-averaged coefficient Fbar is acted on by the hemisphere polynomials and the exact positive-strip corrections in notes/shell.md. There are ten universal q/h shapes, twenty original j/d/h instantiations and thirty-five corresponding source-jet shapes. These counts are distinct from the fifteen original UV slots.

The complete actual unit-W8 endpoint is exchange-even in the centered momentum k-P/2. Its normalized Schwarz symmetry forces only even total UV grades q in those variables. Odd endpoint labels are retained. Consequently the COMPLETE conversion is

Delta U_K = K^3 A3 + K A1 + E_K,

with no K^2 or finite K^0 term. In terms of the complete centered coefficients f0 and f2c,

A3 = -(2pi)^-3 integral_S2 p |u| f0/2 dOmega,
A1 = (2pi)^-3 integral_S2 [p^3 |u|(3-5u^2) f0/16 - p |u| f2c/2] dOmega.

These are bilinear current coefficients, with all source time jets in f2c. The full curved A1 still needs explicit evaluation; it is not replaced by the flat benchmark.

For every external P and K>=2m,

|E_K| < 1e40 ||D||L2 X46[Gamma]/K,

where X46^2=sum_(r=0)^4 ||(1-Delta)^3 partial_t^r Gamma||L2^2 is the unchanged S6.208 norm. The proof covers p<=K/4 and the complementary large-transfer region, including all q4 logarithmic slots. P0 has exactly zero conversion.

This fixes a regulator-conversion structure and its remainder only. Original one-ball UV/contact finite and divergent coefficients, curved A1 evaluation and fixed covariant matching remain required. No physical subtraction choice, full response, mixed inverse, finite-coupling background/stability, parent cutoff/loops or original V/G/B closure is inferred.
