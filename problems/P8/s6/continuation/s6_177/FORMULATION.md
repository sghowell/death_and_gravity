# S6.177: an actual full-function canonical gravitational-decoupling family

Original P8(b), V/G/B and P8 remain OPEN. This step supplies a classical
decoupling construction through the complete S6.174/S6.176
CD-REG-AFFINE-ISO action, not a regulator-removed quantum UV theorem.

Set kappa0=1e800, n=1024, lambda=1e-600, gamma=n/kappa0 and zeta=1e-6.
For canonical Phi and Y=(partial Phi)^2 in the physical +--- metric,
define the COMPLETE fixed functions

    f(Phi,Y)=kappa0 F_base(Phi/sqrt(kappa0),Y/kappa0),
    r(Phi,Y)=kappa0 [R_base(Phi/sqrt(kappa0),Y/kappa0)-1],
    a3(Phi,Y)=r_Y/Y.

For each real kappa>=kappa0, let Phi=sqrt(kappa)u, Y=kappa X and set

    R_kappa(u,X)=1+r(sqrt(kappa)u,kappa X)/kappa,
    F_kappa(u,X)=f(sqrt(kappa)u,kappa X)/kappa.

The generic-R affine construction and the unique vacuum-regular lower q
are retained. Href(Phi)=H_base(Phi/sqrt(kappa0)) fixes the shift/source.
At kappa0 the FULL action, coefficient functions, matter metric and maps
are exactly the old base. No frozen byte changes.

The domain is fixed in CANONICAL coordinates: every real Phi and
-kappa0/4096<Y<6kappa0/5. The physical u,X domain rescales with kappa.
It is not required, or asserted, that the same bounce solves every
off-base member.

With g=eta+2h/sqrt(kappa), Psi=sqrt(kappa)chi and
A=sqrt(kappa*zeta)W, derive the full classical limit on compact canonical
jets, retaining the exact Einstein divergence and all source contacts.
The complete independent scalar functions and all vacuum masses stay
fixed; the dependent Ia gravity corrections are tracked explicitly.

Derive the exact four-scalar tree amplitude of the limit and demonstrate
the distinction between fixed-transfer decoupling and the nonuniform
finite-gravity forward pole. Full quantum matching, contour/cuts,
physical omitted-order control, G/B and original P8 remain unresolved.
