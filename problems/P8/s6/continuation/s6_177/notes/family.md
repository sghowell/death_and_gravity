# Complete fixed functions, exact anchoring and domain

Write v=Phi/sqrt(kappa0), x=Y/kappa0 and sigma=kappa/kappa0.
The literal unmodified S6.109 functions include the rational switch
T=x^1024/[x^1024+(1-x)^1024], exponential bump and full scalar localizer.
They are not replaced by germs. The physical functions can be written

    R_kappa(u,X)=1+[R_base(sqrt(sigma)u,sigma X)-1]/sigma,
    F_kappa(u,X)=F_base(sqrt(sigma)u,sigma X)/sigma.

At sigma=1 these are identical as functions to the base. At fixed
canonical v,x, sigma F_kappa=F_base and
sigma(R_kappa-1)=R_base-1 for EVERY coefficient, not only mass and lambda.

The base bound L<R_base<6/5, L=1/2+1/(8n), holds for every real v and
-1/(4n)<x<6/5. The new R is the convex combination
(1-1/sigma)+R_base/sigma. Consequently the SAME strict lower L and
upper6/5 hold uniformly for all sigma>=1 on the fixed canonical strip.
The physical strip is -kappa0/(4n kappa)<X<6kappa0/(5kappa); calling
the old physical X interval fixed would change the domain and is rejected.

Direct chain rules give R_X=r_Y and R_u=r_Phi/sqrt(kappa).
The physical action kappa times the dimensionless kernel therefore has
the exact canonical scalar coefficients

    f -(kappa+r) R_scalar/2
      +(r_Y/Y) L3
      +[-r_Y/Y-7r_Y^2/(4(kappa+r))] L4
      +r_Y^2/[(kappa+r)Y] L5.

L3,L4 carry four canonical fields and L5 six, which fixes their different
normalizations. The independent f,r,a3=r_Y/Y are fixed functions.
The two r_Y^2/(kappa+r) terms are explicitly gravity-dependent completion
terms; their finite-kappa values are NOT falsely declared fixed.

Literal derivatives of the full base F,R give mass1 and

    f=(Y-Phi^2)/2+lambda Y^2-gamma Phi^4/3+O(field degree6),
    a3=-2gamma+6n Phi^2/kappa0^2+3n Y/kappa0^2+O(field degree4).

The nonzero Y^3 coefficient in f is -109184/(25 kappa0^2).
These higher coefficients remain fixed as well. Both full functions
are even in Phi, so there is no cubic scalar vertex.

The same base bounce and conditional S6.176 state are anchored at
kappa0. No off-base clock equations or automatic continuation of that
state into an interacting quantum family are claimed. A common
background solution for all kappa is not needed for this vacuum
decoupling construction.
