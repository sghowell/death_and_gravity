# Scientific and read-only publication checks

The explicit manifest has eighteen sources: seven modules, seven notes, two root documents and two tests. The report has twenty top-level fields, 34 named exact identities, 34 scalar entries, 52 gates, nine controls and 130 rejected scope inputs.

The scientific file contains 479 tests. Initial independent domain/readout checks passed 76 tests; expansion to actual UV coefficients and remainders passed 157; the complete physical, geometry and guard set passed 313 before integration with the scope audit.

Actual full W8 tests cover the joint inverse-radius/time domain, both analytic Schwarz signs, complex frames, all ten constrained readouts and all sixteen stress components. External scales extend from zero to 10^20. Every retained coefficient and source time jet is checked at separated transfers with two Cauchy resolutions. Fresh actual far and large-transfer near evaluations verify the explicit remainder envelopes, including nonzero remainders above numerical noise.

Independent removed-region quadrature retains both created momenta, every near power/log case, the far inverse-power tail and the unexpanded low massive weight. Weight identities and tests verify the same six-spatial-derivative norm for all finite and tail terms. Exact-order guards reject booleans, floating lookalikes, symbolic/nonfinite and out-of-range indices. Repartition controls retain the S6.201 state/time anchor and reject double-counting of earlier finite pieces.

Formatting/lint, final preflight and fresh private science precede freezing. All eighteen sources are copied exactly, verified by hash and replayed in a fresh repository process. The native report is rebuilt read-only and transferred losslessly. Native, direct science, ordinary and CLI preserve original SymPy. Only the complete regression uses the separately audited exact GCD adapter.

Final process exit-code-zero results, source/report hashes and observed timings belong in the publication audit. A running job is not a passed replay. No frozen scientific byte is edited.

Cauchy estimates, genuine complex norm arguments, Plancherel/Cauchy-Schwarz and dominated regulator removal are written mathematics, not FORMALIZED proofs. Sampling and gate counts do not substitute for those continuous arguments.
