# Uniform original-source nonleading remainder

Let A0>0 be the full original Born amplitude at the Born configuration,
S0 its leading physical soft tensor, and

R1_local=M5_local-A1_local*S0.

Use the exact original S295 recoil. All actual momenta have Euclidean
norm<3; shifted emission legs have norm<4; their difference from the
corresponding Born leg is at most6omega. Consequently all relevant
Gram entries have modulus<=16 and change by at most48omega.

For the complete 24-label quartic vertices:

|V_Y2|<=6144,    |delta V_Y2|<=36864omega,
|V_L|<=196608,   |delta V_L|<=1769472omega.

For a unit-Frobenius physical polarization, the L metric contact
is at most1179648 and its connection part at most27648. The Y2
contact is smaller and the f4 metric contact vanishes in TT.
The existing total current and recoil-current-change bounds are
64/omega and608. With P=|alpha|+|beta|+|gamma|, the complete
unscaled remainder budget is

64*1769472 + 608*196608 + 1179648 + 27648
=233991168.

Keep both polarizations conservatively by a further factor2.
Since16pi^2>144 and2*233991168<4e6*144,

||R1_local||_TT <=4e6 P/sqrt(kappa).

At the original parameters P<10^-1398. The fixed matter Born lower
bound and n<10^200/256 imply
A0>A_m>4g^2/n^3>10^-600. Therefore

||R1_local||_TT/A0 <10^-791/sqrt(kappa),
|A1_local|/A0 <10^-794.

The first bound is uniform on the entire original compact recoil
domain (away from the actual forward Born singularity), not just
within the adaptive soft window. It does not contain a collinear
massless propagator or a finite choice of unknown hard matching.

Three original rational recoil states and both polarizations
independently calibrate the NONZERO remainder, the original A0,
the exact known four-point term and both analytic bounds. They
do not replace the written uniform estimates.

## Finite rate correction still separate from the leading soft part

Set r=10^-791, delta=min(1,-t,-u), a=delta/192 and ell=min(x,a).
The NEW finite difference from using the actual local radiative
amplitude rather than only A1_local*S0 is
2 Re <Mtree,R1_local>/A0^2, with the SAME actual phase measure.

S335's full-tree low/high bounds yield its absolute integral bound

r[128ell+4e9 ell^2/delta]/(4pi^2 kappa)
 + 1_(x>a) 10^18 r (x^2-a^2)/(kappa delta).

This uses J<=1 and radial weight omega/(4pi^2). In the entire
low window it is below10^-1587 at the original kappa.
The low tail vanishes uniformly as the lower cutoff goes to zero.
The high bound is finite at every fixed nonforward hard point;
no uniform forward theorem is claimed.

The separately leading A1_local*S0 piece still belongs to the
retained soft/virtual organization. This finite residual is not
a complete inclusive loop rate or an interacting detector state.
Other polynomial/heavy/mixed/gravitational loop sectors and the
independent hard curvature matching exhibited by S336 remain open.
The six-tadpole result does not assign that matching coordinate.

For clarity, each L metric word is a sum of six products bounded by
2*16^3, giving24*12*16^3. Each connection factor has three terms
|k.p|*|epsilon(p,p)|<=4omega*16. Its remaining Gram factors give
24*9*16^2*omega<=27648 at omega<=1/8. The coefficient P absorbs all
three quartic representative coefficients, including the constant
whose TT contact vanishes but whose external-current change remains.

The high window uses the actual S335 bound
||Mtree||/A0<2e18/(sqrt(kappa)*delta). Multiplication by the remainder
bound and integration of omega/(4pi^2) yields a coefficient
2e18/(4pi^2)<1e18, as stated. It never extrapolates the low-window
recoil estimate beyond its valid range.
