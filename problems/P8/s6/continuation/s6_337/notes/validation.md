# Independent checks and immutable acceptance

Private probes first checked all six diagram identities on an actual
original recoil state, then general hard Gram data and off-shell Wick
factors. Separate general Ward and reduction probes checked all eight
quartic basis operators, five physical identities and six finite
factors. A bound extension checked three actual original states and
both polarizations. All used original SymPy and protected6764 inputs.
The packaged draft further checks each of six literal covariant
quartic connection identities independently.

The draft module probe passed all five scientific modules with8
captured files unchanged in184.11795279197395 seconds. It is diagnostic
evidence, not a replacement for the acceptance sequence below.
Written loop counting and uniform estimates are not inferred from
finite exact-state sampling. This is not kernel formalization.

Cold and independently started fresh preflights use original SymPy.
The fresh process first rebuilds all frozen immediate-parent packets
and waits for one START after byte-identical final source copy.
Completed ancestor caches are explicitly disclosed. Actual source-set,
AST20-field/export checks, parent hashes and scientific guards must
pass before freezing.

Three-way private/fresh/repository equality, one native report
specification, complete raw provenance validation, own ordinary tests,
a separate read-only CLI and a covering FULL whole-wrapper exit0 are
required before publication. Only FULL uses the audited exact-GCD
adapter, with original backend restoration required. Frozen source
and raw evidence are never rewritten to conceal failures.
