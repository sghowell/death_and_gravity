# Same pure reference factor and exact zero translation charge

Use the unchanged scalar/tensor preparation of S251, the exact S240
H SLE, and the original propagated Proca preparation. Their fixed
homogeneous coefficient flows and initial covariances respect spatial
translations and the reality relation between opposite momenta.

S251's complete matrix Gramian is selected once with its original bump
and preparation mass1; neither its initial covariance nor its later
reference evolution is re-minimized. Both symmetric boundary shears,
the time-dependent central canonical swap and both kappa/volume
normalizations act identically on each real cosine/sine pair. The
package binds these complete original maps and checks commutation
with the doubled translation rotation. The H state remains the exact
SLE, not its auxiliary W6-initialized exact KG comparison or a live WKB
state. The Proca graph retains all three modes and cross covariances.

At every nonzero mode pair the full physical configuration has eight
components: two coupled scalars, two TT, three Proca and one H.
At first order of the new slice tau=-t_TT, including the corresponding
dual sign. This is the same linear canonical reference dictionary.
Nothing here defines a nonlinear pushforward of its Gaussian through
the local chart or assigns independent Gaussian laws to S266 invariants.

On any finite symmetric set of nonzero torus modes, evaluating these
same mode prescriptions gives a centered pure Gaussian canonical factor.
It is an explicit IR/UV regulator family, not literally the original
infinite-volume R3 state. The finite-mode assertion does not need to
select a homogeneous quantum state. At zero momentum the shape has
five tracefree components; no two-polarization inverse is applied there.
Translations act trivially on any separately supplied homogeneous factor.

Here is the complete pure-Gaussian symmetry argument. Let
S=[0,I;-I,0] act on real cosine/sine configurations q=(a,b).
Translation-invariance of the pure covariance implies its positive
configuration block and full configuration/momentum block commute
with S. For unit CCR, its wavefunction graph is
Z=(1/2)Vqq^-1-i Vpq Vqq^-1, with Z=Z^T and Re Z>0.
Thus ZS=SZ. In blocks this permits

Z=[A,B;-B,A], A^T=A, B^T=-B.

In particular odd internal cosine/sine correlations are NOT erased.
The normalized wavefunction proportional to exp(-q^T Zq/2) is Schwartz.
The complete generator J=-i hbar(Sq).partial_q annihilates it because
(Sq)^T Zq=0. The trace of S is zero, so there is no Weyl-ordering
constant in this first-order configuration rotation.

The actual finite translation group acts by ordinary coordinate pullback
on L2 of the real mode coordinates. Orthogonality preserves Lebesgue
measure, the representation is strongly continuous and periodic, and its
three commuting self-adjoint generators have the displayed first-order
expressions on Schwartz functions. The Gaussian is in their common
kernel. Its compact-group average is therefore itself: this is neither
conditioning nor a change of normalization or preparation.

This is a configuration-space rotation, not a generic oscillator
phase-space rotation; a spurious metaplectic2pi sign is inappropriate.
Purity is also essential to the stated inference. The equal mixture of
the two radial-Gaussian states proportional to(a+i b) and(a-i b)
is rotation invariant and has zero average charge but charge variance1.
Merely commuting with the generators would not establish zero-charge
support for a mixed state.

For a specified finite translation-invariant Weyl Hamiltonian on a
suitable common domain, quadratic J has no higher Moyal corrections,
so its commutator equals i hbar times the quantized Poisson bracket.
The current result is not the missing construction of that complete
nonlinear Hamiltonian, its self-adjoint realization or an interacting
same-state evolution. Unbounded reference Gaussian support still
extends outside the explicit local classical chart.
