# Independent validation and immutable evidence

The first exact private probe checked the full determinant and cofactor,
zero/nonzero Fourier projectors, nonlinear tangent/dual pairing and a
nonsymmetric horizontal lift. A separate full-convolution error-budget
probe and non-aliased Fourier diagnostic retained generated homogeneous
components and explicitly measured the discarded-mode defect.

The exact full spatial-generator probe retained both matter channels,
the vector Gauss term and the original boundary. Its general coupled
pure-Gaussian check allowed all odd internal blocks for1,2,3 and8
physical components, with a mixed-state counterexample.

The first assembled symbolic packet run stopped on a zero real-Fourier
normalization residual that remained syntactically unexpanded after
integration. Expanding that exact expression supplied zero without
changing its full formula. The shape and cotangent packets had already
passed. A complete rerun is required before freezing.

All209 independent numerical/contract tests passed in2.00 seconds,
with four full exact proof packets deselected. RuntimeWarning was an
error. The Fourier diagnostics use grids of8K points per coordinate
for products of at most degree3 of K-supported fields, so no aliased
product is used. They retain generated means and distinguish a projected
fixed point from the entire determinant equation.

The numerical three-wave examples have the stated unweighted A0 norm;
they are not claimed to have that same radius in the stronger A8 norm.
The contraction proof itself is in A8 (and works in A0). Finite-grid
accuracy does not establish its infinite-convolution conclusion.

Independent checks also vary the full3x3 determinant-normalized metric,
test random complete horizontal/adjoint lifts with a nonsymmetric gauge
block and nonzero constraint bracket, and reconstruct pure covariance
from full complex Gaussian graphs with all cross terms. Nonlinear
cotangent fixtures outside the small theorem ball check algebraic
identities only; they do not enlarge the proved domain.

Final original-SymPy private/fresh preflights, exact18-file manifests,
AST-derived report fields, full packet counts, native raw report transfer,
ordinary/CLI zero exits and the captured complete regression are recorded
in the external assessment only after completion. Scientific sources
and raw report bytes are then immutable. Provisional research remains
outside their source manifest.

Native/direct/ordinary/CLI use original SymPy. Only the separately
captured full regression uses the audited exact-GCD adapter and its
original-path selfchecks. Frozen ancestors are not patched to make
a later replay pass. Written functional, elliptic, probability and
operator-domain arguments are not FORMALIZED, and test entries are
not independent scientific theorems.
