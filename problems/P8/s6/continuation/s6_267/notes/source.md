# Entire original generator and the auxiliary intersection

Let gamma be the complete hat-frame spatial metric, pi its symmetric
contravariant density momentum, W_i the full covector, and M1,H the
two retained scalar fields. The entire primary-surface generator is

D_i=pi^{jk} partial_i gamma_jk
 -2 partial_k(gamma_ij pi^{jk})
 +Pi_W^j(partial_i W_j-partial_j W_i)
 -W_i partial_j Pi_W^j
 +Pi_M partial_i M1+Pi_H partial_i H.

The local package obtains this by setting ONLY the last two scalar
primary momenta of S258 to zero. In particular the vector Gauss term
does not vanish. The exact identity between integral D_i xi^i and
the whole canonical Lie pairing retains the original spatial flux.
Full matrix pairing sums both symmetric off-diagonal entries; treating
one such entry as an independent momentum without its factor2 is wrong.

The S257 primitive boundary is a spatial scalar density. Its integrated
Lie variation is a periodic boundary, so its complete canonical shift
preserves the integrated generator. No boundary or profile contact is
removed from the full Hamiltonian.

The physical map is gamma_phys=C gamma, C=R^(-1/2)>0.
The exact conformal shape identity
det(C gamma)^(1/3)(C gamma)^-1=det(gamma)^(1/3)gamma^-1
holds even for spatially varying lapse; its spatial derivatives agree.
No source-free, trace-only or on-clock replacement of R is made.

The spatial construction is kinematic. On the bounce slice, evaluate all
twelve S266 invariants on the fully reconstructed fields and momenta.
Only data inside its actual box use its unique positive lapse and
normal-vector roots and uniform auxiliary pivots. This is an explicit
intersection condition, not an evaluated canonical-radius theorem.
The normalization of densities, mass-adapted H and its full source
cannot be replaced by independent Gaussian coordinate assignments.

All formula bindings are checked against the entire frozen parent
generator, full bounce Hamiltonian and primitive boundary. The native
report additionally rebuilds the whole S266 ancestry. Neither the
spatial lift nor its intersection condition alters S265's Gaussian
coefficient result or the historical physical-map refutation.
