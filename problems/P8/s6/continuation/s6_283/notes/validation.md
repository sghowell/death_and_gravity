# Immutable report and validation boundary

The manifest contains exactly18 ASCII source inputs: seven modules, two
test files, two root documents and seven proof notes. The native read-only
report contains exactly20 top-level fields, pins S282 and rebuilds its
entire ancestry. Every top-level mutation, missing field and extra field
must be rejected. Source hashes and historical qualifications are retained.

Exact checks cover the whole source, full parameter maps and primitives,
gamma recurrences, both complete species cuts, finite evanescent terms,
literal off-shell Ward identities, all-D self-energy cancellation and the
four-leg completed soft pole. Independent tests use exact alternative
kinematics, parent-warm invariance and coefficient calibrations.
Written uniform integration/continuation proofs are not FORMALIZED.
Numerical diagnostic agreement is not a continuum certificate.

Accept only after original-SymPy private and fresh-root preflights agree on
all18 hashes,20 AST fields,seven exports,all actual counts and science
tests, including full-parent-first ordering. Freeze accepted sources and
native raw bytes. Any subsequent correction requires a successor or an
external assessment, never an in-place repair.

Publication additionally requires independent native raw audits, separate
original-SymPy ordinary and CLI replays, and a captured complete FULL
regression. The exact-GCD adapter is FULL-only. Running or predicted
counts are not successful verdicts. Actual gates are recorded outside this
immutable manifest.
