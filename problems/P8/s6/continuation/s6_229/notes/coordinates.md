# Actual full classical mixed-order coordinates

Work in proper time on I=[-1/2,1/2], with the unchanged CD-REG-AFFINE-ISO-QG1 coefficients, a=(1+t^2)^2, physical q=P^2/a^2, kappa=10^800 and mass1000. The symbol b below is the physical scalar shift divergence, not the complementary high-frequency coordinate of S221.

The complete S220 classical quadratic action per kappa a^3 is

L=-3 vdot^2+(J+wbg^2/2-3Theta^2)n^2+6Theta n vdot
  +sigmadot^2/2+wbg n sigmadot-3ell vdot sigma
  +2b(vdot-Theta n)+ell b sigma
  +q v^2+2E q n v-q sigma^2/2
  +DeltaJ n^2+3Tc n v+9Aret v^2/2.

All original retuning terms are retained. Set

n=eta', zeta=w+H eta,
v=w+H eta-delta eta',
b=c'+q eta, sigma=r+ell eta,

where delta=1/[2(1+t^2)^3], E=1-3delta and wbg=-ell E. The last relation is checked against the actual source coefficients. The prepared inverse is

eta=I n, w=zeta-H eta, c=I(b-q eta), r=sigma-ell eta,

with I the original zero-past primitive. This is a bijection of the smooth prepared scalar amplitude histories. No division by H, Theta, E or P occurs.

For the weighted Euler operator, use W=D+3H. Direct substitution into the ENTIRE action, including coefficient derivatives, gives at row orders(4,4,4,2), in the field order(eta,w,c,r),

principal=diag(-6delta^2,0,0,-1).

Every coefficient above its row order vanishes. The w,c classical rows have order at most3; the eta row has only -6delta^2 eta'''' at order4; the r row has only -r'' at order2. This is a full mixed-order calculation, not a homogeneous specialization, high-P truncation, or removal of matter evolution.

The matter invariant is necessary. Substituting sigma=r without its ell eta shift leaves the nonzero coefficient ell multiplying eta'' in the matter row. With the correct invariant it cancels exactly.

On the actual slab, delta>=32/125, hence

1/| -6delta^2 | <=15625/6144.

This nonzero pivot does not assert positivity of the action or physical stability. All remaining classical coefficients are smooth and bounded on each fixed finite external-momentum ball, including the original retuning derivatives; no numerical bound on every derivative is claimed.
