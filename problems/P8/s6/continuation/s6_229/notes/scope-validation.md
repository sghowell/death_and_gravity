# Scope, independent validation and publication boundary

This checkpoint proves an actual reference LINEAR scalar/clock/matter inverse on smooth prepared Fourier-supported histories, not only an isolated flat or finite-local reference. It retains every original classical retuning coefficient, conditional Gaussian state, finite contact, density factor and direct auxiliary force. The new source restriction is explicit and is not a physical cutoff on the internal loop or a change of the theory.

Its constants are finite for every fixed external-momentum ball but unevaluated. No continuity or inverse estimate on the unrestricted original Sobolev completion follows by taking a union of those balls. In particular compact Fourier approximations cannot be passed through the inverse without a uniform estimate. The smooth band result is compatible with every real spatial Sobolev index at fixed band, not with an unproved spatial Schwartz-output assertion. It does not imply stability or nonlinear existence.

The literal homogeneous scalar constraint sector is not obtained by identifying its physical shift with the limit of a nonzero-transfer scalar amplitude. The single Fourier point has measure zero here. The previously established homogeneous sector remains a separate theorem.

The admitted Sobolev norms are for scalar amplitudes, including the shift divergence b. The inverse-gradient shift vector need not share the same H^r infrared norm. Its distributional reconstruction and the original weak-current extension are specified in notes/inverse.md; no same-H^r shift-vector or Schwartz-output bound is inferred.

The package checks40 named exact residuals containing164 scalar entries,30 proof checks,9 controls and191 rejected unsupported inputs. There are85 matching rows and the same9 original primitive rows. The17 immutable scientific inputs comprise6 package modules,2 tests,2 root documents and7 notes. The native report has20 top-level fields, every one covered by mutation rejection, with separate missing/extra-field checks.

Independent tests verify the mixed classical top terms and required matter invariant; both weighted residual adjoints and an explicit unprepared constraint counterexample; fixed-physical rather than dimension-varying invariant jets; the massive finite-transfer orderzero phase; and every variable-coefficient prepared primitive through its principal order for both n2 and n4. The latter detect commuting coefficients through primitives or dropping the upper multiplication.

A noncommuting8x8 finite mixed-order example checks left/right inverse products, reference-channel factors, row-primitives and right density normalization. A second finite block system uses the ACTUAL S222 C,F,K,Daux formulas at two rational diagnostic coefficient sets and nonzero phase driving. It matches the full coupled system to ordered feedback recovery, and rejects deletion of Daux or reversal of Qbar Taux. These coefficients are diagnostic fixtures only, not modifications of the physical background. The tests do not certify convergence of a continuum numerical scheme.

The analytic obligations are addressed in the written notes: actual all-order state comparison at physicald3; original near-dimensional integrable remainder and full invariant finite-contact matching; bounded-region massive covariance control; uniform compact-ball singular-extension estimates; ordered row primitives; finite weighted Neumann convergence; smoothness retaining the variable eta pivot; prepared first-order residual uniqueness; and both actual smooth band feedback identities. Exact zeros alone do not prove those continuum statements.

Private probes and source tests are completed before the17 inputs are frozen. Native replay uses original SymPy, fully rebuilds the source-pinned predecessors, checks the manifest and emits the report without editing inputs. A final private science run, repository science run, fresh ordinary replay, CLI replay and full P8 regression must all pass before publication. Only full regression uses the audited exact-GCD adapter; all other paths retain original SymPy. Private-draft corrections and actual validation outcomes are recorded in the external publication assessment, not by modifying frozen scientific bytes.
