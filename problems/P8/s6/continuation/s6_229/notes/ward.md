# Both ordered Ward legs and the distinct clock contact

The S219 current supplies the original renormalized Ward identities, not a Ward identity at an unrenormalized finite internal cutoff. The physical scalar metric tuple is(n,zeta,b). For opposite Fourier legs exp(sign iPz), the adapted tuple is

n=eta', beta_z=-sign i(c'+q eta)/P, Q=2(w+H eta)I.

Its synchronous part is(0,0,2wI-2cPi), Pi=diag(0,0,1). The remainder is the full background metric Lie derivative with

xi=(eta,0,0,-sign i c/P).

Both signs are checked directly against S215 kinematics. The eta part is pure time gauge in the physical metric; c also has a synchronous component and is not deleted as a gauge direction.

Let the unchanged one-point stress density be E=diag(-a^3 rho/2,-a pressure/2 I). Apply the SOURCE Ward identity first to the full detector tuple, then the DETECTOR identity to the remaining synchronous source. The full local bilinear is precisely

source_integrand(Dphysical,xi_G,E)
+detector_integrand(xi_D,Gsynchronous,E).

These are the full S215 density formulas, including the original S214 nonlinear ADM chart contact. The Gaussian one-point current is not zero, even though the specified total reference is retuned.

A separate contact comes from the nonlinear physical-to-unitary clock relation

zeta=v-(1/4)log[1+2delta(N^-2-1)],
zeta_N=delta, zeta_NN=4delta^2-3delta.

The S222 extra nn response contributes

3a^3 pressure(4delta^2-3delta) eta_D' eta_G'.

It is added exactly once. It is not already the S214 ADM metric-chart contact and cannot be inferred away from one-point cancellation.

The literal sum in the actual scale factor contains14 nonzero bilinear jet monomials. Each leg has at most one time derivative, so its Euler response has time order at most2. After exact cancellation the denominator contains no P and the numerator has degree at most2 in P. Thus the apparent inverse transfer cancels BEFORE taking norms, and there is no infrared blow-up from these complete Ward/contact terms. The source Ward, detector Ward and additional clock term are separately nonzero. Deleting the latter changes the eta-row eta'' coefficient by 3a^3 pressure/2 at delta=1/2.

The parent Gaussian bridge is also needed. S176 proves that the canonical Proca quadratic Hessian is the ordinary constant-mass metric Hessian on every prescribed metric. The remaining vector source and its first variation vanish at the reference for arbitrary covariant first jets, not only homogeneous lapse variations. Its source-dependent on-shell functional begins at degree4; a second source insertion linear in the zero-mean vector has zero Gaussian mean. Therefore no additional nonlocal r or clock channel appears in this reference linear response. This is not a statement that the nonlinear parent source, its fourth variations or the interacting theory vanish.
