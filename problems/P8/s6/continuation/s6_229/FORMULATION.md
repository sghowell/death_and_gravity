# Actual coupled linear inverse on smooth prepared bounded Fourier support

Fix the unchanged CD-REG-AFFINE-ISO-QG1 reference, a=(1+t^2)^2, proper interval[-1/2,1/2], kappa=10^800, mass1000, original fixed retuning, canonical conditional Gaussian Proca state and full finite renormalization prescription. For each finite external-momentum radius Pmax and each real r, use smooth time histories with Fourier support |P|<=Pmax in H^r, sharing the original nonempty zero initial neighborhood.

## Established result

The actual complete linear scalar/clock/matter Euler operator has a two-sided causal inverse on this source class. In adapted variables

(n,zeta,b,sigma)=(eta',w+Heta,c'+qeta,r+ell eta), q=P^2/a^2,

the full classical row-principal matrix at orders(4,4,4,2) is diag(-6delta^2,0,0,-1). The entire nonlocal quantum block is in the synchronous(w,c) pair; both ordered Ward terms and the distinct nonlinear clock contact are local of time order at most2 and have no inverse external momentum after full cancellation. The reference parent bridge supplies no additional nonlocal matter channel at this linear reference.

The fixed-physical full-dimensional scalar leading matrix is [[4,-4/3],[-4/3,8/5]]. It factors by L0=[[1,-1/3],[0,-1]] into the original trace/shear factors with shear normalization8/3. Complete all-dimensional proper-time matching retains both fixed fourth finite constants and the actual state/contact remainder.

Writing mathcal I=diag(I4,I4,I4,I2), the actual normal form is

mathcal I T_adapt=Fref+V,
Fref=diag(-6delta(t)^2,gamma L0^T diag(Ftrace,(8/3)F2)L0,-1),
gamma=1/(64pi^2 kappa).

For each fixed simultaneous derivative order j, the complete remainder has a uniform weak-log row-sum kernel majorant Cj(Pmax). The proof keeps the orderzero finite-transfer phase and all internal loop momenta. No Cj is numerically evaluated.

Let Kmax=max(||Ktrace||L1(0,1),(3/8)||K2||L1(0,1)) and b0=max(15625/6144,16Kmax/(9gamma),1). With lambda=(4b0 C0+1)^2, the ordered inverse

(I+B0 V)^-1 B0 mathcal I

converges in the weighted continuous norm and has unweighted adapted-source bound2 exp(lambda)b0 times the row-primitive source norm. B0=Fref^-1 retains the original shear pole and time-dependent eta multiplication. Its derivative commutators are kept in the prepared smoothness proof.

The full weighted residual adjoint recovers both original constraints using the unchanged initial zero germ. The actual S222 force equations then give a two-sided inverse of I-Qbar Taux on the smooth Fourier-supported class, including general mathematical phase driving through its stated right side. Daux has rank2 and is not removed. Qbar=(kappa a^3)^-1 Rhat keeps output density order and both contacts.

## Scope that remains open

All solutions lie in the original S222 weak graph, but the proved invariant bijective domain is a SMOOTH BOUNDED-FOURIER SUBGRAPH. There is no claimed inverse bound uniform as Pmax tends to infinity, unrestricted X_r/Y_r surjectivity, maximal graph realization, graph-norm density or Schwartz-output preservation. Literal P0 homogeneous constraints remain a separate mode problem; that point is measure zero in the Fourier L2 class.

Finite unevaluated weighted existence is not physical smallness, stability, absence of growing modes or an EFT cutoff. The internal loop is not cut off. Nonlinear/finite-coupling common-parent control and original V/G/B/P8 remain open. Every original primitive status is unchanged.

The exact algebra is replayed; continuum matching, strong kernel estimates, causal convergence, smoothness and graph recovery are written proofs, not FORMALIZED.
