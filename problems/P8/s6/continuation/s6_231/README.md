# Original scalar partial unitarity and conditional matching

This checkpoint uses the COMPLETE fixed-canonical nongravitational four-scalar tree of the same S182 retuned parent. The scalar mass is1, lambda=10^-600 and gamma=1024*10^-800. It does not import the older GY14 quantum matching.

The labelled invariant amplitude and normalized identical two-body channels give exactly two nonzero tree partial waves. The zeroth wave increases strictly and dominates the second. Its unique nominal real-part ceiling t0=1/2 lies at COM energy

10^133 < E_U < 2*10^133.

At energy10^134, every existing exact unitary normalized scalar amplitude needs a relative zeroth-wave correction greater than1-10^-5. This is a necessary correction, not a certified physical cutoff. A nonzero real tree is not itself an exact unitary amplitude even below E_U.

A second, CONDITIONAL statement uses the exact optical theorem and a full angular-amplitude error on an elastic annulus. If the explicitly unproved physical mass/LSZ, crossing, pole subtraction, positivity and dispersion-arc conditions hold, and the full relative angular L2 error is eta<1 on s in[E²/2,E²], then

b2/(4lambda) > 9(1-eta)²(E/10^125)^8.

Here b2 is HALF the second derivative of the physical pole-subtracted forward amplitude. It is not a freely selected tree parameter. At E=10^125, error eta<=1/2 and b2<=8lambda cannot coexist: the original elastic annulus alone requires b2>9lambda. At that energy the entire original tree amplitude is still below10^-46. The conclusion is a disjunction among the premises and matching tolerances; it does not identify which condition fails.

The S230 tensor-pole frequency lower10^398 uses the same anchored units. This numerical comparison neither establishes quantum decoupling nor repairs, deletes or physically validates the tensor poles.

See FORMULATION.md and all seven notes. The report certifies exact algebra, rational margins and written conditional arguments, not the unproved quantum hypotheses. Original P8 remains OPEN.
