# Nominal real-part ceiling and necessary correction

Let v=S-4>=0. Direct expansion of the complete massive tree gives

a+b=lambda(4v²+16v+24)-8gamma,
a-b=lambda(2v²+16v+24)+(3gamma/2)(v+4)v²-8gamma.

The constant24lambda-8gamma is strictly positive for the actual fixed coefficients, and all nonconstant coefficients are nonnegative. Thus a>abs(b), Q0=a+b/3>2 abs(b)/3 and t0>=5 abs(t2). Every other tree wave vanishes. This argument keeps the threshold mass and potential; it is not a massless asymptotic replacement.

For S>4,

Q0'=lambda(20S-32)/3+(gamma/2)(S-4)(3S-4)>0,
beta'=2/(S² beta)>0.

Hence t0=beta Q0/(32pi) increases strictly from zero at threshold to infinity. There is exactly one E_U>2 with t0(E_U²)=1/2. Its coarse enclosure uses only exact rational comparisons and3<pi<22/7:

- At E=10^133, beta<1 and Q0/(32*3)<1/2.
- At E=2*10^133, beta>999/1000 and (999/1000)Q0/[32*(22/7)]>3.
- At E=10^134, the same lower bounds give t0>50000.

The beta comparisons are squared positive inequalities, so no decimal root solver is needed. Existence and uniqueness use continuity, strict monotonicity and the endpoints; the machine checks the exact rational margins and derivative identities. Both statements are required.

Every existing exact unitary normalized scalar channel has abs(Re t0_exact)<=1/2, including inelastic channels. Since the original t0_tree is real positive,

abs(t0_exact-t0_tree) >= t0_tree-Re t0_exact
                         >= t0_tree-1/2.

At E10^134 this is STRICTLY greater than(1-10^-5)t0_tree. This necessary correction does not assume perturbation theory is accurate. It does not by itself locate a physical threshold or a UV cutoff.

In particular, a nonzero real t already has abs(1+2it)>1. The tree by itself is NEVER an exact unitary amplitude, even below its nominal real-part ceiling. Below the ceiling a small absorptive correction can supply perturbative unitarity; its existence and quantitative control for the original parent are not proved here. The phrase nominal ceiling refers only to the exact allowed real-part range of a normalized channel.

For an amplitude-level comparison at fixed kinematics, physical mass and LSZ matching must also be specified. Those are explicit premises of the dispersion result. At finite gravitational coupling the long-range graviton pole prevents these nongravitational angular projections from being transplanted unchanged. Removing it by hand is not a proof of the original G obligation.
