# Verification boundary

Seven modules,two test files,nine Markdown sources form18 ASCII,
newline-terminated source files. The read-only report has20 AST-derived
fields. Exact scientific payloads contain no floating values.

Cold and independently copied forced-parent-warm preflights must agree
before source freeze. Native capture receives its SPEC once and uses
original SymPy; chunk bytes/hashes and the canonical REPORT path from
the frozen verifier AST are independently checked. Sources and raw
native originals are never rewritten afterward.

Acceptance requires the ordinary original-SymPy test suite, independent
CLI and a complete captured P8 FULL snapshot. Only FULL may use the
audited guarded exact-GCD adapter. Earlier snapshots or private science
runs are not substituted for this coverage. Run-specific evidence is
recorded externally after the source freeze.

Independent checks include full-D angular/log/hypergeometric derivatives,
real/virtual phase limits at several energies and resolutions, complete
dimensionally continued recoil-rate quadratures, all positive majorants
and scope guards. Numerical convergence calibrates formulas, not rigorous
quadrature errors. The analytic inequalities and fixed-domain dominated
convergence prove the stated bounds and limit.

Early unfrozen research residuals required an explicit positive radial
variable and physical rapidity parameter to express their stated domains
before exact simplification. No mathematical formula, frozen source or
physical branch was changed to force a test pass.
