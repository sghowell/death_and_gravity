# Same-state remaining-energy soft insertion

In the SAME physical-state additional-soft prescription as S325,
Z_e(sigma;y)=[delta a_e(sigma)*y^(2e)-delta a(sigma)]/(2e).
At y=x-R it tends to
Z(sigma;x-R)=deltaDelta(sigma)+deltaa(sigma)*ln(x-R).
The Born comparison is taken at the SAME remaining energy.
The inherited15000 R(1+ln(1/R))+1400 R|ln(x-R)| bound divided
bykappa has conditional expectation<33000a*x*(1+L)/kappa.
It supplies a regulator-independent integrable dominator.
Consequently the signed marked measures Z_e dP(.|cut) converge in
total variation. Their limiting TV is bounded by
[10000Mlog+1400Mrem]/kappa
 <23000a*x*(1+L)/kappa
 <19000x*(1+L)/kappa^2 at original a<4/(5kappa).

The regulated absolute conditional mean is bounded by
[15000Mlog+1400Mrem]/kappa.
Since M1<=a*x,1/(a+1)<=1 and the harmonic term<=3/2,
this is<33000a*x*(1+ln(1/x))/kappa.
The limiting expression uses10000 in place of15000 and is
<23000a*x*(1+ln(1/x))/kappa.
Multiplication by original a<4/(5kappa) gives the stated19000
physical TV bound. At a=0 all state-change marks vanish exactly.

Total-variation convergence means the integral of the absolute
difference of these two signed densities tends to zero against
the SAME normalized limiting D4 reference. The common integrable
dominator keeps both R=0 and R=x endpoints.
This argument neither changes the cloud intensity nor proves
matching with dimensionally continued outer hard states.
