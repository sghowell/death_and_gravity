# S6.291: complete original heavy-graviton production and soft threshold

This scoped continuation derives the full original Phi Phi -> H + graviton
tree, its dimension-dependent physical polarization sum and forward cut,
and the complete soft-threshold Laurent identity with an explicit
compact-window regulator remainder. It does not finish physical P8.

The H channel is formal perturbation theory. Its positive forward density
behaves as K0/(s-n); the naive D4 lower-endpoint moment diverges. The full
D-dependent coefficient, including angular and polarization evanescence,
must be retained before combining with virtual/pole terms. No finite
subtraction, width, stable-heavy state or physical IR prescription is chosen.

Run from the repository root using the governed original-SymPy lanes:

```sh
.venv/bin/python -u scripts/p8_replay.py ordinary problems/P8/s6/continuation/s6_291
.venv/bin/python -u scripts/p8_replay.py cli p8_vacuum_affine_heavy_graviton_production_threshold.verify
```

Sources are the7 modules,2 tests,2 root Markdown files and7 written notes.
The raw native report has20 top-level fields. Source and raw acceptance,
ordinary/CLI and the complete captured FULL are distinct gates. This frozen
scientific text makes no claim about later gate completion; the external
assessment records actual validation. Exact-GCD acceleration is FULL-only.

S290 is the immutable parent. Its original frontiers,6 historical
qualifications and rejected S279 history are retained. Scoped P8(a) is
unchanged; V/G/B, physical state/domain/bounce, matching and Regge remain OPEN.
