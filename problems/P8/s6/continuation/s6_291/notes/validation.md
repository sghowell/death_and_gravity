# Immutable-source and independent replay contract

Exactly18 source files:7 modules,2 tests,2 root Markdown and7 notes.
The native report has20 top-level fields, AST-derived and independently
compared with the certificate test's complete KEYS list.

Expected current algebra:174 named checks,563 scalar entries,33 proof
gates,8 controls including73 actual input rejections. All9 primitive,
147 matching and6 historical qualifications remain. The scientific
payload is recursively free of Python/SymPy floats. Independent tests
cover all tensor/projector components in D4/5/6, numerical radial phase
versus the B0 negative-interval integral, full angular hypergeometric
normalization, first evanescent derivative, exact pointwise bounds,
invalid domains and a nonconstant integrated Laurent model.

Before native acceptance, require an original-SymPy cold preflight and
an independent fresh copy with every immediate-parent packet forced first.
Require all18 byte/hash values identical among private/fresh/repository.
Freeze these scientific inputs before sending exactly one native SPEC.
Do not run research probes in the native helper or send DONE.
The raw stream must be captured whole and validated independently.

Ordinary and CLI run with the original SymPy GCD. Complete captured FULL
alone may use the guarded exact-GCD adapter. Their collection and final
exits are separate external acceptance facts, not asserted here.
At most one FULL and one ordinary/CLI pair may run. A snapshot that
predates this packet is not acceptance evidence for it.

The external assessment records actual cold/fresh/native/ordinary/CLI/FULL
outcomes and development failures. Never change frozen sources or the
raw certificate merely to append later validation facts or corrections.
