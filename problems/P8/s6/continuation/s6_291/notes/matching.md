# Preserved matching and unresolved pairing

Append exactly one scoped matching record H8A455 to S290's146 records.
Retain all9 original primitive records,6 qualifications, the rejected
S279 history and scoped P8(a). Copy cached parent dictionaries before
adding current checks. No frozen scientific or raw file is rewritten.

The original classical source fixes the tree here. Flat OS and one-point
conditions do not fix extra curved/EFT matching; no R_old H term is
inserted as a new classical interaction. Its later role is separate.

The present cut is order g^2/kappa and forward only. Other mixed sectors
include elastic light-pair interference and heavy-pair production
interference; this packet does not certify their complete inventory or
four-point reconstruction. The S290 endpoint is only one subset.

The soft pole is left explicit. No finite distribution convention is
silently promoted to a physical matching choice, and the unpaired result
cannot determine its own virtual cancellation. H is perturbatively
unstable at n>4mu. Heavy pole/virtual/width effects must be analyzed with
their actual order and domain before a physical spectral statement.
Neither an exact stable-H atom nor a finite positive low-cut bound is
assumed. This is research still to do, not an arbitrary user-number choice.
