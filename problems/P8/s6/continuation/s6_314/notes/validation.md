# Immutable validation contract

The manifest has eighteen ASCII sources: seven Python modules,
two test files and nine Markdown files. The canonical native report
has twenty fields. It checks321 named residuals/861 scalar entries,
131 proof gates,8 controls,73 rejected inputs,9 primitive records,
170 matching records and6 historical qualifications.

General exact identities cover probability inclusion-exclusion,
the required state connector, Bose factors, whole-series derivative
cancellation, radial majorants and Beta moments. Thirty-six physical
current configurations and six original full434/47-face configurations
calibrate the source. Eighty-digit independent series differentiation
and direct connector quadrature use unchanged10^-60 tolerances.
All symbolic scientific payloads are checked for Python/SymPy floats.

The uniform proofs are written nuclear-norm, Holder, entire-series,
dominated-convergence and signed-measure arguments. Neither sampled
currents nor truncated numerical series alone establish them.

An initial unfrozen scratch probe exposed the shifted-log integration
branch issue documented in notes/series.md. The direct original
interpreter reproduced it. Explicit positive-domain substitution fixed
that single scratch calculation; no imaginary physical term was
deleted, no backend was patched and no tolerance or parameter changed.
Ordinary lint corrections bind immediately used callback parameters
and remove unused locals; they do not change the mathematical inputs.

Before source freeze, run the whole cold preflight, then copy all
eighteen exact files to a distinct fresh directory and force the
complete S313 parent packets before its preflight. Compare private,
fresh and repository bytes/hashes. Only then send one native helper
specification; acknowledge complete buffered chunks only. Validate
both byte-exact raw copies against the twenty-field AST, manifests,
counts and preserved frontiers.

Native/direct/ordinary/CLI retain original SymPy; the audited
exact-GCD adapter remains FULL-only. Own native equality, independent
CLI and captured full-P8 regression are separate acceptance gates.
Their actual commands, times, counts and exits belong in the external
assessment after completion, never as prospective successes in this
frozen packet. No frozen ancestor may be edited to fit a new replay.
