# Acceptance and evidence contract

The source manifest has eighteen ASCII files: seven Python package
modules, two test modules, README.md, FORMULATION.md and seven notes.
The separate native JSON report has twenty top-level fields.
A native report is immutable raw evidence, not an editable assessment.

The original-source checks are copied before new inventory checks.
The proper module retains the full D pair reduction and derives the
complete endpoint tensor, its identical-matter half, both assignments
and all covariant on-shell terms. The cuts module compares the entire
moment discontinuity against all crossed gravitational exchanges,
with full phase factors and convergent angular endpoint subtraction.
The forward module differentiates the whole D moments, evaluates the
complete finite integrals and proves explicit uniform majorants.

Science tests independently evaluate full moment derivatives, the
finite soft-divided coefficient and the whole physical scalar cut at
high precision. Literal D4/5/6 endpoint tensors and exact domain/input
rejections supplement these checks. Numerical cases are evidence, not
formalized all-domain theorems. Written graph, analytic continuation
and uniform-bound arguments are essential; Boolean gates alone do
not establish them.

Acceptance requires final original-SymPy cold and fresh-root preflight,
including all immediate-parent packets before the fresh checks.
Every residual, gate, exact scientific payload, module export and
twenty-field AST contract is checked. Only identical eighteen-file
source bytes/hashes may be copied into the repository and sent to the
native helper. Native replay must rebuild the complete parent ancestry,
protect the input inventory and stream every raw chunk with its hash.
No frozen source or raw-report repair is allowed.

After source/raw freeze, both original-SymPy ordinary and CLI replay
must pass. A new shared FULL snapshot must explicitly include this
packet, preserve test collection and verify the guarded exact-GCD
contract; the adapter is FULL-only. An older snapshot or private
science-only result is not a substitute. The external assessment
records actual lane outcomes, source/raw equality and exact scoped
publication before commit/push.

The original PDF1711.08009 equation3.6 was visually inspected because
its text extraction suggested a mismatch. Its internally inconsistent
displayed coefficient is not imported as a normalization oracle.
The independent tensor/on-shell and full-cut derivations are the
evidence for this packet. No correction to that paper is asserted.

Rejected S6.279 stays outside accepted tests. No P4/P9 file, frozen
ancestor, original matching coordinate, physical prescription or
historical qualification is modified. Original P8 remains OPEN.
