# Known finite forward phase and all-angle real one-loop bound

## Fixed source and observable

Keep the complete original QG2-H8A420 source, profiles and parameters.
Use the unchanged S302 known minimal Einstein/massive-scalar one-loop
finite representative A_known=F_known/(16pi^2 kappa^2). Its raw
dimensional convention, UV-before-IR subtraction and analytic
reference resolution/nu=1 are unchanged.

Retain alpha,beta,delta_kappa as independent unassigned physical
finite coordinates. They are not included in the known-part bound.
Do not identify the known minimal component with the full current
action's amplitude or the physical detector-inclusive observable.

Use mass-one units,nu=1 and25/4<=s<=16. Let
t=-tau,u=4-s+tau,q=s-4,V=s^2-4s+2,D=sqrt(s(s-4)).
For the forward expansion tau tends to zero at fixeds and all original
parameters, after the specified infrared finite reference is formed.

The exact expansion is

F_known = -2i*pi*V^2/(D*tau)[ell-ln(tau)-2/V]
          +3pi^2(5V+6)/(4sqrt(tau))
          +C_log(s)*ln(tau)+O(1),

ell=ln4pi-Gamma_E. The explicit C_log is in notes/phase.md.
The complete complex remainder is bounded over the fixed compact
energy interval. Bose symmetry gives the backward expansion.

## Uniform real interference

Let A_m and A_G be the full positive original matter and gravity Born
amplitudes. The known first-loop interference coefficient in the same
analytic hard reference is

I_known=2Re(A_known)/(A_m+A_G).

For every physical nonforward angle, with no angular-window restriction,

|I_known|<10^12/kappa=10^-788.

The proof bounds the real part after isolating the imaginary endpoint
of the small-channel boxes and soft divisor. Apparent integer poles
in the massive block cancel exactly before a two-derivative Taylor
bound is applied. It uses the full positive Born denominator, not a
matter-only or truncated Newton expansion.

As tau tends to zero,
I_known/sqrt(tau) tends to3(5V+6)/(32kappa V).
For the three-coordinate selected finite family, the independent
Newton matching contribution instead tends to-2delta_kappa/kappa;
the two local coordinates vanish after this normalization. No value
ofdelta_kappa is chosen.

## Explicit boundary

This is a known one-loop hard-reference coefficient, not a physical
inclusive cross section. The Born cross section remains forward
singular. The imaginary logarithmic phase remains in the complex
amplitude, and no bound on its square or all-order expansion follows.
Gravity-Born radiation, full-source matching and loops, physical
analytic/Regge construction, and original quantum state/domain/
measure/common-parent bounce remain open.
