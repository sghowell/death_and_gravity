# Independent exact controls at original parameters

The generic noncommuting trace identity is checked independently by full
four-field and metric differentiation of finite three-site matrices.
The24-labelled normalization is separately matched to the complete S235
flat formula, retaining six ordered boxes. The two-light bubble and literal
heavy resolvent independently fix the normalized TT insertion sign.

Four exact original recoil fixtures use the original n,g,C and both
physical polarizations. Replacing a diagnostic loop kernel by1 compares
the complete labelled triangle/outer-heavy result with the independent
original26graph tree engine, and the box result with its local contact
engine. Replacing it by v checks the ordered outer endpoint C(u).
These are kernel normalization controls, NOT values assigned to actual
loop integrals. There are four coincident polarized channels, eight
nonzero TT state/polarization pairs and eight rejected wrong outer signs.

Every permutation and split line is then checked using the full actual
original-mass denominator and literal stress at a complex contour point:
672 routes,224 timelike and448 crossed;1344 TT contractions,1284 nonzero.
The original scalar source is unchanged. Exact canonical expansion and
cancellation are used before comparing rational complex residuals with0;
a structurally unevaluated product0*expression is not an approximate error.

These finite controls do not replace the covariant operator proof or the
continuous S341 contour and routing bounds. No numerical quadrature or
floating error tolerance establishes a claim in this checkpoint.
