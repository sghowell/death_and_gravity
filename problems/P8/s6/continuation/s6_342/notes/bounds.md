# Uniform complete remainder and finite interference

Let Bc=256000,Bd=32768000. S341 proves
|Cbar|<Bc/n,|Dbar|<Bd/n^2 and each invariant derivative is bounded by
4096 times the appropriate master bound. For the original n>=10^6,
|A(v)|<6g^2/n and |A'(v)|<4g^2/n^2.

The entire unsubtracted labelled hard kernel is bounded, before1/(16pi^2),
by M*g^4/n^2, M=36Bc+6Bd=205824000. The fixed symmetric subtraction costs
at most another M. Relative to A0 this isolated hard estimate is<10^197,
which is NOT small. The cancellation-sensitive total flat result remains
the separate sharper S236 result.

## Coefficient variation and every graph class

Original hard Euclidean shifts are<4omega and|k|<2omega. A shifted
four-point pair moves by<10omega; its current norm<7 and Born norm<6 give
an invariant shift<130omega<144omega. Singleton virtualities move by
<8omega. The straight invariant segment stays inside the same S341 branch
domain, since each endpoint is in that interval.

The triangle coordinate path length is<=144+2*8=160omega and the box
length is<=2*144+4*8=320omega. The complete coefficient-change budget is

 L=6Bc*(6*160*4096+576)+6Bd*320*4096
  =263738720256000,

in units g^4*omega/n^2. The576 term bounds the full A' contribution,
using n>=1 for a deliberately loose estimate.

Each canonical TT external current has norm<=16/omega, so the sum of
individual norms is<=64/omega. The complete current change is<=608.
S341 bounds the sum of internal lines of one triangle by10^12/n and one
box by10^14/n^2 before couplings. The original pair norm<=6 and each
heavy inverse<=2/n give the outer-heavy labelled bound1728Bc/n<=1728Bc.
Consequently

 B=64L+1216M+36*10^12+6*10^14+1728Bc
  =17515528820736000

bounds the entire nonleading bracket in units g^4/n^2.
Restoring1/(16pi^2 sqrt(kappa)) and the original positive
A0>A_m>4g^2/n^3 gives

 ||R_TD||_TT/A0 < B*g^2*n/(576 sqrt(kappa))
                <10^204/sqrt(kappa)=10^(-196).

Adding the existing combined S337/S338/S339 bound10^194 and the zero S340
sector still fits the SAME rounded10^204 budget. This aggregation includes
only those currently known specified matter sectors. The independent
curved and internal-gravity sectors are not silently assigned zero.

## Actual-phase full-tree interference

Let r=10^204,delta=min(1,-t,-u),a=delta/192,ell=min(x,a).
The established S335 low/high full47-tree bounds and the actual phase
measure give

 integral |2Re<Mtree,R>|/A0^2
 <=r[128ell+4e9ell^2/delta]/(4pi^2 kappa)
   +1_(x>a)1e18*r*(x^2-a^2)/(kappa delta).

For x<=a this is<10^(-592), and its lower-cutoff tail tends to zero.
The high bound is finite at each fixed nonforward Born angle, not uniformly
at a forward pole. This first-loop interference does not include an
|Mloop|^2 term, the separately divergent leading-soft loop extension,
virtual hard interference, or a full inclusive probability.
