# Larger complex analysis domain and whole nonlinear remainder

The original physical radius remains R=1e20 and both c=1,2 cutoff
profiles are unchanged. Set only an ANALYSIS radius Rstar=1e150.
At initial complex phase norm4Rstar the SAME fixed reference maps
inside8Rstar. Re-evaluate all eleven original field rows at that
complete image, rather than extrapolating an old numerical conclusion.

domain.bounds explicitly recomputes the full trace, shape, vector and
matter generator source; the complete mean-zero adjoint inverse;
lifted traceless momentum; scalar curvature remainder; all twelve
invariant images; and all twelve average remainders. It retains every
nonlinear generated mean and harmonic. The A2 metric/shape bounds
remain small, so the full determinant and formal-transpose Neumann
arguments apply. No new inverse of a zero mode is introduced.

Both the large and original phase domains fit the SAME1e-120 invariant
box and1e-115 lapse ball, with the1e-122 complex homogeneous neighborhood.
The actual contraction is<1/20. The self-map residual includes full
phase images as well as homogeneous displacement; it is not only a
root estimate at z=0. Every strict inequality is evaluated exactly.
At the old R, all twelve images, average remainders, energy variation
and full lapse residual equal S6.273's original expressions.

The old full-source energy envelope, now evaluated on4Rstar and with
the separate free reference energy retained, gives a complete
centered amplitude A_large<1e531. At each fixed real time the original
finite-dimensional field reconstruction, spatial average and unique
implicit root are holomorphic on this common domain. Uniform spatial
bounds justify integration. This uses no analyticity in real time and
adds no derivative to the original C5 profiles.

The zeroth centered jet vanishes by definition. The complete first
phase jet of the integrated energy also vanishes: at the actual
homogeneous root H_N=0; every original linear nonzero input field has
zero spatial mean; Gauss and linear curvature integrate to zero.
All shear/electric/magnetic/gradient invariants start quadratically.
The full fixed reference is quadratic. This argument concerns the
complete first jet and does not discard any nonlinear mean.

For complex z on4R, apply one-variable Cauchy to g(lambda z)-g0 on the
large radial domain. The entire degree>=3 remainder is bounded by

A_large * (R/Rstar)^3 / (1-R/Rstar) <1e141.

Its exact quadratic part is the fully constrained H2 of
notes/quadratic.md, minus the independently bounded href. The common
physical prefactor is bounded by2*1000*kappa. With the original
phase image8R, sum the whole quadratic, separate reference and
entire nonlinear remainder. Their total is<AH=1e172.

This is an estimate of the full symbol, not a Taylor-defined
Hamiltonian. The scalar center g0 and its full classical force remain.
For every first and second homogeneous derivative use the unchanged
Cauchy radius d=1e-124 inside the old complex homogeneous neighborhood.
The original centered-volume amplitude AF=1e-510 remains sufficient.
