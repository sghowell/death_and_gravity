# Whole original source and refined trace row

All source functions, physical bindings and fixed profiles are inherited
from S6.273. No fixed profile is replaced by a newly chosen live mean.
Its500 full derivative expressions,63 componentwise RFj jets and27
extra normal jets of total order6 excluding the sixth real-time
derivative remain the source envelope. The real profiles remain C5.

The complete phase/homogeneous source box has coordinate radius1e-120;
the lapse radius is1e-115. The real comparison time remains1e-180,
inside the parent source interval1e-130. Refine only the bound of Cp,
the derivative of the ENTIRE lapse constraint with respect to p.
After differentiating first and restricting to the actual clock,

Cp_clock = 3u(4u^6+12u^4+12u^2+3)/(1+u^2)^4.

Integrate the full normal derivative, bounded by1e6, over the lapse
radius. Integrate each of the twelve full mixed invariant rows,
bounded by1e4, over at most TWICE1e-120: both the full invariant and
its clock value lie inside this box. The explicit outward bound is

|Cp_clock|_interval + 1e6 * 1e-115 + 24 * 1e4 * 1e-120 <1e-108.

The24 factor deliberately covers the whole joint source tube, not just
a displacement of the homogeneous center. All other Cz rows keep
the conservative1e4 ceiling. The independent first-variation test
confirms that the original constraint is the entire H_N equation.
The fixed-N density/Gauss cancellation in the quadratic energy never
licenses deleting the Hclock contact from C1.

The physical radius R=1e20, both original cutoff functions, the actual
pure seed and the finite hybrid are unchanged. Rstar=1e150 introduced
in notes/domain.md is only an additional complex analysis domain.
