# Entire constrained quadratic energy

## Momentum constraint and actual trace/shear cancellation

At a uniform isotropic center let r=deltaPV/V, let sigma and h be the
nonzero M1 and heavy field, and set M=pm sigma+ph h, S=3p v+M.
For any nonzero retained wavevector k put L=kk^T/|k|^2. If T is the
complete transverse-traceless momentum, the solved linear constraint is

Sigma_TF = T+(3S-r)(L-I/3)/4,
(rI/6+Sigma_TF)k = Sk/2.

This is the linearization of the whole trace generator
Pi_v grad v-grad(Pi_v)/3 plus the full matter source. The vector
source is quadratic at the zero homogeneous vector. The original
cotangent trace preservation remains exact. No constant mode is inverted.

L^2=L, Tr L=1 and TT orthogonality imply, mode by mode,

2 Tr(Sigma_TF^2)-r^2/12 = 2 Tr(T^2)-rM_total/2+3M_total^2/4,

where M_total=S. The notation distinguishes S from the matter-only M.
Parseval then gives the integrated statement for all retained real
waves and complexified amplitudes. Neither of the cancelling r^2
terms can be deleted in isolation.

With rr=R_full-1, the trace/shear part after ALL density factors is

-rM/2+rr r G/2+27(p^2-B^2)v^2/8+9p vM/2+3M^2/4
-9p rr vG/2-3rr^2 G^2/4+2 Tr(T^2),

times the original common N R_full^(-1/4).

quadratic.literal_hessian starts from the ORIGINAL Hamiltonian,
not from this proposed expression. It substitutes the complete
exp(-3v) density maps and exp(3v) volume, differentiates twice, and
compares the result. The independent test reconstructs the same
second variation using the multivariate chain rule over all twelve
invariant directions. Every B/primitive/time, heavy j, mass, potential,
electric, magnetic, gradient and density contact remains.

## Complete lapse elimination

At the actual uniform root N0(Y), C0=H_N=0. In a phase direction
the full first constraint variation is C1; solving gives N1=-C1/CN.
The on-shell energy's quadratic correction is -C1^2/(2CN).
This includes all trace, matter, heavy, Gauss and curvature responses.
The established |CN|>2 gives the safe modulus bound C1^2/4.

The fixed-uniform-N density times lower-order Gauss term cancels its
quadratic Hclock contact. Its N derivative still contributes to C1.
The implementation and independent tests retain that distinction.

## Full curvature, including tensor and nonlinear metric contacts

For a general symmetric metric tangent H cos(k.x), directly compute
the first Christoffel and Ricci expressions, the quadratic connection
products, and the inverse-metric/volume contacts. Only the integrated
derivatives of Gamma2 vanish by periodicity. The complete normalized
Fourier form is

-|k|^2 Tr(H^2)/4+|k|^2(Tr H)^2/4
+|Hk|^2/2-(Tr H)(k^T H k)/2.

For H=2vI-tau_TT this becomes
2|k|^2 v^2-|k|^2 Tr(tau_TT^2)/4. The nonlinear determinant/shape
second variation adds a linear-curvature divergence with zero integral,
not a new energy term. Since curvature1=-4a^(-2)Delta v, it follows

avg curvature2 =
-10a^(-2)P^2 avg(v^2)-a^(-2)P^2 avg Tr(tau_TT^2)/4.

The3v curvature1 density contact is retained separately. A fully exact
diagonal metric with conformal and tensor profiles independently checks
the scalar, tensor and density terms, including their signs.

## Full covariance and quantitative budget

All rows use the original complete covariance and the same fixed
reference-flow image, not a new vacuum or diagonal covariance reset.
For Proca the fixed initial B=sqrt(I+kk^T/m^2) gives
W=BQ/sqrt(kappa zeta omega) and
PiW=B^(-1)Pquad sqrt(omega zeta/kappa).
The full covariance and reference-flow factors allow the safe factor8
per real wave. In units multiplied by sqrt(kappa),

WT <= 6*8/(sqrt(P)sqrt(zeta)),
WL <= 6*8 sqrt(P)/(m sqrt(zeta)),
PiT <= 6*8 sqrt(P)sqrt(zeta),
PiL <= 6*8 m sqrt(zeta)/sqrt(P).

Divergence acts only on PiL; curl only on WT. Thus Gauss<=2P PiL
and curl<=2P WT, while ALL transverse and longitudinal energies remain.
Using the transverse maximum to bound divergence would lose the key
directional estimate. This is not permission to omit a polarization.

The evaluated trace/shear, matter, potential, spatial, vector and
full implicit-lapse budgets are all positive and are summed. Matrix
traces use |Tr T^2|<=3||T||op^2; the shear row consequently retains
6 PT^2. The full scalar source B and Fhat are bounded before any clock
substitution. The resulting normalized quadratic sum is<1e125,
with much smaller separately displayed exact row values.

No identification with the freely selected reference Hessian is made.
Its independent1e128 bound remains in the next step. The original
nonlinear Hamiltonian itself is never replaced by this second variation.
