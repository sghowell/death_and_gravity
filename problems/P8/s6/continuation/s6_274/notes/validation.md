# Immutable replay and independent diagnostics

This packet has19 manifest sources:8 Python modules,2 test modules,
README, FORMULATION and7 proof notes. Private research probes and
external assessments are not manifest sources.

The exact report contract is20 fields,61 named checks,289 scalar
entries,57 gates,13 controls including496 rejected inputs,
9 original primitive rows and130 matching rows. All predecessor
frontier entries are retained exactly, in order; only the one
explicit finite-hybrid comparison entry is appended.

The immediate native predecessor is S6.273 with SHA256
1ef7c3a7165db3688c4141313ed8fb7ee5a2c43ee0613ddd787144a8b201cdce.
The read-only report builder rebuilds the full predecessor chain.
It does not infer historical truth from a hash or override an
archived physical erratum.

Before freezing, run Ruff and all sibling export checks, an
original-SymPy full local source/AST/hash preflight, and the1210-test
independent science suite. Require exact byte-for-byte repository
copy equality and a fresh-process repetition. Native capture must
then preserve raw bytes, chunk continuity, SHA256, all source hashes
and complete predecessor frontiers. Sources and native report become
immutable; later evidence belongs in an external assessment.

The full ordinary packet has1235 tests:1210 science diagnostics plus25
read-only report tests. These mutate every report field and reject
missing and extra fields. Separate ordinary and CLI replays use
original SymPy. A separately captured FULL regression may use only
the audited exact-GCD adapter with its source snapshot and self-checks.
No other replay gets an adapter shortcut.

Independent diagnostics include all207 radial sums reconstructed by
Bell recurrence, all206 jet ratios and197 original coefficients;
complete DeWitt/projector identities on varied nonaxial modes;
multivariate differentiation of the original energy; the entire
H_N constraint; exact implicit Schur signs; full exact-metric Ricci
and volume contacts; complete Proca canonical maps and directional
divergence/curl; nonzero full Cauchy remainders; integrated heat
remainders through degree18; a counterexample to heat core-support;
both feedback inverse rows; positive-effect transport; and global
phase counterexamples. Every unsupported input is rejected.

Cache guards are tested AFTER valid warmup so booleans and floats
cannot use Python's numeric-key equivalence to bypass exact contracts.
Guards run before the private cached implementations in the new APIs.

Exact residuals and rational inequalities support the accompanying
written analytic/operator proofs. Numerical fixtures are independent
diagnostics, not physical simulations or substitutes for those
proofs. This packet does not claim formal proof verification.
