# Same-model finite hybrid core and regulator comparison

## Fixed model and quantitative conclusion

Use exactly the S6.273 model, not a new background approximation:
L=1, P=1e64, kappa=1e800, zeta=1e-6, all48 original nonzero quantum pairs,
the original pure Gaussian seed with its full covariance, and the
fixed reference flow. The physical phase core is R=1e20. Both original
c=1,2 cutoffs and calibrated-coherent/Weyl orderings remain.

The live classical density coordinates are
Y=(alpha,p,pm,eta=1e100 h,ph). M1 remains its full cyclic quadrature.
All three homogeneous vector and five traceless-shape canonical pairs
obey their zero equations by the actual seed's cubic symmetry. The heavy
homogeneous scalar and every scalar-center force remain live.

For each of the four already unique S6.273 solutions, uniformly on
|u| <= T=1e-180, the outside-core positive coherent POVM probability is
<1e-6. The same reference unitary transports both state and effect.

At fixed ordering, the original two cutoffs have uniform differences:

- five-coordinate sup norm <1e-580;
- phase-factored state norm <1e-12;
- physical-volume mean <1e-514.

At fixed cutoff, calibrated-coherent versus Weyl have differences:

- five-coordinate sup norm <1e-630;
- phase-factored state norm <1e-65;
- physical-volume mean <1e-566.

No bound is assigned to un-factored vectors with their distinct global
scalar phases. The corresponding POVM and volume means are phase invariant.

## Complete energies and readout

The exact S6.273 definitions remain. With the same fixed reference
Hamiltonian and flow, g=(h-href)(u,Y,S_u z), g0=h(u,Y,0), and
b=chi_c(g-g0). Write D=Delta_w/4 in the original whitened coordinates.

The two Hamiltonians are g0 I+Q[(1-D)b] and g0 I+OpW(b).
No polynomial in this packet replaces either operator or the full
nonlinear spatial source. The physical volume is exp(3alpha) times
the mean of F0(Y)I plus the correspondingly ordered compact centered
symbol chi_c(F_Y-F0(Y)), with F0(Y)=R_full(u,N0(Y))^(-3/4).
The actual homogeneous lapse root N0(Y), not an off-shell N=1, is used.

## Proof mechanism

First derive the complete constrained second variation. The large
trace-momentum square cancels against its genuine longitudinal shear
partner after solving the full linear momentum constraint. The
remaining quadratic form retains scalar/matter mixing, primitive,
potential, vector polarizations, full scalar and tensor curvature,
and the complete implicit-lapse correction -C1^2/(2CN).

Recompute the full source and auxiliary domain on the larger complex
analysis radius Rstar=1e150. The old physical cutoffs are not changed.
The vanishing first spatially averaged phase jet and scalar radial
Cauchy estimate bound the ENTIRE degree-at-least-three remainder.
Together with the separately bounded href this gives the safe full
centered amplitude AH=1e172 on4R. The volume amplitude AF=1e-510
is unchanged. Homogeneous Cauchy derivatives use d=1e-124.

Second use an EXACT identity, not an inverse-heat approximation:

OpW(b) = Q[P6(D)b] + OpW(r6),
P6(D) = sum(j=0..6) (-D)^j/j!,
r6 = -integral(0..1) theta^6 exp(theta D) D^7 b dtheta /6!.

The full positive-heat remainder stays in all norms and comparisons.
All207 radial sums and206 growth ratios are evaluated through phase
order206=192+14, using the existing all-order cutoff proof.
The original C5 real-time source regularity is not strengthened.

Finally solve both coupled stability rows, including different live
background histories. The resulting state/core, force and physical
readout estimates apply to the SAME unique S6.273 trajectories.

## Boundary

This is a finite classical-homogeneous/quantum-nonzero-mode result.
It is not a quantization of homogeneous variables or the original
fully quantum mean. No exact support, phase projection, unlocalized
Hamiltonian, regulator-removal limit, mode/volume-uniform estimate,
unique or strict minimum, physical matching, omitted-loop/Regge/UV
control or nonlinear global completion is established.
Original V/G/B/P8, scoped P8(a), S261's physical binding refutation
and S265's unlocalized integrability boundary remain as before.
