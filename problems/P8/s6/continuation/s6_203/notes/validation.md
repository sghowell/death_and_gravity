# Verification boundary

The explicit manifest has18 scientific sources: seven package modules, two test modules, README.md, FORMULATION.md and seven written notes. The report has20 top-level fields,29 named exact checks covering29 scalar entries,38 gates,9 controls and125 rejected inputs/scope mutations. Every report field and missing/extra fields are mutation-tested.

Independent tests compute actual complete unit-W8 time-jet rows for all five endpoints, including zero internal momentum and large external transfer. Full source/detector tensors are noncommuting and all nine physical pairs are summed. Cauchy quadrature with two independent resolutions evaluates the same coefficients. Separate fixtures evaluate the full leading endpoint outside the small spatial Cauchy ball.

Every one of the five near radial orders, including the logarithm, is checked by independent hyperbolic quadrature. The complete raw-plus-Taylor near bound is tested across empty and nonempty regimes. Low and near tails integrate the actual original removed two-leg angular union and moving boundaries. Positive spatial-weight identities and exact sphere-intersection fixtures prevent a false polynomiality/locality inference.

The initial private fixture extraction accidentally carried cache decorators without their two helper definitions. This produced eight immediate unhashable-matrix fixture failures before the affected physical checks ran. The missing helper definitions were restored, with no scientific bound changed; all39 then-current fixtures passed, followed by all70 expanded fixtures. No frozen ancestor was edited.

The S6.202 report is source-pinned and transitively rebuilt, with the S6.176 actual-state Gaussian anchor checked. Native report generation, fresh direct science, ordinary and CLI use original SymPy. Only the separate full-regression runner uses the audited exact-GCD adapter and its original-tuple/snapshot controls.

Scientific sources are frozen before the native report is written. Publication evidence is recorded only after actual process completion. The continuum bounds, complex-analysis inputs and dominated-convergence argument are written proofs, not FORMALIZED. Tests do not establish the unmatched full contact/local sector, a full inverse or original P8 closure.
