# Independent checks and immutable replay boundary

The private radial probe independently converted both complete physical cut densities, with factors64 and768, and checked the opposite shear and trace threshold signs. A separate exact root/cut probe proved the17-term rational enclosure and cut endpoint constants. A65-digit dispersion probe checked four complex/real radial-versus-closed values and four pole-plus-cut reconstructions; both total moment identities agreed within1e-50. Omitting the pole is detected.

The packaged independent suite includes three literal four-dimensional traceless curvature Hessians with off-diagonal entries, two direct spectral substitutions, a shorter13-term binomial moment enclosure, complete high-precision reconstructions, an independent beta-integral forward-kernel bound and three mass-scaling/prepared-source fixtures. The latter fixtures use illustrative rational frequencies and weights and are not claimed to locate the physical reference root.

The first full science run passed243 tests in3.80 seconds. Physical normalization passed11 exact identities including the added frozen-trace bridge; the initial nine-identity probe took1.43 seconds. The exact pole/cut probe passed13 identities and10 gates in0.34 seconds. An early kernel-only assertion encountered an algebraically zero but unfactored Laplace expression; exact factoring corrected the test representation before packaging. No physical formula changed.

There are six package files, two test files, two root markdown files and seven proof notes:17 immutable inputs. The native report has20 fields,37 named identities and scalar entries,26 proof gates,nine controls and165 rejected inputs. Every report field, source hash, missing/extra field and false primitive or matching completion is checked. Fresh replay outcomes belong in the external assessment.

Native, direct science, ordinary and CLI use original SymPy. Only separately audited full regression uses the exact-GCD adapter and the explicit S219 helper-directory PYTHONPATH allowance. Source-pinned finite, cut, trace and immediate-parent reports are rebuilt read-only. Frozen scientific, proof, test and report bytes are unchanged.

Rational enclosure and algebra checks are exact. The contour deformation, continuum oscillatory estimates, causal distribution construction and both graph-domain inverse identities are written proofs, not FORMALIZED. Quadrature supplements these proofs and does not establish global zero counts, analytic continuation, convergence or a full coupled inverse.
