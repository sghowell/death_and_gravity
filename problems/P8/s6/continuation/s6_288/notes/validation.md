# Immutable source, independent native and regression contract

This packet is assembled and probed privately before acceptance.
Seven module exports, two test modules, two root documents and
seven proof notes make18 ASCII source files. The report has
exactly20 AST-derived top-level fields.

A cold original-SymPy preflight checks every exact residual,
gate, control and actual rejection; the preserved source,
matching and historical scopes; every export and source hash;
and the entire science test module. An independent fresh-root
preflight forces the immediate parent's complete packets first,
then recomputes the same result and every hash. This specifically
guards against the rejected S279 mutable-cache failure.

Only after these agree are the exact formatted sources frozen
and submitted once to the native helper. Native raw output is
transported by the specified acknowledged chunks, validated
byte-for-byte and by SHA256, then frozen without rewriting.
The helper receives no research probe and no DONE.

Independent original-SymPy ordinary and CLI replays must both
pass. A captured complete P8 FULL regression must also pass,
with its actual collection count, unchanged manifest and exact
adapter contract evidence. Only FULL may use the exact-GCD
adapter; native, direct, ordinary and CLI keep original SymPy.
At most one FULL and one ordinary/CLI pair run together.

The external assessment records actual preflights, source/native
hashes, raw bytes, test counts and exit codes. This source note
contains no predicted acceptance outcome. Frozen scientific
sources, proof notes and raw reports are never edited to reflect
a later gate; a correction requires a successor or external
assessment. Unrelated P4/P9 changes are not included.
