# Same source, graph sector and conventions

The whole original R,F and constant-vacuum source record is inherited
without mutation through S287 and its frozen ancestry. Its actual
mu=1, kappa=10^800, C, g, heavy mass and Proca mass are unchanged.
S281's vacuum four-Phi higher-derivative cancellations are retained.
There is no restored bare positive four-derivative contact.

We select the connected one-loop four-Phi diagrams made from the
minimal two-Phi metric vertices and the Einstein/ghost action, at
order kappa^(-2), together with their external scalar LSZ factors.
This graph-sector selection is not deletion of other source vertices.
The full C/g/heavy/Proca/M1 and higher-Phi sectors remain separate.

Use g=eta+2h/sqrt(kappa), an ordinary quadratic flat harmonic
gauge-fixing term, and the same dimensional continuation D=4+2EP.
The metric propagator numerator contracts symmetric tensors as
T:U-tr(T)tr(U)/(D-2). The scalar vertex is-iT/sqrt(kappa),
T(p,r)=pr+rp-eta(p.r-mu). All its metric seagulls are included.

S285 already matches each Gaussian volume constant as an entire
sqrt(g) density, not merely one component. The Phi coefficient of
R_old=-R_P8 remains unmatched. Its Newton shift is
delta_kappa_Phi=+2c_Phi_R, not the opposite sign and not a prescribed
MS finite value. The fixed H/Proca Newton shift belongs to those
separate retained sectors; their bound cannot fix the light-inclusive
Newton sign.

The regular finite four-derivative one-loop counterfunctional is
parameterized, not set to zero. After on-shell crossing reduction
only two local combinations enter this selected amplitude. This does
not assign all higher-derivative terms of a UV completion or a full
curved counterfunctional.
