# Actual complete normalization and canonical inverse bounds

All parameters and rational upper enclosures are inherited
unchanged. Define k_up and k_epsilon_up by S6.155, and
r_up=g/(288M). The complete hybrid slope bound is

    T_H = scalar_MS_slope_32
          +direct_fermion_MS_slopes_2
          +paired_insertion_MS_slope_1.

The full regulated field identity gives

    |t_MS|<=T_H+k_up r_up.

Each canceled epsilon field-pole product is also retained
as a separately printed allowance; it is NOT added to
this total after the exact cancellation has been proved.

The complete MS normalization bound and the complete
canonical one-plus-two-loop OS coefficient bound are both
approximately 3.34630300149847e-19 and strictly below
10^-18. These are different exact rational expressions
whose displayed leading digits coincide, not an equality
of coefficients. The first-loop and second-loop conservative
bounds must not be interpreted as a measured growing series.

The direct fermionic unit-disc OS contribution is E/2
and remains below 10^-799. The full polynomial normalization
1+h k0+h^2 t_MS has positive lower bound
1-k_up-|t_MS|_up on 0<=h<=1, a finite-order fact only.

At the actual tree relation, 0<2L-3g/(M-2)<2L.
Therefore the complete scalar bare-map tree allowance is

    3 k_up^2+2|t_MS|_up+2L k_epsilon_up/144
       <10^-18,

approximately 6.69260600299694e-19. It is retained for the
four-point matching work but is not labeled a full
amplitude error. All thresholds use exact rational
comparisons rather than the rounded displays here.
