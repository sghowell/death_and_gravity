# Completed Phi subproblem and the still-open original problem

The two-loop Phi field and local pole calculation now uses
the complete quadratic inventory, assigned counterterms,
the full first regulated coordinate shift and the induced
finite scalar parameter map. This advances the matched
Phi two-point subproblem, not just another primitive row.

The shared ledger's combined pole-and-amplitude obligation
remains open because the four-point assembly is not yet
completed. Vacuum/source values, physical higher-order
truncation, V contour/cut control, finite-gravity G and
common-parent B remain separate research obligations.
External-fermion second-order canonical dictionaries are
also not inferred from a Phi-only observable.

The direct-MS representation avoids an unnecessary extra
zero-proper parameter conversion: the relevant finite MS
anchors were already restored inside their proper graphs.
It does NOT justify dropping regulator-dependent field or
coordinate terms. The full q1_D, its epsilon coefficient
and first-pole re-expression are explicitly retained.

No all-orders UV completion is added to the adopted
finite-EFT/necessary-positivity contract. Scoped P8(a) and
A.20-A.23 are unchanged. Original P8(b) and P8 remain OPEN.
There is no newly identified user-intervention blocker.

This calculation relies on the source-pinned analytic
first-sheet estimates and references already derived in
the preceding chain, plus the exact identities displayed
here. No new external literature is required. Written
proofs and exact tests are not formalization or independent
peer review. Earlier scientific and report bytes remain
unchanged. Native, ordinary, CLI and direct science retain
unmodified SymPy; only full regression uses its separately
audited exact GCD adapter.
