# Exact actual-current decomposition with unit W8

At every common finite regulator,

    J_actual-J_unit=(J_actual-J_alpha)+(J_alpha-J_unit).

The first term is the full S6.197 actual-state remainder and the
second is the complete prefactor correction proved here. Therefore
the actual-to-unit remainder is below 3e23 M[D]M[Gamma], with
error 2e27 M[D]M[Gamma]/K. No term in the actual state or current
has been changed.

Unit-W8 amplitudes obey the same conservative analytic time-disc
bounds as the alpha-weighted ones. Dropping the need for the two
|alpha0|<2 enlargements only reduces their upper bound. The inverse
sum phase is identical. Repeating S6.198's exact six-step identity,
rather than modifying its frozen proof, gives F_unit=B5_unit+bulk6_unit
with bound 1e48 and tail 1e52/K in its original time-Sobolev norm.

Consequently

    J_actual,K=R_actual-unit,K+C_unit,K
               +sum_(j=0)^4 B_j,unit,K+F_unit,K.

Every memory/endpoint retains both created momenta and the full
contact retains its one-mode band. The already controlled piece has
bound 2e48 M[D]N61[Gamma] and error
2e52 M[D]N61[Gamma]/K. Here

    N61[Gamma]^2=sum_(j=0)^6||partial_t^j Gamma||L2^2
                 +||grad_x Gamma||L2^2.

Both external-metric canonical factors give 8e-752 and 8e-748/K.
These bounds exclude the full contact and first five endpoints.

The initial Borel prefactors no longer occur in this remaining
comparison sector, but joint spatial analytic bounds have not yet
been proved. Unit W8 has the canonical Wronskian, not the exact
mode equation. It is not promoted to a physical state or an
independently conserved reference effective action.
