# Independent validation and frozen replay

The finite algebra checks complete initial phase cancellation,
both-leg occupation products, full ten-field covariance and
noncommuting contact, exact massive radial constants, actual-current
telescoping and both external-metric canonical factors.

Independent science uses different complex readouts at detector
and source for all nine pairs, full ten-feature contacts, actual
occupation majorants, unit and weighted Wronskians, a non-bisolution
counterexample, massive radial integrals, full arbitrary-transfer
pair weights and complete removed two-leg unions. The contact's
distinct one-mode tail is checked separately. These tests supplement
the written all-momentum estimates.

The integrated manifest has 28 named identities, 135 scalar
entries, 36 gates, 9 controls and 123 rejected scope mutations.
Seven source modules, seven proof notes, two root documents and
two tests form the explicit 18-source manifest. All 20 report fields,
including missing and extra fields, have mutation controls.
Earlier reports and frozen sources remain hash pinned.

Private preflight and fresh science precede freezing and lossless
native reporting. Fresh repository science, ordinary replay,
independent CLI replay and the complete captured full regression
are separate checks. Native, direct science, ordinary and CLI use
original SymPy; only full regression uses the separately self-tested
exact GCD adapter. The publication audit records completed counts
and times; no running test is treated as passed.

The continuous bounds and reference argument are written proofs,
not FORMALIZED theorem-prover results. No frozen scientific byte
is changed and original P8 is not closed.
