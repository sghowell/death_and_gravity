# Independent validation and evidence boundary

The literal spatial action is derived by expanding the complete transformed ADM density with the original primitive-corrected clock jets. Independent tests reconstruct a full three-by-three extrinsic curvature, conformal spatial curvature and transported matter density, and compare the spatial average at both slab endpoints and the bounce, including momentum1e-9.

The Legendre comparison is independently solved as a four-variable linear constraint saddle. Both velocities, lapse and shift are obtained from those equations, not from the reported solution. Tests cover negative, zero and positive Theta and q from1e-12 to10000. Separate full order1024 switch/volume differentiation keeps a nonzero vacuum term and calibration profiles, detecting the error from retaining only DeltaJ.

The ordered Ward formulas are first verified for general tensor fields. An independent finite coordinate-density push uses the determinant and inverse Jacobian, and the complete metric Hessian is obtained by differentiating the literal metric, not a chart helper. At momentum1e-15 the separate deleted-contact expression exceeds1e20 while the full form stays finite. A separate covariant coordinate pull checks the synchronous detector identity. Full physical clock metric composition detects its additional nonzero second contact.

Both endpoint primitives, both Fourier signs, the norm-one projector at arbitrarily small nonzero momentum and the explicitly noncoercive bounce Hamiltonian are checked. The phase matrices contain no inverse momentum. Exact packet serialization rejects accidental floating-point metadata.

Three private exploratory checks required corrections before freezing: the expected shift-pivot factor was initially2/3 instead of1/3; an unfactored rational zero was tested structurally before simplification; and the expected constant term in a positive generator margin was547 instead of541. None changed the derived action, Hamiltonian, Ward identity or final bound. The corrected literal scalar probe passed all nine identities, the full Ward probe passed both general and Fourier identities with its nonzero negative control, and the independent suite passed32 tests, then41 after strengthening.

There are62 named identities,115 scalar entries,55 gates,nine controls and157 rejected inputs. The manifest has17 scientific/proof/test files and the report20 fields. Every field mutation, missing/extra field, unsupported type, false frontier closure and unannounced full quantum inverse is rejected.

Native, direct science, ordinary and CLI use original SymPy. Only the separately audited complete-regression route uses the exact-GCD adapter. Continuous Sobolev extension, time-primitive, smooth-coefficient and matrix Volterra/Gronwall estimates are written proofs, not FORMALIZED. Fresh run outcomes belong in the external audit, not in mutated frozen inputs.
