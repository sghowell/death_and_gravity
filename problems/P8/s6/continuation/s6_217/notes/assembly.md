# Actual homogeneous anchor, full weak bound and Ward input

At \(P=0\), the S195 covariance tangent has precisely the S194 homogeneous generator, both first-variation source terms, unchanged initial covariance, and complete second-metric vertex. Uniqueness of the finite-mode linear tangent ODE identifies the actual two constructions. The S194 homogeneous current was obtained from the original one-point prescription before the erroneous S207 annihilation bridge; its value and bound need no restoration by a new counterterm.

The full second-vertex reference contact is transfer independent, so it cancels in the spatial difference before integration. It is generally nonzero and remains in \(H_0\). Applying the corrected complete decomposition and original finite prescription gives
\[
 J^+_{\rm ren}(P)=H_0+Known^+(P)-Known^+(0)+F^+_{\rm finite}(P).
\]
All corrected finite endpoints, the sixth bulk, actual-state comparison and finite local term are included. At zero transfer the finite spatial UV difference vanishes exactly, so this expression reproduces \(H_0\).

The S213 homogeneous Hilbert lift is independent of the erroneous phase. A Frobenius-unit detector has operator norm at most one. The common zero source germ and interval length one give
\[
 \sup_t\|\partial_t^jG(t)\|_F
 \le \|\partial_t^{j+1}G\|_{L^2_t,F},\quad j\le12.
\]
Apply the real tangent estimate separately to real and imaginary Fourier components and add their squared output norms. Plancherel then gives \(|H_0|\le10^{95}\|D\|_2Z_{136}\), with no dimension-of-tensor-basis factor.

The same positive polynomial norm comparisons give \(Y<2Z_{136}\) and \(Z_{24}\le Z_{136}\). S216's full local finite bound \(10^6\|D\|_2Z_{24}\) is sufficient for its tracefree restriction. Thus the unrounded full coefficient is
\[
 10^{95}+4(5\,10^{48})+10^6<2\,10^{95}.
\]
Zero norm products give zero exactly, including after completion of the test spaces.

Let \(J^+_{\rm anc,K}\) be \(H_0\) plus the finite-regulator spatial difference minus the subtractor in the preceding note. Its exact error is
\[
 [Known_K^+(P)-Known^+(P)]
 -[Known_K^+(0)-Known^+(0)]+E_K.
\]
The two known tails, the same norm embeddings and the original all-transfer shape tail give
\[
 4(5\,10^{53})+10^{40}<3\,10^{54}.
\]
This is an original homogeneous-anchored auxiliary-cutoff rate, not a rate for an unanchored finite band or an identification of that auxiliary cutoff with a physical EFT scale.

The two external canonical metric factors give \(4/\kappa\). Replacing density readout by physical volume readout divides the detector by \(a^3\ge1\), and cannot increase \(M\), since \(a\) has no spatial dependence. This yields \(8\,10^{-705}\) and \(12\,10^{-746}/K\). The loss of thirteen time and six spatial derivatives persists; small displayed coefficients do not give a same-space inverse.

Finally use the independently established S215 prepared maps and full one-point chart terms. Their bounds are \(M[D_{\rm syn}]\le100V_{04}[D]\), \(Z_{136}[G_{\rm syn}]\le10^{20}U_{138}[G]\), and the full Ward contribution is below \(10^{35}V_{04}U_{138}\). With the new corrected current the known functional is bounded by
\[
 (2\,10^{117}+10^{35})V_{04}U_{138}<10^{118}V_{04}U_{138}.
\]
The six integrations by parts require only the zero initial germ and retain every upper endpoint. The state comparison and homogeneous derivative also require no final source cutoff. Therefore the initial-only source extension used in the synchronous map is valid here. Advanced detectors can be cut off inside the common initial zero-output neighborhood without increasing \(M\), which has no time derivatives.

This restores the known tracefree-plus-Ward input, not the three missing scalar kernels or the full reduced response.
