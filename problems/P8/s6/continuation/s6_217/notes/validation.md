# Independent checks and immutable replay

The independent helper constructs two unequal positive, time-dependent quadratic Hamiltonians:
\[
 K=e^{2bt},\quad V=(\omega^2+b^2)e^{-2bt},\quad
 u_q=e^{bt-i\omega t}/\sqrt{2\omega},\quad
 u_p=(b-i\omega)e^{-2bt}u_q .
\]
It checks the full Hamilton equations, normalized symplectic fundamental matrices and CCR. Finite three-state Fock matrices for each of the two modes suffice for the vacuum expectation of the product of two quadratic cross-mode observables; these products never require a higher occupation. This fixture is an independent finite algebra check, not a truncation of the field theory used to define the current.

For three general source/readout cross blocks the literal Fock commutator, real covariance tangent and complex annihilation-pair kernel agree at several times. All16 quadrature cross-block basis pairs are also checked. A closed exponential-polynomial primitive and high-precision covariance quadrature agree with the entire retarded integral, all six endpoint terms and a nonzero sixth bulk. Wrong phase, omitted bulk and omitted fifth endpoint are nonzero controls.

The fixture's source profile \(s^6\) kills the first six initial jets, sufficient for these six integrations by parts. It is not called an all-orders compact-germ source. The field-theory theorem retains the original all-order initial germ.

Independent original-mask quadrature retains a nonconstant angular factor in the odd logarithmic shell and checks its decaying, nonzero contribution. Exact tests verify all unaveraged odd UV products, the complete angular coefficient, corrected finite coefficient, exact anchored-subtraction algebra, both holomorphic branches, phase moduli, norm arithmetic and scope.

An exploratory assumption that every nonzero unaveraged odd coefficient used source jet0 was false: the 01 and10 source-jet1 magnetic terms survive individually. The probe was corrected to retain them and their full angular cancellation before freezing; no coefficient was changed to satisfy the assumption. Two initially vacuous parametrizations outside the triangular source-jet range were removed before the final private tests.

There are50 named identities comprising395 scalar entries,47 proof gates,9 controls and169 rejected malformed/out-of-scope cases. The manifest has18 source/proof/test files and20 report fields. Every report field, missing/extra fields and false frontier completion are independently rejected.

Native, direct science, ordinary and CLI replay use original SymPy. Only the separately audited complete regression uses the exact-GCD adapter. Continuous-dimensional, infinite-mode, Sobolev and Ward arguments are written proofs, not FORMALIZED; numerical fixtures supplement rather than replace those proofs. Frozen predecessor bytes are preserved. Fresh execution outcomes belong in the external audit, not in a self-mutating certificate.
