# S6.217 formulation

Keep the original conditional CD Gaussian Proca parent, \(m=1000\), \(\kappa=10^{800}\), \(a=(1+t^2)^2\), unit proper-time slab, all-order prepared state and S176 finite covariant prescription. Sources have the same zero initial neighborhood. Initially take smooth spatial Schwartz source/readout tensors, both tracefree; the prepared Ward application uses the initial-only extension described below.

For annihilation pair amplitudes \(b_X=u^tM_Xu\), use the sharp detector with positive detector phase, negative source phase and positive imaginary part. Keep the original ordered-pair multiplicities. S216 independently fixes this orientation from the covariance tangent and finite CCR; it is not chosen to fit a counterterm.

The new complete decomposition is
\[
 J_{\mathrm{actual},K}
 =R_{\mathrm{actual-unit},K}^{+}+F_K^{+}+Q_K^{+}
  +C_{\mathrm{unit},K}+U_K^{+}.
\]
Each first-five endpoint and its UV subtraction changes by \((-1)^j\) from the legacy annihilation-branch row. The fifth endpoint reverses, and the sixth bulk is rebuilt with its full positive-phase integrand. Correcting only the local finite coefficient would be insufficient.

With \(Known^+=R^++F^++Q^+\), the original prescribed current is
\[
 J_{\mathrm{ren}}^+(P)
 =H_0+Known^+(P)-Known^+(0)+F_{\mathrm{finite}}^+(P).
\]
Here \(H_0\) is the unchanged actual S194 homogeneous first variation, identified by the S195 finite-mode tangent ODE and full second vertex, not by the withdrawn S213 phase bridge.

Define the Frobenius norms
\[
 M[D]^2=\|D\|_2^2+\|\nabla D\|_2^2,\qquad
 Z_{136}[G]^2=\sum_{r=0}^{13}\|(1-\Delta)^3\partial_t^rG\|_2^2.
\]
The corrected full current satisfies
\[
 |J_{\mathrm{ren}}^+[D,G]|
 \le 2\,10^{95}M[D]Z_{136}[G].
\]
For \(K\ge2m\), the exact homogeneous-anchored original-mask approximation has error at most \(3\,10^{54}MZ_{136}/K\). Both estimates have strict slack for a nonzero norm product, and vanish for a zero norm product. The two external canonical factors give \(8\,10^{-705}\) and \(12\,10^{-746}/K\). These are derivative-losing weak bounds, not a same-space inverse.

The independently valid S215 prepared maps and full chart Ward terms now use this new current. Their known tracefree-plus-Ward functional is below \(10^{118}V_{04}U_{138}\). The full reconstruction remains conditional on three ordered scalar kernels and their bounds. No original V/G/B or P8 closure, nonlinear background or reduced inverse is inferred.
