## Anisotropic complex hard-core domain

At every positive real center take
 |a'-a|,|b'-b|<=eta*u, |c'-c|<=eta*W, eta=10^-13.
Then |delta Q_A0| and ||delta Q_Asp||<=2eta*u, while the corresponding
total-Q changes are<=3eta*W. These fit strictly inside the S313 recoil
perturbation estimates. Continue its massive square roots and phase
from the real state; A0 remains the fixed positive real Born value.

The u' prefactor never vanishes in this polydisc. The isolated
composite scalar denominator retains magnitude>u/4; its perturbation
is O(eta*u), because changes of the hard scalar multiply Q_A.
A singleton scalar denominator is exactly2c'*p.n_c and its c' pole
is removed algebraically by the prefactor. The topology has no
repeated singleton scalar denominator in one graph. Total-Q light
denominators retain magnitude>W/4. Hard mixed, timelike and heavy
gaps retain the S313 values: (tau+W^2)/600000,45/16 and n/2.

There is NO Q_A^2 or (Q_A+q_c)^2 pure-soft propagator in R2.
This is why c' can cross zero here without applying an invalid
global-W holomorphy claim to the complete three-soft current.
All other denominators have strict closed-disc margins. K2 is
therefore holomorphic after the removable c' factors are canceled.

Write v_A=Q_Asp/u'. It differs from its real vector of norm<=1 by
O(eta); in particular ||v_A||<65/64. On the same complex massive
spatial tube, ||p_sp||<7/4 and |p0|>7/8. Both Doppler factors
p0-p_sp.v_A and p0-p_sp.n_c have modulus>1/8.
For S_A(p)=p.A.p/(p0-p_sp.v_A), the numerator and its spatial
gradient are<4, the denominator gradient is<4, so
 ||grad S_A||<4*8+4*4*64=1056<2048.
The same enlarged bound covers B. These statements are complex
norm estimates on the continued massive branch, not a real-only
mean-value argument. Incoming signed momenta use S(-p)=-S(p).


The composite normalized direction has norm<65/64 because
|u'-u|<=2eta*u and ||Q_Asp'-Q_Asp||<=2eta*u while |u'|>u/2.
The difference from its real-center velocity is bounded by8eta.
Combining this with the S313 convex massive-momentum tube leaves
the Doppler denominator strictly above1/8. No angular lower bound
on an individual graviton dot product is needed by this hard core.

For a composite scalar cut, hard-momentum changes multiply Q_A=O(u),
so its absolute perturbation is O(eta*u), not merely O(eta*W).
For the singleton cut, its exact c' factor is canceled algebraically
before claiming holomorphy. For total-Q cuts the O(eta*W) estimate
is sufficient. The topology has no repeated singleton denominator.

All estimates have strict margins on the closed polydisc, so the
Cauchy formula applies on a neighborhood of its closure. This is
not a claimed global-W disc for the full current or full amplitude.

