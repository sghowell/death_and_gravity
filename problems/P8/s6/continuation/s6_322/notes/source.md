# Same original source and precisely scoped classes

Retain the original n=10^200/512+2,kappa=10^800,g,C and the positive
Born sum A0=Am+AG. S315 supplies every literal arbitrary-valence vertex
and labeled tree; S317 supplies complete temporal reorganization.
No frozen source, old native raw report or parameter is modified.

The full three-real-graviton tree contains5116 terms. Contracting
maximal pure-soft branches gives3767 three-singleton-core terms,
three387 pair-plus-singleton classes and188 one-block terms.
S321 bounds the one-block subtraction. This successor bounds the
three pair classes, leaving precisely3767 terms outside that result.

For the pair A=(q1,q2), let u=a+b,Q_A=q1+q2 and F2=ab*H2/u.
The remaining hard functionalR2(A,B) has387 terms and is bilinear
in an arbitrary spatial tensor A and physical TT singleton B.
Define K2=u'*c'*kappa*sqrt(rho)*R2/A0. Then the normalized class
G387=abc*sqrt(rho)*M387/A0 equals K2(F2,B)/kappa.

The complete pair at timelikeQ_A has both nonzero trace and
nontransverse components in the exact original calibration.
Only the external leaves are physical TT. No multi-off-shell Ward
identity or replacement of the composite by a physical graviton
is made. The Born leading operatorK20 is independent of c but
depends on the composite velocityQ_A/u and on its arbitrary tensor.

These gauge-fixed computational classes are not separately
gauge-invariant detector observables. Their sum is a partial
subtraction result, not an inclusive physical rate.

