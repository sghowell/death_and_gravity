# Validation design and development evidence

Scientific source replay reconstructs the complete pair coefficients
and their96 derivative certificates, both generic and original
off-shell identities, the complete marked387/140 class comparison,
the independent temporal-class comparison, and three exact
anisotropic complex-domain calibrations at original parameters.
The phase is retained through an exact invariant and a triangle bound.

Research precursors:
- Pair hierarchy27536 C0 in4.331247292s.
- Off-shell/original-core4730 C0 in30.630120083s.
- Anisotropic387 calibration71459 C0 in127.360716958s.
- Independent budget arithmetic C0. Its first launch had a missing
  space after Python 'and' and failed before arithmetic; both versions
  and outputs were retained. No scientific formula changed.

The separately consolidated four-module diagnostic11186 returned C0
in247.32150883297436s: source226 checks/5 gates, hierarchy239/6,
algebra22/8 and hard52/50. This precedes the full science test suite.
Only import ordering/formatting and test-only cleanup followed.
Draft packet23878 then returned C0:18 sources,20 report keys,
7 module exports,539 named/668 scalar checks,78 gates and717
science tests in0.27s, after255.1940065830713s reconstruction.
Its18-source snapshot remained unchanged throughout. This remains
draft diagnostic evidence because the actual parent hash is pending.

These precursor passes do not replace cold, fresh, native, ordinary,
CLI or FULL acceptance of this separately owned package.
The final source embeds the actual immutable S321 native report hash
in verify.py. The earlier draft placeholder was replaced only after
that report's complete native capture, source/AST/frontier checks and
byte-identical private/repository raw validation. This proof source
does not claim the successor's later acceptance receipts; those are
recorded separately in the external assessment.

Native/direct/private/ordinary/CLI runs use original SymPy; only FULL
may use the previously validated exact-GCD adapter. A native report
is captured once, not regenerated by a second publication path.
External acceptance evidence is recorded only after complete runs.

The independent fresh replay may prepare the already frozen S321
parent before this successor's final source copy is available. That
process hashes all18 parent sources, reconstructs all16 cases with
original SymPy, and checks a canonical parent-payload digest. It does
not import any S322 source during preparation. It waits for one
explicit START after the final private/fresh copy has been checked;
only then does it snapshot and replay the successor's final18 files.
The inherited parent cache is disclosed, not represented as a second
cold parent reconstruction after START. No own-result cache is reused.

Written uniform arguments own the theorem. Finite exact points are
calibrations and guards, not proofs of uniformity over the full domain.
The result is not kernel-formalized and is not an inclusive closure.
