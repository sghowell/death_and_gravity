## Off-shell scalar vertices and exact ordering

For arbitrary A,Q,p and mass m define
 N_A(p)=p.A.p+p.A.Q-tr(eta A)*(p^2-m+p.Q)/2.
The literal canonical Phi2-h vertex with momenta p,-p-Q equals
-2*N_A(p), with no TT or null premise.

For a shift k the exact numerator difference is
 2p.A.k+k.A.k+k.A.Q
 -tr(eta A)*(2p.k+k^2+k.Q)/2.
If p^2=m, define A_d=p.Q_A+Q_A^2/2,
B_d=p.q_c and Z=Q_A.q_c. The total scalar denominator divided by2
is A_d+B_d+Z. Summing both same-leg orderings gives

 N_A*N_B/(A_d*B_d)
 +N_A*X_B/[A_d*(A_d+B_d+Z)]
 +N_B*Y_A/[B_d*(A_d+B_d+Z)]
 -N_A*N_B*Z/[A_d*B_d*(A_d+B_d+Z)].

Here X_B=N_B(p+Q_A)-N_B(p) and Y_A=N_A(p+q_c)-N_A(p).
These include the trace and nontransverse terms. All formulas are
generic polynomial/rational identities. Probe4730 returned C0 in
30.630120083s. At the original parameters it verified the entire
marked degree-two term of the387 core against the explicit140-graph
formula for both the actual complete pair and an arbitrary spatial
traceful tensor. The actual pair has BOTH nonzero trace and
nontransverse components. The collapsed core also agrees with a
separately filtered387-term class of the complete temporal recursion.


The complete387 core retains all vertices and partitions except
nonsingleton pure-soft graviton branches. A scalar-propagator marker
isolates its degree-two part without modifying a vertex. For both
an actual complete pair and an arbitrary spatial traceful tensor,
that coefficient is independently reconstructed by20 contact,
60 heavy and60 Einstein double-external terms. A separately filtered
temporal tree sum equals the collapsed pair-plus-singleton class.

These checks supplement the generic algebraic proof; finite points
alone would not establish the operator identity or its uniform bound.
All four massive external legs stay on their original mass shell.
The generic scalar formula itself has no mass-shell or TT premise.

