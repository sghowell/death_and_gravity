# P8 S6.322: pair-plus-singleton soft subtraction

For the unchanged original action and couplings, let a,b,c>0,
W=a+b+c<=1/8,5/4<=E<=2 and positive associated elastic transfers.
Fix real future-null directions and arbitrary complex spatial TT
leaf tensors of Frobenius norm<=1. Exact internal propagator poles
are excluded; all stated angular approaches are uniform.

For each complete temporal pair, F2=ab*H2/(a+b) has bounds
(||F2||/(a+b),||partial_a F2||,||partial_b F2||,
 (a+b)||partial_ab F2||)<=(530,1566,586,3104)/sqrt(kappa)
and compatible energy faces.

For the corresponding387-term pair-plus-singleton class, define
G387=abc*sqrt(rho)*M387/A0. Its mixed derivative has modulus below
1e-723/(a+b), and its compatible-face threefold rectangle is bounded by
1e-723*c*I(a,b), where I=(a+b)ln(a+b)-a ln a-b ln b.

The sum of the three pair classes and the prior188 one-block class
has rectangle below2e-723*J, where
J=c*I(a,b)+b*I(a,c)+a*I(b,c). This covers1349 terms, not all5116.

The proof uses a hard-core anisotropic tube of radii
eta*(a+b),eta*(a+b),eta*W with eta=1e-13. The composite pair is
arbitrary spatial, possibly traceful and nontransverse. All its
off-shell corrections are retained. Only the hard operator receives
this complex-domain claim, not the full pure-soft current.

The remaining3767-core subtraction, full probability, all-N,
real-virtual/state/Regge/bounce and original P8 remain open.
See the seven written proofs for hypotheses and verification boundary.

