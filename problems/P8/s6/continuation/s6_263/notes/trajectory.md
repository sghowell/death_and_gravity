# Exact initial constraint and two-sided homogeneous continuation

At u=0 the ENTIRE R has R_u=0 for every N, but R_uu generally
does not vanish. The exact initial constraint at H_hat=h=hdot=0 is

    G(N) + Z_N(N) mc^2/2 = 0,
    U=R^(-3/4), Z=U/N,
    G=partial_N(N U F)-3 U R_uu R_N/(4 R N).

The new packet re-enters the complete original homogeneous lapse
constraint to verify this formula before taking the positive root.
All derivative atoms are replaced simultaneously by exact node identity:
setting R_u=0 at a single time is NOT permission to differentiate that
equality and erase R_uu. The latter mistake produces a nonzero constraint
residual and is an explicit development/test control.

For the bare comparison only, the squared root is exactly

    mc_bare^2 = (1120 N^4-2847 N^2+1728)/(100 N^2),

and its N derivative has absolute value<100 on the larger strip.
The entire initial source-jet Jacobian sum is<10^8 there. For
N=1+epsilon, |epsilon|<=10^-230, the true squared root is positive
and close to1/100. Use the CENTERED identity

    sqrt(x)-1/10 = (x-1/100)/(sqrt(x)+1/10)

with denominator>19/100; do not subtract independently rounded square
roots. The actual M1 rate is within10^-225 of1/10. All source jets
and their interpolation segments lie in the displayed strict box.

The complete original constrained Euler equations determine Ndot,
Hhatdot, M1 acceleration and heavy acceleration, with the actual lapse
Schur pivot J. They keep the exact primitive derivative and both
heavy variables. The bare analytic comparison is
N=1, Hhat=4u/(1+u^2), mc=1/[10(1+u^2)^6],
a_hat=(1+u^2)^2. Direct substitution into every bare Euler equation
and the lapse constraint is checked. This only compares the full
current solution; it does not replace its source or action.

For the four light coordinates N,Hhat,mc,a_hat, every complete state
Jacobian entry is<10^40, including the chain through the ACTUAL J.
Every derivative with respect to the14 relevant independent original
R/F source jets is also<10^40. The a_hat equation is a_hatdot=a_hat Hhat.
The full heavy mass is handled separately by
e^2=hdot^2+N^2 mH^2 h^2. Its exact derivative cancels the fast mass,
leaving friction, lapse-rate and full-source terms. In either time
direction, the upper Dini derivative of e is at most
10^20 e+2*10^-2500 (and |e'| obeys this bound almost everywhere), so
e<=4*10^-60*10^-2500 from zero initial heavy data.

Bootstrap the light distance by10^-200 and the heavy variables by
the original coarse mass-adapted box. The direct J formula has no
inverse-J dependence; its complete derivative bound, the bare pivot
near243/160, and the tiny source error ensure every interpolated
pivot stays in[1,2]. The more precise coefficient re-entry establishes
J in[1.51,1.53], Cnn>1.5 and the original temporal/affine margins.
This avoids assuming the pivot margin in order to prove it.

Gronwall in either direction gives

    light distance <= 2*[10^-225
      +10^-60*(14*10^40*10^-300+2*10^40*10^-1000)]
      <10^-224.

Every heavy and light bootstrap bound is strictly improved. The
unchanged regular implicit-constraint local ODE therefore continues
to the whole closed symmetric slab. The M1 origin can then be
integrated, and W=S and the original affine complement reconstructed
as in the full parent. The clock Euler equation follows the original
covariant identity; no lapse equation has been discarded by gauge
substitution.

The28 complete coefficient value/first-time-jet rows, including the
heavy mass multiplier, have all state and source-jet derivatives<10^80.
The heavy charge, mixed momentum and lapse source, with first jets,
are<10^-780. Combining the improved distance and fixed profile
difference gives a common complete coefficient error<10^-143
relative to the SAME original reference. Every segment remains
inside the explicit finite-band matrix box. Nonzero reference
A,Tcorr and Jc are bounded, not set to zero.
