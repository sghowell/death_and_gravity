# Independent reconstruction and immutable evidence

Private invariant-index probes first proved the general-D gravity
Ward identity, full generic D4 recovery and TT auxiliary decomposition.
Independent canonical component expansions agree in D4,D5,D6.
A wrong fixed-four closed metric trace gives a nonzero Ward numerator.

All-polarization checks use explicit2,5,9 polarizations in D4,D5,D6
at two noncoplanar frozen S304 recoil states. The simpler rational
coupling fixtures are general tensor-identity checks, not original
physical-point calibrations. A separate D5 test uses all five
polarizations and unchanged original n,g,C,kappa. Each full graph sum
retains matter-gravity interference and the extra trace contribution.

The first private decomposition probe failed a structurally
unsimplified epsilon-ratio assertion AFTER its generic tree residual
was exactly zero. Whole-residual rational cancellation corrected that
diagnostic. The first draft package module check required Python True
but encountered SymPy BooleanTrue; explicit bool conversion corrected
the gate representation. Neither correction changed a scientific
formula or frozen parent source.

Cold and independently started fresh reconstructions use original
SymPy. The fresh process first rebuilds the complete frozen immediate
parent packets, then waits for one START after byte-exact final source
copying. Completed ancestor-cache reuse is disclosed. Preflight checks
actual parent raw/source hashes, source-set/20-field AST/export equality,
exact residuals, proof gates, API guards and unchanged source bytes.

Only matched cold/fresh hashes authorize the repository copy and one
native specification. Complete native raw bytes, manifests and inherited
frontiers must validate. Ordinary tests, a separate read-only CLI and a
covering FULL source regression must also finish C0 before publication.
Only FULL may use the audited exact-GCD adapter and it must restore the
original backend. Frozen evidence is not rewritten to hide failures.
The result is written analysis plus exact algebra, not kernel formalization.
