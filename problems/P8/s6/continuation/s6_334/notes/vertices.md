# Complete D-dimensional vertices and general Ward proof

The action's scalar seagull determinant and inverse-density coefficients
1/4,1/2,1 are dimension-independent. So are its Einstein cubic connection
contractions. Change BOTH occurrences of dimension:
closed metric loops eta^a_a=D; de Donder trace reversal I-eta eta/(D-2).
For an on-shell scalar pair the trace-reversed numerator is exactly

Hnum(p,q)=p_lower q_lower+q_lower p_lower+2mu eta/(D-2).

Use the same four external emissions, two scalar seagulls and one
Einstein cubic emission for each of three partitions as S304.
The invariant-index contraction engine gives the channel Ward numerator
L R M(k xi+xi k)=0 as a rational identity in D,mu and unrestricted
on-shell Gram variables. Thus the full 21-graph Ward identity holds
without a dimension-specific Gram determinant identity.
The complete generic rank-one contraction at D4 equals the frozen S304
expression, including trace/gauge parts.

Keeping metric loops at four while changing only the propagator is wrong:
the same symbolic Ward numerator is nonzero, proportional to
mu(D-4)/(D-2)^2. Independent component contractions in D4,D5,D6 check
both the rank-one amplitude and gauge contractions. Those calibrations
supplement rather than replace the general rational identity.
