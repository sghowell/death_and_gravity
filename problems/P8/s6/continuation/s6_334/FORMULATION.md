# Complete selected tree, not finite hard-loop matching

Use the SAME original minimally coupled canonical action, formal tree
vacuum and fixed masses/couplings/matching coordinates. Continue the
S304 gravity21 diagrams in D=4+2epsilon, retaining both closed metric
loops and the de Donder trace projector. Combine with the ALREADY
general-D matter26 diagrams of S295. Credit the rank-two polarization
sew and fixed-disk matter continuation already proved in S296.

All scalar momenta are outgoing, p_i^2=mu, k^2=0, sum p_i=-k,
and all propagators are nonzero. General rational identities use
unrestricted on-shell Gram variables, not special D4 Gram determinants.
The physical original APIs fix mu=1,5/4<=E<=2,0<omega<=1/8,
incoming COM and positive outgoing energies, original n,g,C,kappa,
and0<=epsilon<=1/8.

For t=(D-4)/(D-2)=epsilon/(1+epsilon), the gravity TT amplitude is
G_D=G4alg+2mu^2*t*Saux. Saux is an auxiliary algebraic unit massless
scalar-exchange tensor, not a modification of the original heavy
scalar or the introduction of another physical particle.
G4alg is its specified canonical invariant-index representative;
two D4 helicities alone do not determine its continued trace.

With A=M/sqrt(kappa)+G4alg/kappa^(3/2) and
B=2mu^2*Saux/kappa^(3/2) on the canonical rank-two transverse span,
the all-polarization rate is
Q=||A+tB||_F^2-(1-t)|tr(A+tB)|^2/2.
Its exact cubic coefficients and explicit pointwise regulator bound
are in the written proofs. Literal D4,D5,D6 component expansions
check every polarization. A separate D5 test retains the original
physical parameters rather than the simpler rational fixtures.

The promised result is a complete selected TREE and polarization
identity, not finite hard-loop evanescence, integrated gravity
real-minus-soft/virtual matching or original V/G/B/P8 closure.
