# Selected signed logarithmic addition

Use C_Born from the proved coefficient and the complete original positive
Born normalization A0=Am+AG. The original S319 N1 envelope is
|F1|<=B/(sqrt(kappa)*w), B=6e16*n^2<1e412 at the unchanged numerical
heavy mass n=1e200/512+2. The coefficient bound gives
|L_log|<320ln(1/w)/kappa^(3/2).

For the named signed difference |F1+L_log|^2-|F1|^2, keep BOTH the
interference2Re(F1*conj(L_log)) and |L_log|^2. The original recoil
phase satisfies0<rho<=1; summing two unit-norm helicities and all
directions gives w dw/(2pi^2). The elementary primitives are
 int_0^x ln(1/w)dw=x(1+ln(1/x)),
 int_0^x w ln(1/w)^2dw=x^2/2*(ln(1/x)^2+ln(1/x)+1/2).
Since pi>3 the full-tree cross term is below4e-1187*x(1+ln(1/x))
and the square term below3000*x^2*(ln(1/x)^2+ln(1/x)+1/2)/kappa^3.
With the leading Born seed alone, |Sbar|<=48, replace the cross-term
coefficient by2000/kappa^2.

The square term is not mislabeled as one-loop perturbative order.
The signed addition is not a positive normalized detector probability,
and is not equal by definition to a matched virtual contribution.
The omitted S0*H4_one-loop, uniform finite remainder and detector-scale
pieces remain explicit obligations. No hard matching constant is set
to zero, and no common-regulator finite rate follows from this bound.
