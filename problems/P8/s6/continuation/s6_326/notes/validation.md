# Validation boundary and retained draft failures

The private massive-kernel probe passed16 exact checks,12 arithmetic
gates and5 independent100-digit calibrations. Its initial failure was
a symbolic square-root factorization without an assumption t>1; using
the explicit positive interior factors corrected the branch treatment.
The separate original-source convention probe passed19 exact identities.

The new radiative research probe passed120 exact identities,24 bound
checks and16 calibration records in0.6301112499786541s. Ten coefficient
values cover Born,one,two,split-three andfour original recoil states,
both real and complex TT polarizations. Six auxiliary-mass convergence
records at1e-8,1e-16,1e-30 agree with the finite coefficient at110-digit
working precision; final errors are below1e-55. These are independent
calibrations, not the analytic uniform proof.

The first packaged scientific-module run passed source235checks,
conventions19, kernel12, radiative120 and bounds5. It then stopped in
the measure arithmetic gate because symbolic N was used where the
original numerical HEAVY_MASS2 was required. Only that numerical
substitution was corrected; no bound or physical parameter changed.
An unused variable and import formatting were fixed before that run.

The complete21-source draft reconstructed400named checks,433scalar
entries and64gates in104.11070583295077s, with all85negative controls
and182matching records checked. The science phase passed636tests and
failed one test that compared an expanded logarithmic primitive directly
with its factored form. Comparing their expanded difference to zero
corrects that structural assertion; no integral or physical estimate
changes. A payload index typo Di/Dj in the written normal derivative
was corrected to agree with the already passing exact identities.

The separate fresh process reconstructed all frozen S325 parent packets
in89.07794491702225s before any S326 import. It waits for one explicit
START only after the final cold-source run and exact21-source copy have
passed. Its own run therefore discloses completed ancestor cache reuse,
while remaining independent of the cold process.

Final acceptance requires fixed final source bytes, complete science
checks, an independent fresh-copy replay, three-way source equality,
a native raw report from the unmodified verifier, ordinary tests,
CLI replay and the full source regression. All retained failures and
later acceptance receipts are recorded externally without changing
frozen source or native bytes. Original SymPy is used except in FULL,
where the separately audited exact-GCD adapter is scoped and restored.

These certificates are executable exact checks with explicit written
proofs, not kernel-formalized proofs of the external one-loop theorem.
No sample set is promoted to uniformity, no unavailable physical
matching value is selected, and original P8 remains OPEN.
