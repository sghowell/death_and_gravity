# Mathematical statement and scope

Retain n=10^200/512+2,g=1/8192,kappa=10^800 and the original tuned
contact. The amplitude M6 is the complete four-Phi/two-graviton
canonical tree with434 labeled topologies, not a selected external
emission approximation. M5 is the complete47-tree one-real amplitude.
No source, primitive parameter or physical matching value is changed.

Fix 5/4<=E<=2, a unit outgoing pair-rest-frame direction u, two real
null directions n1,n2 with unit time component, and unit-Frobenius
spatial TT polarizations. Allow complex helicity combinations.
Let q1=a*n1,q2=b*n2,a,b>0,W=a+b<=1/8. Use the S300 exact recoil,
at all nonforward hard Born directions. The hard positive Born
normalization A0=Am+AG is fixed at the corresponding elastic state.
Angular endpoint bounds do not assign a unique helicity-coordinate
value exactly at coincident emitted directions.

Set rho_N=E*rprime/(r0*Eprime), F1=sqrt(rho1)*M5/A0,
F2=sqrt(rho2)*M6/A0, and G=abF2. Define S_a=a*J_a, with J_a the
leading soft current on the entire indicated reduced hard state.
The state sigma_b has the four massive recoiled legs AND q2.
Then G(0,b)=b*S_a(sigma_b)*F1(b)/sqrt(kappa), and likewise for G(a,0).
Both iterated double-leading limits are
G00=S_a(Born)*S_b(Born)/kappa.

Define
 B2=J_a(sigma_b)*F1(b)/sqrt(kappa)
    +J_b(sigma_a)*F1(a)/sqrt(kappa)-G00/(ab),
 R2=F2-B2.
The theorem is
 |partial_a partial_b G|<10^-326/(a+b),
 |R2|<10^-326*I(a,b)/(ab),
 I=(a+b)ln(a+b)-a ln(a)-b ln(b).
Derivatives keep E,u,directions and polarizations fixed.

For 0<x<=1/8, integrate the signed density |F2|^2-|B2|^2
over a+b<=x with the exact two-radiation phase measure, four physical
polarization pairs and1/2! for identical gravitons. Its total variation
is strictly below
 2*10^-725*x+10^-652*x^2,
uniformly in lower soft cutoff and all stated angles. It has a unique
finite signed-measure limit as that cutoff tends to zero. Relative
to the earlier unexpanded elastic leading-soft reference P0, it is
bounded by4*10^-725*sqrt(x)+2*10^-652*x^(3/2), tending to zero.

This defined subtraction is gauge consistent but is not matched to
all virtual and integrated counterterms. In particular it cannot be
added to S309's different decomposition without a matching proof.
No complete inclusive rate, all-N nonleading bound, finite hard
matching, all-loop amplitude, quantum state, absolute complex Regge
estimate or original V/G/B/P8 closure is asserted.
