# Verification and immutable source boundary

The exact report rebuilds S6.178 and separately validates the
source-pinned S6.85 complete ordinary response. Its own result
selects only the physically matched vector block; the old
scalar-profile and background-cancelled clock fields are not used.

New identities check the full arbitrary-lapse/scale canonical
operator map, physical stress and normalization source vertices,
the regular Cartesian zero-momentum longitudinal limit, exact
homogeneous mean-source cancellation, the complete analytic
chart's clock jets, nonstationary volume/readout and second-map
contacts, continuous derivative envelopes and exact norm assembly.

Independent tests vary the actual constrained mode actions,
differentiate the full rational/exponential chart numerically
at high precision, pull back generic nonstationary densities,
check the sharp contact inequalities and exact Leibniz formula,
exercise the original all-order Cauchy preparation, and reject
old profile totals and unproved scope promotions. Numerical
chart values supplement the exact identities and continuous
enclosures; they do not replace them.

The fixed certificate has67 named identities and74 scalar entries,
27 proof gates,9 controls and103 rejected inputs. Its explicit
18-file scientific manifest and every one of20 report fields
are checked, including missing/extra-field mutations.

Private and fresh repository science, native report generation,
ordinary replay and CLI use original SymPy. The full captured
P8 test run alone uses the separately audited exact GCD adapter
with128 original tuple comparisons. Runtime recursion and exact
integer-string allowances change no scientific functions.

Native report text is transferred losslessly, and its SHA and
every scientific source hash are independently checked before
fresh full replay. No frozen parent or scientific byte may be
edited to make a replay pass. Root audits record the final
replay counts, timings and hashes, not this frozen proof.
This is exact algebra and written analysis, not FORMALIZED.
