# Whole physical operator and readout bridge

S6.176 already fixes A=sqrt(kappa*zeta)W and the canonical source
J=sqrt(kappa/zeta)S. The mass is constant as a function on the
full domain, not just at the reference clock.

For physical homogeneous N,a and q=k_com^2/a^2, eliminating the
actual temporal constraint gives the transverse and longitudinal
coordinate actions

    L_sector = g_sector^2 [A_dot^2 - N^2(q+m^2) A^2]/2,
    g_T^2=a/N, g_L^2=a*m^2/[N(q+m^2)].

The S6.85 longitudinal scalar-potential coordinate has canonical
factor k_com^2*g_L^2. This is a fixed, metric-independent coordinate
factor at nonzero momentum and drops out of every metric source
vertex. At zero momentum the Cartesian longitudinal factor tends
to a/N; a polarization is not deleted by using the singular
scalar-potential coordinate at k=0.

At fixed comoving momentum z=q/(q+m^2), direct differentiation gives

    delta log frequency = n-z v,
    delta log g_T=(v-n)/2,
    delta log g_L=((1+2z)v-n)/2.

The complete time-derivative source functions therefore agree with
S6.85, including the moving canonical normalization. The proof also
varies the physical energy and pressure readouts BEFORE canonical
substitution and keeps their normalization contacts. An oscillator
frequency match alone would not establish stress-response equality.

The common Cauchy covariance is exactly the S6.55 all-order
cutoff-summed state used in S6.176, with no independent variation.
The finite-order W8 comparison reference is not the state.
The conditional physical-mode measure is unchanged. The constant
mass-source jets vanish identically in the shared dimensional
prescription, including its finite curvature terms at mu=m.
S6.176 supplies the covariant signature and normalization bridge.

These whole-family equalities identify the physical conditional
retarded stress derivative with the ordinary-Proca physical
operator. S6.85's independently fixed scalar profile and total
background-cancelled clock response are separate fields and are
not part of this identity.
