# Complete pair factorization at arbitrary finite multiplicity

## Unchanged source

Use the same selected four-dimensional covariant Phi/H/Einstein action,
canonical normalization and physical parameters as S315. On the original
physical domain, mu=nu=1, kappa=1e800, 25/4<=s<=16, and the positive
radiated energy is at most1/8. No omitted operator coefficient is chosen.
The finite tree construction is considered away from its internal poles.

A complete one-off-shell graviton root current R(P), with all other
external leaves physical on-shell free waves, obeys P^T eta R=0.
The root P is not required to be null. The proof is a finite coefficient
induction from the ungaugefixed Noether identity and the same linear
de Donder inverse used in the frozen source.

## Completed fixed-pair class

Fix two emitted physical gravitons i,j of energies a,b. A pair branch
means that these two labeled leaves join the same cubic Einstein vertex,
whose third leg is an internal graviton. Its complementary remainder
is the COMPLETE four-Phi/(N-2)-real/one-off-shell current R_ij.

The sum over every tree containing that fixed branch is exactly

A_ij=V_hhh(eps_i,eps_j,-trace_reverse(R_ij)/Q_ij^2)/sqrt(kappa),
Q_ij=q_i+q_j.

Collapsing the branch gives a bijection to the T_(N-1) source trees.
The completed current is conserved. The exact S311 coefficient identity
therefore yields, for unit-Frobenius physical TT polarizations,

|A_ij| <=530*(a+b)^2*||R_ij,spatial||_F/(sqrt(kappa)*a*b).

This removes the EXPLICIT pair-propagator angular pole. It does not
bound R_ij as its own propagators approach other singular configurations.

## Pair intersections and counterexamples

Two distinct branches can coexist precisely when their external pairs
are disjoint. For a matching M of k pairs, let A_M sum all trees containing
every branch in M. Then the exact finite amplitude identity is

A_full=A_no_pairs+sum_(nonempty matchings M) (-1)^(|M|+1)*A_M.

The corresponding graph inventory is
R_N=sum_k (-1)^k N!/[2^k k! (N-2k)!]*T_(N-k),
with graph exponential generating function R(y)=T(y-y^2/2).
For N=3, the groups are disjoint:5116=3814+3*434.
For N=4, R_4=73444-6*5116+3*434=44050.

A pure-three-soft cluster, contracted even with the actual conserved
original47-tree hard source, can have a pair-collinear pole while its
total root remains timelike. A complete hard remainder with two
off-shell legs can also fail the single-root conservation premise.
These are counterexamples to shortcuts, NOT full-amplitude or
inclusive-rate divergence claims.

## Strict scope

No uniform bound on all completed remainders, simultaneous multi-ray
collinear region, all-N nonleading amplitude or inclusive probability
sum is supplied. Finite radiative hard loops, real-virtual/evanescent
matching, the interacting quantum state, absolute complex Regge and
common-parent bounce remain separate. Original V/G/B/P8 remain OPEN.
