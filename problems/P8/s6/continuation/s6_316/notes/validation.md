# Independent validation and publication boundary

Eighteen ASCII, newline-terminated sources comprise seven modules,
two test files and nine Markdown files. The native report has twenty
fields. Exact scientific payloads contain no floating-point values.
There are398 named checks covering581 scalar entries,97 proof gates,
8 controls and73 rejected inputs. Initial style checks reported six
import/export-order and dictionary-literal issues; all were corrected
before preflight without changing the scientific assertions.

The first cold preflight passed all exact residuals and gates but one
selector rejection test failed: the untyped cache equated SymPy Integer1
with a previously cached Boolean True, bypassing validation inside the
cached function. A minimal exact reproduction isolated this interface
bug. The public selector now validates before entering a private cache;
the original rejection test is retained. A pytest iterator deprecation
was also removed by materializing its finite parameter tuple. The full
cold/fresh validation was restarted after these unfrozen corrections,
with no changed scientific assertion, tolerance, backend or parameter.
Native/direct/ordinary/CLI retain original SymPy; the audited exact-GCD
adapter is restricted to the captured FULL regression.

The private probes first found the isolated pure-three-soft pole and
then identified its nonconserved nested cubic remainder. The complete
hard-current calculation passed its exact Ward identity. An independent
full three-real decomposition passed at both diagnostic and original
parameters:5116=3814+3*434, with exact amplitude equality.

Separate probes confirmed the symbolic-r complete pure-three-soft
root Ward identity, its nonzero quartic-omission defect, and independent
root-cut no-pair inventories through N4. Explicit matching enumeration
and graph-EGF composition provide separate finite calibrations of the
general combinatorial proof.

The actual original47-tree conserved hard current has a nonzero
isolated-cluster residue. The complete four-soft root is conserved,
whereas a two-off-shell remainder has a nonzero exact Ward defect.
These are deliberately retained negative controls, not ignored failures
or claims that the full physical amplitude diverges.

The packet additionally compares each new completed434-tree current
entry by entry with the frozen independent lower-source implementation.
All calculations use the unchanged canonical action and parameters.
No assertion, tolerance or algebra backend was relaxed to obtain a zero.

Cold and distinct fresh-root preflights check module exports, exact
counts, proof gates, float-free serialization and every science test.
The fresh run forces the complete immediate parent's packets first.
Exact private/fresh/repository byte comparison precedes source freeze.
The native helper receives one specification and only documented chunk
acknowledgments. Raw hashes, source manifest,20-field REPORT AST and
all retained frontiers are validated before raw freeze.

Original-SymPy own ordinary/CLI and the complete captured P8 suite
are subsequent publication requirements. Their eventual outcomes
belong in the external assessment, never retroactively in these frozen
sources. At most one FULL and one own pair run at a time.
No resource diagnostics or blind retries are used.
