## Evidence boundary and acceptance

This file describes the scientific checks. The external assessment
alone records accepted cold/fresh, native, ordinary/CLI and full P8
regression receipts. No private cached amplitude is a proof input.

The packet reconstructs49 free scalar-line coefficients,62 numerator
monomials and16 independent signed complex-TT original mappings; the
125-assignment original3767 forest and all eight nonzero cumulant
classes;192 generic endpoint groups and776 exact coefficient divisions;
literal recoil derivatives, massive-current gradients and exact
uniform-bound arithmetic. Original SymPy is used outside FULL.

The written proofs establish the uniform domain and bounds. Finite
calibrations audit their original-action implementation; passing integer
inequalities alone would not establish the analytic hypotheses. The
certificate is not Lean- or kernel-formalized.

### Retained development failures

The first physical-mapping diagnostic used the symbol range v10:4,
which produced an empty vector and a shape error. Its corrected v2
uses explicit indexed symbols and passed the original16-fixture
mapping. No original vertex formula was changed.

The first generated forest source removed a progress-print body
but left its empty conditional. Compilation stopped before execution;
the unused conditional was removed. A later consolidated replay
passed source228/5, line332/13 and the original forest amplitude
identities, but its final named count residuals failed because the
loop name central overwrote the generating-function variable. The
latter is now named central_egf in both early and returned checks.
This is retained as a failed run, not counted as acceptance.

The earlier line-only replay passed332 checks/13 gates before explicit
loop-default binding cleanup. The analytic replay passed34 checks and
45 gates. The corrected consolidated source replay passed228/5 source,
332/13 line,42/6 forest and1552/6 grouping checks/gates in
400.3566615830641 seconds, with all five captured files unchanged.
The full draft package replay subsequently passed2403 science tests
in6.03 seconds after398.4466107080225 seconds of reconstruction:
2188 named residuals,2200 scalar entries,84 gates,77 rejections,
19 ASCII source files and20 report fields. All captured118975 source
bytes were unchanged. This receipt predates this paragraph and the
eventual parent-hash substitution; it is a draft diagnostic, not the
required accepted cold/fresh/native replay.

### Required acceptance

The amplitude-only19-file draft was subsequently extended, before
source freeze, by the signed-measure module and its written proof.
The independent precursor algebra/entropy/measure diagnostic passed
in1.3733017080230638 seconds with original SymPy. The enlarged full
package requires new captured-source science and acceptance passes;
the earlier2403-test receipt is not claimed for these expanded bytes.
The expanded draft then passed2492 science tests in5.71 seconds
after403.4532365829218 seconds of reconstruction:2243 named checks,
4215 scalar entries,98 gates,79 rejections,21 ASCII files totaling
141651 bytes,20 report fields and9 module exports. Its entire source
snapshot was unchanged. This paragraph and the actual S322 native-parent
hash substitution are later edits; final cold/fresh/native acceptance
is required on the final source bytes and recorded externally.

The independent fresh interpreter may first reconstruct all frozen
repository S322 parent packets, with their source and raw report hashes
pinned. It imports no S323 source during that warmup. Only after the
final private science pass and exact final source-copy verification may
one explicit START release its S323 preflight. Full parent packets are
forced again before its own residuals; no private or native cached
S323 amplitude is reused. This is a disclosed parent-warm fresh check.

All21 source files must be exact ASCII, newline-terminated, and match
the independent private/fresh/repository manifests. Native rebuilding
must use the actual frozen parent report, retain all20 report fields
and original arithmetic, and leave old inputs unchanged. Ordinary and
CLI replay must compare the complete report, not selected fields.
FULL may use only the independently audited exact-GCD adapter, with
captured-input invariance and all adapter contracts. Scope remains
finite-tree amplitude subtraction and its associated signed measure.
