## Original source, parameters and proof obligations

The action and numerical parameters are inherited unchanged:
n=10^200/512+2 and kappa=10^800, with the original cubic and tuned
contact. There is no diagnostic-coupling replacement or freely chosen
physical matching datum.

S315 supplies the literal all-valence action; S317 supplies the complete
temporal tree reorganization. At three real leaves its partition is
5116=3767+3*387+188. The first class has only singleton pure-soft branches;
the second uses one complete pair; the last uses the complete four-tree
three-soft current. S321 treats188 and S322 treats the three387 classes.
This packet supplies the remaining3767 amplitude subtraction.
Its seven-face baseline then defines a complete finite signed
three-real density difference. S319 supplies the unchanged complete
amplitude bound used for that baseline; all interferences are retained.

The forest evaluator independently filters the original action only
by topology, never by a postulated Ward identity. The central kernel
has no internal light scalar propagator or nonsingleton pure-soft
branch. Removing the four external scalar chains is an exact,
label-preserving decomposition; central contacts, heavy exchange,
all hard Einstein paths and the amputated-root attachments remain.

The original-source calibration uses unit-bound complex-capable
physical TT fields and original parameters. Exact equality at the
retained fixture audits the implementation; the generic recursion,
endpoint polynomial identity and analytic proofs establish the
uniform statement. Neither a fixture nor an integer inequality alone
is presented as a uniform bound.

Parent source and native reports remain immutable. The verify module
requires the exact S322 report and the exact S320 hard-majorant input.
The external assessment, not this source note, records acceptance.
