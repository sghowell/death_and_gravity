# P8 S6.323: complete three-real amplitude and finite signed subtraction

Use the unchanged original action and couplings. Let a,b,c>0,
W=a+b+c<=1/8,5/4<=E<=2 and positive associated Born transfers.
Fix real future-null directions and complex spatial TT leaf tensors
of Frobenius norm<=1. Original internal propagator poles have no
assigned point value; the estimates are uniform on their allowed
angular approaches.

Let A0>0 be the original positive Born amplitude, rho the exact
phase-space ratio, and M3 the complete temporal5116-tree amplitude.
Define G3=abc*sqrt(rho)*M3/A0 and the anchored rectangle

 R3 G3 = G3(a,b,c)-G3(0,b,c)-G3(a,0,c)-G3(a,b,0)
         +G3(0,0,c)+G3(0,b,0)+G3(a,0,0)-G3(0,0,0).

Every face here is a compatible limit of the normalized coefficient,
not a value assigned to a bare propagator pole. With
 I(a,b)=(a+b)ln(a+b)-a ln a-b ln b,
 J(a,b,c)=c*I(a,b)+b*I(a,c)+a*I(b,c),
the complete amplitude rectangle has modulus below1e-670*J.

The proof first separates3767 hard-core terms from the prior1349
nonsingleton terms. A125-assignment external-chain forest gives eight
signed cumulant classes. Five use explicit vanishing factors; a sixth
uses the retained two-current forward comparison. Three independent
singleton currents require a second-order origin remainder; a central
emission with two singleton currents requires a first-order remainder.
The latter estimates use hard-only physical and origin polydiscs.

No global total-energy disc is claimed for connected soft currents:
an actual connected-pair pole supplies a negative control. The full
amplitude is not uniformly holomorphic on that disc either.

Define F3=sqrt(rho)*M3/A0 and H3=G3-R3 G3, the full seven-face
inclusion-exclusion baseline. Set B3=H3/(abc). For0<x<=1/8 the signed
density |F3|^2-|B3|^2, with its exact three-real phase-space measure,
all eight polarization triples and1/3!, has total variation below
1e-1424*x^2+1e-1340*x^4 and a unique common lower-cutoff limit.
Every interference term is retained. This is not the anchored
rectangle of |G3|^2 or the squared amplitude remainder alone.

The theorem does not establish positivity or normalization of an
inclusive probability, matching to real-virtual counterterms, all-N
summation, finite hard or
evanescent matching, interacting state, unitarity, absolute complex
Regge, common-parent bounce or original V/G/B/P8 closure.
