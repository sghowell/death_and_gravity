# What this matching packet resolves and what it does not

S288's one-loop minimal-gravity representation has one finite
light Newton and two regular crossing-local anchors. This packet
gives the explicit covariant coordinates of the two latter
structures: the entire D-dependent eight-operator map and its
six-dimensional kernel. It does not determine eight independent
off-shell finite coefficients from two on-shell measurements.

For the polynomial conventions, if S288 writes its regular term
as[alpha2 sum a^2+alpha0 mu^2]/(16pi^2 kappa^2), then
alpha2=16pi^2 A2 and alpha0=16pi^2 A0. The D dependence of
the local map must be retained through raw1/EP multiplication.
The known Newton shape is separate from these regular coordinates.
A change of Newton coefficient cannot be counted again as a
local b20 after consistently subtracting that same known pole.

For the H/Proca Gaussian metric sector the S285 prescription
already supplies all finite local curvature terms. This packet
therefore fixes that entire sector's crossed amplitude and
bounds its full forward coefficient with no new finite choice.
It also identifies the exact nonlocal light Gaussian coefficient,
while displaying its own unassigned curvature combination.
The light Gaussian metric diagrams are already included in the
S288 minimal scalar/gravity loop inventory.

Neither the fixed-heavy sign nor the positive light nonlocal
constant determines the complete original-source b20. Original
C/g interactions, the non-Gaussian matter and gravity diagrams,
their finite local matching, and their higher-loop remainders
are not removed by the projection. In particular, a rank-two
on-shell counterterm quotient does not choose a renormalization
prescription or match the physical bounce action in all fields.

For the original finite-gravity route the graviton pole and
fixed-negative-transfer contour/Regge contribution must be
combined before the forward limit. This selected gapped metric
coefficient is useful input to that calculation, not its
substitute. S288's massive Coulomb phase and the physical
detector/dressing error requirements are retained.

No arbitrary finite coefficient or high-energy remainder is
requested from the user as a way to manufacture closure.
They remain concrete research/matching obligations. Original
V/G/B/P8 stay OPEN and the earlier scoped P8(a) result is unchanged.
