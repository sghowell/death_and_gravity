# Immutable source and independent replay contract

Seven module exports, two test modules, two root documents and
seven proof notes make18 ASCII scientific source files. The
report has exactly20 AST-derived top-level fields. Source
formatting and all private corrections precede source freezing.

The cold original-SymPy preflight validates each exact residual,
the complete packet recursively free of floating-point values,
every proof gate, actual rejected input, retained scope,
module export, source hash and the whole science test module.
A fresh independent root forces all immediate-parent packets
before its own residuals, then repeats the same checks and
tests. This guards the rejected S279 mutable-cache failure.

Only after exact private/fresh/repository byte and hash agreement
are the formatted sources frozen. The native helper receives
the new packet specification once, never a research probe or
DONE. Its entire acknowledged raw byte stream is checked by
SHA256 and source manifest, validated against the20-field
contract and actual counts, and frozen without rewriting.

Independent original-SymPy ordinary and CLI replays must both
pass. A captured complete P8 FULL regression must also pass with
actual collection count, unchanged captured manifest and exact
adapter contract evidence. The exact-GCD adapter is FULL-only;
native, direct, ordinary and CLI retain original SymPy. At most
one FULL and one ordinary/CLI pair run concurrently.

The external assessment records actual counts, bytes, hashes,
timings and exits after they are captured. This immutable
scientific note does not predict acceptance. Frozen sources,
proofs and native raw reports are not edited for later gates;
corrections require successors or external assessments.
Unrelated P4/P9 work is not included in this packet.
