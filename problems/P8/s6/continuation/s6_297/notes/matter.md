# Complete same-OS4 matter loop in the missing interference

S239's fully rebuilt result includes the entire matter loop, not only the
renormalizable C/g diagrams. It retains the six local degree-six tadpoles,
the tadpole-dressed heavy source, all mixed3+1 contractions and the complete
heavy-onepoint source cancellation. Its dimensional tensor factors precede
pole-only MSbar; light mass and residue are on shell through this order.

The symmetric finite-value prescription is the old H8A420-VAC-OS4
condition at s=t=u=4/3. That point is subthreshold, not a physical scattering
configuration. No new gravitational finite coefficient is fixed by it.

Let A_original be the older target scalar tree used to normalize the frozen
bound. The explicit S239 rational upper bound r obeys
|L_m|<=r A_original and0<r<10^-199 throughout4<s<=10^196 and all physical
angles. The separate S233 tree comparison gives
A_m>=59 A_original/60. Hence

|L_m|/A_m <= eta=60r/59 <2*10^-199.

Both the real and imaginary parts are included in this absolute bound.
The new source proof licenses reuse as an equality of formal matter
diagrams at finite kappa; it does not assert a full quantum limit.

Set x=A_G/A_m>0. Then

|2 A_G Re L_m|/(A_m+A_G)^2 <= eta *2x/(1+x)^2 <= eta/2 <10^-199.

The global maximum is fixed by
1/2-2x/(1+x)^2=(x-1)^2/[2(1+x)^2].
There is no small-x expansion. In particular the bound remains valid in
the narrow forward region where gravity dominates the matter Born term.

The raw interference can still have the forward graviton pole. A bounded
ratio to a singular reference is not a finite forward differential or
total cross section. The strictly nonforward domain and the distinction
between full Born normalization and perturbative Newton ordering remain
part of the claim.
