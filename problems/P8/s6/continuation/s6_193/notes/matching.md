# Original fixed covariant prescription and finite contacts

The physical state and convergent S6.192 actual-minus-J_ad4 integral
remain in three dimensions. Only the local subtraction polynomials
are dimensionally continued. Neither the state nor its finite
reference is chosen anew.

Write d=3-2epsilon_DR and ell=log(m^2/mu^2). In the named MSbar radial
normalization the common gamma factors have the weight
exp[epsilon_DR(EulerGamma-ell)]. The normalized local terms, before
1/(64pi^2), contain

    vacuum: 2d exp[...] Gamma(epsilon_DR-2) m^4,
    Einstein: (d-6) exp[...] Gamma(epsilon_DR-1) m^2 R/3,
    curvature: 2 exp[...] Gamma(epsilon_DR) a4V(d+1).

The meromorphic continuation is evaluated before the dimension
limit. Vacuum integrals may equivalently be defined first with an
auxiliary analytic exponent; no divergent unregulated integral is
assigned an ordinary value.

Subtract the ORIGINAL fixed four-dimensional scalar pole
coefficients 3m^4+m^2R+2a4V(4), while continuing metric volume and
invariant contractions as specified in S6.176. The finite density is

    -(3ell-5/2)m^4 -(ell-5/3)m^2R -2ell*a4V(4)-4a4s.

The d-dependent polarization count produces5/2, not9/2.
The Einstein coefficient is5/3, not1. The evanescent part
a4V(d+1)-a4V(4)=-2epsilon_DR*a4s produces the finite -4a4s
contact. These terms cannot be dropped before pole subtraction.

The generic variational identity in variation.md identifies the
local polynomial being subtracted with the SAME J_ad4 in S6.192.
The full-shear angular/radial identity then fixes its finite
covariant matching; it does not merely establish convergence or
assume that an arbitrary adiabatic subtraction is covariant.

Therefore on the admitted homogeneous histories the original
renormalized metric current is the S6.192 convergent comparison
plus the compact variation of this unchanged finite local action.
At mu=m this is 5m^4/2+5m^2R/3-4a4s. The sign convention is
R_old=-R_P8. Agreement of the whole local action on arbitrary
shears transfers all its metric contacts by variation, rather
than matching only isotropic one-point values.

No new finite counterterm, reference state, scalar stress-canceling
profile, regulator prescription or parent is selected here.
