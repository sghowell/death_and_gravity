# Uniform continuum spaces, initial domain and physical units

For eachq=|P|^2, Eq(t) and Eq'(t) are measurable Fourier multipliers on the two scalar amplitudes. Their bounds375t^3/16 and1125t^2/16 are independent ofP. Plancherel therefore bounds their operator norms onH^r(R^3;R^2) by the same constants for every realr. No directionwise zero-transfer limit or spatial derivative of the multiplier is needed. The bounded projector embedding identifies the directionwise quotient away from the measure-zero origin.

For f inC([0,T];H^r), Minkowski gives

||E*f||_(C H^r)<=375T^4/64 ||f||_(C H^r),
||partial_t(E*f)||_(C H^r)<=375T^3/16 ||f||_(C H^r).

For each Fourier momentum, the kernels and their first derivative are continuous in time. The same uniform majorants and dominated convergence give strong H^r continuity after integration, including the initial boundary. Thus the output isC1, not merely pointwise bounded. Eq(0)=0 removes the source boundary term when differentiating the convolution. Young's inequality gives the same375T^4/64 bound onL2([0,T];H^r).

The forward graph is well-defined on continuous amplitude histories as a causal distribution. Each G_i,q is a bounded time kernel uniformly inq; applying(D^2+q), then the two differential B factors, introduces only finite polynomial time/spatial derivative growth. For example the resulting expression is a finite-order time distribution with values inH^(r-6) when the amplitudes are inH^r. This crude forward distribution space is sufficient to define the equation and does not assert a bounded same-space forward map.

After a harmless compact future extension, the histories have Hilbert-valued Laplace transforms in a sufficiently negative spatial space. The exact Fourier/Laplace inverse identities hold there and hence as distributions. Restriction to the finite window is independent of that extension by causality. The domain used for inversion consists of continuous y for which the forward distribution equals an ordinary C_tH^r source on the complete initial boundary and the interval. No initial delta derivative is ignored. The reconstructed y=E*f belongs to that domain and is unique there. This does not assert density or invariance of the distinct curved quantum-force graph.

The6T^4 estimate uses the Euclidean norm of y=(zeta,c) and its dual source; it is not a bound in an unspecified physical norm. The spatial metric embedding Tsp has squared norm below14. Reconstructing a Frobenius-metric response to a Frobenius dual metric source therefore costs at most another factor14 through Tsp E Tsp^T. This finite factor does not introduce momentum dependence.

The normalized operator A_q is64pi^2 times the isolated amplitude-coordinate metric Hessian. Restoring that physical Hessian gives inverse64pi^2 E and bound375pi^2 T^4 in the same amplitude/dual-source norm. With the S222 force normalization on a flat unit-density reference,

Qbar_ref=A_q/(64pi^2 kappa),
||Qbar_ref^-1||<=375pi^2 kappa T^4.

The fixed kappa is10^800. This is not a kappa-small inverse or a full small-feedback conclusion. The actual curved density factor depends on time and cannot be commuted through the response kernel using this flat formula. Translating back to the full lapse/shift/clock/matter equations needs the full ordered curved interface.

All estimates apply to a mathematical reference with unchanged original state/prescription inputs. Their uniformity over computational momenta is not a physical cutoff statement. The exact q0 kernel extension is not the literal global homogeneous constraint.
