# Full covariant scalar geometry and opposite gauge legs

At spatial Fourier momentum P, choose a rotation with Pi=diag(1,0,0); the tensor formula is rotationally covariant. Put q=|P|^2 and p=lambda^2+q. The S200 full spatial numerator uses A=-pI+PP^T, not the center-of-mass projectors at nonzero transfer. For Q=2zeta I-2c Pi, direct contraction gives

Q0(QD,QG)=(A:QD)(A:QG)/3=12 S_D S_G,
Q2(QD,QG)=tr(AQD AQG)-Q0=(8/3)W_D W_G,
S=(lambda^2+2q/3)zeta-lambda^2 c/3,
W=-q zeta-lambda^2 c.

These Q_i are p^2 times the restricted covariant spin projectors. The actual spin0 factor has the trace normalization12 established in S223. Hence the reference quotient is B^T diag(Ftrace(p),(8/3)F2(p))B.

The independent four-dimensional check starts instead with k_a=(lambda,iP), signature+--- and T_ab=p eta_ab-k_a k_b. For the complete metric perturbation,
h00=2n, h0i=+iP_i b/q, hij=-2zeta delta_ij on the source, with the opposite spatial sign on the detector. Raising both indices before contracting with T gives the same projectors. Three rational unit directions, including fully nonzero components, are checked.

In the full scalar metric tuple the source channels are

S=q n/3+(lambda^2+2q/3)zeta-lambda b/3,
W=q(n-zeta)-lambda b.

The detector channels replace lambda by-lambda. Thus the ordered reference is M(-lambda)^T diag(Ftrace,(8/3)F2)M(lambda). It is not a symmetric three-by-three matrix at fixed lambda: the shift cross entries are odd. The gauge vector(lambda,0,q) is in the source kernel and(-lambda,0,q) in the detector kernel.

The full prepared gauge parametrization n=D eta,b=D c+q eta gives

T(lambda)=[[lambda,0,0],[0,1,0],[q,0,lambda]],
M(lambda)T(lambda)=[0,B(lambda)].

The opposite detector chart gives the same even-in-lambda B. Consequently T(-lambda)^T R3 T(lambda)=diag(0,A_q) exactly. The zero gauge block is retained, not declared invertible. The gauge chart determinant islambda^2, with no division byq. The local endpoint/preparation qualifications are addressed in the causal domain proof.

For the finite fourth-order part, literal linearized Riemann tensors on opposite legs give

R_D R_G=36 S_D S_G,
C_D:C_G=(4/3)W_D W_G,
Euler_(D,G)=0.

The original finite density coefficients-C^2/30-R_old^2/18 therefore give the Hessian-4 S_D S_G-(4/45)W_D W_G. These are precisely Ftrace(0)=-4 and(8/3)F2(0)=-4/45. The Einstein, volume, chart-one-current and other lower-order/state terms are not absorbed into this fourth-order reference.

The embedding from y=(zeta,c) to Q has Gram matrix[[12,-4],[-4,4]]. Both G-2I and14I-G are positive definite, with determinant4 and positive first diagonals. Hence2||y||^2<||Q||_F^2<14||y||^2 for nonzeroy. The quotient is geometrically nondegenerate in its own amplitude norm.

Theq0 extension below is a continuous directionwise spatial tensor quotient. At literal P0, an originally isotropic unitary scalar metric has a different homogeneous constraint interpretation. No direction-independent projector limit or global homogeneous lapse/shift solution is inferred.
