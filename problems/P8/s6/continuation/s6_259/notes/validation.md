# Immutable sources and read-only complete replay

Freeze only after the complete private payload, scope, source-manifest
and independent science checks pass with original SymPy. The source
manifest is six package modules, two test modules, two root Markdown
files and seven proof notes: 17 files. The report has 20 top-level
AST-derived fields and pins the raw S258 parent SHA256

    23053dbcf675d2737666366ee1b73e5d1a2cdac9b14405fc5d60794b3f8306cc.

The packet has 74 named identities, 29960 scalar entries, 42 proof
gates, nine controls and 326 rejected inputs. The large scalar count
includes complete matrix products, not that many independent physical
theorems. Exact inputs retain all 64 connection coordinates, all 56
complement directions, every determinant block, all source terms and
the original projective gauge.

Independent diagnostics solve the whole finite matrices, verify exact
rational congruences, integrate convergent full 56-direction Gaussians
toward the specified Fresnel limit, test full coefficient and scale
variations, change all-mode complement bases with their densities,
and retain the nonlinear source and derivative-coordinate constraints.
The ordering counterexample is a complete differential-operator
identity, not a claimed new physical quantum correction for P8.

After freezing, copy exact bytes into a new checkpoint and perform
fresh direct payload and science checks. Native construction rebuilds
the complete ancestry with original SymPy. Transfer raw ASCII JSON
using exact offsets, chunk lengths and observed CONTINUE
acknowledgments. Validate the un-reserialized report SHA, every
private/native/repository source hash, all 20 fields and all
previously protected scientific inputs. Never edit frozen sources
or reports to repair a launch or test error.

Ordinary package tests and CLI replay use original SymPy. Only the
separately captured full regression uses the audited exact-GCD
adapter with its self-checks and unchanged-file manifest. Launch it
with the existing S219 helper import path:

    PYTHONPATH=problems/P8/s6/continuation/s6_219/tests \
      .venv/bin/python scripts/p8_replay.py full

Only observed zero exit statuses count as completed validations.
External publication notes record actual counts, timings, hashes,
development fixes and any retries; these frozen notes do not predict
completion. Publish only after all required validations pass, using
an explicit exact staging manifest and an independently checked
remote hash. Preserve unrelated P4/P9 work.

The written finite-jet, canonical-density, inertia-continuation and
Fresnel-limit arguments are supported by exact identities and finite
diagnostics, not claimed as Lean FORMALIZED or as a complete
interacting continuum quantum construction.
