# Full projective quotient and source-centered coordinates

Let G be the original 64-by-4 projective columns,
(delta Gamma)^a_bc=delta^a_b g_c, using the original derivative-last
connection convention. Let F Gamma be the four traces Gamma^a_ac.
Then FG=4I. The full 64-dimensional action and the retained trace
are projectively invariant; no connection symmetry is imposed.

The original trace-free quotient embedding E sends 60 independent
coordinates to F Gamma=0. Include the complete split from the
complement note:

    Gamma_displacement = E K z + E L t + G g.

The report gives its exact full inverse. The projective coordinate
is g=F Gamma_displacement/4. Both full inverse products are checked.
In the displayed coordinate order its configuration Jacobian is -256,
not one. The original gauge F Gamma=0 has Faddeev-Popov matrix 4I
and determinant 256; the normalized coordinate gauge g=0 has matrix I.
Gauge-parameter normalization and coordinate Jacobians must be
transformed together. Their apparent difference is not a physical
determinant or a new vector polarization.

A full cotangent lift of this invertible point map preserves the
phase-space Liouville measure: the coordinate determinant and inverse
transpose momentum determinant cancel. This statement is different
from declaring the configuration Jacobian to be one.

## Entire source center at fixed retained trace

Let Gamma_star be the whole original source-pinned stationary
connection, retaining every derivative-forcing component. Let
Tstar=N_full Gamma_star. After the Xi update its full Hessian and
source are

    M64_new=M64_old+N_full^T Xi N_full,
    j64_new=j64_old-N_full^T Xi Tstar.

Their complete stationary Euler equation is still satisfied by
Gamma_star. At a prescribed retained unshifted trace T, the whole
conditional center is

    Gamma_bar=Gamma_star+E L(T-Tstar).

The report checks all four retained traces and all 56 complementary
Euler equations at this center. For arbitrary z the action difference
from the center is exactly z^T A z/2, with no omitted source-linear
complement term.

Finally T=W+B(u,X)du, with the original fixed regular lower coefficient,
not T=W with B erased. The complete physical source square and
dynamical Proca curl remain. The retained trace directions have not
been integrated out.

These source shifts depend on finite metric and clock derivative data.
They are treated as point maps only after introducing the independent
derivative coordinates and retaining their defining constraints, as in
the jet note. A fixed-history configuration integral can already use
their exact Jacobian, but that alone is not a proof of a complete
quantum time-slicing or ordering prescription.

The source chart here is regular and timelike. The classical action's
broader analytic coefficient domain does not authorize extending a
quantum scalar-unitary gauge through a vanishing clock gradient.
