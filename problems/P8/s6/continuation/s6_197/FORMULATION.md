# S6.197 formulation

## Actual comparison

Retain the same full affine parent, canonical Gaussian Proca
sector, mass1000, kappa1e800, profile, all-order prepared pure
state and fixed covariant prescription. On the unit CD slab
a=(1+t^2)^2, let D and Gamma be real smooth compact spatial
tracefree probes. Gamma vanishes near the common initial
Cauchy surface. Their time supports MAY overlap.

At each common spherical regulator K, take the actual first-order
retarded current J_K from S195, including its second-metric
contact. Define J_ref,K by substituting the same constant-alpha
W8 readouts into that finite-regulator mode formula.
This defines an algebraic comparison, not a new physical state,
a new subtraction prescription or an effective action.

The limit R(D,Gamma)=lim_K[J_K-J_ref,K] exists absolutely,
retaining theta(t-s), the complete local metric contact,
all3 physical polarizations and all external momenta. Define

    M[f]^2=||f||L2(dt dx;F)^2+||grad_x f||L2(dt dx;F)^2.

For nonzero input norms,

    |R(D,Gamma)| <2e23 M[D]M[Gamma];
    |R-R_K| <1e27 M[D]M[Gamma]/K, K>=1000.

For h=sqrt(kappa)gamma/2, both canonical tensor chain factors
give8e-777 and4e-773/K in the same norms. All corresponding
quantities are exactly zero if either test is zero. K is a
computational regulator and is removed, not a physical cutoff.

## Unclosed boundary

The constant-alpha W8 reference is generally not CCR-normalized
and does not solve the exact mode equation. Its singular
retarded memory/contact must still be matched to the ORIGINAL
covariant prescription, including the diagonal. The finite
remainder alone does not establish that matching, a Ward
identity for the reference or a full renormalized response.

No finite-amplitude remainder, full feedback inverse, interacting
background, stability, full parent/cutoff matching or V/G/B
closure is established. Original P8 remains OPEN.
