# Validation boundary

The continuous proof uses the actual S186 pointwise mode
remainder, full mixed pair-product expansion, massive
radial integrals, the low/high external split and ordinary
Cauchy inequalities with the retarded step retained.
It is a written analytic argument, not FORMALIZED.

Independent tests integrate full and removed two-momentum
weights at zero and nonzero external momentum, retaining
both mixed and quadratic weights. Angular primitives are
integrated independently and48-point Gaussian radial
quadrature is supplementary numerical evidence, not the
continuous majorant proof. Exact complete/removed J5
checks validate the separate contact radial integral.

Literal Fock operators on a two-mode fixture compare
the retarded actual-minus-reference current against the
complete pair formula, including its sign and factor.
The error-square contribution is independently nonzero.
The fixture's time quadrature tests the algebra, not a
replacement for the smooth-support continuum theorem.

Literal noncommuting ten-field contact calculations retain
all3 polarizations, the covariance cross terms and error
square at nu1000,1e6,1e16. Wronskian tests reject the
constant-alpha reference as a new CCR-normalized state.
High-transfer contacts and a missing low-external
hypothesis are explicit negative controls.

The report has28 named exact checks covering51 scalar
entries,38 gates,9 controls and119 rejected scope mutations.
Each of20 report fields is mutation-tested. Eighteen
explicit scientific source files are hash-pinned.
Native, direct science, ordinary and CLI use original
SymPy. Only full regression uses the audited exact GCD
adapter. No frozen predecessor is changed.
