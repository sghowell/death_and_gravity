# S6.242 formulation — exact prepared-state difference, not the full heavy response

## Unchanged physical input and auxiliary comparator

Keep CD-REG-AFFINE-ISO-QG2-H8A420, the exact S240 SLE T, full covariant scalar prescription, fixed reference profile, S238 parent and S241 current quadratic/local response. The actual parameters are n=10^200/512+2 and kappa0=10^800. On the physical reference a=(1+t²)², the quantitative response interval is I=[-1/2,1/2].

Let S be the exact normalized massive KG solution initialized to full W6 data at t=-1, precisely the existing S240 comparison basis. It is NOT WKB evolution and is NOT asserted to be a newly physical Hadamard state. On the reference, T=alpha_p S+beta_p conjugate(S), with the complete fixed S240 coefficients. For later prepared metric perturbations both Cauchy covariances are held fixed in the common t0 neighborhood; no live reminimization occurs.

The object is the ENTIRE linear current response difference R_T-R_S, defined first with the same finite reference-mode projector and then by its absolutely convergent limit. The common state-independent counterterms and identical finite local action cancel in this difference. The remaining R_S is not set to zero. No nonlinear-family differentiability or finite-amplitude theorem is inferred.

## Full response and declared norms

Derive the exact first and second Hamiltonian variations for all ten ADM directions: lapse, three shifts and a general symmetric3x3 spatial metric logarithm. The finite covariance source retains both adjoint blocks. The complete current includes its instantaneous second-Hamiltonian contact. Its ordered Wick form reflects both momenta in the reverse branch before directions are complexified linearly.

Use v(D)=|n_D|+a|beta_D|+2||Q_D||F and
norm_phys=||sqrt(1+|P|²)v(D)||L2(I dP).
The whole normalized state-selection response plus its fixed mean-profile summand is below10^-1280 times the two physical norms. The profile split changes no physical sum, state or prescription.

In the actual common-clock scalar variables(n_lapse,v,B), use
norm_clock=||(1+|P|²)(|n_lapse|+|v|+|B|)||L2(I dP).
The complete matched summand has the same displayed10^-1280 bound. The nonzero current/profile second-chart contacts cancel only in the complete unprojected matched summand. At finite K their mean-tail remainder is explicitly retained.

The auxiliary reference frequency is Omega_p=sqrt(n+|p|²/16). For K>=2sqrt(n), both memory legs and the single contact leg use the same projector Omega_p<=K; the full profile stays fixed. The complete same-clock approximation error is below10^-1180/K times the same two norms, all external momentum.

## Boundary

The state difference and profile summand are an algebraic way to study the already specified physical response, not another matching model or finite counterterm choice. No global smoothness of the auxiliary comparator's profile summand is asserted. Required slab jets follow from the full reference comparisons.

The exact-comparison state's full renormalized response, complete quantum constraints and inverse, interacting light/mixed loops, quantum gravitational decoupling, nonlinear same-state bounce, physical UV, finite-gravity Regge and original V/G/B/P8 remain open. Seventeen scientific/proof/test sources and twenty report fields become immutable only after native freezing.
