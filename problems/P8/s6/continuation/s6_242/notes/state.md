# Exact fixed-state comparison and full physical readouts

The physical state T is the already specified S240 SLE. The auxiliary S is the EXACT KG solution initialized to the full W6 data at t=-1. Its later evolution is not replaced by W6. No claim that S is a new Hadamard physical state is needed for this finite-cutoff linear comparison.

The S240 complete mode proof supplies, for every momentum,

Omega²=n+|p|²/16,
|beta_p|<=B Omega^-11, B=10^83,
T=alpha_p S+beta_p conjugate(S),
alpha_p=sqrt(1+|beta_p|²).

The coefficients are fixed in time and in the shared Cauchy neighborhood. On later perturbed histories one propagates the two original initial covariances; one does not repeat the SLE minimization or apply a time-varying modewise selection.

The full exact comparison has energy-scaled phase y=(sqrt(Omega)chi,chi'/sqrt(Omega)), chi=a^(3/2)S, with ||y||1<20 on[-1,1]. Since a>=1, |H|<=2, |p|/a<=4Omega and m<=Omega,

||F_S||2<=8sqrt(Omega)||y||1<200sqrt(Omega).

This uses all five features, including the canonical time derivative S'=a^-3/2[chi'-(3H/2)chi], not just the field amplitude.

The actual mass gives B Omega^-11<1/2. The elementary square inequality alpha-1<=|beta|²/2 therefore gives

||F_T-F_S||2<=300B Omega^-21/2,
||F_T||2<=400sqrt(Omega).

Spatial reflection of the conjugate comparison mode changes only harmless feature signs. These bounds keep beta's complex phase, alpha-1 and all cross terms.

The same full exact-mode derivative recursion used in S240 gives finite constants for feature jets through five, with the j-th feature jet carrying Omega^(j+1/2). The complete T-minus-S stress difference then has radial order at most Omega^(j-10). This is integrable for j<=5. It justifies the required reference derivatives and conservation of the state-selection stress on the slab. No global smoothness of an auxiliary profile summand is asserted from this finite differentiability argument.

Common adiabatic subtraction and identical finite covariant matching cancel in the difference. Its mean energy and pressure are the absolutely convergent differences of the FULL physical mode readouts. Their uniform zero-jet bound is10^6 B n^-7/2. The remaining comparison-state subtracted response is a separate unclosed term.
