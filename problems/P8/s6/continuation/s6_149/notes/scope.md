# All nine primitive rows have scoped bounds; full matching stays open

Only scalar_Phi0_W0_F0 and gauge_Phi0 advance. The three
quadratic rows and four quartic rows are unchanged. No primitive
row is now wholly unevaluated: all nine have bounds in their
explicitly stated observable and subtraction scopes.

This is not completeness of the full two-loop model calculation.
The primitive ledger does not replace the separate counterterm,
finite parent-parameter and canonical-normalization ledger.
The complete vacuum reference, matched amplitude, pole and error
budget remain open. So do truncation and V contour/cut control,
finite-gravity G and the common-parent B domain/state/heavy/
cutoff/error dictionary.

No all-orders UV construction is added to the adopted finite-EFT/
necessary-positivity task. Scoped P8(a) and A.20-A.23 are unchanged.
Original P8(b) and original P8 remain OPEN.

The parent report and all eighteen source/proof/test files are
pinned. Numerical checks are independent checks of finite
integrals and dimensional continuation, not evidence that
replaces the written uniform bounds. Exact replay and written
analytic arguments are not formalization or independent peer
review. Native, ordinary, CLI and direct science use unmodified
SymPy; only full regression uses its separately audited adapter.
