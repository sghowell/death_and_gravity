# Entire original coefficient and exact lapse weights

Put m=1024, A=10^420, h=(1+u^2)^3 and
T=X^m/[X^m+(1-X)^m], b=m X^2 exp(-m X^2),
B=T+(1-T)b.
The literal current coefficient is
R=1+B(X-1)/h+m X^2(1-X)^8 exp(-A X^2).
The actual identity Gamma*K0=m and full heavy-parent binding
are checked. No switch, profile, mass or determinant is retuned.

For X>=0,0<=T<=1 and0<=b<=1, hence0<=B<=1.
The heavy correction is nonnegative.
On0<=X<=1 use the inherited full rank-regular floor
R>=1/2+1/(8m). On X>=1, R>=1.
Consequently R>1/2 globally for X>=0 and every real u.
This concerns this coefficient, not every auxiliary pivot.

On X>=1, T>=1/2, so B>=1/2 and R>=X/(2h).
Since |1-X|<=X and exp(y)>=y^5/5!,
deltaR<=m X^10 exp(-A X^2)<=m*5!/A^5<1.
The original remaining part is at most X, giving R<=2X.
On0<=X<=1, deltaR<=m X^2 exp(-A X^2)<=m/A<1,
so R<=2. All inequalities retain the entire heavy term.

With X=N^-2, for0<N<=1 one has
1/(2h N^2)<=R<=2/N^2; for N>=1,1/2<R<=2.
Thus each stated negative power is bounded by2.
The right limit of every inverse power at zero lapse is0.
The complete ratio R/X tends to1/(2h) as X goes to infinity:
T tends to1/2, while both exponential terms vanish by positive
exponential-series bounds. The proofs of integrability below use
the explicit inequalities, not an uncontrolled asymptotic replacement.

The full original reduced canonical Hamiltonian is retained in the
report, with its scalar/heavy/vector/metric, curvature, gradient,
Gauss and source terms. Its exact second derivative in either
M1 or H momentum is N/U; its trace-momentum second derivative
is-3N/(2M), and its curvature coefficient is-N C3.
The full homogeneous density has kinetic part-3(M/N)H_hat^2.
A literal second derivative of the complete S256 density verifies
this last binding; it is not guessed from the name of a coefficient.

Integrability of a selected coefficient is not an evaluation of this
entire Hamiltonian. All other channels remain; no quantum ordering,
global branch, constrained expectation or physical no-go is inferred.
