# Reproducibility, failed controls and immutable publication boundary

The18-file scientific manifest contains seven package modules,
two test modules, README, FORMULATION and seven proof notes.
The read-only report has20 fields. Its contract expects62 named
exact checks,141 scalar entries,52 proof gates, nine controls and
303 rejected inputs, preserving nine primitive and121 matching rows.
Matrix components are not independent scientific theorems.

Exact source checks bind both original switches and full R, the full
canonical Hamiltonian lapse weights and the complete homogeneous
density. Fifteen rational Laurent reconstructions and outward interval
bounds retain the full finite-width lapse derivatives. Complete
bounce rows, their nonzero momentum minor, weighted commutator,
pure-covariance inverse identities and radial lower bounds retain all
normalizations and the nonzero Tcorr term.

The complete cubic Gaussian polynomials keep every mixed term.
Their full centered expectations, square-moment ceiling, eighth-moment
bound and exact rational bulk/tail budget are checked. Exact singular
comparison primitives establish the different first/second-moment
integrability classes. Passing a finite cutoff diagnostic alone would
not prove either convergence or divergence.

Independent science diagnostics test full switch functions at finite
fixture parameters, pure noncommuting Gaussian covariances, covariance
lower bounds, positive joint momentum-row covariance, independent
symbolic center jets, correlated Hermite cubature and full nonlinear
Gaussian integrals. Cutoff-dependent full singular integrals and a
finite canonical-matter first moment are evaluated by variables that
expose, not numerically hide, the endpoint behavior.

The finite switch/localizer and moderate-variance fixtures are explicitly
diagnostics, not new actual parent parameters, original tiny-variance
numerics or a replacement state. RuntimeWarning is an error.
The actual tiny positive density at zero lapse stays symbolic and
is never replaced by an underflowed numerical zero.

Private exact/science preflight and Ruff checks precede freezing.
Fresh repository preflight/science, the original-SymPy native rebuild
and all private/repository/native source/report hashes are checked.
Independent ordinary, CLI and full regression must each reach observed
zero exit for their captured snapshots before publication.
Collection, partial progress or a different completed manifest does
not establish completion.

Native/direct/ordinary/CLI use original SymPy; only captured complete
P8 regression uses the separately audited exact-GCD adapter and its
original-path selfchecks. Frozen predecessor sources and raw reports,
including private originals, are never edited.
External assessments record real counts, timings, development failures
and later successful checks. The written functional-calculus, Gaussian,
covariance and integrability arguments are not FORMALIZED.
