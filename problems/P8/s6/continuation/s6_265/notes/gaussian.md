# Entire reference moments with a quantitative remainder

For a single field at any target time, apply bounded spectral functional
calculus to f_alpha(1+n), where f is the COMPLETE positive-lapse
inverse-power function, zero on nonpositive lapse. The Gaussian
state itself is unchanged. At the bounce ONLY, v_hat and n strongly
commute and permit a joint Gaussian spectral law.

The positive composite Vref=exp(3v_hat) f_(3/4)(1+n) is integrable:
f<=2 and every Gaussian exponential moment of v_hat is finite.
This is a full nonpolynomial background-map composite of LINEAR
reference fields, not the solution of the nonlinear auxiliary
constraints or an interacting physical volume expectation.

On |n|<=delta=10^-3 expand the full composite through total degree3
along the ray(v,n)->(sv,sn). The complete derivative bounds give
a fourth-order remainder at most
(10^8/24) exp(3|v|)(3|v|+|n|)^4.
All mixed terms are included.

With Cvv<10^-740 and Cnn<10^-490,
E(3|v|+|n|)^8<=128*105*(3^8 Cvv^4+Cnn^4)
 <10^5 Cnn^4.
Also E exp(6|v|)<=2exp(18Cvv)<4.
Cauchy-Schwarz yields a bulk error less than
(10^8*2*10^3/24)Cnn^2.
For the single-lapse functions set v=0; the same outward bound applies
without a joint probability interpretation away from the bounce.

The complete cubic polynomial has absolute coefficient sum at most1309,
using derivative bounds[2,12,192,4782] and3^i/i!.
Each degree<=3 monomial has squared Gaussian moment below10^5
when both variances are below1, by Cauchy-Schwarz and the exact
even Gaussian moments. Consequently E|Taylor3|^2<10^20.
This estimate does not require independent v and n.

On the complementary event, boundedness of the COMPLETE f and
Cauchy-Schwarz give a volume tail at most
4 exp[-delta^2/(4Cnn)].
The polynomial tail is at most2*10^10 times that exponential.
These are separate estimates; the local Taylor bound is not used there.
Set z=delta^2/(4*10^-490) and use exp(z)>=z^3/3!.
The resulting exact rational tail ceiling, plus the bulk ceiling,
is below10^-960. No underflowed probability is called zero.

Take the expectation of the COMPLETE untruncated cubic polynomial
before using centered-Gaussian odd-moment cancellation.
For each scalar coefficient this gives1+c2 Cnn/2.
For the bounce volume it gives
1+(9/2)Cvv+(9/2)Cvn+(3/8)Cnn.
Both statements therefore approximate the full nonpolynomial means
with error below10^-960. The complete cross covariance remains.
The full bounce volume mean differs from1 by less than10^-489.

Independent correlated Gaussian cubature, exponential-tilt integrals
and finite localizer fixtures test the complete polynomial, full
nonlinear integrals, fourth-order scaling and nonzero cross contribution.
Those finite diagnostics are not numerical evaluation of the actual
astronomically small original tails or a new state prescription.
