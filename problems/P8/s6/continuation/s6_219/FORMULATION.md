# Fixed formulation and result

Work on the original unit CD slab I=[-1/2,1/2], with a=(1+t^2)^2, the original actual all-order Gaussian preparation, Proca mass1000, kappa1e800 and the original fixed finite covariant prescription. Sources are real smooth symmetric spatial tensors, Schwartz in space, zero in the common initial neighborhood. The initial-only extension retains every upper retarded endpoint. Fourier coefficients are complexified linearly with the original unitary convention.

Let M[D]^2=||D||2^2+||grad D||2^2, Z136[G]^2=sum_r0..13||(1-Delta)^3 partial_t^r G||2^2. All tensor norms are Frobenius; the target is weak L2 time/Hminus1 space.

The exact full spatial response at the reference metric is
\[
 J_{\rm ren}(P)=H_{0,\rm full}
 +{\rm Known}_{\rm full}(P)-{\rm Known}_{\rm full}(0)
 +F_{\rm finite,full}(P).
\]
Known includes the actual-to-unit-W8 state/contact correction, correctly phased fifth endpoint and sixth bulk, and complete low/near/far UV-subtracted first five endpoints. The unit contact is kept in the full homogeneous anchor. Ffinite is precisely S216's six-invariant original finite term, not an adjustable target.

For nonzero norm product,
\[
 |J_{\rm ren}[D,G]|<2\,10^{95}M[D]Z_{136}[G].
\]
The exact homogeneous-anchored original-regulator approximation has error below3e54 M Z136/K for K>=2000. Both expressions vanish if a norm factor is zero.

Applying the original source-retarded and detector-advanced prepared maps and full one-point/chart Ward terms gives the entire reference Gaussian metric response
\[
 |R_{\rm full}(D,G)|<10^{118}V_{04}[D]U_{138}[G].
\]
V04 is the L2 norm of (1-Delta)^2 applied to the ADM tuple(n,beta,Q). U138 square-sums time derivatives0..13 of (1-Delta)^4 times that tuple. No reduced inverse or finite-amplitude conclusion is built into these derivative-losing coordinate norms.

Only the original reference parent Hessian is identified: the parent source and its first variation vanish there, and the second-source insertion has zero Gaussian mean. The determinant's nearby homogeneous C2 family is not a full sourced-parent nonlinear remainder.
