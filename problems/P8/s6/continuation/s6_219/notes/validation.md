# Independent verification and immutable replay boundary

The literal metric fixture constructs h=a^2 exp Q and its determinant/inverse numerically at high precision, retaining the full two-form field-strength contraction, mass, electric and temporal-constraint terms. Direct first and mixed second derivatives are compared against the full physical feature for I/I, I/S, S/I and noncommuting arbitrary-trace matrices. Deleting the temporal contact is a nonzero negative control.

The independent six-phase-space fixture compares the complete real covariance source with all nine annihilation-pair contractions under two distinct symplectic transports. An initial version used an unsqueezed block-diagonal source. All four correct comparisons passed, but the proposed wrong-phase control was degenerate because its source amplitude was real. The independent run therefore reported33 passed and4 failed. Before freezing, nonzero real symplectic preparation shears were added so the source pair is genuinely complex. All four stronger phase-control cases passed, and the complete37-test independent suite then passed. The physical current formulas were not changed to satisfy the control.

Original-cutoff fixtures integrate the literal ray intersection from the exact shell boundary to K, with angular endpoint p/(2K). They include every q0..4 scalar coefficient, the nonzero logarithmic shell and each source jet0,1,2 separately in all three ordered channels. At three increasing K values the remainder after K^3 and K subtraction has the checked1/K scaling. Omitting the grazing strip is nonzero. Independent integrated polynomial tests check the ordered formal transpose and show that replacing both cross kernels by one kernel fails.

Exact complex-dimensional tests include96 full scalar geometry/inverse-basis majorants,35 grouped transverse moments,30 normalized-to-original geometry identities and nonreal parameter probes. All full state/time/contact, low/near/far, shape-tail and prepared-response constants are checked against their source-pinned input inequalities. Full feature identities include arbitrary complex-tensor Frobenius contraction.

The integrated suite initially reported351 passed and one failed case-sensitive scope-string assertion ("Weak" versus "weak"). The assertion was made case-insensitive before freezing; no mathematical coefficient or bound changed. Formatting and import-order issues were corrected before final lint.

There are73 named identities comprising457 scalar entries,68 proof gates,nine controls and171 malformed/out-of-scope rejections. Twenty source/proof/test files and20 report fields are in the immutable manifest. Every report field, missing/extra fields and false primitive/matching completion are rejected. Private research notes are not part of the certificate.

Native, direct science, ordinary and CLI use original SymPy. Only the separately audited full regression uses the exact-GCD adapter. Fresh outcomes and timing belong in the external audit, not a report that modifies its own sources. The infinite-mode, angular, dimensional, Sobolev, source-germ and Ward arguments are written proofs; the numerical fixtures supplement them rather than establish continuum theorems by finite sampling.
