# Full state, time, endpoint and contact estimates

The extension from shear to arbitrary spatial directions requires hypotheses, not a change of labels. The full physical feature proof supplies ||S_D||op<=||D||F and ||C_DG||op<=||D||F||G||F. All original ten fields, including mA0, were already in the S186/S208 mode norms.

The full phase-stripped reference readouts satisfy ||v_ref||<=2*4000 sqrt(nu); the actual error satisfies ||e||<=4000 R nu^(-11/2), R<2e6 and R/nu^6<1. Consequently the complete full-spatial pair is bounded by64000000 sqrt(nu mu), below the original Cref=1e9. Its error is bounded by
\[
 4000^2R\sqrt{\nu\mu}
 [2(\nu^{-6}+\mu^{-6})+R\nu^{-6}\mu^{-6}]
 <3\,4000^2R\sqrt{\nu\mu}(\nu^{-6}+\mu^{-6}),
\]
below Cerr=1e15. This retains both mixed terms and the error square, with the original constant initial alpha, not a differentiated oscillatory mixing.

The full second contact now has a nonzero temporal block, but its product norm is unchanged. Therefore the complete three-polarization, half-Hamiltonian S197 covariance comparison and its original one-mode tail apply without enlargement. S201's occupation covariance identity applies to the same full feature and all ten fields. This controls contact DIFFERENCES only; the unit contact is not zero.

The S197 memory proof uses the full Cref,Cerr pair product, the exact retarded triangle and the all-transfer relation mu<=L nu and nu<=L mu. Its low/high external tail partition includes large-small internal pairs. The occupation comparison has the same hypotheses. Their actual-to-unit current bound3e23 and original tail2e27/K therefore extend to the full spatial form.

The six-step time identity uses the corrected annihilation branch throughout. The fifth endpoint reverses relative to the old mathematical row; the sixth bulk retains positive detector phase, negative source phase and positive imaginary part. Both have the same absolute inverse-phase/Leibniz majorants, giving1e48 and1e52/K for their sum. Combined state/time displays remain2e48 and2e52/K. No final source cutoff is used; all upper endpoints remain, and the common zero initial germ removes the lower ones.

For low real momentum the full pair retains the Cref bound and g<=2/(nu+mu); the complete row has the same harmonic-mean suppression and the unexpanded low band is still below2e38. On the joint domain |x|<=.01/(m+|P|), every normalized ten-field norm is below20. The full spatial pair is at most400, below the existing1000 bound. All first-five endpoint/source-jet Cauchy rows, near terms and far Taylor errors thus retain S208's constants. The ten algebraic products do not add ten independent physical pairs: the estimate is taken on the complete nine-pair sum BEFORE factorization.

It follows that Q_full has bound2e48 ||D||2 X46 and original two-leg tail4e53 ||D||2 X46/K. No noninteger-dimensional norm is used for these physical constants. Combining the two finite pieces gives
\[
 |{\rm Known}_{\rm full}|<5\,10^{48}M[D]Y[G],\qquad
 |{\rm Known}_{\rm full}-{\rm Known}_{\rm full,K}|
 <5\,10^{53}M[D]Y[G]/K,
\]
where Y^2=N61^2+X46^2. These are bounds on the entire spatial tensor form. All tests use the original mass and unit slab; neither auxiliary cutoff nor canonical normalization creates a new state.
