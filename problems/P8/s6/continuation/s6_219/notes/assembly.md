# Full spatial assembly and prepared metric reconstruction

At every original finite regulator the correctly oriented full Gaussian current is R_actual-unit+F_correct+Q_correct+C_unit+U_correct. The first/second constrained feature bridge proves that this is the actual metric tangent rather than an unrelated stress-pair formula. The same finite-mode covariance source includes both reverse blocks and the full second metric contact.

S218 supplies the actual full homogeneous trace/tracefree kernel H0 and its identity-multiplier bound1e95||D||2 Z130. Both ordered trace/tracefree cross anchors vanish separately by rotation covariance. The full QQ bare contact is local in the two external tensors: no derivative acts on D or G in its Hamiltonian coefficient. Its one-mode Fourier integral is consequently independent of the external transfer. It cancels only in the P-minus-zero comparison, while its nonzero value remains in H0.

The full UV difference is S216's six-invariant coefficient, with its exact original dimensional finite term. The complete original shape calculation gives the new scalar A3/A1 and retains the corrected tracefree rows. Therefore
\[
 J_{\rm ren,full}(P)=H_{0,\rm full}
 +{\rm Known}_{\rm full}(P)-{\rm Known}_{\rm full}(0)
 +F_{\rm finite,full}(P).
\]
No previous finite remainder or local-action target is added again. The explicit zero-transfer Known kernel is lifted as an identity multiplier before Fourier estimates, not obtained by point-evaluating an arbitrary weak operator.

Y<2Z136 and Z24<=Z136, and the original finite six-invariant term has full Frobenius bound1e6||D||2 Z24. Thus
\[
 10^{95}+4(5\,10^{48})+10^6<2\,10^{95}.
\]
This controls the complete spatial form before any scalar decomposition. The two ordered normalized cross forms have no homogeneous anchor, so each is bounded by4(5e48)+1e6<3e49; the normalized trace/trace form is bounded by2e95. Equal upper bounds are not equality of retarded cross kernels.

The homogeneous-anchored original cutoff subtraction is
\[
 K^3A_3+KA_1+F_2(K^2-m^2)/2+F_4\log(K/m)-F_{\rm finite}.
\]
The exact approximation error is Known_K(P)-Known(P)-Known_K(0)+Known(0)+E_K. Its numerator is below4(5e53)+1e40<3e54. K>=2m is the original auxiliary-regulator domain. The finite lower band and logarithmic shell stay explicit; this is not a physical EFT cutoff or an unanchored cutoff rate.

For full ADM metric directions, use S215's exact source-retarded and detector-advanced synchronous maps. They require no inverse H or momentum. Their spatial bounds are M[Dsyn]<=100 V04 and Z136[Gsyn]<=1e20 U138. The complete density/chart Ward correction is below1e35 V04 U138. The spatial formulas need only the original zero initial source germ; all upper endpoints were retained. The detector can be cut off inside the initial zero-output neighborhood at no time-derivative cost in M.

Consequently the entire reference Gaussian metric response, including lapse and shift, is below
\[
 [100\,10^{20}(2\,10^{95})+10^{35}]V_{04}U_{138}
 <10^{118}V_{04}U_{138}.
\]
As a separate arithmetic check, inserting Ltt=2e95 and Lt0=L0t=3e49 into S215's three-missing-block implication also fits1e118. Its geometric conditional statement is now instantiated by actual reference kernels.

The current is a bounded weak derivative-losing reference operator. A scalar constraint reduction changes the norms and may remove naive kappa suppression; none is inferred here. The two spatial canonical factors give8e-705 and12e-746/K only in their stated coordinate weak norms. No same-space inverse, nonlinear continuation, quantum bounce or stability follows from these displays.
