# Full scalar dimensional dominated limit

Retain the original disk |d-3|<=1/4, all four continued W8 orders and both original complex time discs. Physical reference norms are not positive Hilbert norms in noninteger dimension. The proof instead continues the exact finite active/passive tensor algebra and provides coefficient-wise bounds.

For each of the three new ordered scalar channels, all ten exact far geometries are rational in eta=d-3,y and u. On |eta|<=1/4,|y|<=1/100,|u|<=1, the algorithm separates each denominator's constant coefficient from the absolute weighted sum of its other coefficients. The positive difference is a verified denominator floor. Numerator coefficient sums divided by this floor give30 explicit far bounds. The same method verifies all36 inverse six-invariant basis entries; their poles do not enter the disk.

The low/near region needs separately normalized real directions, not the small-y expansion. Write n=(cX,cY,cZ,u), w=(hX,hY,hZ,v), with |c|,|h|,|u|,|v|<=1 and common transverse orientation X,Y,Z. Here Z is a grouped radius, not a positive extra dimension. The normalized projector/two-form algebra, including d-4 passive trace terms, has30 scalar polynomial expressions. Their exact coefficient sums give uniform bounds with only safe dimension denominators.

To connect those expressions to the original geometries, set c=1,h=-1/R,v=(y-u)/R with R=sqrt(1-2uy+y^2), then multiply by the appropriate R power0,1 or2. All30 azimuthal identities are checked exactly. This verifies that the normalized near representation is the same original vertex, not a separate ansatz.

For grouped moments X^(2A)Y^(2B)Z^(2C), the absolute dimension-dependent factor is bounded by
\[
 (1/2)_A(1/2)_B(1/8)_C/(7/8)_{A+B+C}.
\]
All35 cases of total degree at most4 are checked to be<=1. Odd X/Y moments vanish, and grouped Z powers are even. Thus the coefficient-sum bounds survive the analytic transverse average uniformly for all real momentum pairs. The normalized representation removes spurious small-|ell| projector denominators; at a zero internal momentum choose any real direction, with no singular massive amplitude.

The full scalar C amplitude carries rk*r_l/(a^2 omega_k omega_l), not an inverse bare momentum. Each r/(a omega) on the complex clock discs is bounded by2 using the unchanged mass gap and relative scale/frequency bounds. Thus |C_normalized|<=2|pk pl|. The continued longitudinal rate is bounded by3[(1+1/4)/2+1.01]=981/200<5; the transverse rate is smaller. All four explicit W8 defects remain below1/1000. The full scalar modes, inverse phases and ten products therefore have finite d-uniform constants and no additional transfer degree.

On real clock times the entire frequency-phase defect is below1/40 per leg. Both opposite exponential phases have modulus<20/19<2. The analytic current is(Fplus-Fsharp)/(2i) at the same complex d; conjugation of d would destroy analyticity and is not used.

The complete source-jet Cauchy recurrence now gives far bound C_j U^(5-j)r^-4 with U=m+|P|. The dimension factor yields integrable radial envelope r^(-7/4). Low r<m has integrable r^(7/4), while the near raw and all UV power/log rows have transfer degree at most21/4<6. The angular weight has integrable envelope(1-u^2)^(-1/8), with its gamma normalization finite on this disk. The source norm supplies the required six spatial derivatives. Dominated convergence therefore identifies the full scalar dimension limit with the physical Q, for all external momenta in the admitted pairings.

First reconstruct all six invariant coefficients and only then differentiate while keeping the physical source invariants fixed, as in S216. The actual-state correction and finite time remainder stay in physical dimension. The finite constants2e48 etc come from the proved physical full-feature estimate, not from an unspecified continuity bound in d.
