# P8 S6.182: a separately named fixed Gaussian retuning

The S6 conditional contract permits future corrections on the
clock tube only as a new candidate with a stated error budget.
Construct CD-REG-AFFINE-ISO-QG1. Do not overwrite S6.174 or
silently reintroduce an older profile into that unchanged action.

Add one explicit fixed scalar coefficient DeltaF(u,X) to the
complete regular affine/CD/M1 parent. Compute its coefficient
functions once from the specified reference Gaussian vector
state, full CD history and unchanged covariant prescription.
Hold them fixed under every later source/state/metric variation.

Cancel the specified vector one-point stress on the CD history
and the same-prescription Minkowski vacuum energy in this one
new parent. Quantify mixed coefficient derivatives on the full
original X strip and sharpen them near the clock. Preserve the
known finite vacuum scalar germs through a high-order switch.

Re-derive the actual new stationary metric currents, including
all profile, density and nonstationary chart contacts. Retain
the nonlocal vector response. Establish the applicable prepared
retained linear inverse and its local lapse-recovery bound.

This is a controlled fixed coefficient choice for the
classical-plus-specified-Gaussian-vector truncation, not a full
interacting quantum solution, stability, a physical cutoff or
UV matching. No naturalness assumption is imposed or inferred.
Global smoothness and exact retained mean identities are
distinct from the compact quantitative derivative bounds.
Original V/G/B and P8 remain OPEN.
