# Verification boundary

Exact checks use the complete rational switch, literal
clock and vacuum jets, the actual covariant vacuum
coefficient, scalar Ward equations, arbitrary-lapse
profile density and currents, nonstationary chart
contacts, complete fixed-canonical pullback, continuous
complex-disc Cauchy constants, the actual classical
lapse pivot and the new prepared local response.

The source-defined reference stress is the exact same
mode integral and state, not a free symbolic ansatz.
Its smoothness, Ward identity and quantitative derivative
bounds are bridged through rebuilt frozen source proofs.
The new functions are then fixed as action coefficients.

Independent tests differentiate literal coefficients,
evaluate continuous-bound fixtures at high precision,
check alternate density/current and charge normalizations,
retain nonzero response controls and verify coefficient
and vacuum scope. Numerical samples supplement, not
replace, the global rational/complex-disc written bounds.

The source manifest explicitly lists18 scientific files.
The report has20 fields, with complete equality,
per-field mutation rejection, unsupported-scope rejection
and source-hash checks. New scientific bytes are frozen
before independent native report capture. Prior
scientific bytes and reports are unchanged.

Native, direct science, ordinary and CLI use original
SymPy. Only full captured regression uses its audited
exact GCD adapter. Written analytic and external
Hadamard arguments are not FORMALIZED. No retained
one-point cancellation is called full quantum closure.
