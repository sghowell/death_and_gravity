# Exact retained mean equations and compatible reference state

All equations below are divided by kappa. For a physical
scalar density F(u,X), its clock pressure is F and energy is
2X F_X-F. The new clock jets therefore give

    p_profile=-p, r_profile=2B-A=-r.

They cancel the actual specified Gaussian vector one-point
stress, not merely its finite local piece. The original CD/M1
classical background equations were already zero.

The scalar profile Euler expression on u=t is

    A'-2B'-6HB=r'+3H(r+p)=0,

by the actual covariant vector Ward identity. The retained
Gaussian functional has no separate clock scalar force on
this homogeneous reference: its connected operator depends
only on the physical metric, and the complete affine source
has S=0 and vanishing full mean source/contact force.
The new term has no connection or M1 equation. Thus all
retained mean equations, not only one Friedmann component,
hold on the unchanged CD history with its original matter
charge and affine/vector mean.

The coefficient functions are defined from the full smooth
reference history. These mean identities hold at every
finite reference time. This does NOT provide a uniform
global-time small-correction or perturbation bound; the
numerical coefficient error budget below is only for
|u|<=1/2. Global nonlinear stability is not asserted.

At the Minkowski vacuum u=0,du=0, the same new coefficient
equals -p_v, with vanishing nonconstant low-order jets.
Its energy is +p_v and pressure -p_v. They cancel the
conditional Minkowski vacuum's energy -p_v and pressure +p_v
in the SAME prescription. The new classical term alone need
not have zero cosmological constant: the claimed vacuum is
stationary for this explicitly retained quantum truncation.

The CD and Minkowski quantum observables are distinct states
of the same conditional field theory on their respective
backgrounds. No global scattering history or single preferred
state interpolating between them is assumed.

The CD metric, Proca operator, mass and Cauchy surface remain
unchanged. The old all-order state is therefore already
compatible with the new stationary retained reference.
No arbitrary higher metric jets or covariance are reset,
and no nonprepared reference residual is fed into the
prepared causal inverse.
