# Actual stationary response, with fixed profile contacts retained

Let r,p now denote the fixed normalized physical reference
stress. For a homogeneous metric perturbation n=delta N,
v=delta log a, the literal profile density
N exp(3v)[A+B(N^-2-1)] has mean currents (r,-3p) and Hessian

    [ -(r+p)    3r  ]
    [    3r     -9p ].

The vector's mean currents are (-r,3p). Its uncancelled
physical current variation is
(-delta r-3r v, 3delta p+3p(n+3v)).
Adding the fixed profile Hessian gives

    E_N=-delta r-(r+p)n,
    E_v=3delta p+3(r+p)n.

The coefficient functions are NOT differentiated as functionals
of the perturbed metric. In particular no nonlocal delta r
or delta p is cancelled by an adaptive profile.

Under the actual clock chart, first-map pullbacks act on the
sum and second-map terms multiply the mean current. The
individual profile and vector second-map contacts are
-3p omega_NN and +3p omega_NN. They cancel in the total
because its mean current vanishes. They are not set to zero
separately, and the unretuned S6.179 result is unchanged.

Use prepared coordinates n=eta',v=w+H eta. The exact same
Proca time covariance and selected state give
delta r=R[w]+r'eta and delta p=Q[w]+p'eta.
The varied Ward identity is
R'+3H(R+Q)+3(r+p)w'=0. With S=r+p, the actual new currents are

    E_eta=S eta''+p'eta'-3H'S eta-3S w',
    E_w=3Q[w]+3p'eta+3S eta'.

The time channel is local, but its second-derivative term is
not dropped. Their local compact density is
-S eta'^2/2-3H'S eta^2/2-3S eta w'.
The code checks this density, the physical current route,
and the old UNCANCELLED response plus the literal newly
added profile as independent algebraic routes.

The actual S6.180 classical reference tree remains the
reference part of the new equations; the new scalar action
is included explicitly in the local currents above. The
fourth coefficient A=-6delta^2 and matched massive scalar
F_m are unchanged. The change is local of at most second
time order, so after four prepared primitives it is a
bounded Volterra kernel. The full nonlocal pure-scale
operator, evanescent finite matching and all continuum
frequencies remain those already matched. No old complete
profile-total tree or inverse is copied blindly.

The existing Volterra argument therefore gives unique
smooth prepared retained linear responses about this
stationary reference. The same scalar inverse has the
S6.181 half-line norm bound below3e11. The full curved
remainder constant and a useful/small full inverse norm
remain unevaluated.

For local physical lapse recovery, use the actual first
row coefficients: S at time order2, p' at order1,
-3H'S at order0, and -3S at scale order1. Their I4 kernel's
first-output derivative is bounded by, respectively,

    7epsilon, (3/2)epsilon,12epsilon,9epsilon.

The sum is (59/2)epsilon instead of the old
(21/2)epsilon. Thus the complete C1 is the already quantified
S6.180 C1 plus19epsilon, still below15000. No arbitrary
C0 scale is differentiated to reconstruct the lapse.

This response result neither proves nonlinear stability
nor makes the full inverse small. Mean stationarity of
this retained truncation is a different result.
