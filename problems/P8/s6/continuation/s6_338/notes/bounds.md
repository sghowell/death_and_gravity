## Original-source remainder and finite interference

Let A_m be the original tuned matter Born amplitude and A0 its full
positive matter-plus-gravity Born value. The ORIGINAL contact is
C=-g^2[3/(n-2)-2/(n-2)^2]. Its symmetric value is

A_sym=C+3g^2/(n-4/3)
     =4g^2/[3(n-2)^2(n-4/3)]>0.

Convexity of 1/(n-a), with s+t+u=4 and every channel below n,
gives 0<=A_centered=A_m-A_sym<A_m<A0. Equivalently use the exact
second-order divided remainder at a=4/3, whose linear terms sum to zero.

The canonical nonmetric current change is at most608/sqrt(kappa);
the original matter radiation remainder is at most
330000 A_m/sqrt(kappa) by S295/S335. The centered remainder equals
R_m-A_sym(J_actual-J_Born). Thus its canonical Frobenius norm is at
most330608 A0/sqrt(kappa), which also controls both physical TT
polarizations without selecting one helicity.

At the original source n<10^200/256 and kappa=10^800,
|Q|<10^(-600)/4608<10^(-603), using16pi^2>144. Therefore

||R1_source||_TT/A0<10^(-598)/sqrt(kappa),
|A1_source|/A0<10^(-603),
R1_source=M5_source-A1_source S0.

Three original rational recoil states, both physical polarizations,
independently compare the 42-plus-counterterm sum with the complete
centered tree; the remainder is nonzero. Samples calibrate the
formula and do not substitute for the written uniform bound.

For delta=min(1,-t,-u), a=delta/192, ell=min(x,a),
0<=x<=1/8, r=10^(-598), the actual-phase integral of
|2Re<Mtree,R1_source>|/A0^2 is bounded by

r[128ell+4e9 ell^2/delta]/(4pi^2 kappa)
 + 1_(x>a) 1e18 r(x^2-a^2)/(kappa delta).

This uses the complete original47-tree low/high bounds of S335.
It is <10^(-1394) for x<=a, and its lower-cutoff tail vanishes.
The high bound is finite at fixed nonforward angle, not uniform at
the forward poles. The divergent leading-soft extension is separate.
Adding the known S337 local tadpoles gives the conservative ratios
r_total=10^(-597), h_total=10^(-602), with low interference
<10^(-1393). Other loop sectors and matching coordinates remain open.
