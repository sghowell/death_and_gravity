# Validation boundary and independent controls

Check the complete24 source labels, symbolic-D tadpole and massive-loop
insertion, covariant product rules and Green-function delta contacts,
both independently completed massive triangles, their exact-D and
finite-MS parameter identities, all24 EOM marked-field emissions,
three generic traceful Ward identities and three generic TT reductions.
Recover the complete frozen S239 four-point source result and original
Born contact exactly, retaining all previous scope qualifications.

The independent component calibration uses the frozen S295 complete
matter engine at three original rational recoil states and both
physical polarizations. The source remainder is nonzero. Convexity,
canonical current-change estimates and original inequalities prove
the uniform bound; these finite states only calibrate it.

Negative controls retain the nonzero kernel insertion, the separately
nonzero EOM external and metric terms, and the wrong single-endpoint
operator variation. Domain/type guards reject floats, invalid energy
resolution, forward endpoints and unsupported sector promotions.

Private research caught and corrected three diagnostic setup errors:
a nonzero Symbol assumption in a solve, Float promotion from a Python
trace default, and a scratch symmetric-Born formula with the wrong
contact. Failed probes are not counted as successful evidence. Final
code sympifies the trace, checks exactness, and directly recovers the
unchanged frozen contact.

Scientific tests use original SymPy. Source, independent fresh-copy
and whole native report checks precede external publication acceptance.
The full report has20 fields and every mutation must be rejected.
The exact algebra and written proofs are not kernel formalization.
Later run timings and acceptance receipts belong outside this frozen
source manifest, not in a rewritten scientific certificate.
