# Independent checks and immutable validation boundary

Four exact packets check the actual coefficient/characteristic pair, both complete finite-q charts, all48 coefficient enclosures and the full energy/phase conversion argument. Positive polynomial numerator coefficients and positive-even denominator envelopes are exact continuous estimates. Polynomial majorants cover the entire declared jet/momentum boxes.

The independent fixture separately enters the full retuned weighted Hamiltonian with a nonzero smooth rational retuning. At fourteen chart/time/transfer combinations it computes the original phase generator and time derivative, differentiates the coordinate map, and checks the complete second-order Euler equation. Its retuning is a deliberately nonzero algebra fixture, not a numerical stand-in for the actual tiny expectation values.

Six separate finite-q auxiliary saddle solves recover K,B,D from the literal stationary action and reconstruct two distinct original phase vectors. Another fixture differentiates the energy along an arbitrary curve with a nonsymmetric lower-order matrix and its calculated forcing. Negative controls detect deleting the weighted transport boundary and show the complementary chart degenerates at q=152/25 although the original phase Hamiltonian remains regular.

The first independent suite passed29 tests in74.04 seconds. Earlier exact coefficient/principal/full-chart module checks passed, and the complete48-entry and energy module probe passed in58.42 seconds. A conservative central forward-velocity display was strengthened before freezing to show the matter velocity contribution explicitly; its500 bound was unchanged. Both valid chart characteristic determinants were also added before freezing. Integrated and fresh outcomes are recorded in the external audit.

The source manifest contains seven package files, two independent science/certificate tests, two root markdown files and seven proof notes:18 immutable inputs. The native report has20 fields. Every field, source hash, missing/extra field and false primitive or matching completion is checked. Fresh replay outcomes belong in the external audit, not a report that changes its own frozen input.

Native, direct science, ordinary and CLI use original SymPy. Only the full P8 regression uses the separately audited exact-GCD adapter. The full launch explicitly retains the S219 independent-helper PYTHONPATH allowance; no frozen predecessor test is edited. Chart patching, Gronwall, Duhamel, Plancherel and dominated continuity are written proofs, not FORMALIZED.
