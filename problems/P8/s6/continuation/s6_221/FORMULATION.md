# S6.221: uniform two-chart classical scalar comparison

Fix the original unit CD slab I=[-1/2,1/2], a=(1+t²)², the original state, mass1000, coupling and current frozen S182 QG1 profile. Start from the complete S220 coefficient-sector Hamiltonian with both scalar phase pairs and all retuning terms. No predecessor is edited.

The scoped objective is a uniform polynomial-momentum comparison for this classical coefficient-sector linear system, not elimination of its nonlocal quantum constraints. For Z=(v,sigma,pv,ps), q=|P|²/a² and every ordered s<=t in I, establish
||U(t,s;P)|| <= exp(1e29)(1+|P|²)^6.
Consequently, for any real r, initial data in H^(r+12) and mathematical additive phase forcing in L1(I,H^(r+12)) give a unique comparison evolution in C(I,H^r), with the same bound on the sum of those two input norms.

The proof uses the regular original phase system at |P|<=100. Above that transfer it uses the v chart on |t|>=1/8 and the complementary b=-pv/(2q) chart on |t|<=1/4, with switches at+-3/16. No division by Theta is used through its zero. The b chart is not claimed regular at every finite q.

The earlier core p8.gamma module already provides complementary principal algebra. Its exact source hash and factor-two convention are checked. This successor's new content is the full current retuned finite-q action, explicit chart domains, actual coefficient jet estimates,48 uniform coefficient enclosures and a quantitative patched phase propagator.

Completion requires exact full-chart reductions, actual coefficient/denominator positivity, all lower-order terms and q time derivatives, a full energy identity, explicit conversions, independent canonical-flow/saddle tests, negative controls, an immutable native report and fresh ordinary/CLI/full validation.

This estimate loses twelve spatial derivatives and has a constant that is not small relative to kappa. It is not a same-space inverse, a contraction for the Gaussian response, a physical-source map, quantum stability, a nonlinear solution or a physical EFT cutoff. Full quantum constraints, sourced-parent remainder, background/heavy-sector control and original V/G/B/P8 remain open. Scoped P8(a), A.20–A.23 and all nine primitive statuses are unchanged. Written continuous arguments are not FORMALIZED.
