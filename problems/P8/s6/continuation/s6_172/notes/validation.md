# Evidence and replay boundary

Exact residuals check the correlated local cancellations, full
rational component sums, lapse/scale Einstein equations, positive
clock term, complete central-interval factor, signed additional
budget, and the actual clock-map first jet. All inherited reports
and scientific bytes remain frozen.

Independent tests derive the connection, Ricci tensor and physical
null Einstein sign directly from the metric, test finite map
variations rather than only map values, retain nonzero opposite
local components at700-digit precision, and check positive scalar
metrics and gauge sources. Large additional budgets are accepted
as diagnostics with nonpositive margins rather than silently
declared excluded. A leading nonminimal term cancels the pointwise
null equation but loses positive Planck coefficient, preventing a
claim that this elementary control is a healthy all-time solution.

All own payloads must pass the unchanged exact serializer before
freeze; decimal diagnostics are strings. Native, direct science,
ordinary and independent CLI use original SymPy. Only full
regression uses its separately audited exact GCD adapter.
Numerical controls supplement written identities and inherited
analytic bounds; they are not a full interacting calculation,
validated numerical integration or formalization.

The later root assessment records fresh native/source hash checks,
repository science, ordinary and CLI replays and the captured full
suite before publication. No runtime result is fabricated inside
the frozen source manifest.
