# S6.240 formulation — actual added-heavy-field Gaussian reference

## Fixed inputs and separately named candidate

Freeze S238's entire covariant H8A420 parent and S239's full classical-limit first loop with its H8A420-VAC-OS4 finite prescription. Keep the physical CD metric a(t)=(1+t²)², u=t,X=1, the old Proca/M1 choices and the recomputed S238 affine mean. Do not reset the old connection trace. The added classical heavy mean is H_heavy=0. Its mass squared is n=10^200/512+2, kappa0=10^800. The complete heavy source has an eighth-order clock zero, so the heavy Gaussian operator on this reference is precisely minimal massive KG.

The new candidate is **CD-REG-AFFINE-ISO-QG2-H8A420**. Its new scalar coefficient is specified below; no frozen candidate is overwritten. Quantitative estimates cover t in[-1,1], all comoving p>=0 and both physical stress components with derivatives0..5. The global state and coefficient are smooth at every finite real time; a global-time numerical bound is not asserted.

## Actual state and renormalization

The state **H8A420-SLE-PRE-T0** is the exact massive state of low energy for the fixed nonnegative normalized weight w=f². Put x=8(t+5/8), phi=exp[-1/(1-x²)] on |x|<1 and0 elsewhere, Z=integral phi²dt and f=phi/sqrt(Z). The sampling support[-3/4,-1/2] precedes the common t0=-1/2 Cauchy surface.

Its definition uses all exact KG modes and the phase-regular minimizing coefficients, not an instantaneous vacuum or a finite adiabatic-order state. Fix positive frequency exp(-i omega t), physical Wronskian i/a³ and W2(t,t')=T(t)conjugate(T(t')). The applicable primary Hadamard theorem supplies regularity; a separate explicit comparison proves the quantitative estimate.

For later smooth compact metric/source perturbations strictly to the future of the shared Cauchy neighborhood, retain this covariance and evolve it with KG, adding the retarded smooth coherent mean. No re-minimization or state reset is allowed.

Use full fourth-order adiabatic subtraction plus the finite scalar local action matched by varying the complete spatial-D=3-2epsilon pole counteraction before taking its limit, at mu=1. The prescription retains the nonzero heavy flat vacuum pressure. The old Proca scheme stays unchanged.

## Fixed local profile and certified outputs

Define rho_H_ref(u),P_H_ref(u) to be the actual full renormalized stress integrals just specified, not freely adjustable functions. Let T=X^1024/[X^1024+(1-X)^1024], p_v_H=(3/2-log n)n²/(64pi²), p_v_Phi=3/(128pi²), and

DeltaF_HQ={-p_v_H+T[-P_H_ref+p_v_H-(rho_H_ref+P_H_ref)(X-1)/2]-p_v_Phi(1-T)}/kappa0.

These reference functions are computed once and held fixed in all later variations. This is a local physical coefficient, not live nonlocal stress subtraction. It is C-infinity in u, not asserted real analytic.

The actual heavy energy and pressure, and all their first five derivatives, are bounded by10^-400 after normalization by kappa0. The displayed fixed profile restores the retained Proca-plus-H Gaussian mean equations on the same physical CD metric and S238 affine mean. It also cancels the heavy and mass-one light flat Gaussian vacuum constants in the formal limiting-action one-loop vacuum. Nonconstant terms start at field degree2048 and cannot enter S239's first four-point loop. All complete normalized clock four-jets of the new profile, prior finite extensions and classical additions fit a NEW10^-399 budget on |u|<=1,7/8<=X<=9/8.

## Exclusions

The new scalar clock jets change the light Hessian. No old stability row, full quantum response or compatible inverse estimate is transferred automatically. No full interacting light/metric clock state, omitted-loop error, quantum gravitational limit, nonlinear same-state bounce, physical UV, finite-gravity Regge or original V/G/B/P8 closure is asserted. A small reference stress and a coefficient tube are not substitutes for those requirements.

Seventeen source/proof/test files and twenty certificate fields form this result's immutable manifest. The nine original primitive statuses and all prior matching records stay unchanged; one separate Gaussian-state/reference-mean record is added.
