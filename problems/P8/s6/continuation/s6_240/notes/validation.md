# Independent validation and immutable replay contract

The science suite checks every physical mode/CCR, dimensional, Ward, profile and exact bound identity, every gate and every unsupported input. It preserves the nine primitive rows and95 earlier matching records. The new result has57 named exact scalar checks,74 gates,nine controls and343 rejected unsupported input/scope cases.

Three independent diagnostic families supplement the written proofs.

First, exact KG modes are integrated at finite test mass squared25 and comoving momenta0,2,10 by DOP853. Independent160- and256-node Gauss rules agree on the complex c2 and beta, not merely their magnitudes. The selected smeared energy, vanishing self-pair, physical Cauchy commutator at13 times and basis-independent covariance are checked. Separate80-digit fixtures include a genuinely complex c2 and the c2=0 limit; conjugating the wrong beta phase fails.

Second, independent Taylor-jet arithmetic evaluates all seven UNEXPANDED WKB iterations at the ACTUAL n=10^200/512+2. Tests use1800 versus2000 decimal digits and compare the fully scaled W6 residual for p/M=0,1,4 at a complex time. The discrepancy must be below10^-150 relatively, and the residual must be nonzero and inside the exact analytic bound. Auxiliary complex epsilon-disk values check the complete square-root branches and readout bound. Direct finite-mass physical mode energy and pressure independently verify the a^-3 volume factor and detect the incorrect a^-3/2 factor. These numerical checks are diagnostics, not a replacement for the uniform all-momentum complex-domain proof.

Third, an independent lapse-and-scale Euler-Lagrange implementation retains the lapse derivative and scale-factor second derivative in the full1,R,R²,Ricci²,Riemann² actions. Exact rational fixtures in spatial dimensions3,4,5 agree with both stress components. Independent expansion of the whole auxiliary energy AND pressure checks every adiabatic0,2,4 coefficient. The false component-only dimensional subtraction remains a negative control.

Additional checks retain the full exact mode cone, the arbitrary-D pointwise Ward identities for all three complete adiabatic mode subtractions, actual mass without floating underflow, exact full radial antiderivative, all sampling/Volterra/forced-jet constants, required1/2048 tail margin, complete fixed profile, both vacuum constants, high-order vacuum/clock switches, changed-Hessian scope and the new combined tube budget.

The source manifest has17 files: six modules, two tests, two root documents and seven proof notes. The certificate has20 fields. Read-only replay rejects mutation of every field and any missing/extra field and checks every source hash. The external assessment records actual private corrections and post-freeze outcomes.

Before freezing, require private science, lint/format, exact17-file/20-field preflight and the protected long-lived native rebuild. After freezing, independently rerun repository science, ordinary pytest, standalone CLI and the full P8 regression. Only the latter uses the audited exact-GCD adapter and the S219 helper allowance. Native/direct/ordinary/CLI use original SymPy. Source, proof, test and report bytes become immutable; publication includes only the exact21-file manifest and excludes unrelated work. These algebraic and analytic arguments are not FORMALIZED.
