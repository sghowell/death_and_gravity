# Immutable sources and complete read-only replay

The source manifest is six package modules, two test modules,
two root Markdown files and seven proof notes: 17 files.
The report has 20 AST-derived top-level fields and pins the
S259 raw parent SHA256

c259d101b5029c4b50dacc29cfc9597653885bfd2ef172dbe6a0849119658c65.

The packet contains 45 named identities, 413 scalar entries,
34 proof gates, nine controls and 259 rejected inputs. The
counts measure exact algebra and regression checks, not that
many independent physical theorems. All nine primitive and
115 previous matching items remain unchanged.

Before freezing, run the entire private local payload and
all independent science tests with original SymPy and
RuntimeWarning treated as an error. Check the complete source
manifest, source bytes, field structure, exact residuals,
scope and all controls. The local preflight does not replace
the later full native ancestry rebuild.

Freeze only after success. Copy exact bytes into the new
repository checkpoint and repeat a fresh direct payload and
science run there. Native construction uses original SymPy,
rebuilds the whole ancestry and protects every earlier
scientific source and report. Transfer the raw ASCII JSON
by exact offsets and lengths with observed CONTINUE
acknowledgments. Validate raw SHA256 without reserializing,
all private/native/repository hashes and all 20 fields.

Ordinary package tests and CLI replay use original SymPy.
Only a separately captured full regression may use the
audited exact-GCD adapter and its self-checks. Include the
existing S219 helper path in the launch:

    PYTHONPATH=problems/P8/s6/continuation/s6_219/tests \
      .venv/bin/python scripts/p8_replay.py full

Do not edit frozen science to repair a launch, formatting
or test error. Only observed zero exit statuses count as
completed validation. External publication notes record
actual results, timing, raw hashes, development failures
and retries. Publish only after all required checks, with
an explicit exact manifest and independent remote-hash
verification, preserving unrelated P4/P9 changes.

Independent tests compare exterior products to full finite
Fock matrices, solve the whole seven-ghost matrices by a
separate numerical method, integrate the complete finite
source-dependent Gaussian and test a noninvariant boundary
weight. They retain invalid-domain, translation and omitted
time-contact controls. These finite diagnostics support
the written classical/conditional proofs; they do not
FORMALIZE or complete the quantum continuum problem.
