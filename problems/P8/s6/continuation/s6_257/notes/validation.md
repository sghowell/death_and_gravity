# Immutable source and read-only replay contract

Freeze only after the entire private source set, serialized payload,
scope controls and independent science diagnostics pass with original
SymPy. The manifest is six package modules, two test modules, two root
Markdown files and seven proof notes: 17 files. The certificate has 20
top-level fields and pins the unchanged S256 parent report SHA256

    260d5c682b63eee5634810a421e969c01f1ef531558d78be2147020899ed50b8.

The packet contains 53 named identities, 451 scalar checks, 37 proof
gates, nine controls and 294 rejected inputs. Independent diagnostics
solve the original joint trace/temporal Euler equations, evaluate
nonlinear root Jacobians, form complete finite-cell brackets including
nonzero spatial secondary couplings, and inspect the full 32-dimensional
combined point/boundary phase Jacobian. These are finite-parameter
checks, not numerical simulations at the giant physical mass or
substitutes for the written arbitrary-finite-dimension proof.

After freezing, copy exact bytes to the new checkpoint and run fresh
direct payload and science checks. Native construction rebuilds the
complete ancestry with original SymPy. Transfer raw ASCII JSON using
exact offsets, chunk lengths and CONTINUE acknowledgments. Compare the
un-reserialized report SHA, all native/private/repository source hashes,
all AST-derived fields and every previously protected scientific input.
Never change frozen files to repair a test or launch error.

Ordinary package tests and CLI replay use original SymPy. Only the
separately captured full regression uses the audited exact-GCD adapter
and its self-checks, captured manifest and unchanged-input guard. Launch
the full replay with the existing S219 helper import path:

    PYTHONPATH=problems/P8/s6/continuation/s6_219/tests \
      .venv/bin/python scripts/p8_replay.py full

Incomplete runs and collection failures do not count as passes. External
publication notes, rather than frozen source notes, record actual
completed counts, timings, hashes and any retries. Advertise and publish
only after all required validations pass, using the exact explicit
manifest and independently checked remote hash. Preserve unrelated P4/P9
work. Exact replay supports but does not formalize the written local
implicit-function, finite delta-measure or continuum-scope arguments.

