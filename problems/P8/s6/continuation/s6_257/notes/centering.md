# Nonlinear sources and the old reference constraint

The formal generating-function source J is not the retained physical
heavy source j(u,N). If original positions x are mapped by x=F(q,a),
an original linear source term pulls back as J dot F(q,a). It is not
J dot (q,a). With Hamiltonian convention H_J=H-J dot F,

    C_J=H_a-J dot F_a,
    D_J=H_aa-J dot F_aa.

The source changes the secondary equations and their full auxiliary
Jacobian. The same finite second-class block theorem applies wherever
that source-dependent branch is regular; it cannot be applied with
the unforced D while retaining a different source-dependent constraint.
No physical force or new choice of source/state is made here.

For a complete action or local functional under a nonlinear map,

    d^2(S composed F)=DF^T d^2S DF+dS dot d^2F.

Functional versions use the adjoint and the retained boundaries. The
one-point contact vanishes at an actual full classical stationary point,
or after centering with the complete pulled-back source. Replacing the
pulled-back source by a linear term in the new variables misses this
contact. The exact cubic diagnostic differentiates the full source-
dependent Hamiltonian and shows this nonzero discrepancy explicitly.

The full current old reference at u=0, N=1, Hhat=h=mh=0 and M1 rate 1/10
has classical lapse residual

    Tprofile=rho(0)-3P(0)/2.

Both profiles are the unchanged fixed normalized functions. The nearby
unforced N=1 classical constraint center instead has

    M1_rate^2=1/100-4 Tprofile.

S255 bounds this shift but proves neither that Tprofile is zero nor that
it is nonzero. Its symbolic appearance in the report is a retained
binding, not a numerical proof of nonvanishing. S256 uses its separately
specified off-clock, constraint-satisfying full-current datum.

S252 is the already spatial-gauge-fixed QUADRATIC reference with its
specified lapse/shift auxiliaries, Gaussian phase measure and boundary
state. Its coefficient action need not be stationary by itself. The
nonlinear system here instead eliminates lapse/normal-vector auxiliaries
before the remaining spatial gauge calculation. These facts forbid an
automatic identification of their two reduced measures or Hessians.
The complete source-dependent gauge, field-map and quantum-mean
construction is still required to establish that bridge.

