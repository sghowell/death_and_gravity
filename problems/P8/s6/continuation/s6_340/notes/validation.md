# Validation boundary, negative controls and immutable evidence

Check arbitrary endpoint inverse Ward identities and all components
of the literal free tensor, both null physical polarizations of the
homogeneous tensor, the existing renormalized inverse conditions,
the correct Dyson sign and fixed kinetic counterterm, eight complete
noncommuting local powers, both literal exact-D massive triangles,
full log remainder and original positive gap. All source frontiers
and historical qualifications must remain unchanged.

The actual original calibration retains12 external legs, two physical
polarizations and three rational Feynman parameters. The72 finite
pointwise controls use both separate triangle endpoints, not an
assumed soft theorem. The uniform written analytic proof is separate.

Negative controls keep the individually nonzero graphs and expose
the residual from omitting the kinetic metric vertex or using the
wrong propagator sign. Unsupported sectors, exact-propagator/LSZ
promotions and off-shell/four-hard curvature promotions are rejected.
Every one of the20 report-field mutations and missing/extra fields
must fail whole-report equality.

A scratch solve initially declared the coefficient to be solved as
zero to be a nonzero Symbol, causing its gate to fail. The corrected
probe gives that assumption only to the nonzero transfer p.k and
preserves the first failed output. Failed diagnostics are not receipts.

Cold source, byte-identical independently warmed fresh source and
complete native checks precede ordinary/CLI/FULL publication
acceptance. Ordinary science uses original SymPy. Operational timings
and later receipts belong outside the frozen source manifest.
Exact symbolic checks and written derivations are not kernel proofs.
