# Exhaustive original two-point input and already fixed conditions

## Exhaustive old matter two-point input

For a connected graph sum(degree-2)=E+2L-2. AtE2,L1 the frozen original
action permits only the old pair of cubic H Phi^2 vertices and one
quartic Phi^4 vertex. The higher degree-five/six terms of S239 cannot
enter this two-point coefficient. Their distinct four-point
contractions have already been treated separately; no finite jet
of the original action or matching coefficient is changed.

The covariant first trace Tr(G_phi W)/2 contains three terms:
the quartic coincident tadpole, the heavy-source onepoint-reducible
tadpole, and the mixed heavy/light bilocal quadratic kernel.
The full existing zero-heavy-onepoint condition cancels the second.
The quartic tadpole is momentum independent and is removed for every
virtuality by the existing light mass condition. Its linear metric
response has only eta_mu_nu and k_mu k_nu, hence vanishes for a real
physical TT graviton. This is not a statement about its curved action.

The remaining mixed bubble and its fixed local counterterms yield
the ORIGINAL inverse correction with the S234 sign

K(v)=v-1+hbar Pi_OS(v),
Pi_OS(v)=g^2/(16pi^2) integral_0^1
 [-Log(1-alpha(x)(v-1))-alpha(x)(v-1)] dx,
alpha=x(1-x)/[(1-x)^2+nx].

The source parameters n,g,C are the unchanged S239 original values.
Pi_OS(1)=Pi_OS'(1)=0 are already imposed first-order conditions,
not new physical choices and not exact nonperturbative pole data.
The finite clock extension V=1+O(X^1024) does not change these jets.
