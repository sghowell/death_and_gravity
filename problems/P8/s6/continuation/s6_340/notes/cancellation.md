# Complete selected first-loop external cancellation

## Complete first-order external-leg cancellation

The two graphs attached to any shifted hard subamplitude H_i(r)
are the corrected vertex times the free propagator, and the free
vertex times the single self-energy propagator insertion. The latter
has the MINUS sign fixed by i/(d+hbar Pi):

[V1_TT/d -2epsilon(p,p) Pi_OS(r^2)/d^2] H_i(r)=0.

This is an exact first-order identity for each of all four legs,
both real physical polarizations, and arbitrary shifted H_i.
It is not an expansion of H_i about the Born configuration.
Nonradiating external legs have zero first-order mass and residue
correction by the already fixed conditions. No scalar cubic leaves
an unaccounted light-reducible hard four-point tree.

The two displayed graphs are individually nonzero in general:
Pi_OS has strictly positive nonzero second derivative from alpha^2.
The zero is a cancellation of the complete selected response,
not permission to delete the self-energy or triangle diagrams.

The selected zero is stronger than a small bound. It adds zero to
both the physical amplitude and its finite full-tree interference.
It does not change the previous S337--S339 known-sector bounds and
cannot remove the distinct hard triangle or ordered-box contributions.
