# Exact original finite controls, not a proof by samples

At the three frozen S304 rational recoil configurations, retain all
four external scalar legs and both physical TT polarizations from
S334. There are12 original external virtualities and16 nonzero
polarization contraction factors among the24 leg/polarization pairs.

For each pair and x=1/7,1/2,5/6, form the ORIGINAL mixed denominator
M=(1-x)^2+n x and M_r=M-x(1-x)(r^2-1). Both are positive.
Evaluate the light and heavy finite triangle antiderivative endpoints
SEPARATELY:

x[log M_r-log M]/(p.k),
(1-x)[log M_r-log M]/(p.k).

Give their sum the physical vertex sign, include the fixed kinetic
counterterm -2epsilon(p,p)alpha, and divide by r^2-1.
Independently form the propagator graph from the full nonpolynomial
inverse integrand -log M_r+log M-alpha(r^2-1).
All72 sums are exactly zero, with no Float and original n throughout.

No finite quadrature is used to approximate the actual integral.
The pointwise identity and positive parameter gap hold on the whole
parameter/recoil domain as proved in loops.md. These finite states
provide independent normalization and sign controls only.
