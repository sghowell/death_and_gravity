# Immutable validation and independent negative controls

The proposed scientific manifest has six Python modules, two tests, README, FORMULATION and seven notes: seventeen inputs. Private algebra, independent source/influence/graph diagnostics, complete source/report-field preflight and formatting precede freezing. The native report has twenty fields and records every source hash.

The native session rebuilds the pinned ancestry with original SymPy while protecting all earlier scientific bytes. Transfer the original report in acknowledged length-checked chunks and verify its raw SHA256 without large-integer reserialization. Every private/repository/source hash and all report fields must agree.

Run fresh repository science/preflight, ordinary package replay, independent CLI replay and a complete captured regression including this checkpoint. Native/direct/ordinary/CLI retain original SymPy. Only full regression uses the separately audited exact-GCD adapter and frozen S219 helper allowance. A shared complete snapshot is allowed only with all individual checkpoint gates retained.

Independent tests derive full canonical inverse-metric/source jets from unexpanded fixtures, complete CTP oscillator cumulants and positive noise, density/operator Gaussian variations, actual graph edges and counterterm contractions, finite Gaussian high moments, physical forced KG energy/normalization, and the explicit coherent-mean, one-copy-retarded, wrong-loop-grade and source-free-loop counterexamples. Finite fixtures do not replace continuum proofs or establish nonzero renormalized amplitudes.

Private diagnostics corrected Python/SymPy boolean representation and a determinant fixture whose chosen first derivative accidentally vanished. Six source tests also needed exact normalization of two equal exponential arguments; independent exact cancellation gave zero before that normalization was used in the tests. Their next endpoint assertion exposed ordinary Python division inside the source helper for native integer input. The helper now sympifies all inputs before arithmetic, and the tests separately require exact vacuum coefficients with no Float atoms and agreement between Python and SymPy zero. These changes occurred before freezing; no mathematical identity, physical formula or tolerance was weakened.

Publish only after all gates pass, using seventeen sources, the native report, external assessment, root claim ledger and root README: exactly21 files. Exclude all later checkpoints and unrelated P4/P9 work. Never edit frozen scientific bytes to make a later gate pass.
