# Entire physical source and canonical clock normalization

The actual physical heavy action has the specified quadratic kinetic/mass term and linear source
J(u,X)=g kappa u²(1-X)^8 exp(-A X²)/2-j1 V(X),
V=(1-X)^1024/[X^1024+(1-X)^1024],
j1=-g/(32pi²), g=1/8192, kappa=10^800, A=10^420.
The first term agrees with sqrt(kappa) times the literal S238 normalized source. The second is the separately retained S239 finite onepoint extension; it is not multiplied by the different source localizer. S240 changes the scalar mean profile, not this physical H source.

The physical equation is (Box_g+n)H=J. Dividing the entire action by kappa divides BOTH the Euler operator and source by kappa. A normalized coefficient or J/sqrt(kappa) must not be inserted into the physical KG equation without its corresponding field map. In coordinate-measure Gaussian notation the density source is j=sqrt(-g)J and the density operator K=sqrt(-g)(Box_g+n). Keep both density factors and their variations.

At every reference clock point X1, the entire source has a zero of order at least8 in the full metric/light perturbation variables. The localizer is not replaced by a Taylor polynomial; its exact factor gives all lower zeros. The finite V contribution has order at least1024. Generic field derivatives at fixed u=t give the nonzero eighth jet proportional to t² exp(-A).

For the full canonical maps Phi=sqrt(kappa)t+pi and g=gbar+2h/sqrt(kappa), differentiate the ENTIRE inverse metric identity. Since the background clock gradient is purely temporal,
deltaX1=2(pi_dot-h00)/sqrt(kappa).
The generic leading source is therefore
J8=128g t² exp(-A)(pi_dot-h00)^8/kappa³
=t² exp(-A)(pi_dot-h00)^8/(64kappa³).
The metric factor2 and original physical source normalization are both essential.

At the single point t0, the explicit u² prefactor can raise the pointwise classical degree. It does not raise the uniform degree on the slab or eliminate nonlocal histories and loop graphs sampling other clock times. On the ENTIRE synchronous submanifold phi=t,N1,beta0, X1 independently of the spatial metric, so J is exactly zero. Consequently no generic nonheavy loop count is a nonzero pure tensor/vector amplitude claim.

## Complete physical forced-H estimate on the original FLRW metric

Let a=(1+t²)² on[-1/2,1/2], n the actual positive squared mass, and give H its original zero source germ. For any real r use the spatial Fourier-weighted energy
E=1/2(||Hdot||Hr²+a^-2||grad H||Hr²+n||H||Hr²).
The full equation, without discarding any momentum, gives
E'=-3Hubble||Hdot||²-Hubble a^-2||grad H||²+Re<J,Hdot>
 <=6|Hubble|E+sqrt(2E)||J||.
The expanding and contracting signs are independently checked.

Hence sqrt(E(t))<=1/sqrt2 integral_s^t exp(3 integral_v^t |Hubble|) ||J(v)||dv. On this entire slab exp(3 integral |Hubble|)<=(25/16)^6<15, with exact margin7517615/16777216. It follows that
||H||C Hr <=15T/sqrt(n)||J||C Hr,
and each of ||Hdot||C Hr and ||a^-1gradH||C Hr is <=15T||J||C Hr.
Approximation by smooth Fourier cutoffs and energy completion gives the same statement on this graph. This estimate concerns the prescribed FLRW metric, not the nonlinear equation for that metric or a proof that an arbitrary J is small.
