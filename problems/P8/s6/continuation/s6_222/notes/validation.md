# Independent checks and immutable replay boundary

The exact force saddle is independently solved before any classical constraint substitution, with nonzero lapse, curvature and shift forces. Nine fixtures cover negative, zero and positive Theta and transfers1e-12,2,10000. They check both momenta, both constraints, all four weighted phase equations and the physical scalar metric tuple. Both classical-only lapse and shift substitutions fail nontrivially in the controls.

Three independent nonlinear local clock functionals retain a nonzero one-current term. Their full Hessians agree with the first-jet pullback plus the extra nn contact exactly once, and detect omission or duplication. The pressure symbol is real with no assumed physical sign.

An independent two-time rational causal matrix fixture compares direct full-block inversion with ordered Schur reconstruction, with both probe drives and a nonsymmetric response. Deleting the direct auxiliary block, reversing the output-density order, or deleting its second formal coefficient changes the answer. The initial private block probe failed because one test-only literal used Python floating division. It was corrected to an exact rational before packaging; the physical formulas did not change.

The initial independent suite passed18 tests in2.05 seconds. The exact force/contact module probe passed in0.49 seconds and the ordered block/domain probe in1.47 seconds. Further integrated and fresh outcomes belong in the external audit.

The continuous norm control includes a comparison multiplier showing both that the available derivative-losing bound is insufficient for a source-space contraction and that failure of such a contraction does not exclude a different bounded inverse. It is explicitly not a model of the actual Proca response or a physical instability test.

There are six package files, two test files, two root markdown files and seven proof notes:17 immutable inputs. The native report has20 fields. Every report field, manifest hash, missing/extra field and false primitive or matching completion is checked. Private exploration files are not part of the report.

Native, direct science, ordinary and CLI retain original SymPy. Only separately audited full regression uses the exact-GCD adapter and the explicit S219 helper-directory PYTHONPATH allowance. No frozen predecessor is edited. Riesz representation, spatial Sobolev shifts, Duhamel reconstruction and graph-domain equivalence are written proofs, not FORMALIZED. Finite matrix tests supplement those arguments and do not certify the continuum quantum inverse.
