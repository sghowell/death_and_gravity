# Independent validation and replay boundary

The continuous proof uses the actual S186 oscillatory stress
bounds, all-momentum radial integrals, the low/high external
split, Hilbert orthogonality and support/locality. These are
written analytic arguments, not an all-orders formal proof.

Independent tests evaluate complete massive J4/J10 tails at
K1000,1e6,1e16, and integrate the two-momentum union tail at
zero and nonzero external transfer. The latter uses independently
integrated angular primitives and48-point Gaussian radial
quadrature; it is supplementary numerical evidence, not a
replacement for the continuous upper bounds.

Literal three-mode Fock matrices verify the pair-exchange
factor, creation vector, both-leg projection, vanishing cross
terms and i/4 response factor. Tests reject a one-leg projection,
missing low-external hypothesis, touching/overlapping times,
invalid slab intervals and nonspacelike enclosures.
A smooth-bump oscillator example distinguishes the retarded
triangle from the zero full-square commutator. Exact scalar
checks retain both canonical chain factors.

The report contains32 named exact checks covering81 scalar
entries,37 gates,9 controls and118 rejected scope mutations.
Every one of its20 top-level fields is mutation-tested.
Eighteen explicit scientific sources are hash-pinned.
Native generation, direct science, ordinary and CLI replays
use original SymPy; only complete regression uses the audited
exact GCD adapter. No frozen predecessor is rewritten.
