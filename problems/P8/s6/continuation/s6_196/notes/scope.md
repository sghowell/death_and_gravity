# Unchanged original frontier

This stage advances the conditional Gaussian vector part of B.
It retains the full affine parent, canonical map C=R^(-1/2),
fixed profile, actual state, finite prescription and all vector
polarizations. It does not replace the interacting parent by
a free vector theory. The metric shear is an external probe,
not a newly constructed quantum-corrected background.

The nine original primitive rows and all prior matching rows
are unchanged; exactly one separated-support response row is
added. In particular, no V/G/B primitive is closed.

Still open are the fixed full spatial/mixed response including
coincident times, its suitable controlled inverse and nonlinear
remainder, the interacting state/measure/other affine loops,
heavy spectrum/cutoff/threshold and omitted-order matching,
vacuum cuts/contour/truncation and the finite-gravity IR/Regge
remainder. Original P8 remains OPEN. Missing estimates are
research work, not numerical choices to delegate to the user.
