# Independent validation and publication boundary

The explicit manifest consists of seven source modules, seven notes, two root documents and two tests: eighteen sources. The report has twenty top-level fields. The audit contains 34 named identities, 272 scalar entries, 38 gates, nine controls and 128 rejected scope inputs.

The scientific file contains 346 tests. Its independent physical calculations use the actual full W8 coefficients and total time derivative, all nine pairs, noncommuting source/detector tensors, three compact times, three momentum directions and internal scales 10^6, 10^10 and 10^20. A separate control verifies the surviving longitudinal contribution.

Exact identities check the full tracefree stress contraction, all leading physical pairs, all 81 fourth angular-moment components, the complete five-channel matrix, original shell geometry, complete scaled W8 limits, current sign and canonical factors. Independent angular quadrature covers all monomials through degree four; exact original-shell integration tests each tensor channel and includes the grazing strip. Rotations and the non-polynomial cusp supply further controls.

An initial private core test exposed an unencoded positive-root assumption at the grazing boundary. The draft was corrected before freezing by checking the exact squared identity together with the positive-root condition K > |P|, rather than assuming a symbolic square root automatically chooses that branch. The initial physical-mode tests passed.

Moment-order validation occurs before memoization. Valid entries are prewarmed before booleans, floating lookalikes, symbolic/nonfinite values, negative exponents and excessive total degree are rejected. Unsupported scope mutations cannot mark original P8 closed.

Formatting/lint, final preflight and fresh private science precede freezing. Frozen source hashes and fresh repository science are checked, followed by a lossless read-only native report rebuild. Native, direct science, ordinary and CLI retain original SymPy; only the full P8 regression uses the separately audited exact GCD adapter. Publication requires final exit-code-zero results, not merely running jobs.

The uniform fixed-P asymptotic and dominated shell arguments are written mathematics, not FORMALIZED proofs. Test counts and gate declarations do not replace those arguments. The publication audit records actual final timings and hashes without changing frozen scientific bytes.
