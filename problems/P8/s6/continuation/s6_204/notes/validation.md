# Verification and geometric boundary

The manifest contains18 scientific sources: seven package modules, two test modules, README.md, FORMULATION.md and seven written notes. The report has20 top-level fields,37 named exact checks covering177 scalar entries,36 gates,9 controls and126 rejected inputs/scope mutations. Every report field, plus missing and extra fields, is mutation-tested.

Independent tests construct the literal four-dimensional metric/inverse jets, Christoffel derivatives, full Riemann and Ricci tensors and scalar. They check the inverse identity and the background/linear curvature coefficients before extracting the quadratic action. Both tensor, both vector and scalar basis directions are checked. Generic spatial directions and explicitly noncommuting matrix time jets supplement those basis cases.

The literal quadratic densities are compared to their Euler-Lagrange Hessians. Their Weyl time-boundary difference is tested explicitly, not removed pointwise. Further exact tests cover full polynomial maps under nontrivial rotations, including zero momentum.

Actual CD clock tests independently apply partial_eta=a partial_t and the action density factor. Wrong-measure controls are nonzero. High-precision compact-profile integrals check proper-time self-adjointness and the full fixed local L2 bound at zero through large external momentum. The explicit derivative norm and both canonical factors are retained.

The source-pinned S6.203 report is transitively rebuilt and the S6.176 actual-state Gaussian anchor is checked. Native report generation, fresh direct science, ordinary and CLI use original SymPy. Only full regression uses the audited exact-GCD adapter with original-tuple and captured-snapshot controls.

Scientific sources are frozen before native report construction. No frozen ancestor is changed. Publication evidence is recorded only on actual completion of each replay. The compact geometric variation, integration by parts and continuous norm estimate are written arguments, not FORMALIZED proofs. These checks do not establish the remaining quantum contact matching, a full mixed inverse or original P8 closure.
