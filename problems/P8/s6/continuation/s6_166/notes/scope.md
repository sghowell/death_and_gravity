# The state property gained and the estimates still absent

This continuation strengthens the SAME free flat SAT8
external-mass state. It changes neither the mass profile nor
the source map. It proves all-order time tails and an explicit
uniform one-particle in/out approximation error, then matches
the hypotheses of an external Hadamard theorem.

Hadamard regularity supplies the appropriate local
short-distance structure. It is not a numerical upper bound
on the state-dependent smooth remainder or its stress-relevant
derivatives. The L2 projector norm bound does not supply such
a bound either.

A quantitative local stress calculation still needs its
observable, finite renormalization prescription, the external
mass-source energy exchange, and controlled state-dependent
and local terms. The preceding out-particle energy is not a
substitute for transient stress or a relative bounce error.

Gauge/fermion interactions, curved FLRW propagation, a common
parent background solution, heavy/cutoff dictionaries, quantum
target matching and physical finite-EFT errors are unchanged
open obligations. No global Fock implementation is asserted.
No flat-space inequality is applied independently at every
bounce time.

All frozen sources and reports are retained. The primitive
ledger and earlier matching rows are unchanged; one explicitly
scoped free-state item is appended. External theorem reliance,
written proofs, exact replay and numerical diagnostics have
different evidence roles and are not labeled FORMALIZED.
Scoped P8(a) and A.20-A.23 are unchanged. Original P8 is OPEN.
