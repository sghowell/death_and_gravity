# Immutable validation and independent controls

Build and test the complete bundle in a private scratch directory before copying the scientific source manifest to S248. The scientific manifest is six Python modules, two tests, README, FORMULATION and seven notes: seventeen files. The raw native report has twenty top-level fields. The report records the source hashes, exact identities and explicit scope.

Rebuild every pinned prior report using original SymPy in the native session, with every previous frozen P8 scientific byte protected. Transfer the native JSON in acknowledged, length-checked chunks and compare its raw SHA256 without reserializing large integers. Exact private, repository and native source hashes and all twenty fields must agree.

Run a separate fresh repository science/preflight, fresh ordinary package replay, standalone CLI replay and a complete regression snapshot containing this checkpoint. Native, direct, ordinary and CLI use original SymPy. Only the complete regression uses the separately audited exact-GCD adapter and frozen S219 helper allowance. The adapter does not provide the new scientific result.

Independent science tests reconstruct the full profile from an unexpanded density, the entire forced saddle with current-profile additions, local row primitives and upper contacts, unequal-time force/density coordinate maps, mixed-order noncommuting inverse products and quantitative weak-log kernel inequalities. They include counterexamples for a duplicate heavy profile, omitted matter shift, discarded initial constraint germ, wrong kernel/density order and commuted variable pivot. Diagnostic coefficients are not alternate physical candidates.

Proof gates refer to the accompanying written continuum arguments. Passing finite residuals or diagnostic matrices alone is not used to assert the actual infinite-dimensional theorem. Original state, scheme, two-threshold reference and current coefficient inputs remain pinned; no frozen input is edited to make validation pass.

Only after all gates pass may the separate external assessment, root README and claim ledger be updated and the exact21-file publication manifest committed. Exclude later checkpoints and unrelated P4/P9 work. No report or scientific source is changed after freezing.
