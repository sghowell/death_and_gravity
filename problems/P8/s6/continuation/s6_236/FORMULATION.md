# Fixed first-loop angular remainder problem

The input is exactly the separate S235 V2S-T1-OS4 prescription, including its complete first four-light loop, all UV subtractions, S234 first-order light mass/residue normalization and the finite contact deltaC_fin=-A1_base(4/3,4/3,4/3). The S233 classical parameters and original target are unchanged.

The objective is to prove a bound for the full complex first-loop coefficient on EVERY real physical configuration

s=S, t=-(S-4)(1-x)/2, u=-(S-4)(1+x)/2,
4<=S<=10^196, -1<=x<=1.

The comparison is to the full original massive tree, not selected coefficients or a massless approximation. The endpoints, threshold logarithm, real and imaginary loop parts and all ordered heavy/light box routings are retained.

Set n=M_H²=D+2, D=2lambda/gamma, lambda=10^-600, gamma=1024*10^-800 and g²=1/2^26. A single parameter integral T(n,L,b) generates both Cbar and Dbar. A convergent expansion of its analytic coefficient functions P,Q, NOT of an uncontrolled large-loop-momentum integrand, gives complete remainder bounds. Its logarithmic light branch is explicitly integrated.

The full base amplitude has the exact decomposition

A1_base=g4/(16pi²)[-6ln(n)/n²+(-16ln(n)+10/3)/n³]+R_base,
abs(R_base)<10^7 g4 S²/(16pi²n4).

The same decomposition at the symmetric point uses S=4. Because the two displayed terms are independent of the on-shell kinematics, the existing OS4 contact subtracts both. The resulting full first-loop coefficient obeys

abs(A1_OS4)/A_original<10^-199.

Together with S233 this proves the stated1/59 bound only for the tree-plus-first-loop expression. It does not control higher loops, derivative jets at other analytic points, exact UV analyticity/unitarity, a resonance, gravity or the common-parent bounce. All nine original primitive statuses remain unchanged; one explicitly first-loop matching record is added.

The exact residuals accompany written complex-analysis and integrable-log proofs. They are not an all-orders formal proof or a certificate for unproved physical assumptions. See every listed note before interpreting the report.
