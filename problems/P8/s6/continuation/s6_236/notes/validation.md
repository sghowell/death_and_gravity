# Independent validation and frozen-source discipline

The package has47 named identities containing47 scalar entries,30 proof gates,nine controls and243 rejected unsupported inputs. It adds one first-loop angular matching record for92 records while leaving all nine original primitive statuses unchanged. The17 scientific sources are six modules, two tests, two root documents and seven notes. The report has20 top-level fields with every-field mutation and missing/extra rejection.

Independent positive-parameter quadratures verify the entire primitive and full box integral, including the removable b=L case. A numerical heavy-mass derivative verifies the separately coded analytic derivative. Physical negative-L fixtures compare the imaginary part with an independent Feynman delta-function root calculation and reject the opposite branch sign.

Complex-circle fixtures test the analytic P,Q bounds for positive, zero and negative parameters, independently of the symbolic jets. Actual-hierarchy diagnostics use900 decimal digits to resolve both complete primitive tails after their finely cancelled leading terms. These values test the formulas; the full disk proof and Cauchy coefficient estimates, not those samples, certify the continuum tails.

Independent root-log integrals cover endpoint and coincident threshold roots. Exact kinematic fixtures include both angular endpoints, the light threshold and the actual upper energy. The full three-channel expansion uses distinct independent B and J symbols for each channel, checks every cancellation and detects an omitted bubble. A separate control demonstrates why differentiating ln(n) in the box is required.

A complete forward-loop diagnostic integrates the closed primitive and every mass-ordered box, using split light roots and smooth heavy-angle quadrature. Its imaginary part agrees with an independent integral of the entire rational tree squared in the original identical-particle normalization. The full real expression is also compared with the two universal terms at two masses. This is an independent numerical diagnostic, not a quadrature error certificate.

Scope mutations reject a new finite prescription, an absolute integral through Feynman double poles, exchanged box arguments, arbitrary energies, exact quantum amplitudes, higher coefficient matching, omitted-loop control, original-parent replacement and P8 closure. The tree-plus-first-loop comparison is always identified as a truncation.

All private science, source and report-field manifests, lint and format checks precede freezing. Native/direct/ordinary/CLI retain original SymPy and rebuild the source-pinned lineage read-only; only full P8 regression uses the audited exact-GCD adapter. Every fresh replay path must pass before publication. Actual timings and any private validation corrections belong in the external assessment, not in modifications to frozen source bytes.

The exact residuals accompany written analytic and integral proofs; the result is not FORMALIZED. Certification does not claim that the full original physical matching assumptions have been established.
