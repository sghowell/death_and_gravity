# Source freeze, native transport and independent acceptance

Seven Python modules, two test modules, two root documents and
seven proof notes comprise18 ASCII scientific files. The report
has exactly20 AST-derived top-level fields. Every formatting
change and private scientific correction precedes source freeze.

A cold original-SymPy preflight validates all exact residuals,
every gate/control and actual rejected input, module exports,
the full20-field test contract, source hashes and complete
retained scope. The entire scientific payload is checked
recursively for Python or SymPy floating-point values.
All independent science tests must pass.

A fresh root then forces every immediate-parent packet before
its own residuals, repeats the checks/tests and compares every
hash. This specifically guards the rejected S279 cache-mutation
failure. Exact private/fresh/repository bytes must agree.

Only then are sources frozen and the new native specification
submitted once. The helper receives no research probe or DONE.
Its full acknowledged raw stream is validated by byte equality,
SHA256, complete source manifest,20-field AST contract, actual
counts and retained frontiers, then frozen without rewriting.

Independent original-SymPy ordinary and CLI replays must both
pass. A complete captured P8 FULL snapshot including this packet
must pass with its actual collection, unchanged manifest and
exact adapter-contract evidence. Only FULL may use the exact-GCD
adapter; direct/native/ordinary/CLI retain original SymPy.
At most one FULL and one ordinary/CLI pair run concurrently.

The external assessment records the actual outcomes and all
development corrections. This source note predicts no acceptance.
Frozen scientific inputs and raw reports are never edited to
reflect a later gate; corrections need a successor or external
assessment. Unrelated P4/P9 work is preserved and excluded.
