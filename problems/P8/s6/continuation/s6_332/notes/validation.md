# Independent reconstruction and immutable acceptance

The written uniform arguments are supported by exact source-modulus,
density-normalization, logarithmic calculus and budget checks.
Independent quadratures cover tail logarithms and both excluded-layer
branches, including rare-index and extreme-resolution cases. A
finite-Poisson scalar mark deltaa=R/kappa, deltaDelta=0 is a bounded-class
model calibration, not the original physical current or a proof of L1.
The constant-mark density split is a separate algebraic negative control.
None of these numerical examples substitutes for the written proof.

The private candidate probe passed before packaging. Its initial decimal
samples had been constructed before the high-precision context; accepted
tests instead create exact rational values or strings inside that context.
The draft source-API name was checked against the actual frozen module
and corrected before its first module execution. No frozen parent changed.
The initial module diagnostic then rejected an expanded, unsimplified
zero in two source-modulus residuals. A direct check established exact
zero after symbolic simplification; the new residuals were normalized
before acceptance, with no change to a physical formula or frozen source.
Import-order and literal-comparison lint diagnostics were corrected
before the final cold reconstruction and source freeze.

Cold and independently started fresh reconstruction use original SymPy.
The fresh process first rebuilds the immediate frozen parent's complete
own packets, then awaits one START after byte-exact final source copying.
Any completed ancestor-cache reuse is disclosed. Preflight checks actual
direct parent report/source hashes, source-set/20-field AST/exports,
exact residuals, proof gates, input guards and unchanged source bytes.

Only matched cold/fresh hashes authorize the repository copy and one
native specification. Complete native raw bytes, hashes, counts and all
inherited frontiers must validate. Publication further requires ordinary
tests, separate read-only CLI and full source regression to finish C0.
Only FULL uses the separately audited exact-GCD adapter and restores the
original backend. Frozen sources/raw evidence cannot be rewritten to
hide failures. This is exact algebra plus written proofs, not a
kernel-formalized theorem or a claim of original P8 closure.
