# Immutable evidence and independent validation

Native, direct science, ordinary and CLI runs require original SymPy.
Only a captured complete FULL regression may use the existing audited
exact-GCD adapter. Do not alter earlier sources, private accepted inputs
or native raw reports to obtain a successful comparison.

This manifest includes seven Python modules, two tests, two root
documents and these seven proof/scope notes. The verifier derives all
source hashes and compares every report field with the original native
result. It rebuilds the entire accepted parent chain first and retains
the rejected S279 evidence through S280, without running the rejected
verifier.

Exact invariant algebra is checked independently of the COM tensor
calculation. The generic angular master is proved by a full quadratic
primitive and endpoint ratio, with separate angular moments. The finite
part has a written endpoint/dominated-convergence proof. The Proca
polarizations and tensor are checked pair by pair and calibrated to the
original M1 cut. Numerical probes are diagnostics only, not proofs of
continuum or all-spin claims.

Private/fresh source/AST/export/count/science preflights must precede
freezing. Parent-warm order must agree. Native raw bytes are retained
without reserialization and compared against that same accepted source
manifest. Independent ordinary/CLI and a complete captured FULL run are
publication gates, with actual outcomes recorded in an external
assessment rather than by editing these frozen inputs.
