# Translated full auxiliary branch and the volume symbol

The complete derivative majorant M=10^200 holds on the absolute
coordinate box 10^-120 and |N-1|<=rho=10^-245. All actual homogeneous
coordinates plus their full phase deviations fit this larger box.

At fixed real u, expand C(u,1,z_b+delta z) in invariant directions.
The full center formula and actual gradient bounds give the residual

    b <= (5/2)PROFILE
          +100T delta_p +delta_pm
          +4 sum(other nonzero-gradient invariant deviations)
          +(M/2)(sum all twelve deviations)^2
      <10^-324.

The exact vanishing center gradients in the Gauss and two heavy linear
slots justify their absence from the linear sum only. Every slot
remains in the complete quadratic remainder. The large trace
deviation is multiplied by its actual O(T) center gradient. A generic
M*T or M times the full first-order phase deviation would not prove
this estimate.

The center CN is bounded from its exact time-dependent expression,
not transported from u=0 using the broad MVT constant. Only lapse
and invariant directions contribute the variation

    eta=M(rho+sum deviations)<1/1000,
    q=1/30+eta/3<1/20.

For N->N+C/3, b/3+q rho<rho. The entire complex map is contractive
and its full root satisfies |Nstar-1|<=b/[3(1-q)]<10^-324.
This is the full original source root on every reconstructed spatial
point. Real data give the positive real branch; CN stays below -2.9
and the original temporal auxiliary pivot remains nonzero.
The implicit function is holomorphic in finite complex phase inputs
and smooth in real time. No lapse-gradient term is reintroduced.

The exact clock identity RN=-2/(1+u^2)^3 and the full RNN bound keep
|RN|<4 and |U_N|<12 for U=R^-3/4 on the unchanged lapse tube.
With the complete exp(3v) factor and spatial average,

    |F_red-1| <=12 v_scaled+24|Nstar-1|<10^-320.

The same holds on the complex initial ball after the entire reference
pullback. It includes the full implicit composition and every
higher phase contact, not just a linear Gaussian lapse substitution.

For the Hamiltonian, restore kappa and the complete torus integral,
whose measure is below 1000. Keep the full parent and moving-chart
terms -Hclock Pi_v-ell Pi_M, all original primitive boundaries and
the scalar source center. In the nonzero-mode variational chart the
homogeneous momentum boundary is proportional to integral v=0;
this vanishing is imposed only after pulling back the full one-form.

The free quadratic Hamiltonian in symplectic whitening has norm equal
to the whitened generator norm: S0^T H_ref S0=-Omega S0^-1 G_ref S0.
On the full image ball its bound is G(8R)^2/2. Combining it with the
entire source bound gives |g|<10^1000. This is a finite bound for exact
unitary existence, not a small integrated-interaction estimate.
