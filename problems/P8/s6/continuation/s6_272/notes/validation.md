# Read-only validation and publication contract

There are exactly 18 source files: eight package modules, two tests,
README.md, FORMULATION.md and these six notes. The report has exactly
20 top-level fields and binds the immediate parent SHA-256
970319d0dc460d46e9474e2ef9c17c1624aa7244b584461ac6cab1ff46771c7e.

The exact packet has 37 named checks, 153 scalar residual entries,
75 proof gates, nine controls, 298 rejected inputs, nine unchanged
primitive rows and 128 qualified matching rows. Residuals must be
exactly zero; every gate and actual rejection must succeed.

The independent science suite has 737 cases. It checks full packet identities, every
translated derivative bound and high-phase ratio, full moving-center
gradients, the complete 3D curvature operator, rotated exponential
metrics with both tensor and conformal terms, full sublattice
products, endpoint-error thresholds, noncommuting state flips,
and all scope/input rejections. Generic numerical fixtures do not
define the actual full quantum dynamics or extend its stated domain.
Runtime warnings are errors in science validation.

The 25 certificate tests check complete read-only replay, exact source/field
manifests, independent mutation of all 20 fields, missing/extra-field
rejection and exact counts/scope. These integrity checks do not
formally verify the written analytic theorem. The expected ordinary total is 762.

Before publication require:

1. Complete private original-SymPy preflight and independent science.
2. A byte-identical accepted repository source copy and fresh preflight.
3. Native original-SymPy full-ancestry construction, complete raw report
   transport/hash validation and all source/field comparisons.
4. Separate original-SymPy ordinary and CLI zero exits.
5. A complete captured full-P8 regression with all captured inputs
   present and unchanged. Only this FULL run may use the separately
   audited exact-GCD adapter.

The external assessment records observed results without editing
frozen source bytes. Private originals and accepted repository
sources are immutable after freeze. The report is added only after
its native raw bytes and complete manifest have been validated.
No ancestor source or certificate may be silently repaired.
Both historical errata and the original open frontiers remain.
