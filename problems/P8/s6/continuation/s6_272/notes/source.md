# Whole source and the actual moving center

Use the entire S6.270 Hamiltonian and auxiliary constraint C=partial_N H,
after the complete temporal elimination and retained primitive boundary.
All twelve invariant slots remain. The actual homogeneous invariants are

    p_b=-2H=-8u/(1+u^2),
    delta_pm,b=ell-1/10, ell=1/[10(1+u^2)^6],

with the other ten slots zero. Setting p=0 and pm=1/10 off the bounce is
not this moving center.

The entire R and F differences from their clock germs have (X-1)^8
factors; the original complement has order 1024. The complete physical
heavy source has an eighth factor and its fixed onepoint extension has
order 1024. These facts license the low N jets used at N=1, not any
off-clock replacement. The two total fixed profiles bind to their original
functions, including the heavy correction and all constants. They are
not free parameters or live quantum means varied with the probe.

At the clock the full functions have

    R=1, RN=-2/(1+u^2)^3,
    F_clock=F_tree-Pfixed-(rhofixed+Pfixed)(X-1)/2.

Always differentiate the full constraint first. Positive N derivatives of
the primitive are eliminated by its exact IN formula; I(u,1)=0 implies
only its pure time jets vanish there. Mixed derivatives such as RuN and
the corresponding primitive contacts are not discarded.

The resulting exact moving-center identity is

    C(u,1,z_b)=3Pfixed/[2(1+u^2)^3]-rhofixed.

The tree contribution cancels identically, for the actual moving momenta.
The full center gradients satisfy C_G=C_ph=C_eta=0, |C_p|<100T,
|C_pm|<1, and every other gradient has modulus less than 4.
The complete center CN has the reported rational expression and its
outward enclosure lies strictly in (-3.1,-3) on |u|<=T.

The fixed total profiles obey their original bound 2 x 10^-400.
Their contribution to the center residual is therefore bounded by
(5/2) times that bound. The full source remains smooth in real time;
no holomorphic-time continuation of those profiles is assumed.

On |N-1|<=10^-245 and absolute invariant coordinates <=10^-120,
re-evaluate all 187 complete source derivatives used by the contraction:
H,C,CN,Cu,CNN,CNu,T and all Cz,CNz,Cuz,Czz entries. The exact majorant
engine retains every term and permits only proved rational powers and
reciprocals. All values are less than 10^200; H itself is below 10^62.
The larger coordinate box includes the nonzero moving background.

The original 21 mixed RFj source-jet bounds remain applicable: the
complex-N neighborhood is unchanged, T+10^-4<11 x 10^-4, and the real
time interval is inside the original C5 slab. Pure primitive time
derivatives are bounded by integrating the full IN derivative along
the complex N segment. This is why no source term is lost when obtaining
the translated majorant.
