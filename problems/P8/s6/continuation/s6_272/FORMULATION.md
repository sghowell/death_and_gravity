# Frozen scope: a longer-domain finite volume turnaround

Keep exactly the S6.268-S6.271 finite variational regulator: torus L=1,
P=10^64, kappa=10^800, all eight configuration channels per real Fourier
basis function, 48 canonical pairs, and the original pure covariance.
There are 96 phase variables. Keep R=10^20 and the two original smooth
radial cutoffs c=1,2. No source function, state prescription, physical
parameter, canonical boundary, or original report is replaced.

The new interval is |u| <= T=10^-130. The initial complex phase ball 4R
maps inside the full reconstructed image ball 8R. Every nonlinear
harmonic and generated homogeneous shape correction remains; this is not
a finite Fourier invariant submanifold of the nonlinear PDE.

## Defined operator statement

For each cutoff c, retain the whole interaction symbol
g=(h_red-h_ref)(u,S_u z), including its scalar center g(u,0). Its extension
is g_ext=g(u,0)+chi_c[g-g(u,0)]. The full normalized physical volume is

    F_red = spatial_average exp(3v) R_full(u,Nstar)^(-3/4),
    F = F_red(u,S_u z),
    F_ext = F(u,0)+chi_c[F-F(u,0)].

Use the same two explicit orderings: calibrated-coherent
A_C=Q_V((1-D)g_ext), F_C=Q_V((1-D)F_ext), and Weyl
A_W=OpW(g_ext), F_W=OpW(F_ext), with D=Delta_w/4 in original symplectic
whitening. These are not assumed to agree exactly. Each full Hamiltonian
generates its own exact unitary from the same original seed.

The theorem establishes ||F_C-I||<2 x 10^-320,
||F_W-I||<10^-264 and F_W>I/2 on the entire stated interval, for either
cutoff. The inequalities hold on the full Hilbert space and its reducing
zero-translation-charge sector, not only on a projected occupation space.

For either corresponding evolved normalized state, let

    nu(u) = (1+u^2)^6 <psi(u),F_operator(u)psi(u)>.

Then nu(-T) and nu(T) both exceed nu(0) by more than 5T^2. The continuous
mean has a global minimum in (-T,T); all minimizers lie in |u|<10^-132.
The mean is C1, hence a minimizer is critical and there are points of
negative and positive derivative on its two sides. Uniqueness and strict
positive second derivative are not conclusions.

## What remains outside the theorem

Homogeneous reduced variables are still external reference functions.
No homogeneous quantum state or self-consistent mean equations are supplied.
A minimum of this extended readout is not evidence that the evolved state
stays mostly inside the classical phase core; no such longer-time leakage
bound is established. No old tiny-time near-identity, ordering-state,
cutoff-state, or localization estimate is reassigned to this interval.

The unlocalized singular Hamiltonian, its interacting volume, uniform
mode/volume limits, physical Wilsonian matching, omitted loops, UV/Regge
control and nonlinear global completeness remain separate. Original
V/G/B/P8 stay OPEN. Scoped P8(a), the S6.261 physical-binding refutation and
the S6.265 unlocalized Gaussian integrability obstruction are unchanged.
