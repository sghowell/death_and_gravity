# Coupled finite hybrid solution and physical-volume turnaround

The model has five live classical homogeneous density coordinates
Y=(alpha,p,pm,eta,ph), cyclic M1 by quadrature, and the unchanged48
quantum nonzero-mode pairs. The source profiles, full nonlinear source,
two cutoffs and initial pure seed are fixed. This is a separately named
finite HYBRID model, not quantization of the homogeneous variables.

## Complete fixed-point domain

Let T=1e-180, r=1e-390 and
Ybar=(2log(1+u^2),-8u/(1+u^2),ell(u),0,0).
The initial classical value is exactly Ybar(0); the lapse is its actual
small root, not N=1 imposed by hand. On [-T,T] take continuous real paths
with Y(0)=Ybar(0) and sup|Y-Ybar|_infty<=r. Pair them with continuous
normalized quantum paths phi in the closed cubic-invariant,
zero-translation-charge Hilbert subspace, having the original seed at0.

Use the weighted MAXIMUM metric
d((Y,phi),(Ytilde,phitilde))=
max{sup|Y-Ytilde|_infty, w sup||phi-phitilde||}, w=1e-350.
This is a complete closed path space. The normalized sphere need not
be convex for the contraction theorem. A sum metric would require
different matrix-norm estimates; it is not what is proved here.

Given an input pair, the classical part of the map integrates the entire
scalar-center force plus the quantum expectations of notes/quantum.md.
The quantum part is the exact unitary evolution generated by K_[Y],
applied to the seed; it does not depend on the input quantum path.
The same construction works forward and backward from zero.

## Reference error and self-mapping

The whole homogeneous clock equations differ from Ybar' only by the
nonzero fixed-pressure residual in p'. The full constraint center
residual is at most5e-400, giving
|N0(Ybar)-1|<=5e-400/[3(1-q)].
The entire normal force derivative and reduced homogeneous Jacobian
are<1e112. Thus the complete reference rate error is<1e-280.
It follows for every allowed input pair that
sup|Y_new-Ybar|<=T(1e-280+1e112 r+B_quant)<r/2.
The quantum output remains normalized and in the invariant subspace by
unitarity and equivariance. Its displacement need not be small.

## Contraction and uniqueness in the declared local domain

For bounded B and normalized vectors,
|<phi,B phi>-<psi,B psi>|<=2||B|| ||phi-psi||.
The classical row in the weighted maximum metric is therefore
T(L_hom+L_quant)+2T B_quant/w.
The exact nonautonomous unitary Duhamel identity gives
||U_K[Y]-U_K[Ytilde]||<=T*5 max_i||partial_i K|| sup|Y-Ytilde|.
No exponential exp(T||K||) is used. Its weighted state row is
w T*5 max_i||partial_i K||.
Each row is strictly<1e-50. Thus their maximum is a strict contraction
constant. Banach's theorem gives one and only one coupled solution in
the stated domain. Continuity of the full rates and bounded operator
family gives C1 classical and quantum paths by their integral equations.

Integrate the complete M1 rate from its unchanged Cauchy value.
The identity pm'+3pm alpha'=0 gives exact conservation of a^3 pm.
All three homogeneous spatial-vector and all five traceless-shape
canonical pairs solve their zero equations by notes/symmetry.md; the
heavy scalar remains live. The temporal normal vector is always
reconstructed from the complete source.

The comparison variable solves i phi'=K_Y phi. Restore
psi=exp[-i integral_0^u g0(s,Y(s)) ds] phi;
then i psi'=(g0 I+K_Y)psi. The scalar-center force was never removed
from the classical map. Finally use the same fixed Uref to restore
both physical state and readout. Norm differentiability is asserted
for the bounded interaction-picture equation; no new domain claim
for an unbounded reference generator is needed for its expectation.

## Full volume conclusion

On the resulting inner path,
|N0(Y)-1|<=[5e-400+12e4 r]/[3(1-q)].
Combining the entire centered volume operator with the scale error gives
|exp[3(alpha-alphabar)]<F_operator>-1|
<=12|N0(Y)-1|+12r+2 O(A_F) <epsilon=1e-380.
The factor2 retains the scale/operator cross product. Hence the physical
normalized volume obeys
nu(u)=(1+u^2)^6(1+e(u)), |e(u)|<epsilon.
For either cutoff and either ordering, direct exact rational inequalities
give nu(+-T)-nu(0)>5T^2=5e-360. Also, for r_min=1e-188,
[(1+r_min^2)^6-1](1-epsilon)-2epsilon>0.
Every global minimum therefore lies strictly inside |u|<r_min.
The mean is C1, so any such minimum is critical; comparison with the
two larger endpoints yields a negative derivative point on its left
and a positive derivative point on its right.

This does NOT prove a unique minimum, strict positive acceleration,
or monotonicity of either half-interval. It gives no long-time core
leakage estimate. A turnaround of the complete EXTENDED readout does not
prove that the state stays in its unmodified classical core. The result
also does not establish original fully quantum means or any limit.
