# Entire source jets on the larger lapse domain

The source is exactly the immutable S270/S272 full Hamiltonian, including
R,F,j,Hclock, all twelve invariants and the original primitive I.
Only its estimates and the separately declared finite hybrid dynamics
are new. The fixed profiles remain the original functions, never live
state means or adjustable counterterms.

## Clock jets and real-time regularity

On N=1 the actual full R,F have their exact clock germs. Their differences
have eighth-order (X-1) factors, and the original switch contributes
order1024. These factor identities are inherited with the complete
source bindings and unchanged ancestry. Real-time differentiation up to
five preserves the clock factors. The normalized heavy source has the
same vanishing low clock jets. None of these facts permits replacing
the full functions away from the clock.

For every i+k<=5, evaluate the exact clock derivative d_u^i d_N^k.
All fixed profile derivatives through time5 are bounded by2e-400 on the
original real source slab. Rational interval arithmetic on
|u|<=1e-130 bounds each clock derivative separately.

To reach |N-1|<=1e-115, integrate ONE additional N derivative. The original
joint analytic source neighborhood has Cauchy radius1e-4 and distance
11e-4; the new lapse and real-time radii still lie strictly inside it.
The entire analytic scalar modulus is<1e7, metric modulus<2 and normalized
source modulus<1e-100, including all fixed vacuum constants, switches
and heavy localizers. For fixed smooth profiles use N-only Cauchy and
their real-time C5 bound. Thus for i<=5 and i+k<=6,

    R_bound <= 2 i! k!/(1e-4)^(i+k),
    F_bound <= 1e7 i! k!/(1e-4)^(i+k)
                 +8(2e-400) k!/(1e-4)^k.

Every one of the27 entries is<1e35. The entry(i,k)=(6,0) is deliberately
absent: no sixth time derivative or holomorphic-time profile is assumed.
The complete off-clock MVT error is therefore at most1e-80. Add this
error to each of the42 actual R,F clock bounds; all21 full j bounds keep
the same error. Their individual outward decimal powers are recorded.

The full R value on the clock is1. Its complete RN bound keeps R away
from zero on the larger tube. Consequently all inverse and fractional
powers use the same branch with absolute inverse bound2. The full
temporal auxiliary pivot 1-3(R-1)^2/(2R) remains greater than99/100.

## Full primitive and derivative majorants

I(u,1)=0, but I and Iu are not zero away from the clock. Its entire
derivative is

    IN=3 R^(-7/4) Ru RN/(4N).

Eliminate positive N derivatives only with this identity. Every pure
time derivative of I is bounded by its actual N integral, using the NEW
radius1e-115. Derivatives of IN retain mixed RuN and all product contacts.

The independent magnitude evaluator exactly reproduces all187 earlier
values when its legacy branch uses the OLD radius and bounds. Its new
branch uses the componentwise bounds and the enlarged primitive integral.
It evaluates the original187 expressions, CNNN, all CNNz and CNzz, all
Hz and Hzz, for500 total expressions. All are<1e9 and |Hbar|<100.
The constraint is exactly quadratic in the twelve invariant variables,
so Czzz=0; no invariant polynomial truncation is performed.

Safe componentwise group ceilings are

    |Cz|<1e4, |CNz|<1e6, |CNN|<1e9, |Czz|<1e4,
    |Hz|<1e3, |Hzz|<1e3.

These are full off-clock bounds, not clock-only derivatives.
The explicit real hybrid time10^-180 is a subset of the proved source
interval. High PHASE and homogeneous derivatives below use holomorphy
in those finite parameters and N, not additional time regularity.
