# Defined scope: full-source finite self-consistent hybrid feedback

Keep the original source functions, fixed real-time profiles, primitive,
physical parameters, state prescription and full nonlinear reconstruction.
Keep L=1 on the 2pi torus, P=10^64, kappa=10^800, zeta=10^-6,
the original vector and heavy masses, six real nonzero Fourier functions,
all eight configuration channels and 48 quantum canonical pairs.
Keep the original full pure covariance V0, fixed reference flow S_u and
Uref, R=10^20 and the two original c=1,2 smooth radial cutoffs.

This is a NEW, explicitly named hybrid model. Its homogeneous variables
are classical; its nonzero modes are quantum. The hybrid construction
is not asserted to equal the original fully quantum theory.

## Actual canonical variables and Hamiltonian

Use absolute homogeneous alpha=log(a), density p, pm, eta=10^100 h and
ph, with canonical PV=3a^3p, PM=a^3pm and PH=a^3ph. The homogeneous
one-form is

    kappa Vol [PV d(alpha) + PM d(M1) + PH d(eta)/10^100].

M1 is shift-cyclic and is reconstructed from its full rate and original
Cauchy value. All three homogeneous spatial-vector and FIVE homogeneous
traceless-shape canonical pairs have zero initial data. Their equations
are satisfied at zero by the actual cubic symmetry proof, not omitted.
The homogeneous heavy scalar is not set to zero by symmetry.

The full pointwise lapse and temporal normal vector are reconstructed
from the original complete constraints. With gamma=a^2 exp(2v)Q^-1,
define the entire absolute Hamiltonian

    h(u,Y,z) = kappa integral V Hbar_full(u,Nstar,Z) d^3x.

The complete S267 canonical one-form splits into homogeneous and
zero-mean input-fluctuation parts. Generated means and higher spatial
harmonics are retained. No finite Fourier invariant-PDE ansatz is made.
The external-chart scalar connection is accounted for when comparing
with older packets; it is not incorrectly reused as an absolute force.

Set g=(h-h_ref)(u,Y,S_u z), g0=h(u,Y,0), and
K_symbol=chi_c(g-g0). The reference is fixed and independent of live Y.
For each cutoff separately use either

    K_C = Q_V[(1-D)K_symbol],   K_W = OpW(K_symbol),   D=Delta_w/4.

The complete generator is A_Y=g0 I+K_Y. The classical homogeneous
equations are generated by h0+<K_Y> with the stated canonical one-form.
Both the scalar-center force and every quantum derivative are retained.
The initial quantum factor is the unchanged pure nonzero-mode seed.

## Evaluated domain and theorem

Use |u| <= T=10^-180. The original reference is
Ybar=(2log(1+u^2), -8u/(1+u^2), ell(u), 0, 0).
Set Y(0)=Ybar(0) and use the actual constraint root, not N=1 by fiat.

The proof uses complex initial phase ball4R, its complete image ball8R,
complex homogeneous radius10^-122, homogeneous Cauchy radius10^-124,
full invariant absolute radius10^-120 and complex lapse radius10^-115.
The unique real coupled solution remains within10^-390 of Ybar.
Only real-time C5 regularity of the original smooth profiles is used.

For either full operator ordering and cutoff, define the complete
physical-volume symbol

    F_Y=average exp(3v) R_full(u,Nstar)^(-3/4),
    F0(Y)=R_full(u,N0(Y))^(-3/4),
    F_ext=F0+chi_c(F_Y-F0),

with the same reference pullback. Quantize in the corresponding ordering.
The physical volume mean is nu=exp(3alpha)<F_operator>; conjugate both
state and readout by the same Uref in the physical picture.

Then |nu/(1+u^2)^6-1|<10^-380, nu(-T)-nu(0)>5T^2 and
nu(T)-nu(0)>5T^2. Every global minimum is inside |u|<10^-188.
The C1 mean has a critical minimum and negative/positive derivative
points on its two sides. No unique or strict positive-acceleration
minimum, or monotonicity of either whole half-interval, is concluded.

## Nonclaims

This is not a homogeneous quantum state or original interacting mean.
There is no longer-time phase-core leakage estimate, no unlocalized
Hamiltonian or cutoff/mode/volume-removal limit, no physical Wilsonian
matching or omitted-loop/Regge/UV bound, and no nonlinear global
completion. An extended-readout turnaround does not establish that the
state stays inside the unmodified classical core. Original V/G/B/P8
remain OPEN; historical refutations and completed scoped P8(a) remain.
