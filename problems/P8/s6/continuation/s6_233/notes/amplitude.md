# Literal vertices and the exact full original tree identity

The new Lagrangian contains gHphi²/2 and Cphi4/24. Labelled differentiation gives vertex factors g and C. Two cubic vertices and the Feynman heavy propagator give

(ig)² i/(s-M_H²)=i g²/(M_H²-s).

Adding the three channels and the contact yields the complete new four-scalar tree. There are no higher fundamental vertices in this two-field model that change this tree. No heavy channel is discarded.

Set xi_i=channel_i-2. The physical on-shell identity s+t+u=4 gives

sum xi_i=-2,
sum xi_i³=3stu-8.

The second identity is MASSIVE and retains the potential constant. Replacing it by a massless identity loses part of the original amplitude.

For every channel away from its pole,

1/(D-xi)=1/D+xi/D²+xi²/D³+xi³/D4
            +xi4/[D4(D-xi)].

The contact C=-g²(3/D-2/D²) cancels the summed n0 and n1 terms exactly. The original parameter relations g²/(2D³)=lambda and g²/D4=gamma then give

A_V2S=A_original+R,
A_original=2lambda sum(channel-2)²+3gamma stu-8gamma,
R=gamma sum(channel-2)^4/[D-(channel-2)].

This is a rational identity, not a Taylor expression with an unstated remainder. The original target is read from the source-pinned S177 report/package and independently reconstructed in the tests. All three exchange denominators remain.

At t0, the shifted channels are(v,-2,-v), so

R(v,0)=16gamma/(D+2)+2gamma D v4/(D²-v²).

The v² coefficient and its first transfer derivative agree exactly with the original4lambda and-3gamma. The first new forward coefficient is b40=gamma²/lambda, and the constant differs by16gamma/(D+2). The complete forward amplitude and all higher coefficients are not falsely identified with the original tree. The positive heavy atom and its moments are the same as in S232 because the new contact affects only the subtraction function.

This result matches an on-shell four-point observable with an explicit remainder. It does not match off-shell actions, full independent functions, six-point and higher amplitudes, physical quantum coefficients or the bounce dictionary. A separate parent construction is required for those claims.
