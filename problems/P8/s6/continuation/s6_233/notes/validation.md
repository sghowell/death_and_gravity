# Independent model checks and immutable publication boundary

This checkpoint has42 named exact identities containing42 scalar entries,23 proof gates,nine controls and213 rejected unsupported inputs. It adds one separately named matching model, for89 matching rows, while retaining the same nine original primitive statuses.

Independent tests derive the full kinetic and vacuum mass matrices from the Lagrangian, complete the heavy potential square and verify a second global coercivity identity. Removing the contact or violating the required gap condition provides negative potential controls. The labelled cubic/contact factors and the Feynman exchange sign are reconstructed independently.

The full original on-shell amplitude is compared using the exact massive shifted-channel identities and a finite geometric remainder. Tests retain the potential term, all three heavy denominators, the forward constant shift and the nonzero higher coefficient. Exact physical-angle fixtures and rational actual-parameter margins verify the uniform relative error.

Complete angular primitives are differentiated independently, with finite threshold limits. Separate80-digit angular quadratures test the full integrals and the first elastic comparison. Positive Rodrigues projections detect the new higher even partial waves. Actual-hierarchy diagnostics use1000 digits to resolve the cancellation of the contact and exchange pieces at low energy, including the much smaller remainder. They are diagnostics, not continuum certificates.

Negative scope controls reject exact unitarity, a full quantum1/60 error, original-parent replacement, complete independent-function matching, a physical cutoff, omitted heavy poles, an l0/l2-only new cut, a massless target, unproved mass/residue or width control, finite-gravity completion and P8 closure.

The17 immutable scientific inputs are six modules, two tests, two root documents and seven notes. The report has20 top-level fields with individual mutation rejection and separate missing/extra checks. Source-pinned predecessors are fully rebuilt in the read-only native lineage.

Private science, source/field manifest and lint/format checks precede freezing. Native/direct/ordinary/CLI retain original SymPy. Only full P8 regression uses the audited exact-GCD adapter. Every fresh replay path must pass before publication. Actual timings and private probe corrections are recorded in the external publication assessment, not by editing frozen source bytes.

The global potential and uniform angular arguments are written classical proofs, not FORMALIZED. None of the tests establishes a full quantum UV completion or the original common-parent bounce.
