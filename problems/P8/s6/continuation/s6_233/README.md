# V2S-T1: a separate local two-scalar tree matching model

This checkpoint constructs a SEPARATELY NAMED local polynomial light/heavy scalar model. It does not change or identify itself with the original affine/Proca/DHOST parent. Original P8 remains OPEN.

V2S-T1 has canonical kinetic terms, light mass1 and

D=2lambda/gamma,
M_H²=D+2,
g=1/8192,
C=-g²(3/D-2/D²).

Its potential is nonnegative and coercive with a unique flat classical vacuum. At the actual parameters an exact sum of squares proves the global bound

V>=(phi²+H²)/6.

The literal exchange/contact tree amplitude equals the COMPLETE original S177/S182 massive scalar tree plus a displayed rational remainder:

A_V2S-T1=A_original+gamma sum(channel-2)^4/[D-(channel-2)].

For every physical angle and4<=s<=10^196, this remainder is positive and less than1/60 of the original tree amplitude. This is a controlled complete-TREE comparison through COM energy10^98, not an error bound for the full quantum amplitude.

The model retains the required tree b20=4lambda, b21=-3gamma and nonzero b40=gamma²/lambda. Its forward constant and higher coefficients are explicitly different. The heavy pole is retained, not removed.

Complete rational angular integrals include infinitely many even partial waves. In the named window, the first elastic coefficient lies between the original one and(61/60)² times it. Its relative difference is therefore below121/3600, with both coefficients vanishing at threshold. This is not the full real loop amplitude or an all-loop error estimate.

A bounded classical potential, a positive tree pole and these matching bounds do not provide a full quantum UV theory or a common-parent bounce. The original full independent functions, state, finite-gravity Regge problem and nonlinear matching remain separate.

See FORMULATION and all seven notes. The package uses17 frozen sources, independent algebra and numerical diagnostics with explicit scope; its written arguments are not FORMALIZED.
