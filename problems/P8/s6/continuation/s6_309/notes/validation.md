# Validation boundary and independent diagnostics

Eighteen ASCII, newline-terminated sources define seven modules, two test
files and nine Markdown files. The native report has twenty fields.
There are 352 named/scalar checks, 63 proof gates, 8 controls and
73 rejected inputs. Scientific symbolic payloads contain no floating-point values.
Native/direct/ordinary/CLI retain original SymPy; the exact-GCD adapter
is confined to FULL regression.

Independent diagnostics check state-dependent marked Bose counting
through four total labels, literal one- and two-soft simplices, marked
energy moments, and complete finite-regulator series with decreasing e.
Both a>0 and a=0 with negative analytic conversion are checked; the
latter contains alternating finite-e terms and tests the signed boundary.

Convolution calibrations retain state-dependent index and finite
prefactor, a signed marked kernel, the remaining-energy endpoint, and
an exponentially small threshold. Three actual original47-tree samples
separately verify negative residual densities, positive full Born and
the full massive-plus-null soft index. Numerical calibration is not
claimed as a certified quadrature error bound or as the uniform proof.

The first independent probe correctly rejected one synthetic convolution
state whose chosen negative index slope crossed below zero. That private
test input was changed from -10*omega/kappa to -2*omega/kappa, keeping
its nonconstant negative slope while remaining inside the stipulated
nonnegative physical-index domain. Its assertions and tolerances were
not relaxed. The corrected entire probe passed, including all actual
original-source checks. No frozen scientific source was changed.

Cold and distinct fresh-root preflights check exports, exact counts,
proof gates, float-free serialization and all scientific tests. The fresh
run forces the entire immediate parent's packets first. Exact three-way
source comparison precedes freeze. The native helper then receives one
specification, followed only by documented chunk acknowledgments.

Raw bytes, hashes, canonical REPORT AST, source manifest and all retained
frontiers are checked before raw freeze. Independent own ordinary/CLI and
the captured full P8 suite are later publication requirements. Their
outcomes belong in the external assessment, not frozen sources.
At most one FULL and one own pair run; no resource diagnostics or blind
retries are used.
