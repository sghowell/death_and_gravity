# Validation contract

The scientific checks use literal derivatives of the full old F and R, complete new vacuum couplings, exhaustive connected degree patterns and all720 permutations of each six-field vertex. Dimensional finite parts are checked by Laurent expansion, not by substituting d4 first.

An independent implementation treats the six plane-wave amplitudes as commuting nilpotent variables. It multiplies the literal gradients, Hessians, L3 and L4 directly and extracts the coefficient containing every external/internal mode once. A signed sphere rule, exact on degree-four moments, evaluates the isotropic contraction independently in dimensions3,4,5. Its axis weights are (4-d)/[2d(d+2)] for each signed axis; its signed two-axis weights are1/[d(d+2)] at (plus/minus e_i plus/minus e_j)/sqrt(2). Negative d5 weights are a quadrature identity, not a physical measure. Moment identities and all six local factors are tested at high precision; numerical agreement is diagnostic, not an interval proof.

The mixed loop is also computed independently by shifting its full numerator in Feynman parameters. The resulting positive log integral agrees with the complete A0/B0 reduction at several finite mass ratios. The exact integration-by-parts identity supplies the analytic equality and uniform bound.

Tests separately check all source classes, full source-onepoint cancellation, exact centered value subtraction, analytic forward derivatives including the nonzero b40 term, exact rational actual-parameter inequalities, the finite switch and its full mixed four-jet bound, and rejection of unsupported scope claims.

The package fixes17 source/proof/test inputs and20 report fields. It contains66 named exact scalar checks,44 gates,9 controls and285 rejected invalid input/scope cases. Every report field, extra/missing field and source hash is checked. The original nine primitive rows and94 previous matching records remain unchanged; one new scoped record is appended.

Before freezing, run private scientific checks, formatting/lint, exact source/count/field preflight, and the long-lived native rebuild with original SymPy. After adding the frozen bytes, independently rerun repository science, ordinary pytest, CLI replay and complete P8 regression. Only full regression uses the audited exact-GCD adapter; native/direct/ordinary/CLI use original SymPy. Record all actual outcomes and private corrections in an external assessment; do not rewrite frozen scientific history.
