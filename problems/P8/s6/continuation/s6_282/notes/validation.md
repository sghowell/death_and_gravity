# Immutable validation contract

Seven source modules, two tests, two root documents and seven proof notes
make exactly18 ASCII source inputs. The read-only native report has exactly
20 top-level fields. It pins the accepted S281 report and rebuilds the whole
ancestry; S279 stays rejected. Every field mutation, missing field and extra
field is rejected. Exact source hashes are part of the report.

The proof checks are algebraic identities plus named gates to the written
analytic arguments; written complex analysis is not Lean FORMALIZED.
Independent tests check real kernel branches, full forward agreement,
two-axis reduction, threshold removal, complete pole-running identities
and preserved source/cache/frontier contracts. Numerical whole-angle
quadrature diagnostics are not substituted for these proofs.

Freeze only after original-SymPy private and fresh-root preflights agree on
all18 hashes, counts, gates, import exports and science tests, including
forced full-parent warm ordering. After that, sources and raw native bytes
must never be edited. Any correction is a successor or external assessment.

Publication additionally requires the native raw byte/source/AST audit,
separate original-SymPy ordinary and CLI replays, and a captured complete
FULL regression. The exact-GCD adapter is FULL-only. A running session or
predicted count is not a success. Actual validation evidence is recorded
outside this immutable source manifest.
