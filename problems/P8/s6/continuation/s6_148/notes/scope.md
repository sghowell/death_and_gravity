# One-row advance, original P8 open

Only scalar_Phi2_W1_F0 advances to bounded paired nonlocal and
finite MS local references. The two direct quadratic primitive
mass/slope rows and all four quartic primitive rows are unchanged.
The two vacuum primitive rows remain wholly unevaluated.

The whole enlarged two-loop matching task is not reduced to the
primitive ledger: additional local parameter/counterterm and
canonical contributions remain. Vacuum source cross terms are
explicitly kept open. Full matched pole/error, higher-loop
truncation and V contour/cut control, finite-gravity G and the
common-parent B dictionary remain unproved. Scoped P8(a) and
A.20-A.23 are unchanged. Original P8(b) and original P8 remain OPEN.

The adopted task concerns controlled finite-EFT/necessary-positivity
tests; no all-orders UV existence theorem is added. A failure of
one chosen matching ansatz would not classify an entire ladder row.

The parent report and all 18 source/proof/test files are pinned.
Exact algebra and independent numerical tests support written
analytic arguments, not formalization or external peer review.
Native, ordinary, CLI and direct science use unmodified SymPy.
Only complete regression uses its separately audited GCD adapter.
