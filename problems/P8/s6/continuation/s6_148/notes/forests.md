# Fixed source, stationary H and whole-forest ownership

The complete scalar quadratic half-trace differentiated once in
its light covariance is the S6.136 family. Its local stationary-H
coefficient is (L-g/M)T_insert/2, and its mixed coefficient is
-g F(M;s), with g=G^2. There is one inserted light line, not two.

The associated H one-point graph is G T_insert/2. The original
reference condition H one-point=0 fixes this sector's contribution
to the source counterterm to J_H,2=-G T_insert/2. It is not an
additional adjustable source. Eliminating H in

    M H^2/2+G H Phi^2/2+J_H,2 H

gives the source-induced Phi mass -G J_H,2/M=+g T_insert/(2M).
It cancels the stationary-heavy local piece, leaving

    f_row,MS(s)=L T_insert,MS/2-g F_MS(M;s).

The cancellation holds for the whole regulated expressions and
therefore for their poles and finite parts. No isolated divergent
tadpole is assigned a finite value before the whole reference is
specified. The displayed finite tadpole is fixed by the common
regulator and MS outer prescription after the complete inner
on-shell fermion mass/residue forest.

The literal H elimination also gives a vacuum constant -J_H^2/(2M).
The order-two source shown here does not itself contribute this
square at order two. Cross terms with the order-one source belong
at their actual loop order. The already present order-one source
square does contribute at order two and remains an explicit
vacuum-reference obligation; no source square is declared absent
from the full theory.

This family is already paired with its inner proper counterterms.
Do not add the same occurrences again as independent insertions.
The raw primitive chord rows and finite parameter changes inside
other one-loop graphs remain separate. Computing this reference
does not complete their shared canonical matching.
