# Exact boundary of the first-loop coefficient result

Only the unchanged separate V2S-T1-OS4 prescription is used. Its finite contact, all UV subtractions, first-order light mass/residue normalization and full mass-ordered loop representation are retained. Original affine/DHOST/Proca sources are not edited.

The new result controls the ENTIRE first-loop b20,b21,b40 corrections. The complex domain is proved directly, and every later inverse-mass term is bounded before Cauchy estimates are applied. It is not an inference about derivatives from a bound on real scattering angles.

The two originally matched low coefficients remain close through first loop order. The higher forward coefficient remains positive in the explicitly tree-plus-first-loop amplitude. No sign is assigned to its first correction alone, and no exact Gram saturation or all-order positive-measure property is asserted.

The existing OS4 constant drops out of all three derivatives. No new finite derivative condition is imposed and no unknown heavy exchange or spectral weight is subtracted. The first-order external normalization is not silently promoted to exact quantum LSZ data.

These results do not bound omitted loop orders. An explicitly truncated coefficient is not an exact physical S-matrix coefficient, even when its first correction is small. Exact analyticity/unitarity, the full high-energy arc, heavy resonance control and a quantum UV construction remain separate.

Nothing here identifies the separate flat-space model with the original common parent. Its connected field/derivative domain, induced bounce operators, quantum state, thresholds, finite-gravity Regge remainder and nonlinear stability still require their own calculations. No flat-space inequality is applied independently at every bounce time.

One first-loop coefficient record is added for93 matching records. All nine original primitive statuses remain unchanged. Original V/G/B/P8 remain OPEN. The missing estimates are research work, not a user-choice blocker.
