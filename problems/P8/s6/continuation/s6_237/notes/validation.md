# Independent checks and immutable publication boundary

The package has60 named identities containing60 scalar entries,36 proof gates,nine controls and273 rejected unsupported inputs. One first-loop coefficient matching record gives93 records while retaining all nine original primitive statuses. Its17 sources are six modules, two tests, two root documents and seven notes; the report has20 top-level fields with every-field mutation and missing/extra rejection.

Independent derivatives of the FULL analytic P,Q functions verify every coefficient through degree3, including the removable beta0 case. A dedicated helper-origin regression distinguishes log((1+d)/2) from a log1p expansion with the wrong increment.

Direct complex light-parameter quadratures verify every logarithmic moment recurrence. Independent polynomial integrals check the light moments and heavy-angle factorials. The complete channel calculation retains distinct logarithmic contributions until the exact identities cancel them; crossing-symmetric polynomial forms and all coefficient factors are checked separately.

Complex coefficient-circle samples use genuinely complex L and b, not only real fixtures. Complete primitive and box remainders are checked at small masses and at the actual hierarchy, with1000 decimal digits in the latter case. The written disk and Cauchy proofs, not those samples, establish the uniform tails.

Joint-bidisk fixtures cover real, imaginary and mixed boundary points and every parameter endpoint. Complete finite product sums independently verify all bubble/triangle cross terms and the final constant budget. Actual rational comparisons establish the signed b20/b21 intervals and all three relative-error margins without floating-point logarithm assumptions.

A separate numerical calculation differentiates the complete UNEXPANDED loop expression. It integrates every triangle and all six mass-ordered boxes on a smooth subthreshold domain, then obtains b20,b21,b40 directly, with the correct factorials. All three agree with their complete-error enclosures at an independent large-mass fixture. This is a high-precision quadrature diagnostic, not an interval-quadrature certificate.

Unsupported scopes reject exact all-loop coefficients, derivative bounds inferred from a real window, new finite derivative conditions, heavy-weight subtraction, a sign for the first b40 correction, exact Gram saturation, original-parent replacement and P8 closure.

Private science, lint/format and all source/field manifests precede freezing. Timings and any private implementation corrections belong in the external publication assessment; frozen source bytes are never edited to update them. Source-pinned predecessors are rebuilt read-only. Native/direct/ordinary/CLI retain original SymPy; only full regression uses the audited exact-GCD adapter. Every fresh replay path must pass before publication.

The exact algebra accompanies written joint-holomorphy and complete analytic-tail arguments. These are not FORMALIZED and do not certify unproved exact physical assumptions.
