# What advances and what does not

All nine primitive statuses are copied unchanged from S6.149.
Only the isolated fixed-sigma nonlocal order-two conversion is
discharged. The remaining scale/parameter/field map and cross
terms, other counterterm/vacuum/source assembly, full matched
two-loop pole and amplitude error, physical truncation, V
contour/cut control, finite-gravity G and common-parent B are
still open. This is an explicit matching ledger, not a new
claim that primitive completion implies full-model matching.

The report recursively validates the frozen parent and pins
18 source/proof/test files and every report field. Independent
tests include radial integration at nonzero regulator,
nonconstant affine maps, literal finite-order coupling and
amplitude specializations, both-heavy-term mutations and
epsilon-times-pole finite-defect checks. Written arguments and
exact replay are not formalization or independent peer review.

Native, ordinary, CLI and direct science use unmodified SymPy.
Only the complete regression runner uses the separately audited
exact GCD adapter. No frozen ancestor is edited. Scoped P8(a)
and A.20-A.23 are unchanged. Original P8(b) and original P8
remain OPEN under the adopted finite-EFT/necessary-positivity
contract; no all-orders UV construction is newly required.
