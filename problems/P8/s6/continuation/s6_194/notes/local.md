# Full nonlinear finite local-current estimate

Only after S193 takes the fixed general-dimensional prescription
limit, use the four-dimensional identity

    -4 a4s = -R^2/30-Ricci^2/15-Euler/45.

The compact Euler variation is zero in physical dimension4.
It was not discarded before the dimension limit. At mu=m the
unchanged local density is
[5m^4/2+(5/3)m^2R-4a4s]/(64pi^2).

The volume term is identically independent of unimodular gamma.
Indeed det exp(gamma)=1 and
tr(exp(-gamma) Dexp_gamma[D])=tr D=0.
It therefore contributes no shear current or amplitude derivative.
This is an exact cancellation, not an estimate of its large m^4
coefficient. The remaining lower-index Euler tensor is

    Efinite=(5/3)m^2 E_R-E_R2/30-E_Ric2/15.

For m=1000, all terms of the amplitude jet norm through order2
are bounded by

    (5/3)m^2*300G^6+(1/30+1/15)*1e6G^11 <1e10G^11.

The current pairs its twice-raised form with
delta g_D=a^2 Dexp_(epsilon Gamma)[D]. Ordered Frechet derivatives
in epsilon give operator bounds <=2a^2 for amplitude orders0,1,2,
since ||D||,||Gamma||<=1. Hence its C0,2 component jet norm is
at most30(25/16)^2<G. No time derivative of D is required after
the complete Euler integration by parts. The detector may remain
an arbitrary smooth compact test with the stated value bound.

Both raised inverse metrics contribute G^2, the detector contributes
G, and a^3<(25/16)^3<4. Bound1/(64pi^2)<1 conservatively. Passing
from the factorial-weighted amplitude norm to any raw derivative
of order0,1,2 costs at most2. The complete local-current bound is

    8e10 G^14 =1.31072e85 <1e86.

This retains every metric-index, density and detector factor.
It is uniform on the admitted nonlinear shear amplitude interval,
not only the Hessian at gamma0. Compact variations justify the
Euler identities; their local current representatives then have
the pointwise bounds used in the C0 estimate.
