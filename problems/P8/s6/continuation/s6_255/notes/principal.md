# On-shell full principal application and regular field-map bridge

For each sufficiently small FIXED epsilon nonzero, the exact initial
root has r=R-1 nonzero and L2 nonzero. The preceding local classical
solution has positive N,a,R,D,Z,J,K,Y,Yv and gamma near its central
data. Continuity gives a closed subinterval of positive length on
which every positive and nonzero margin stays strict. Its heavy field
is generally rolling; holding it at zero is not used.

The full rolling parameter map belongs to the complete generic
four-mode Lagrangian of S254. Its exact all-auxiliary reduction and
time-dependent canonical transformation therefore apply to every
entry of this operator. The lower on-shell volume contacts derived
above vanish, but the whole fast quartic coefficient is unchanged:

    Bfast=r^2 L2^2 Yv/(8D^2 J a^4)>0.

The coupled8-phase characteristic at orderP^(3/2) is
lambda^4(lambda^4-Bfast). The two TT and two transverse-Proca pairs
are retained and give the full16-phase characteristic
lambda^12(lambda^4-Bfast). This is not a determinant of a chosen
subsystem or a separate-vector approximation.

On this fixed interval the S254 full rational remainder is uniformly
O(P^-1/2) after normalization. Its exact time connections, smooth
fast-cycle/slow-nilpotent chart and invariant-cone proof give a lower
bound c P^-m exp[c1 P^(3/2)|I|] for the physical linearized propagator
along suitable data. Thus these actual local classical solutions lack
the stated all-momentum finite-loss Sobolev estimate for their Jacobi
operator. The same Fourier-packet argument applies on R^3. Neither
a nonlinear Cauchy theorem nor a physical cutoff verdict is inferred
solely from this linearized conclusion.

The on-shell status also addresses the specific off-shell caveat of
S254. For a nonlinear field map F and the full local classical action,
the complete second-variation chain rule is

    delta^2(S composed F)=DF* (delta^2 S) DF + delta S D^2F,

with the functional adjoint and retained boundaries. For compactly
supported variations on the constructed solution, every full Euler
expression is zero by the preceding reconstruction. The onepoint term
therefore vanishes. The independent polynomial field-map calculation
checks a genuinely nonzero off-shell contact and its on-shell removal;
the functional integration-by-parts argument is written, not FORMALIZED.

Within regular local charts whose induced physical phase matrices C(P,t)
and inverses are bounded by a finite polynomial in P on I, the exact
evolutions obey U_new=C(t) U_old C(s)^-1, including the Cdot time term.
Their norms differ by at most polynomial factors in either direction,
which cannot remove exp(cP^(3/2)|I|). This includes the stated regular
physical, auxiliary-recovery and canonical charts. An arbitrary nonlocal
or singular map, a different high-derivative/UV action, or the full
nonstationary quantum effective Hessian is not covered by this statement.

The order of limits remains essential: fix the solution and compact
strict-margin interval first, then send P to infinity. The constants
and momentum threshold may diverge as epsilon tends to zero or the
heavy mass grows. No threshold is compared with a Wilsonian cutoff.
