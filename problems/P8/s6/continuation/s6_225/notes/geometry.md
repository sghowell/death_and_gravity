# Actual curved first-curvature coordinates and coefficient bounds

Choose an axis along the comoving spatial Fourier momentum. Rotations recover arbitrary direction because the background is spatially isotropic; Pi is a bounded directionwise projector. The exact scalar metric chart is

ds^2=a(eta)^2[deta^2-exp(2(w-c))dx^2-exp(2w)(dy^2+dz^2)].

The background conformal clock is fixed before variation. The source/detector amplitudes multiply exp(+ikx) and exp(-ikx) respectively. Production contracts the complete four-dimensional Christoffel and Ricci tensors through both independent variations, with R_old the source-pinned sign opposite the displayed standard Ricci contraction.

Its unvaried scalar curvature is6U/a^2. The two first variations are6S_i phase_i/a^2. The first Weyl component is a^2 W_i phase_i/3 and its background vanishes. The independent linear test checks all256 Weyl components, including24 nonzero components, against their conformally rescaled flat values. Their complete contraction gives4W^2/3 after density cancellation, hence the mixed Hessian gives8WD WG/3. The argument uses the full four-dimensional contraction, not a single-component guess for the norm.

The actual curvature-coordinate operator is Bc=L D^2+Q+C D, with

L=[[1,-1/3],[0,-1]],
Q=q[[2/3,0],[-1,0]],
C=[[3h,-h],[0,0]].

L is invertible. For the original proper-time variable t,

h=aH=4t(1+t^2),
h'=a d_t h=4(1+t^2)^2(1+3t^2),
U=h'+h^2=4(1+t^2)^2(1+7t^2).

These use the actual source-pinned scale factor and the same R_old as S189, whose background expression equals6U/a^2. On |t|<=1/2, polynomial positivity and parity give

|h|<=5/2, |h'|<=175/16, U<=275/16<18,
a^4<=390625/65536<6.

Since the Frobenius squares are10h^2 and10(h')^2, the Euclidean operator norms satisfy ||C||<8 and ||C'||<35. These are whole-interval rational enclosures, not sampled bounds.

A conformal primitive is t/[2(1+t^2)]+atan(t)/2. The complete conformal length is2/5+atan(1/2)<1; alternatively a>=1 bounds the integral of1/a by the proper length1. These facts fix the actual slab and do not change the physical background or preparation.

The q=0 formulas have a continuous directionwise scalar-amplitude extension. A projector at a single Fourier point is immaterial to the Sobolev multiplier statement. This is not a replacement of literal homogeneous lapse/shift constraints with a nonzero-transfer limit.
