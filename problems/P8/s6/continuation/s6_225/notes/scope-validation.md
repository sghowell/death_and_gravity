# Scope, independent tests and validation boundary

This checkpoint appends exactly one matching input after S224. All nine original primitive rows and all prior matching rows retain their exact statuses. Its accepted stages are actual curvature coordinates, complete finite local Hessian and ordered curved REFERENCE inverse. Type-invalid scope requests, changed mass/kappa/preparation, missing rows and false promotions to a full inverse, stability or P8 closure are rejected.

The exact report has38 named residuals,55 scalar entries,24 gates,nine controls and169 rejected inputs. Its17 source inputs are six package files, two test files, two root documents and these seven notes. It has20 top-level fields; missing, extra and every-field mutations are rejected by exact read-only replay.

Independent science includes:
- all256 linearized four-dimensional Weyl components and the complete contraction;
- a separate ADM/warped-spatial-metric reconstruction of the full local Hessian;
- original S189 homogeneous unit-Frobenius tensor coefficients;
- whole-interval polynomial coefficient and conformal-clock bounds;
- three nonconstant-coefficient integrated formal-adjoint fixtures, includingq0;
- three finite causal matrix fixtures, both inverse products, coefficient-derivative deletion, reversed factor order and reversed density multiplication;
- exact Volterra and simplex constants, serialization, and all scope/false-completion controls.

A private local probe initially extracted a coefficient before expanding a product; expanding first fixed the test without changing a curvature formula. The first integrated independent suite passed244 tests and failed one finite-matrix fixture: its coarse sample h=1/3 made a discrete adjoint diagonal vanish atq0. Replacing only the fixture's sample coefficients with1/5 and2/7 made that independent finite algebra nonsingular at all three test momenta. The corrected suite passed245 tests in1.95 seconds. This is not a change to the actual profile, continuum operator, Volterra theorem or q0 boundary. It also illustrates why a finite ordering fixture is not being presented as a continuum discretization certificate.

Before freezing, format/lint, native exact-packet preflight and a fresh private scientific run must succeed. After freezing, the native report is rebuilt with original SymPy and transferred without reserialization, then every source/proof/test/report hash is verified. Fresh direct, ordinary, standalone CLI and full P8 regression must terminate successfully before external publication. Their final outcomes belong in the separate audit, not in retrospective edits to these frozen files. Only full regression may use the audited exact-GCD adapter; native/direct/ordinary/CLI retain original SymPy.

## Still unproved

The first-curvature coordinates and complete local Hessian are ACTUAL. The middle scalar factors in Aref remain the explicitly chosen FLAT reference. On the true curved background the conformal mass scale ma(eta), ultraviolet local log-scale terms, actual state, contacts and the complete classical/matter interface require a matched normal form and compatible-space bounds. The explicit finite local remainder is not zero, and its differential order does not prove it small onC_tH^r. The older weak high-derivative current bound does not supply that missing self-map.

This checkpoint therefore does not establish the complete curved quantum inverse, finite-coupling or nonlinear common-parent control, quantum background/stability, heavy/physical cutoff, original V/G/B or P8 closure. The scope is not a user-intervention blocker: the next work is the actual matched normal form and a compatible joint time-space estimate. Written functional/geometry arguments are not FORMALIZED.
