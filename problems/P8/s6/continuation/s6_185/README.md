# Actual CD state: complete source-force noise

This continuation bounds a conditional smeared scalar
observable in the unchanged all-order CD vector state.
It supplements the separate Minkowski-vacuum result;
neither state is substituted for the other.

The full three-mode CD covariance has coefficients108/m
and144/m^3 in its L2/spatial-gradient estimate. The complete
near-clock source gives a mixed spatial Frechet bound
4e8 delta in the declared H3 norm.

The force divided by kappa has standard deviation below
2e-392 delta per raw-clock test norm. For a canonical
test the kappa suppression cancels: the bound is2e8 delta.
The explicit smaller ball delta<=1e-14 therefore gives
canonical source noise below2e-6 and the prior canonical
mean relative coefficient below4e-18.

These are source-sector bounds, not metric stress noise,
quantum solution errors, cutoff or V/G/B closure.
See FORMULATION.md and the seven written proof notes.
The read-only verifier uses an explicit18-file manifest.
Original P8 remains OPEN.
