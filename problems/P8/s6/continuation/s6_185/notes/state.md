# Preserve the actual all-order state

The source-aware conditional canonical measure in S6.176
specifies the actual S6.55 Borel Cauchy preparation on CD.
A source shift at fixed physical metric changes only the
coherent mean. It leaves the connected covariance, the
homogeneous free mode functions and their physical
constraint reconstruction unchanged.

Use S6.85's exact variation-of-constants comparison to W8
only as a proof device. Its source-pinned initial mixing
and evolution constants obey

    |beta| <= B6/nu^6 + E/nu^10
           <= B6/m^6 + E/m^10 < 1,

where B6=1813229, E=5347035781757616 and nu>=m=1000.
The readout constraint alpha' fref+beta' conjugate(fref)=0
and the exact conserved indefinite norm imply
|alpha|^2-|beta|^2=1. Thus |alpha|+|beta|<3 on the whole
slab for every momentum, not just the initial slice.

No finite adiabatic-order initial state replaces the actual
locally finite all-order Cauchy sum. Independent initial
fixtures use that very sum at frequencies1000 and3000;
the latter activates its sixth-order term. Evolution
fixtures solve the actual time-dependent oscillator from
those data, retaining the canonical momentum shift.

These mode estimates are specific to the unchanged state
and compact observation slab. They are not an assertion
about every Hadamard state or global-time behavior.
