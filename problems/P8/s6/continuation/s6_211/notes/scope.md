# Unchanged scope and open research obligations

The homogeneous P0 anchor includes its full original contact and fixed prescription. The same actual state, CD slab, unit-W8 comparison correction, mass, coupling and physical metric are retained. No physical state or exact bisolution is obtained by declaring the reference modes normalized.

The prior known finite remainder and original pair-band conversion are neither removed nor counted twice. The new result is a spatial UV coefficient/contact difference and exact fixed pole identity. It is not a claim that the fixed finite local action already equals the full remaining quantum spatial response.

Still required: dimensional finite spatial matching with all evanescent terms; full spatial and scalar/metric reduced inverse estimates; a controlled finite-coupling background and stability; the remaining parent sectors, measure and loops; heavy/threshold/cutoff and omitted-order matching; canonical vacuum cuts/contour/truncation; and finite-gravity IR/Regge bounds. Small metric normalization alone cannot settle any of these.

All nine primitive frontier rows are unchanged. Only one scoped matching-obligation row is appended. Primitive/status substitutions, changed matching rows, altered mass/coupling/slab and unsupported exact input types are rejected. Original P8(b), V/G/B and P8 remain OPEN. Scoped P8(a) and A.20-A.23 are unchanged. No user-choice blocker is inferred from missing estimates.
