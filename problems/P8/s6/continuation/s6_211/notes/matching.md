# Actual spatial pole identity and finite boundary

The existing local Hessians are in conformal time eta with d_eta=a d_t. Transform the source derivatives first, then divide the conformal Hessian by a to use the same proper-clock measure as the quantum current. Let B_P be the tracefree symmetrized longitudinal map and C_P the double-divergence map; their detector contractions are p^2 V and p^4 W.

The spatial differences of the existing Hessians are

    DeltaH_Rold=-a p^2(T-2V)Gamma/2,

    DeltaH_C2=2a p^2(T-V)(Gamma_second+H Gamma_first)
                +p^4/a*(T-2V+2W/3)Gamma,

    DeltaH_Rold2=-6aQ p^2(T-2V)Gamma+2p^4 W Gamma/a.

Here T,V,W indicate the corresponding bilinear tensor forms, separately applied to each displayed source jet. Literal differentiation of the existing conformal operators verifies the proper-time formulas in every channel.

The actual complete angular logarithmic coefficient computed from the constrained W8 modes agrees exactly with

    [m^2 DeltaH_Rold+(13/60)DeltaH_C2+(1/36)DeltaH_Rold2]/(32pi^2).

In particular the Einstein spatial mass term is negative under the existing R_old convention. No curvature sign is changed to obtain agreement.

The original Proca pole has density[3m^4+m^2R_old+2a4V]/(64pi^2 epsilon). In four dimensions,

    2a4V=13C^2/60+R_old^2/36-7Euler/20.

The dimensional radial tail integral is integral_m^infinity r^(-1-2epsilon)dr=m^(-2epsilon)/(2epsilon). Its residue is half the physical logarithmic coefficient, exactly accounting for the displayed normalization. This proves the spatial pole coefficient identity, not the finite dimension limit.

The fixed finite local curvature density at the already selected scale is

    -4a4_scalar=-C^2/30-R_old^2/18+Euler/90.

It comes in part from evanescent dimensional coefficients. Agreement of physical dimension3 logarithms cannot determine those terms: d-dependent polarization multiplicity, angular contractions, Hamiltonian and curvature continuation remain in finite matching. Compact Euler variation is removed only after the original dimension limit. The existing finite target is not inserted as if its spatial quantum matching had been shown.

The one-ball power coefficient is not covariant: the vector channel has a nonzero DeltaF2 even though its static Einstein spatial Hessian vanishes. The unchanged original two-leg conversion and this power term must be treated within the original prescription, not relabeled as new physical counterterms.
