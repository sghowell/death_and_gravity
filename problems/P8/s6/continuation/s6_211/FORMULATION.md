# Formulation: actual spatial matching difference

## Unchanged setting

Use the same CD slab -1/2<=t<=1/2, a=(1+t^2)^2, H=a'/a, Q=H'+2H^2, m=1000, kappa=10^800, original physical metric and prepared state. The unit-W8 reference is only the unchanged comparison after the full state correction. It is not a substituted exact bisolution or physical state.

For symmetric tracefree spatial detector/source matrices, p=|P| and e=P/p, define T(D,G)=tr(DG), V(D,G)=(De).(Ge), W(D,G)=(e.De)(e.Ge). At P0 polynomial combinations extend continuously. The source time derivatives are independent slots; detector time stays fixed before coincidence.

## Exact symbol and contact difference

Compute every angular coefficient F_(j,r,d) of the original endpoint labels0..4, source derivatives0..j and inverse-radius degrees0..4-j. The three rotational channels give105 entries. Subtract only their P0 multipliers. Exactly11 differences are nonzero.

The total second-grade coefficient is

    DeltaF2[D,Gamma]=-p^2(47T-50V)/(3360pi^2 a).

The fourth-grade source-value coefficient is

    [-a p^2(m^2/2+Q/6)(T-2V)
       +p^4/a*(13(T-2V)/60+W/5)]/(32pi^2).

The fourth-grade first/second source derivative coefficients are respectively

    13a H p^2(T-V)/(960pi^2),
    13a p^2(T-V)/(960pi^2).

The full original one-leg contact depends on the pointwise mixed matrix(D Gamma+Gamma D)/2, hence only total external momentum. For P,-P it has no P dependence at each internal momentum. Its difference vanishes before integration, but its generally nonzero homogeneous anchor is retained.

For the one-ball symbol and K>=m, the exact radial difference is

    DeltaU_ball=DeltaF2*(K^2-m^2)/2+DeltaF4*log(K/m).

The lower-band finite polynomial is not omitted. The original two-leg conversion from S209/S210 remains separate and unchanged.

## Fixed pole, not finite dimensional matching

The actual curved logarithmic difference equals

    [m^2 DeltaH_Rold+(13/60)DeltaH_C2+(1/36)DeltaH_Rold2]/(32pi^2).

These are the existing full spatial Hessians, their R_old sign and their proper-time measure. The d=3-2epsilon radial residue is half the logarithmic coefficient, giving the original pole normalization. The finite dimensional target has different coefficients, including the evanescent scalar curvature contact. A physical dimension3 pole identity alone does not determine that finite match.

In the stated two-time/four-spatial-derivative norm Z24, coefficient bounds are1/200 and1e4 for DeltaF2 and DeltaF4. Multiplication by K^2/logK is still nonuniform.

## Limits

No fixed finite spatial matching, complete response or reduced mixed inverse is supplied here. No state reset, source switch, discarded odd endpoint, changed finite counterterm or covariant reinterpretation of the hard-cutoff power term is allowed. The nine primitive frontier rows remain unchanged; original V/G/B and P8 remain OPEN.
