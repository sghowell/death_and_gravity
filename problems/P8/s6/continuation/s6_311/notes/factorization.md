# Exact434=387+47 graph factorization

Cut the unique internal graviton edge separating {q1,q2} from all four
massive scalars. The hard side is the complete off-shell one-graviton
current U^{mn}(Q), Q=q1+q2:26matter+21Einstein trees. The two-real side
is one cubic Einstein vertex. Thus precisely47 of the434 graphs give

M_pair=-Vhhh(eps1,eps2,H_U;q1,q2,-Q)/(sqrt(kappa)*Q^2),
H_U=eta U eta-eta tr(eta U)/2,

where Vhhh includes the unit-coupling -4 cubic coefficient. The remaining
387 graphs contain no propagator whose momentum is q1+q2. This is a
root-cut bijection, not a soft approximation. A distinct evaluator
suppresses exactly the q1+q2 rooted current and verifies the factorization
for pure gravity198=177+21, the full diagnostic434=387+47, and the original
parameters434=387+47. All exact residuals vanish.

The rooted evaluator provides an independent check by suppressing only
the current whose external-label mask contains the two null rays and
no scalar. Every tree containing this pair propagator is removed once;
its two-scalar-root competitors remain. The count difference and entire
amplitude difference agree with the analytic cut formula at original
parameters as well as diagnostic pure and full couplings.

This factorization is exact at nonzero internal denominators. The
collinear bound applies to its conserved quotient by continuation from
noncollinear rays. It is not an arbitrary diagram deletion or a claim
that the remaining387 graphs are negligible.
