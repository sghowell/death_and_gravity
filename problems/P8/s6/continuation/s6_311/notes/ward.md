# General off-shell conservation before collinear contraction

For the Einstein part, the independent S304 invariant-index algebra has
been extended to Q^2=ell. Its base Gram entry p2.p3 is now
-mu-a-b-u1-u2-u3-ell/2, so p4 remains on shell without a null-Q assumption.
The scalar propagator is2p.Q+ell. The scalar gauge numerator is
-2(2p.Q+ell)p.xi, so the external Ward reduction stays-2p.xi.
The entire channel Ward polynomial still vanishes for arbitrary ell,
gauge contractions and massive-shell invariants, without a4D Gram
determinant. Bose relabeling covers all three channels. Two independent
rank-one tensor comparisons agree with the component21-tree current.

For the matter part define F_i=T(pi,pi+Q)/(2pi.Q+Q^2). Then
Q_lower.F_i=pi exactly. The C-sector divergence is C(sum_i pi+Q)=0.
For a heavy channel PL+PR+Q=0, dL=PL^2-n, dR=PR^2-n,
Q_lower.T(PL,PL+Q,n)=dR*PL-dL*(PL+Q).
The external, internal and two metric-density divergences sum to

PL/dR+PR/dL+PL/dL+PR/dR+Q(1/dL+1/dR)=0.

Hence Q_lower.U=0 for the COMPLETE47-tree hard current at arbitrary
admitted off-shell timelike or null Q, not just at a finite point.

The checks preserve a free Q_squared symbol throughout the invariant
polynomial. They also compare the full rank-one projected current,
not only its longitudinal part, with the component implementation.
The independently written matter tensor uses the complete26 graphs
and agrees with the difference of the full47 and pure21 rooted currents.
All comparisons are exact and keep original SymPy unchanged.

General conservation follows from the displayed polynomial/rational
identities. The finite timelike-root samples are normalization and
implementation calibrations, not their general proof.
