# Independent verification and immutable replay boundary

The report contains35 named exact residuals,35 scalar entries,25 gates,nine controls and183 rejected inputs. It has20 top-level fields and17 source inputs: six package files, two tests, two root documents and seven notes. Every field mutation, missing/extra field and improper scope promotion is rejected.

A private probe independently compared the stable exterior branch formula against eight original complex radial integrals at110-digit precision. It then checked132 channel/exterior/weighted-line cases, including both near-cut banks, exponentially large radii and momenta near the wave cone. These are diagnostics supplementing the continuous harmonic proof.

Two initial private algebra assertions compared equal but differently factored SymPy expressions structurally. Factoring the entire difference fixed the assertions; no mathematical formula was changed. The corrected probe passed in0.329 seconds.

The integrated scientific suite passed265 tests in0.65 seconds. It independently checks:
- both original complex radial factors and their stable first-sheet branch;
- full exterior and both-bank diagnostic points;
- joint frequency lines with zero, resonant and very large spatial momentum;
- exact whole-domain polynomial nonnegativity and cut-tail margins;
- five exact norm-bound profiles and their strict contraction constants;
- a noncommuting finite causal solve, both inverse products and an exact Neumann tail;
- the norm-below-one growing-reference-pole comparison;
- weighted integral, curvature-composition and physical density constants;
- all scope rejections and unmodified frontier rows.

Before freezing, native-packet preflight, formatting/lint and a fresh private scientific run must pass. After freezing, the native report is rebuilt with original SymPy, transferred without reserialization and checked against every source/proof/test hash. Fresh direct science, ordinary replay, standalone CLI and full P8 regression must all terminate successfully before publication. Outcomes belong in an external audit, not in post-hoc edits to frozen proof files.

Only full regression may use the audited exact-GCD adapter after its original tuple comparisons. Native/direct/ordinary/CLI retain original SymPy. Harmonic minimum principles, Fourier-Laplace functional analysis and causal graph arguments are written proofs, not FORMALIZED. Numerical diagnostics and finite matrices do not replace them.
