# Same reference and the precise finite family

The seed is the original pure nonzero-mode preparation, evaluated on
a stated finite torus mode set and propagated to u=0. Scalar and
tensor sectors retain the S251 full weighted canonical flow,
selection matrix, fixed bump and support, both symmetric boundary
shears and the central canonical map. The heavy scalar remains
the original fixed S240 SLE state, not the W6 auxiliary comparison.
The Proca sector remains the original pure coupled three-mode
graph. No occupation-only or diagonal covariance substitutes for
these prescriptions.

The finite product is well-defined and pure. It is not literally
the original R3 state, and no homogeneous reference is supplied.
It is a finite IR/UV evaluation family of the same nonzero-mode
prescription. S267 proves that its Gaussian wavefunction belongs
to the common kernel of the THREE residual torus translation
generators, allowing all invariant internal cross blocks.

The coherent window is this same fixed vector. Its displacements
resolve the identity but do not redefine the initial state.
Free evolution is not re-minimized: U_ref is the whole original
metaplectic mode evolution from0, and the interaction acts on the
unchanged seed. All full-H minus reference-H terms remain in the
interaction, even if linear or quadratic.

Coherent covariance follows directly from
T D(z)T^*=D(Tz) and T psi0=psi0. Consequently
T Q_V(a)T^*=Q_V(a composed with T^-1).
The full reduced source and moving-chart connections are spatially
translation invariant, as are the phase cutoff and free flow.
All three residual constraints are therefore preserved by the
actual finite regulated propagator.

There is no assertion that a nonlinear canonical pullback of a
Gaussian measure is again Gaussian. The canonical invariants of
S266 are not independent Gaussian oscillators. The full local
symbol is quantized as a function of the original finite CCR
variables, with its complete nonlinear auxiliary and spatial
contacts. Its coherent ordering, phase extension and finite-mode
restriction are declared explicitly.

S265's infinite expectation for individual unlocalized ADM lapse
coefficients under the linear Gaussian reference remains true.
A bounded local extension makes a DIFFERENT operator well-defined;
it does not retroactively make those divergent integrals finite.
S261's physical coefficient binding remains refuted, and S262's
correct full-parent physical-volume coefficients are unchanged.
