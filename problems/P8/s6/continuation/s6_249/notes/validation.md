# Immutable proof and replay validation

The scientific manifest contains six Python modules, two tests, README, FORMULATION and seven notes: seventeen files. Complete private algebra, independent science tests, source/field preflight and formatting precede any repository freeze. The native report has twenty fields and records every source hash.

The native session rebuilds every pinned dependency with original SymPy while protecting all preceding frozen scientific bytes. Transfer the original JSON in acknowledged length-checked chunks, verify its raw SHA256 without large-integer reserialization and compare every private/repository/source hash and all twenty fields.

Run an independent fresh repository preflight/science check, ordinary package replay, standalone CLI replay and complete regression snapshot containing this checkpoint. Native, direct, ordinary and CLI use original SymPy. Only complete regression uses the separately audited exact-GCD adapter and frozen S219 helper allowance. A deliberately shared complete snapshot may validate multiple independently frozen checkpoints, provided each retains all individual gates.

Independent tests use literal four-dimensional Christoffel curvature contractions, noncommuting directions, full spatial Ricci and weighted local action variations, arbitrary-axis orthogonal projections, both vector Ward legs with nonzero current, distinct advanced/retarded primitives, compatible force recovery and original-germ counterexamples. Exact finite ordered inverse matrices and complete weak-log quadrature test order and normalization, not continuum discretization convergence.

The inverse relies on the written full-mode, fixed-dimensional singular-extension and causal arguments, not finite tests alone. Do not promote the inverse to unrestricted forcing or stability because algebra passes. The proofs are not FORMALIZED.

After every gate passes, publish only the exact21-file manifest: seventeen sources, native report, separate external assessment, root claim ledger and root README. Exclude all other checkpoints and unrelated P4/P9 work. No frozen scientific byte is edited to make a later gate pass.
