# Full spatial little-group decomposition without a polarization-frame assumption

Fix nonzero external P and first choose its axis as z. The actual FLRW geometry, Proca and heavy Gaussian states, original radial two-leg regulator, full curvature prescriptions and fixed clock profiles are invariant under O(2) fixing this axis, including a transverse reflection. The full conditional Proca metric Hessian is the ordinary parity-even Proca Gaussian Hessian; the heavy scalar is minimally coupled and parity even. The full current classical action contains no parity-odd curvature or source term in this reference linear sector.

Rotating or reflecting BOTH internal momenta, every canonical feature and the detector/source directions leaves the complete ordered retarded pairing unchanged. The masses, actual radial preparation and both frequency masks are invariant. Angular integration, the fixed-dimensional finite part and the local metric contacts preserve this property. Retarded time order does not require the spatial scalar cross kernels to be symmetric in their time legs.

Use the orthonormal Frobenius basis
S0=I/sqrt3, S1=diag(-1,-1,2)/sqrt6,
Vx=(xz+zx)/sqrt2, Vy=(yz+zy)/sqrt2,
Tplus=diag(1,-1,0)/sqrt2, Tcross=(xy+yx)/sqrt2.

The generator and reflection are computed by their literal conjugation action on these matrices. Their common six-by-six commutant is diag(M_scalar,V I2,T I2), where M_scalar is an arbitrary ordered two-by-two matrix. Its dimension is six. The rotation-only commutant instead has dimension eight: without reflection, antisymmetric kernels within both nonscalar doublets would still be allowed. Thus neither parity nor scalar reciprocity is silently imposed.

For a general unit axis n=P/|P| put Pi=I-nn^T. The coordinate-free projectors are
P_T Q=Pi Q Pi-Pi tr(Pi Q)/2,
P_V Q=Pi Q nn^T+nn^T Q Pi,
P_S Q=nn^T tr(nn^T Q)+Pi tr(Pi Q)/2.
They are orthogonal and sum to the identity on symmetric matrices. Their operator norms are one. The basis calculation verifies these formulas at z; orthogonal conjugation proves them for every n. This supplies global bounded measurable Fourier multipliers without requiring a continuous global transverse polarization frame. The literal P0 gauge/constraint problem is not assigned by this almost-everywhere construction.

Both nonscalar directions are fixed PHYSICAL unit-Frobenius tracefree matrices. Their full leading invariant is
C_total(d)=(2d²+d-7)/[2d(d+2)],
C_total(3)=7/15, C_total'(3)=83/450.
The Proca and scalar invariants are added before contraction. The radial leading coefficient is224/15; its Abel integral has coefficient56/5 multiplying [a(t)^4 a(s)Delta_sigma^5]^-1. This equals the fourth derivative of7/(15tau).

The full prescribed fourth finite coefficient is-(log(n_heavy)+2)/60. These are precisely the leading logarithm and finite constant of S247's COMPLETE summed shear factor F2,total. The frozen proper-momentum identity a0^(-d-2)k^(d+1)dk=p^(d+1)dp holds for every nearby dimension; its physical first jet is retained. Equality only at d3 would not fix the finite contact.

Tensor and vector full finite-transfer kernels need not be equal. O(2) proves doublet degeneracy and absence of inter-sector mixing, not equality of its inequivalent representations. The scalar block remains exactly the separately certified S248 problem.
