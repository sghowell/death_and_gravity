# Transverse gauge quotient, distinct time legs and original shift constraints

For one transverse polarization, write Q=V v with V=(ez e^T+e ez^T)/sqrt2, beta=e beta_amp. A transverse spatial gauge chi(t)exp(iPz)e gives
delta beta_amp=chi', delta v=sqrt2 iP chi.
The complete gauge-invariant velocity is v'-sqrt2 iP beta_amp.

The SOURCE synchronous amplitude is v_syn=v-sqrt2 iP Iminus beta_amp, with Iminus from the original lower endpoint-1/2. The DETECTOR uses Iplus=-integral_t^(1/2), so its gauge displacement vanishes at the final endpoint. Both derivatives equal the input. Do not use the retarded primitive on both legs.

Apply the actual S246 weight-one current formulas with full nonzero Gaussian rho(t),P(t). For every detector/source transverse pair the COMPLETE source-Ward and detector-Ward integrands vanish after their metric second-variation contacts are retained. There are eight independent ordered cases. In particular the terms involving the shift and energy cancel between the first-metric-density variation and q00=-2a² beta_D beta_G; the spatial pressure terms cancel only with the full second spatial metric jet. This is not a deletion of the Gaussian mean. The distinct common-clock nn contact vanishes here because both lapse directions are zero.

The result gives an exact Gaussian vector completion from the synchronous pairing. The full current classical action and profiles are spatially covariant at phi=t, and their complete reference equations hold. Their transverse identity agrees with the same completion. Both physical polarizations are retained, and the scalar/clock/M1 fields have no linear O(2) vector component.

Let E_v,E_beta be the ENTIRE coordinate Euler residuals divided by the OUTPUT a³, not by kappa. In real space, integration of
E_v sqrt2 partial_z chi+E_beta chi'
gives W E_beta+sqrt2 partial_z E_v=0, W=D_t+3H. Therefore at nonzero Fourier P,

W E_beta+sqrt2 iP E_v=0.

The distinct detector primitive is important for causality:
(Iplus)^dagger=-I_W in the weighted measure,
I_W f=a(t)^-3 integral_(-1/2)^t a(s)³f(s)ds.
Thus the full response has E_beta=-sqrt2 iP I_W E_v with the original zero germ. Initial output/source flux and final detector flux vanish for different reasons. All upper response contacts are kept.

## Compatible source graph and canonical normalization

For an arbitrary smooth prepared canonical quotient force f, choose the original applied pair

e_v=sqrt(kappa) f/2,
e_beta=-sqrt2 iP I_W e_v.

The pair satisfies the same Ward identity. The one-leg conversion follows from gamma=2h/sqrt(kappa): the canonical Euler current is(2/sqrt(kappa))E_v. It must not be confused with the TWO chain factors in a response operator.

Solving the full synchronous vector equation gives E_v+e_v0. Hence W(E_beta+e_beta)0, and the ORIGINAL zero germ forces the complete transverse shift equation. There is no discarded constraint. Without that germ, E_beta=a^-3,E_v0 is a nonzero counterexample.

On the actual slab 1<=a<=25/16, so ||I_W||<=4T. The source pair therefore obeys
||e_beta||<=2sqrt(2kappa) Pmax T ||f||
on each finite external ball. This is a bound for the explicit compatible force graph, not arbitrary shift forcing.

Conversely a prescribed shift force requires
e_v=-W e_beta/(sqrt2 iP)
in the admitted quotient-force space. Multiplication by1/P is not bounded on L2 on a full ball: normalized annular packets approaching P0 give a diverging norm, and an e_beta proportional to1/P is L2 locally in three dimensions whereas its extra1/P image need not be.

The synchronous representative has beta0 and a regular H^r spatial metric amplitude. A zero-spatial-vector representative instead has beta_inv=-v_syn'/(sqrt2 iP); it is a tempered distribution but not necessarily H^r. No unrestricted shift-vector or raw-force norm equality is claimed. The literal homogeneous P0 problem is distinct.

All arguments first hold for smooth Fourier data away from zero, then extend through the bounded sector projectors and the stated quotient/compatible-force estimates. This is an explicitly defined causal subgraph, not a new physical infrared cutoff.
