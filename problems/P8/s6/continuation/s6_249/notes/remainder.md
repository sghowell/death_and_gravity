# Actual nonscalar strong remainder on each fixed external ball

The reference is the SAME actual QG2-H8A420 geometry, conditional Proca and heavy SLE states, whole current action and fixed prescriptions. The q0 flat shear factor is a comparison operator, not a new curved state or a claim that the tensor/vector finite-P responses coincide.

Fix Pmax finite and a fixed simultaneous time-derivative budget j. The original internal momentum integral is split only for estimation into a bounded region and k>R, with R>2Pmax and above fixed mass/background thresholds. Both internal legs k and P-k are uniformly large in the second region. No internal loop momentum is removed.

The all-order physical Proca comparison is the original prepared one. For the actual scalar SLE, S248 supplies the arbitrarily deep finite WKB/exact-KG comparison for the SAME state: full compact-bump nonstationary phase estimates and the strict sampled-energy cone make mixing arbitrarily inverse-power small at each specified finite derivative order. The initial comparison can be changed without changing the SLE, because the state is basis independent. The exact mode equations bound all required finite time jets.

For ANY fixed unit physical tracefree direction, the complete mode features and vertices have the same high-k symbol bounds used in that argument. Tensor/vector contractions are finite unit-norm linear combinations of the full spatial current, not replacements by isolated adiabatic terms. Their complete leading coefficient and first dimension jet are newly matched in sectors.md, and their full fixed local actions and tree are independently derived in local.md.

The sum-frequency phase is
omega_k+omega_(P-k)=2k/a-(nhat dot P)/a+O(k^-1).
Keep exp(-i(nhat dot P)Delta_sigma) as an orderzero radial amplitude. Its difference from1 contains a time lag. The proper-time ratio tau^5/[a(t)^4a(s)Delta_sigma^5] also equals1 on the diagonal, with first lag coefficient-3H(s)/2. Subtracting the full q0 reference therefore reduces the unmatched leading singular order by at least one. Tensor/vector dependence of the finite-P amplitude remains in their DIFFERENT remainders.

Under Dt+Ds differentiation, every additional leading radial power comes with a factor a(t)^-1-a(s)^-1 containing a lag. Repeated simultaneous derivatives preserve the stated singular order. Deepen the full physical comparison after choosing the finite derivative budget, making its error uniformly absolutely integrable. The bounded internal region uses complete massive covariance and feature sums, including either zero leg, and contributes an ordinary smooth-time kernel. No derivative of the chosen state in external momentum or global polarization frame is required.

The unchanged undifferentiated dimensional finite part is justified by the original full Proca and scalar subtracted mode estimates. Take that limit before using deeper physicald3 comparisons. The all-dimensional invariant/proper-measure match fixes the highest finite contact. There is no freedom to tune a new fourth-order local coefficient; it is exactly the constant in F2,total.

After subtraction, the remaining fixed extensions have off-diagonal singular order at most4, and local time derivatives of order at most3. Every other finite spatial term, current Einstein term and metric contact stays in the remainder. Spatial powers are bounded only on the explicitly fixed external ball.

Four OUTPUT primitives of a fixed causal tau^-n finite part, n<=4, give constants times tau^(4-n)log(tau), plus its specified extension polynomial. A finite Taylor expansion of smooth amplitudes about the diagonal makes the rest ordinary. Each local term c_j(t)D^j is integrated by parts in the full coefficient order, retaining every upper contact. The full canonical Einstein operator is lower order; it is not deleted because its coefficient is large.

Consequently, for s=T,V,
I4 T_s,total=F2,total+V_s,
with
ess sup_(0<|P|<=Pmax) |(Dt+Ds)^j V_s(t,u;P)|
 <=C_(s,j)(Pmax)(1+|log(t-u)|).
These are finite ACTUAL constants containing both masses, states, prescriptions, complete lower terms and16pi²kappa. They are UNEVALUATED and need not be small. The source/detector order and original lower zero germ remain intact.

This is a direct full-mode/singular-extension argument. The weak derivative-losing response estimate alone would not establish this normal form. The coordinate-free projectors give the same bounds for the whole nonscalar doublets. S248 separately treats the actual scalar/clock/M1 block.
