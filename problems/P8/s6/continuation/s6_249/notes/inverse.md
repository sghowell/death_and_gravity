# Ordered actual nonscalar inverse and direct-sum statement

Use smooth prepared histories in C^infinity(I;H^r_ball), with a common nonempty original initial zero neighborhood, for every fixed finite Pmax and real r. Tensor and vector amplitudes use their unit-Frobenius physical directions and canonical gamma=2h/sqrt(kappa). Their complete applied vector force graph is the one in ward.md.

S247 proves that the COMPLETE q0 summed shear factor has an ordinary inverse K2,total in half-line L1. Both physical thresholds, the fixed finite constant and the actual reciprocal measure remain. The old Proca-only pole is not inserted into this different summed inverse. Let K denote a finite bound for this full L1 norm, and C the maximum of the complete tensor/vector weak-log remainder bounds C_(s,0).

The weighted time kernel estimate is
integral_0^1 e^(-lambda tau)(1-log(tau))dtau<=2/sqrt(lambda), lambda>=1.
Choose lambda=(4KC+1)². Then
||K2,total V_s||_lambda<=2KC/(4KC+1)<1/2.
The ordered inverse is
E_s=(I+K2,total V_s)^-1 K2,total I4.

The physical canonical equation has source g=-16pi²kappa f. Therefore
||h||C_tHr<=2exp(lambda) K16pi²kappa ||I4 f||C_tHr.
For the four-component nonscalar Hilbert norm the kernel is block diagonal with two identical entries per sector, so the maximum sector remainder majorant suffices. No extra polarization-frame norm is hidden.

All constants are finite but UNEVALUATED. They may be enormous, and this is neither physical smallness nor stability. Choosing a large exponential weight does not prove that a physical perturbation is small. There is no all-Pmax estimate, physical time-frequency cutoff or convergent physical Born expansion.

The reference convolution is stationary in proper time. Prepared simultaneous time translations, the complete fixed-j weak-log bounds and the bounded ordered inverse give every fixed smooth derivative by induction. No nonstationary reference multiplication is discarded here; all variable coefficients are in V_s. The variable eta-pivot commutator in the separate scalar S248 block remains part of that block and is not overwritten.

Both full forward factors preserve smooth prepared histories through their complete paired spectral primitives. Applying four output derivatives to the normal-form solution recovers the actual original sector equation, including its initial distributions. Conversely every smooth prepared solution yields the normal-form equation and is unique. No order reduction, final-time condition, deleted pole, new branch data or reset state is introduced.

For vectors the two-sided quotient inverse is followed by the complete Ward-compatible applied pair and original-germ shift recovery in ward.md. Arbitrary raw shift forcing in the same infrared norm is NOT claimed.

O(2), including reflection, now gives a direct sum with the S248 scalar/clock/M1 inverse on the direct sum of their SPECIFIED prepared amplitude and compatible-force domains. This covers the conditional metric/M1 sectors in that stated sense, not unrestricted physical-force surjectivity or a nonlinear field space. Coherent Proca/heavy mean perturbations remain separately free at this reference by the already pinned parent source jets; their independent physical normalizations are not silently folded into this metric Gaussian inverse.
