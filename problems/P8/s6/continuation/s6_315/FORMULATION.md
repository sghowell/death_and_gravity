# Canonical action source at every finite graviton multiplicity

Retain the original four-dimensional selected covariant Phi/H/Einstein
action, linear de Donder gauge, all-outgoing momenta and lower-index
metric polarizations. The exact original parameters remain
n=10^200/512+2, g=1/8192, kappa=10^800 and the tuned contact C.
No loop-induced or unknown matching operator is supplied.

For a finite label set S, expand the metric
 g_metric=eta+2 sum_(i in S) x_i A_i/sqrt(kappa).
Every vertex is a full mixed coefficient [product_i x_i], not an
undifferentiated equal-field Taylor coefficient. Arbitrary-order
determinant, inverse-density and Christoffel coefficients define
Phi^2 h^r,H^2 h^r,H Phi^2 h^r,Phi^4 h^r and every EH r-vertex.

The finite root-current recursion with these untruncated vertices
enumerates every connected tree of that action once. In its
four-Phi/N-graviton sector the exact topology count is
 T_N=N![y^N]{c z/(2-z)^4+3(1+b^2)z^2/(2-z)^5},
 h=y+exp(h)-1-h, z=exp(h).
Here c,b are formal contact/heavy graph markers, not changed couplings.
The total count at c=b=1 obeys T_N<88*8^N*N! for all N>=0.
It gives7,47,434,5116,73444 at N=0,1,2,3,4.

With Frobenius field norms, Euclidean momentum norms <=L and unit
metric coupling, the all-order vertex majorants are
 |D_r|<=r!2^r(r+1) product||A_i||,
 ||I_r||op<=r!2^r product||A_i||,
 ||K_r||op<=r!2^r binom(r+2,2) product||A_i||,
 ||Gamma_r||F<=3*2^(r-1)r! L product||A_i||,
 |V_EH,r|<=36 r!2^(r-2)binom(r+2,4)L^2 product||A_i||
          <=r!32^r L^2 product||A_i||, r>=3.
Restore kappa^(1-r/2) for Einstein vertices. The analogous scalar
bound is specified in notes/bounds.md, with kappa^(-r/2).

Physical point evaluation requires four mass-one scalar shells in the
incoming center-of-mass domain5/4<=E<=2, future null emitted momenta,
total emitted energy<=1/8, exact momentum conservation and symmetric
spatial TT polarizations. Any finite list is allowed. The point API
excludes exactly collinear null pairs and other internal poles; it
does not claim a continuous collinear extension. Polarization norms
are explicit in the bounds, not silently fixed for raw coefficients.

The complete original5116-tree N3 coefficient is evaluated exactly.
Its two diagnostic gauge directions vanish; omission of EH5 or the
scalar fourth metric vertex gives a nonzero Ward defect. Independent
literal action and combinatorial implementations supplement the
general action, root-cut, analytic-majorant and Noether proofs.

No all-N nonleading remainder, soft-cluster collinear majorant,
finite hard real-virtual/evanescent matching, positive event measure,
interacting quantum state, absolute complex Regge estimate,
common-parent bounce or original V/G/B/P8 closure follows.
