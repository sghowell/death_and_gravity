# Validation protocol and independent calibrations

The18 ASCII newline-terminated sources define one20-field native
report. Every symbolic payload remains exact and float-free.
There are381 named exact checks,531 scalar entries,94 proof gates,
8 controls and73 rejected inputs. All9 primitives,170 preceding
matching records and6 historical qualifications are preserved.
Native/direct/ordinary/CLI use original SymPy; the separately audited
exact-GCD adapter is allowed only in the FULL regression.

A private prototype recovers determinant/inverse/density jets through
r3 and the frozen EH3/EH4 vertices. A separate literal4x4 determinant,
adjugate and indexed Christoffel expansion independently gives EH5
coefficient16438 and determinant fifth coefficient-2088. Another
literal adjugate polarization checks all entries of the new scalar
fourth metric vertex. None uses a numerical zero tolerance.

The solved all-N EGF is independently checked against literal labeled
root-cut counts for N0..4. Its analytic majorant and the general
vertex-norm proofs are written arguments; the finite coefficient
calibrations through order12 are supplementary, not all-order proofs.

The new full point evaluator reproduces every previously implemented
external multiplicity. Its5116-tree N3 example has two exact vanishing
gauge directions; missing EH5 and scalar fourth-metric vertices give
nonzero defects with5113 and5110 remaining trees. The original
parameter N3 coefficient is separately evaluated, not extrapolated
from diagnostic couplings.

An initial private prototype output recorder tried to store a missing
session identifier when its command completed immediately, causing a
transport serialization error before retaining the result. The source
parsed successfully; the recorder was corrected to handle completed
and running commands. The unchanged probe then returned C0. This was
not a scientific assertion failure, parameter change or backend patch.

Cold and separate fresh-root preflights must validate all exports,
counts, exact packets, proof gates and every science test. The fresh
run forces the entire immediate parent's packets first. Only then
are private/fresh/repository source bytes compared and frozen.
The existing native helper receives one specification, followed only
by complete-chunk continuation ACKs. Both raw copies, digest, source
manifest, REPORT AST and frontiers must pass validation before freeze.

Own ordinary/CLI and the separately captured full P8 suite remain
external acceptance requirements. Results are recorded in an external
assessment, not retroactively inserted into frozen scientific files.
At most one FULL and one own pair run. No frozen ancestor is changed.
