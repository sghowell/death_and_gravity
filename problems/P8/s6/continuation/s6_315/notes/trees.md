# Complete finite tree source and Ward identity

The inherited root-current algorithm is cardinality-independent.
Choose one external Phi as the final amputated root. At each root
vertex, the remaining labeled leaves form a unique unordered set
partition. A current of one of the three species occupies each block.
A non-leaf current uses a proper subset, so induction on the number
of leaves terminates at every finite N.

Removing the root vertex of any connected tree gives exactly such
data; conversely reattachment gives one connected tree. This is a
bijection, not an asymptotic counting prescription. The coefficient
engine supplies the full symmetric vertex once. Internal Phi/H
currents carry the negative inverse scalar kinetic operator and
Einstein currents the negative trace-reversed inverse, precisely as
in the frozen source. The final Phi root is not multiplied by a
propagator. Each evaluator owns its caches.

The new dispatcher allows Phi^2 h^r,H^2 h^r for r>=1,
H Phi^2 h^r,Phi^4 h^r for r>=0, and Einstein r-vertices for r>=3.
There is no remaining external-multiplicity truncation. Other
operators and loops do not arise by merely increasing N.

## General tree Ward identity

The full selected action is diffeomorphism invariant. Its Noether
identity relates a divergence of the metric Euler derivative to
matter Euler derivatives and terms proportional to the metric
Euler derivative. Expand that identity in the separately labeled
external free waves. The inverse kinetic operator at each internal
tree edge is exactly the one used in the current recursion.
Contracting an external graviton with its momentum turns the adjacent
vertex identity into inverse-kinetic terms on its branches. Neighboring
root partitions cancel the internal terms. Repeating on smaller
proper subsets leaves only inverse kinetic operators on external
legs, which vanish on their physical shells with physical
polarizations. This finite induction yields the on-shell tree
replacement epsilon=q_lower xi_lower+xi_lower q_lower ->0.

All metric orders are needed in that argument. Linear de Donder
gauge supplies the same free inverse as the old source; ghost lines
with no external ghosts cannot form a tree contribution to this
external sector. No loop Ward identity or regulator interchange is
asserted.

## Full first new multiplicity

The recorded three-real state has incoming E=5/4 and outgoing
ep=(v+1/v)/2,rp=(v-1/v)/2 with v=31/16.
Put W=2(E-ep) and w=W/12. The three emitted directions are
(1,0,0),(0,1,0),(-3/5,-4/5,0), with energies3w,4w,5w;
their spatial momenta sum to zero. The two outgoing massive
directions are opposite along(3/5,0,4/5).
All momenta, shells, total conservation and spatial TT tensors
are exact rational data.

The complete current recursion gives5116 graphs. Its two gauge
directions vanish. Removing the three EH5 graphs leaves5113 and
a nonzero Ward defect. Removing six scalar fourth-metric graphs
leaves5110 and a different nonzero defect. The original-parameter
physical coefficient is separately evaluated and is nonzero.
Lower N0/N1/N2 full amplitudes exactly match the frozen evaluator.

The point API retains exact input checks and rejects collinear
null pairs before attempting their0/0 internal propagators.
This domain restriction must not be mistaken for a collinear theorem.
