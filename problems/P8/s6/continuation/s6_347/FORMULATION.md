# Exactly the selected known scalar coefficient

Keep the original mass1 light scalar, heavy mass squared n, cubic g,
contact C, physical metric eta+2h/sqrt(kappa), and fixed finite scheme.
The heavy source coefficient c_src=-4g/kappa is NOT the S346 ratio C/g^2.

Use the same six full unnormalized24-label Bose jet words:

    (00)(11)(22), (00)(11)(23), (00)(12)(12),
    (00)(12)(13), (01)(01)(23), (01)(01)(01).

Each scalar uses its fully symmetrized covariant derivative jet;
each edge contracts with the inverse physical metric. The Fourier
sign is normalized to positive three-Gram flat vertices. All metric
and connection variations remain literal.

Project entire off-shell flat homogeneous degree6 polynomials before
imposing any scalar mass shell. The radiative comparison uses only
sum p_i+k=0, k^2=0 and physical TT polarization. A common external
momentum scaling with internal masses fixed defines the analytic-origin
coefficient. Equal flat polynomials have equal external emissions,
which cancel in the comparison.

The scalar E4,L1 graph inventory exhausts the classical limiting-action
four-light coefficient. Internal gravitons, finite-gravity decoupling
and an independent added curvature coupling are explicitly outside it.
This is not a physical massless limit, full curved functional, or
above-threshold Taylor approximation.

The report determines
chi_known_selected=chi_core+g^2 E(n)/(16pi^2 kappa), where chi_core
is frozen S346 and E contains every mixed-source class. The local
tadpole comparison is zero. No physical counterterm is retuned.
