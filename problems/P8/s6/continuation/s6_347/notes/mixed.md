# All mixed-source classes and counterfunctions

Use c_src=-4g/kappa, distinct from the S346 ratio C/g^2.
For d_i=p_i^2 and B(d)=B_MS(d;1,n), the entire off-shell scalar kernel
before1/(16pi^2) is

 -2g*c_src sum_i[A1-An+(n-1-sum_(j!=i)d_j)B(d_i)].

This follows directly from the literal derivative source numerator
and both propagator denominators. It also follows from the exact
covariant product/Green equation with the marked(Box+1)Phi retained:

 gc_src(4-n)Phi(x)Phi(y)^3 G_HG/3
 -gc_src delta(G-G_H)Phi^4/3
 -gc_src Phi(x)Phi(y)^2(Box+1)Phi(y)G_HG.

All24 labels give the displayed off-shell kernel; the EOM term is
not dropped. Both massive loop insertions in frozen S338 yield the
full B(L) divided difference for arbitrary p_squared and real nullTT k.

Let b_r=(1/r)int_0^1[z(1-z)/M]^r dz, M=1+(n-1)z.
The degree6 part is
 -2gc_src[(n-1)b3 sum_i d_i^3 - b2 sum_(i!=j)d_i^2 d_j].

The full local conversions are
sum_i d_i^3=(2/3)W003 with curvature coefficient-4 and
sum_(i!=j)d_i^2d_j=2W012 with curvature coefficient-4.
Therefore

 chi_across=-32g^2[(n-1)b3-b2]/(16pi^2 kappa).

## Within J4 and within J2

The fixed within-J4 source is c_src I H(Y+Phi^2), with
I=-1/(16pi^2) at mass1,mu1. Both heavy endpoints give the complete
off-shell flat contribution

 4g^2/kappa sum_channels(4-2v+sum_i d_i)/(n-v)

before1/(16pi^2). The exact covariant identity
Y=Phi L Phi-(1/2)L(Phi^2), L=-Box, fixes its comparison without an EOM.

Its homogeneous degree6 flat polynomial, before4g^2/kappa, is

 (4/n^4-2/n^3)sum v^3 + (1/n^3)sum (sum_i d_i)v^2.

The two common-basis conversion factors are-4 and-8. The apparent
n^-3 terms cancel, leaving

 chi_withinJ4=-64g^2/(16pi^2 kappa n^4).

All three actual frozen S338 channel kernels have been checked:
first subtract their explicit external emissions, compare the complete
amputated internal rational kernel, and then take its exact analytic
degree6 coefficient. No off-shell scalar LSZ current is assumed.

The within-J2 graph cancels with the complete existing heavy-onepoint
counterfunctional before any truncation. Its metric and internal
real-TT responses are retained and cancel or vanish as established
by their literal tensor integrals. Fixed mixed UV terms have at most
two derivatives; the finite OS4 term is constant. Neither changes
this degree6 comparison.
