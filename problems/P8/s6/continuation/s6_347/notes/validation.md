# Complete immutable scientific contract

The21 sources contain9 Python modules, two tests, README, FORMULATION
and eight proof notes. The20-field report has378 named exact checks,
397 scalar entries,45 proof gates,eight controls and859 rejected inputs.

The exact scalar inventory, every off-shell exactD local contraction,
both mixed loop insertions, marked-EOM/delta terms, common-basis
conversions, mass primitives and finite positive-weight bounds are
recomputed. Three actual frozen rational kernels and18 original
massive-vector comparisons independently check the normalization.

Every direct raw report and complete source manifest is checked.
Each report field is mutation-tested. Missing/extra fields, changed
frontiers, invalid exact masses/orders/classes and overbroad
parent/finite-gravity/physical-Taylor scopes are rejected.

Cold and fresh verification use original SymPy. A separate fresh
process verifies the immutable immediate parent before receiving
the byte-identical new sources. Private/fresh/repository equality
and full export/count checks precede a single native build.
Complete raw bytes, source hashes, AST report paths, all direct
inputs, counts and frontiers are validated after it.

All operational timings and ordinary/CLI/FULL acceptance receipts
remain outside this immutable manifest. A covering snapshot must
actually include S347. Not kernel-formalized and not a proof of
omitted physical P8 obligations.
