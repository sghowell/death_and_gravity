# Immutable report and independent validation protocol

The source manifest contains18 ASCII files:7 package modules,2 independent
tests,2 root documents and7 proof notes. The report contract has20
AST-derived fields. The exact assembled target is164 named residuals,
317 scalar entries and36 proof gates, with8 controls,73 rejected
unsupported audit inputs,9 primitive records,143 matching records and6
historical qualifications. Test collection is measured by actual
runners; no predicted count is an accepted replay verdict.

Validation proceeds through cold original-SymPy preflight, a fresh
source copy with all immediate-parent packets forced before the new
residuals, exact source-byte/hash checks, then one native report request.
The parent SHA is3018ff11f02ada528818d123caa62a5f552f83c72ff267ed08975b227550c8ab.
The prior native raw report and every inherited source hash are checked.

Whole-report equality, mutation rejection for every field, missing/extra
field rejection, full source-manifest equality and exact scope/count
checks are independent certificate tests. Science tests include full
metric directions, parameter identities, real-domain bounds, direct
F1 extraction, raw triangle signs and preserved unresolved boundaries.
Finite rational test points corroborate the implementation; general-D
and all-domain conclusions use the complete written arguments.

The native, direct, ordinary and CLI paths retain original SymPy GCD.
Only a separately captured complete FULL replay may use the audited
exact-GCD adapter, with its contract tests, exact captured-file hashes
and actual collection/completion evidence. The native helper receives
only the declared protocol, never scratch probes or DONE.

Accepted source/proof/test bodies and native raw reports are immutable.
Any correction requires a successor or an external assessment, never
editing a frozen ancestor. Development corrections, actual durations,
chunk counts, raw hash and final ordinary/CLI/FULL verdicts are recorded
outside this manifest. No stale report hash is substituted for a
completed replay or claimed as physical P8 closure.
