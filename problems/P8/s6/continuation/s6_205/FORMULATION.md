# Finite Taylor restoration and exact boundary

Keep the same actual CD sector, mass1000, kappa=10^800, prepared state, unit-W8 comparison, unit slab and original finite prescription. The complete actual-state/reference correction remains. Let nu=sqrt(m^2+|k|^2/Amax^2), Amax=25/16, delta=10^-6 and Rtime=1/20000.

The first-five-endpoint pre-current coefficients have endpoint order j0,...,4, spatial Taylor degree n0,...,4 and source time-jet index r0,...,j. Their complete individual bound is

9 Cref^2 c_jr Rtime^(-(j-r)) delta^-n |P|^n nu^(1-j-n),

with Cref=10^9 and the full S6.198 coefficient rows. No Borel-state, Fourier-test or regulator-domain derivative is used. Taking the final current imaginary part is delayed until after the analytic coefficient argument.

The ten cells j+n>=5 have radial exponent s=j+n-1 in4,...,7 and are absolutely integrable. The fifteen cells j+n<=4 remain candidates for UV matching, not asserted actual divergences. The partition retains all75 source time-jet entries:40 finite and35 candidate.

At the original two-created-mode cutoff, split the high-band spatial Taylor sector as

P_all,K=P_fin,K+P_UV,K,

keeping chi_K=1_(|k|<=K,|-k+P|<=K) and |k|>=m in BOTH terms. The complete contact keeps its distinct one-mode band.

The finite coefficient tensors have dominated limits on the fixed P-independent high band. Only then does P_fin become a genuine finite spatial polynomial. It obeys

|P_fin|<1e50||D||L2 X46[Gamma],
|P_fin-P_fin,K|<1e55||D||L2 X46[Gamma]/K, K>=m,

where X46^2=sum_(r=0)^4||(1-Delta)^3 partial_t^r Gamma||L2^2. Every finite cell has n>=1, so the restoration vanishes at exact zero external momentum.

Restore this finite piece to S6.203:

J_actual,K=(known_S203,K+P_fin,K)+(C_unit,K+P_UV,K).

The updated known bound is5e54 M[D]Y[Gamma] with tail7e60 M[D]Y[Gamma]/K, using the unchanged M and Y^2=N61^2+X46^2. The finite polynomial alone has canonical bounds4e-750 and4e-745/K; the updated known actual piece has2e-745 and28e-740/K.

S6.204 is only the already-fixed local target. It is not added again as a matched quantum term. The remaining candidate sector and full contact still need actual finite/divergent coefficient and regulator-artifact matching. No full response, inverse, background, stability, cutoff, remaining parent loops or finite-gravity IR/Regge result follows. Original P8 remains OPEN.
