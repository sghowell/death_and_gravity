# Convergent local moments and integrated Cauchy estimates

On the positive simplex P=1/(1/x+1/y+1/z)<=1/9.
Consequently 0<-log(1-P)<=P/(1-P)<=1/8, so 0<C<=1/16.
In the sector representation w dx dy=t/v^3 dr dt.
The log r contribution to |T1| is at most
6 integral t dt integral -log r dr=3.
Both 1<=v,a<=3, so the remaining logs have magnitude at
most 3 log 3. Since T0=1/2 and log 3<2, |T1|<6.
At ell<1200 the local slope is bounded by
L^2(600+3+1/16)/(6Q^2).

The remaining functions use S6.126's first-sheet holomorphic
neighborhood of |s-1|<=2. Its real Euclidean denominator
combination is extended by uniqueness of analytic continuation,
using the entire p(s) embedding and strict positive gaps.
No unproved complex contour shift or integrand-only assertion
replaces those already-integrated majorants.

The mixed integral is first anchored at s=0 with the same
regulator; this removes its remaining constant divergence.
The magnitude bounds on the closed outer disc are, including
all graph weights:

    mixed constant-anchored: 33 Lg/Q^2;
    same-heavy:             5 g^2/(2Q^2);
    different-heavy:        56 g^2/Q^2;
    nested R:               4 g^2/Q^2.

Cauchy's first-derivative estimate divides each by two.
Add alpha_0^2, with alpha_0<=g/(2QM), and the local MS slope.
The resulting g^2 prefactor is 125/4. Dropping heavy-mass
suppression in these particular estimates retains convergence.
Unequal conservative upper bounds are not signed coefficients
and cannot by themselves diagnose a growing perturbative series.
