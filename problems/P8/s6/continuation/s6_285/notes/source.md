# Exact source and finite-prescription boundary

S253 fixed_functions retains the entire heavy-modified R and F plus the
fixed S240 profile. Its vacuum_pressure_total is the Proca constant plus
(p_v_H+p_v_Phi)/kappa. The constants are

p_v_H=(3/2-log n)n^2/(64pi^2),
p_v_Phi=3/(128pi^2),
p_v_A=5M_A^4/(128pi^2).

The full F(0,0) is minus their sum divided by kappa, while R(0,0)=1.
The exact whole functions, not a replacement finite jet, are retained.
Their low field-degree identities put the three gapped Hessians at this
reference in the minimal Gaussian sectors; H Phi^2 and the nonminimal
higher sources are not quadratic vacuum mixing.

For a minimal scalar, the S240 H prescription has finite heat density
before64pi^2

(3/2-log nu)nu^2+(log nu-1)nu R_old/3
-2log nu[Weyl^2/120-Euler/360+R_old^2/72].

S176's fixed Proca prescription at scale M_A gives

5M_A^4/2+5M_A^2 R_old/3
-4[Weyl^2/120-Euler/360+R_old^2/72].

Both full inherited expressions are substituted and checked. In particular
the Proca prescription uses its stated fixed four-dimensional invariant
coefficients with the metric and volume continued and varied before the
dimension limit. A different component-count convention cannot replace it.

The source explicitly specifies the H curved prescription, but for Phi it
pins only the displayed flat vacuum constant here. S239's limiting-action
MS/OS4 conditions do not supply the missing finite-gravity curvature
polynomial. Its three coefficients remain unmatched. The generic H
formula at nu1 is a possible reference calculation, not the actual Phi
matching condition. This distinction is essential to the result's scope.

The fixed H/Proca curvature coefficients are

c_R=[n(log n-1)+5M_A^2]/(192pi^2),
c_W=-(log n+2)/(3840pi^2),
c_R2=-(log n+2)/(2304pi^2),
c_E=(log n+2)/(11520pi^2).

The last has zero compact metric variation. The volume coefficient still
includes ALL THREE original constants.
