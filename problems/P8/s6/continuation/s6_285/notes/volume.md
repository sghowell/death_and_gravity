# Whole-volume cancellation and the curvature sign

Let g=eta+2h/sqrt(kappa). Jacobi's determinant identity gives

sqrt(-g)=1+tr(eta h)/sqrt(kappa)
+[(tr eta h)^2/2-tr(eta h eta h)]/kappa+O(h^3).

The code independently expands the determinant with all ten symmetric
metric components. Its unrestricted first and second jets agree with this
formula. More importantly, the coefficient of the WHOLE volume density
is zero: the same-scheme three Gaussian constants plus the fixed source
constant cancel identically. Every metric derivative therefore vanishes,
not just the tracefree first derivative. The original fixed profiles are
held fixed under variation; no live mean is differentiated or retuned.

This cancels the Gaussian cosmological insertion that would otherwise
generate a double pole in the metric inverse. It does not authorize
discarding arbitrary rational terms in the full four-scalar amplitude.

The fixed curvature convention is R_old=-R_P8. Hence the original
-kappa R_P8/2 is +kappa R_old/2, so delta_kappa=+2c_R.
The independent S230 full Proca response has
C=16pi^2 kappa+5M_A^2/6 and confirms this sign.

For a unit TT metric mode gamma, the local quadratic density is

c_R gamma'^2/4+c_W gamma''^2/2,

with Euler expression -c_R gamma''/2+c_W gamma''''.
For a unit spatial trace mode it is

-c_R gamma'^2/2+3c_R2 gamma''^2,

with Euler expression c_R gamma''+6c_R2 gamma''''.
The canonical chart gamma=2h/sqrt(kappa) supplies the factor4/kappa.
The force operator is minus the unsourced Euler residual, as in S230.

The conserved Einstein tensor structure is p(P2-2P0). Its apparently
highest inverse powers of p cancel between the projectors; p^2 times
either projector is also local. D_i(-p)=O(p^3) then gives an analytic
complete gapped tensor at zero. After gauge fixing the massless numerator
is P2-P0/2, not a new propagating scalar polarization.
