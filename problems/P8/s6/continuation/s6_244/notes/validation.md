# Independent validation and immutable-report boundary

The scientific packet contains58 named identities with70 scalar entries,53 explicit proof gates, nine controls and297 rejected unsupported inputs. All nine primitive rows and99 earlier matching records are preserved; one matching record is appended.

The exact packet verifies the full time-dependent canonical frame, symplectic form, every covariance-flow entry, pure determinant, full spatial vertex/current, instantaneous metric contact, exact Bogoliubov graph change, entire WKB-frequency-to-graph defect identity, complete error derivatives and marker parity. Positive rational recurrences retain every one of195 coefficient-jet majorants and all whole remainder/profile constants.

Independent science checks include:

- Six literal scalar Euler/covariance identities for the zeroth, second and fourth adiabatic actions, using raw omega and log-K jets rather than the inherited noncommuting-word implementation.
- Full Christoffel/Riemann contractions and Cartesian angular/radial integration at dimensions2,3,4,5, with two genuinely noncommuting extrinsic-curvature fixtures each:32 exact comparisons.
- Full six-iterate W6-versus-order-ten initial graph tails at the ACTUAL mass, k/sqrt(n)=0,1,4. Results at1300 and1500 digits agree beyond150 relative digits, with nonzero scaled tails.
- Independently evolved canonical covariance and its first two source tangents on a smooth prepared noncommuting metric history. Full matrix-exponential current finite differences agree within3*10^-8 and3*10^-6. Both contact contributions are nonzero and larger than10^-4 in each fixture.
- Complete covariance Frechet majorants, full radial/tail normalization, all twelve history-jet gates, nonzero fixed-profile contacts, and rejection of unsupported resets, inverse transfers, double counterterms and closure claims.

Numerical fixtures diagnose signs, contacts and the finite coefficients; they are NOT the all-momentum proof. The full mass-uniform estimates and general-dimensional arguments are written in the other notes.

The first complete private science run passed446 tests. Initial private corrections included a test fixture that accidentally had commuting matrices, an incorrectly typed Euler term-count expectation, duplicate imports and loop-variable capture bindings. The full noncommutation and exact-count assertions were retained. No scientific inequality failed and no frozen bytes were edited.

The read-only report pins17 source inputs and20 top-level fields. Every field is mutation-tested, including missing/extra fields. Native/direct/ordinary/CLI replay retains original SymPy. Only full regression uses the separately audited exact-GCD adapter and frozen S219 helper allowance. Native report chunks, source hashes, private/repository bytes and report-field manifests are checked before publication.

The written proofs are certificate-backed mathematical arguments, not a Lean or other FORMALIZED development. Fresh replay results and any later private corrections are recorded outside the frozen17-source bundle.
