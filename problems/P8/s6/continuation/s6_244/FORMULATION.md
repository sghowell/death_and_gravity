# Complete actual homogeneous heavy-field response: frozen scope

## Unchanged inputs

Retain S240's exact prepared SLE for the single minimally coupled heavy scalar, n=10^200/512+2, kappa=10^800, a(t)=(1+t²)², its physical CCR/annihilation convention and complete covariant MSbar prescription at mu=1. The complete fixed QG2 reference profile is unchanged. No later source-dependent state minimization is permitted.

The S243 report is the immediate predecessor. S193 is used only for the universal one-oscillator local action/current identity, not its vector polarizations, curvature coefficients or finite weights. All prior scientific/report bytes remain frozen.

## Actual observable and finite history tube

Work per coordinate spatial volume on I=[-1/2,1/2], N=1, X=1, h=a²exp(Q). Q is any real symmetric smooth homogeneous matrix history with a common zero germ at the initial time and max over j=0..12 of ||Q^(j)||op <=1/100. The prepared state Cauchy covariance is fixed there. Source directions G have the same zero germ and full C12 norm at most one. Differentiate on histories inside this admitted tube; boundary histories are controlled by interior limits.

For any real symmetric detector D(t), J_D is the complete heavy Gaussian spatial current plus the ENTIRE fixed finite action and reference-profile current, including the physical second Hamiltonian contact. Bound current source derivatives a=0,1,2 by C||D||F ||G||C12^a. The second derivative is directional; mixed derivatives follow by polarization with the corresponding harmless factor if needed. No mixed-direction numerical constant is silently identified with the diagonal one.

The resulting first time-bilinear response has detector L2 times source H13 norm less than 10^-350 after kappa normalization. This is the zero-transfer spatial kernel, not an assertion about nonzero physical Fourier transfers or a spatially homogeneous source being L2 on infinite space.

## Full renormalization and regulator

Subtract the entire local adiabatic current through orders0,2,4, with all general-dimensional angular/radial and full-volume counteraction terms before the physical limit. Include exactly S240's finite scalar action and fixed profile once. Neither S241's finite action nor S243's vanishing zero-transfer difference is added again.

All internal momenta are included. For the auxiliary regulator retain exactly Omega_star²=n+k²/16 <=K², K>=2sqrt(n), independent of the varied history. The complete finite action/profile remain unprojected. The normalized first-response tail is below 10^-748/K² on the same time graph. A projected mean is not set equal to the full matched mean.

## Certificate boundary

Exact matrix/scalar residuals, positive rational majorants, full literal variational checks, noncommuting Christoffel/Cartesian fixtures, actual-mass W6 tails and independently evolved covariance/contact derivatives support the written arguments. No finite WKB reference is declared the physical state.

This finite prescribed homogeneous C2 current bound is not a nonlinear self-consistent solution, an unrestricted quantum inverse, stability, interacting-loop control, quantum decoupling, UV/Regge admissibility or original V/G/B/P8 closure.
