# Evidence boundary and required acceptance

The written arguments establish the domains, uniform bounds and
measure limit. Literal symbolic identities and original434/5116
calibrations audit the action and normalization; finite samples or
integer gates alone do not establish uniformity. No result here is
Lean- or kernel-formalized. Original SymPy is used outside FULL.

Before packaging, the independent probability-transfer arithmetic
probe passed in0.04921320895664394 seconds. The generic literal
EH3 residue with ten soft, two TT and six conserved-root parameters
passed in0.16272279107943177 seconds. The original two-marked face
calibration passed all three exact rational5116 points and its434
parent in160.37040708307177 seconds. These are private diagnostic
receipts, not native acceptance or the uniform proof.

The first five-module private source replay passed source231/5,
current52/37, faces10/18, projection14/4 and measure6/7 named
checks/gates in253.5955299170455 seconds. All six captured source
files were unchanged. Audit, tests and written proofs were added
after that run; the receipt is not claimed for the full package.

The first full20-file draft reconstructed313 named checks,667 scalar
entries and80 gates in254.24369804095477 seconds, with74430 captured
ASCII bytes. The science suite passed509 tests but failed one test
that compared an expanded polynomial to its factored form using
structural equality. The complete complex probability-transfer
identity in that same test had already passed. The final assertion
now expands their difference and requires exact zero; no physical
formula or negative control was removed. The failed run is retained,
not counted as acceptance. The corrected full draft then passed all510
science tests in0.28 seconds after255.74373341700993 seconds of
reconstruction. It retained313 named checks,667 scalar entries,80 gates,
81 rejected cases,20 ASCII files totaling75051 bytes,20 report fields
and8 module exports, with the entire captured source snapshot unchanged.
This receipt predates this paragraph and the actual S323 native-parent
hash substitution; final cold/fresh/native acceptance on the final
bytes is required and recorded externally.

A separate fresh interpreter may first reconstruct every frozen
repository S323 parent packet with all21 source hashes pinned. It
imports no S324 source during warmup. Only after the actual S323
native report is validated, the final private S324 science pass
succeeds, and the exact final source copy is checked may one START
release its S324 preflight. The full parent packets are forced again
before its own residuals. This explicitly disclosed parent-warm check
reuses no private/native S324 amplitude or own-result cache.

Required final acceptance uses the ACTUAL frozen S323 native report,
not a pending digest or private draft claim. All20 ASCII source
files must be newline-terminated and identical across independent
private, fresh and repository copies. Full-source snapshots must
remain unchanged during their passes. Native reconstruction checks
the entire20-field report and all frozen inputs, with ordinary and
CLI replay comparing every field and source hash. FULL alone may
use the audited exact-GCD adapter under all adapter contracts and
captured-input invariance. The external assessment, not an edit to
a frozen proof source, records final acceptance receipts.
