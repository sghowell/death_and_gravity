# Finite signed scheme transfer with the original measure

With L=10^-735,Q=10^-723 and the proper-component estimates, the
exact overlap satisfies
 |T|<=2*L*Q*J+2*Q^2*K,
 K=Iab*Iac+Iab*Ibc+Iac*Ibc.
S323 proves int_cube J/(abc)<=24*x^2. The elementary entropy
bound I(u,v)<=2*sqrt(uv) also gives
 Iab*Iac/(abc)<=4/sqrt(bc).
Its cube integral is16*x^2, and the other two products give
 int_cube K/(abc)<=48*x^2.

The normalized probability density carries1/(a^2*b^2*c^2).
Multiplying the three original on-shell measures leaves the radial
weight1/(abc). Summing two physical polarizations for each leaf,
integrating each full solid angle and retaining1/3! gives
 8*(4*pi)^3/[3!*2^3*(2*pi)^9]=1/(48*pi^6).
Thus the total variation of the transfer on W<x is bounded by
 (L*Q+2*Q^2)*x^2/pi^6<10^-1424*x^2.
Only the positive elementary majorant is enlarged from the physical
simplex to its containing cube. The physical amplitude is never
evaluated outside the proven total-energy domain.

S323's different signed subtraction has total variation below
10^-1424*x^2+10^-1340*x^4. By the exact transfer identity and the
triangle inequality the probability rectangle therefore obeys
 TV<2*10^-1424*x^2+10^-1340*x^4.
The shared integrable absolute majorant implies that removal of
a common positive lower energy cutoff converges in total variation
to a unique signed measure. Angular exceptional pole points have
no assigned bare value; the uniform a.e. bound is sufficient.

If divided by the same optional frozen reference P0, using its
bound1/P0<2/sqrt(x) gives
 TV/P0<4*10^-1424*x^(3/2)+2*10^-1340*x^(7/2).
This is only a comparison of measures, not evidence that this
subtraction is the full positive detector probability. There is
no claim of normalization, positivity, monotonicity or actual
real-virtual matching. Cutoff APIs reject Boolean, floating,
symbolic and out-of-domain inputs before evaluating fractional powers.
