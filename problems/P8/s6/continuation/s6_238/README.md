# Separate covariant heavy-scalar candidate

S6.238 defines a new full smooth classical affine/DHOST/Proca/M1/heavy-scalar action with the original inputs preserved. Its vacuum tree matches the separate V2S-T1 model, its full classical potential is coercive, and an explicit clock-localized change preserves the needed bounce jets with a complete mixed-four-derivative coefficient bound.

The original connected affine R range is retained by a sharp endpoint argument. All affine maps and the regular lower dictionary are recomputed. An exact forced-heavy calculation includes the derivative-dependent source stress, retains an unrestricted-domain counterexample, and proves only a scoped two-chart corridor bound.

Read [FORMULATION.md](FORMULATION.md) and every listed proof note. This is an algebraic/classical matching checkpoint, not a transferred quantum model or a completed P8 result. All nine original primitive statuses remain unchanged; one separate matching record is added.

The verifier is read-only. Frozen source/report bytes are never updated to hide a failed calculation. Actual validation corrections and completed fresh regression results belong in the external publication audit.
