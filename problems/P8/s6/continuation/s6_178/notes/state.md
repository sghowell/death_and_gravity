# Conditional state, coherent mean and the energy observable

Use exactly the S6.176 canonical three-polarization measure and
all-order Cauchy covariance. The metric is fixed at the CD geometry,
and psi vanishes in a neighborhood of the preparation surface.
Consequently the connected Proca covariance and its covariant
renormalization prescription do not change. The actual mean is

    Abar=G_Proca,ret J,  J=sqrt(kappa/zeta)S.

The complete source is smooth and compactly supported. The prior
normally hyperbolic reduction/global-hyperbolicity argument gives
the smooth retarded solution with compatible zero initial mean.
Adding it is a coherent shift of the SOURCED affine algebra.
No global Fock-unitarity assertion is needed. The Hadamard
singularity, connected covariance, CCR and positivity are those
already checked in S6.176.

At fixed metric the connected Gaussian determinant is independent
of u. The source-dependent mean response is therefore classical
coherent response within this conditional quantum sector, not a
new metric-determinant loop estimate. Noise and metric response
depend on further observables which are not bounded here.

The free Proca stress difference between this coherent state and
its centered reference is exactly the classical tensor of Abar.
Its spatially integrated physical energy is

    Efree=integral[pi^2/(2a)+F_ij^2/(4a)+a m^2 A_i^2/2
                         +(div pi-a^3 J0)^2/(2a^3 m^2)].

This is not E0 and not the full source-interaction tensor.
Using (x-y)^2<=2x^2+2y^2 gives

    Efree<=2E0+a^3||J0||2^2/m^2.

On I, a<=25/16, m=1000 and the full-source tame constants give

    F_S^2 <= [(25/16)512^2
                +((25/16)3+9)2048^2/1e6]delta^2 U_psi^2
           <641^2 delta^2 U_psi^2.

The divergence bound uses the sum of three spatial derivatives;
the gradient-J0 bound uses their sum of squares. With
B=(5/4)^6 and J=sqrt(kappa/zeta)S, energy.md now implies

    E0[Abar]/kappa <3e12 delta^2 (integral_(t0)^t U_psi)^2,
    Efree/kappa <=6e12 delta^2 (integral_(t0)^t U_psi)^2
                        +1e6 delta^2 U_psi(t)^2.

The instantaneous coefficient is exactly B*512^2=1e6.
For psi=epsilon f these are fourth-order mean-energy bounds.
They are spatial integrals on the fixed finite slab, not
pointwise stress, functional metric-response, all-time constants
or a controlled quantum-corrected bounce.

For homogeneous closed sources W=S the TOTAL extra classical
first variation cancels, even when Efree is positive. Adding
Efree to the reference stress without the interaction/source
metric variations would therefore be incorrect.
