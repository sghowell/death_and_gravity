# A bound on an existing reference state, not a new quantum solution

## Objects and quantifiers

Fix exactly the S251 complete two-mode scalar and two-polarization
tensor preparation at t*=-1/2, the original smooth sampling amplitude
f(t)=exp[-1/(1-1024(t+7/16)^2)] on(-15/32,-13/32), zero otherwise,
and its unchanged preparation mass scale1. The associated initial
Cauchy covariance is selected once, on the original full reference.

All nonzero original scalar/matter profiles and their first jets remain.
Their already proved C5 envelope is smaller than the1e-390 box used
here. This is not the S263 varied classical solution or a re-minimized
vacuum along a different history. Both H/Proca product preparations,
all masses, kappa=10^800 and all determinants/nonlocal responses remain.

For each finite real P>=10^64, propagate this same covariance to
|u|<=10^-60. Use the exact canonical chart maps and their two symmetric
boundary terms. In canonical balanced phase r=(sqrt(P)y,Pi/sqrt(P)),
the full scalar symmetrized covariance satisfies V_r<=10^21 I.
Each original tensor block satisfies V_rT<=10^5 I.
The weighted phase x=(P y,Pi)=sqrt(P)r has CCR P Omega, not Omega,
and corresponding covariance V_x=P V_r.

The executable input contract accepts exact admissible rational
representatives. The written energy proof covers the entire stated
real interval of times and every finite real P in the domain.

## Physical band statements

Restrict, without reselecting, the same original R3 two-point function
to10^64<=|k|<=2*10^64. The radial measure is P^2 dP/(2 pi^2).
At each fixed time in the slab and point, retain all four entries of
the scalar rows, the exact canonical density factors, both momentum
terms, the full reference symmetric shear and the nonzero Tcorr.

The resulting bounds are Var(v_hat)<10^-740, Var(n)<10^-490,
|Cov_sym(v_hat,n)|<10^-615 and the combined TT metric Frobenius
variance<10^-660. The single self-adjoint LINEAR band lapse n has
a centered Gaussian spectral law, with
Pr(|n|>=10^-230)<=2 exp(-10^29). Larger thresholds obey the same
ceiling. There is no assertion of a joint spectral law for
noncommuting v and n, or a uniform event over spacetime.

For the correct physical map a_physical=a_hat R^(-1/4),
the physical linear log-scale is v_hat+n/(2h), h=(1+u^2)^3.
The normalized volume linear jet is3v_hat+3n/(2h); its variance
is below10^-488. The expectation of the complete explicitly defined
second-order Weyl polynomial is below10^-489 in absolute value.

## What is not inferred

These are complete fixed-reference scalar/tensor covariance and finite
polynomial-jet statements, not the full sixteen-phase interacting
covariance or an interacting physical onepoint. H/Proca fields remain
in the parent and original state; they are not estimated away by this
local theorem. No quantum stress, state/endpoint Ward cancellation,
nonlinear gauge ordering, regulator or physical curved subtraction
is established.

An unbounded Gaussian is not supported inside the nonlinear auxiliary
branch. A pointwise Gaussian tail does not prove a spacetime supremum
or infinite-volume admissibility. The mathematical band is not a
Wilsonian cutoff. A finite Weyl polynomial is not the full nonlinear
volume operator on unbounded Gaussian data. Nonlinear inhomogeneous
stability and original V/G/B/P8 closure remain open.

The S261 physical-binding refutation, unchanged S263 classical family,
and completed scoped P8(a) qualifications are preserved. The written
matrix, interval, energy and Gaussian arguments are not FORMALIZED.
