# Actual scalar SEE past compatibility in affine units

Use the SAME actual one-real-scalar thermal state and low SEE branch
as A.20, with its own scalar occupation and anomaly coefficients,
at delta=10^-14, beta_S=Lambda=Tother=0, a0=1 and H0=-2/tau.
The parent's anomaly parameter is delta/360; it must not be confused
with this theorem's affine clock lambda. The parent is fully replayed
through A.22/A.21, not treated as a separately assigned stress tensor.

On the certified proper past interval [-tau/100,0], a>=1 and H<0,
with dimensionless proper Hubble derivative caps (21/10,9,70,800).
Therefore affine lambda=-tau/1000 is reached inside the shorter
proper interval [-tau/1000,0]. This proves existence of the whole
new affine past segment before using any bound on it. On that segment

    1<=A<=exp((21/10)/1000)<=10000/9979<2.

Since d/dlambda=a^-1 d/dt, the actual affine derivatives are

    a'=H,
    a''=Hdot/a,
    a'''=(Hddot-H Hdot)/a^2,
    a''''=(Hthird-Hdot^2-3H Hddot+2H^2 Hdot)/a^3.         (18)

Their dimensionless upper bounds are (21/10,9,889/10,70069/50),
strictly below (P). The actual initial field amplitude obeys

    zeta0^2=(20/9)delta(1-delta/90)<10^-12,

and a>=1 makes this an upper bound on the ENTIRE affine past.
The thermal state is spatially homogeneous, so its cap applies on
the full finite plane, not just the central normal. State homogeneity
is a property of this example, not a hypothesis of the theorem.
Its quantum and other-source budgets also lie inside (Q).

This establishes actual state/source/past compatibility. On the future
low branch a decreases from one and its proper endpoint is strictly
below tau/4 by the parent certificate. Hence its actual affine
endpoint integral a dt is also below tau/4, well before 2tau.
The field cap grows too large near that endpoint. Neither (F) nor
the conditional future state cap is proved by this example. Its
relative radiation and the branch dynamics already supply an
endpoint mechanism, so it is not advertised as a new QEI-only
thermal singularity proof or as fundamental maximal inextendibility.
