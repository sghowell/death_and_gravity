# Future-complete geometry satisfying all geometric hypotheses

The caps must not by themselves preclude a long positive-scale
extension. Define the ACTUAL smooth scale in affine units by

    A(x)=1-exp(-100x)Q(x),
    Q(x)=2x+204x^2+(31240/3)x^3+354720x^4,               (15)

for x>=-1/1000, with a small smooth extension further into the past.
It has A(0)=1,A'(0)=-2 and matches (1-6x)^(1/3) through fourth
derivative at zero. These are exactly the radiation affine jets,
not merely a fitted initial slope.

For x>=0 put y=100x and p(y)=100Q(y/100). Every coefficient of p
is nonnegative. The elementary inequality exp(-y)y^n<=n! gives

    S=sum_n p_n n!=65129/3125,
    247371/312500=1-S/100<=A(x)<=1.                      (16)

Differentiating exp(-y)p(y) j times and applying the same inequality
term by term gives, for j=1,...,4,

    |A^(j)(x)|<=2^j 100^(j-1)S
       =(130258/3125,1042064/125,8336512/5,333460480)_j.

Every component lies strictly below its corresponding future cap.
This bound holds on ALL x>=0, not a time grid. A tends to one, so
both affine duration and proper time t=tau integral dx/A diverge
to the future. The resulting global flat FLRW geometry is genuinely
future null complete, with its scale bounded above and below.

For x in [-1/1000,0], exp(-100x)<=exp(1/10)<=10/9. Form the exact
derivative polynomial (d/dx-100)^j Q before taking absolute values;
the cancellations at the anchor must not be discarded. Sum the
absolute coefficient bounds at |x|<=1/1000 and multiply by 10/9.
Native polynomial differentiation and independent Fraction binomial
convolution give

    |A-1|<=41526901/16875000000<1/10,
    |A'|<=416109941/168750000,
    |A''|<=17084681/1687500,
    |A'''|<=12479621/16875,
    |A''''|<=933975044/675.                               (17)

All lie strictly inside the past caps. Equations (16)-(17) and
the anchor identity prove that the geometric hypotheses permit
a future-complete extension; they do not force the theorem's
endpoint on their own.

There is also an actual conformal-vacuum Hadamard state on this
geometry, with relative w=0, satisfying every stated field cap.
This geometry/state pair is NOT claimed to satisfy the permitted
small-source SEE. The theorem excludes that additional conjunction.
Assigning an arbitrary remainder stress to force (1) would require
checking its null lower budget and is not an allowed-source witness.
