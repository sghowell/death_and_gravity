# P8 S6.276: the full-source lapse-chart boundary

Source, private/fresh science, native, ordinary, CLI and complete FULL
validation have passed. This is a scoped global-chart obstruction, not
original V/G/B/P8 closure.

## Outcome and precise limit

This packet rules out one proposed next step: extending the established
local lapse Hamiltonian as a globally C1 function on all of the same
finite canonical phase space. It does not refute S275's corrected finite
hybrid, prove P8 impossible, or prescribe a new quantum state.

The old sequential K-first elimination uses a pivot proportional to
Gamma=1-3(R-1)^2/(2R). The ENTIRE joint K,T stationary Hessian instead
has determinant-2/(3sqrtR), nonzero for allR>0. Eliminating T first and
then K reproduces the original complete Hamiltonian and every first/
second lapse envelope. The apparent Gamma obstruction is a coordinate
problem in that elimination order, not a full stationary singularity.

A separate full-source calculation produces a genuine lapse fold with
N=R=Gamma=1,T0. The actual fixed Proca and BOTH scalar reference profiles,
all three vacuum constants and the nonzero primitive contact are retained.
Only exact finite clock jets are used; no off-clock source is replaced
by a tree truncation. If rho,P denote those fixed profiles,

    s*=81/160-2P/3+7rho/12,
    m*^2=1/100+6P-4rho,
    D*=(11391+1400P-1600rho)/400.

The old profile bound gives0.49<s*<1,m*^2>0,D*>28.

The actual input path uses original axis modes atPwave=1e64. A conformal
scalar cosine, transverse traceless momentum cosine and a tiny M1
momentum cosine preserve the original means. The complete exponential
metric and cotangent lift satisfy all three spatial momentum generators
and residual translations exactly. No reconstructed harmonic is dropped.
At a designated point the invariants are shear=s,curvature=-12s,pm=m*,
and the exact constraint obeysC(N1,s)=0,C_N(N1,s)=6(s-s*).

The whole initial field is inside the established S266 invariant domain.
Continuity and local uniqueness connect that original branch to the fold.
A transverse tensor-momentum variation holding the metric fixed would
require0=C_N N_s+C_s=3 for a C1 extension, a contradiction. A failure
elsewhere along the spatial path would already defeat the proposed
global regular map. Disconnected remote roots are not excluded.

The full N,T Hessian at the fold isdiag(0,-1), unlike the removable
Gamma pole. The exact regular-branch envelopeH_ss=-C_s^2/C_N diverges.
The stationary-shear graph has a nondegenerate maximum and two nearby
positive roots on one side. The cubic normal form is only a leading
Taylor description, not the full action.

A finite primary/secondary block has determinant(detD)^2 and rank2 at
this fold, even with an arbitrary secondary bracket. This is NOT a
continuum functional determinant, a new gauge symmetry, proof that an
evolved solution reaches the fold, a clock-time turning point, or a
unitary quantum branch construction. Constraint preservation, measure,
state, domains and original UV/loop/Regge matching remain separate.

## Research and accepted-source evidence

The original-SymPy joint-atlas probe passed in92.95101062499452s.
A separate nonlinear-momentum diagnostic first failed a structural
comparison of equivalent rational polynomials; the exact residual fix
passed in80.75648600002751s. Its global positive TREE-only matter-momentum
branch is not transferred to the actual full source.

The literal full-profile fold and independently constructed Christoffel/
Ricci/momentum-generator probe passed in84.85816579102539s.
The connected-input-path probe replayed that proof in80.86668795894366s,
then passed all extra full raw-Hessian, branch-envelope and leading
normal-form checks. These are private research diagnostics, not complete
ancestral replay or quantum dynamics.

All four assembled science modules passed in90.92195474996697s.
A separate complete audit passed in88.88293299998622s:
97 named checks,158 scalar entries,48 proof gates,8 controls including72
actual rejected inputs,9 primitive rows and132 matching rows. All6 prior
physical qualifications remain. Scalar counts are not counts of
independent physical theorems.

Ruff first fixed2 import-order issues and reported2 unused variable
bindings; those were removed without changing mathematics. The final
read-only checks confirm all9 Python files formatted and passing Ruff.

The first private preflight failed its ASCII-source assertion after the
earlier source/AST/count/gate/Float-free assertions: two superscript2
characters occurred only in explanatory strings. They were replaced
with ASCII^2. No science execution occurred in that first run.

The second private preflight passed source/AST/hash checks in
92.85012120904867s, then reported1failed250passed in0.89s.
The zero-profile diagnostic accidentally used Python0/3 and introduced
floating-point test invariants. Inputs were sympified first, and explicit
Float-absence guards were added to the independently constructed H and
solved shear. No source equation or written proof changed.

The accepted original-SymPy private preflight passed in
90.38525154197123s, then ALL251 science tests passed in0.76s.
It checked17 ASCII source files,20 AST-derived report fields,7 sibling
module export contracts, all exact counts/gates, the immediate parent's
raw report and all19 parent source hashes. This is not the full ancestry.

All17 accepted files,70663 ASCII bytes, were captured and copied exactly,
with byte equality and every private/repository SHA256 matching the
accepted manifest. They were frozen at2026-09-15 03:47:06 UTC.
Neither the private originals nor repository science sources may now
be edited.

The fresh repository original-SymPy preflight passed in
90.02000704105012s, then ALL251 science tests passed in0.88s.
Every source hash equals the accepted private manifest. This was a
new process, not reuse of the private module caches.

The separate original-SymPy native helper protected5632 scientific inputs
and completed its full ancestral rebuild. Its113775-byte ASCII report
was captured in10 exact chunks with9 intermediate acknowledgments;
the final chunk began at108000 and contained5775 bytes. Raw SHA256:
d24260483929473a16611060d16ee19166c17f749d6c85cb2385463ddf3dc5ee.
Private and repository raw-byte validations passed, including all17 source
hashes,20 report fields,97/158/48 counts,8 controls/72 rejected inputs,
all9 primitive and131 previous matching rows, and the complete6 prior
physical qualifications. The raw report was accepted and frozen at
2026-09-15 03:54:28 UTC. The helper returned READY and now protects5633
inputs; no research probe or DONE was sent to it.

The independent original-SymPy ordinary replay actually collected all276
tests from both unchanged captured files and passed276 tests in2909.34s
(0:48:29), exit0. The complete captured output is untruncated; the final
verdict was consumed at2026-09-15 04:49:56 UTC.

The separate original-SymPy CLI replay returned exit0 with the complete
success line at2026-09-15 04:42:39 UTC: full-source joint atlas and connected
lapse-chart obstruction replay passed, with original P8 explicitly OPEN.

The complete captured P8 regression passed69825 tests in5430.08s
(1:30:30), exit0. Its actual805-file snapshot SHA256 is
a7ffca9433767b236d7f22b3ab1b642b1d068e775cdc34b060a63944ed11a60d.
Actual collection confirmed all805 files present and unchanged.
The runner used627 static namespace ancestors and5 frozen helper directories;
all128 original-versus-adapter tuple checks passed. Final counters were
68099 domain fallbacks,7388 exact descents and94 mixed fallbacks.
The complete output was captured without truncation. Its final verdict
was consumed at2026-09-15 05:38:45 UTC. This snapshot excludes S277 and
S278; it must not be relabelled as their validation.

## Publication and continuation

The full snapshot that validates S274/S275 does not include S276 and
must not be relabeled as its validation. Native/direct/ordinary/CLI use
original SymPy; only a captured complete regression may use the audited
exact-GCD adapter. Native bytes, full source/frontier equality, actual
successful verdicts, exact scoped staging and remote verification are
publication gates. Unrelated dirty P4/P9 are excluded.

The actual constraint-preservation and quantum-domain problem, or a
physically matched EFT restriction with controlled omitted contributions,
remains next. A formal path integral or arbitrary global extension cannot
be presented as its solution. No new numerical matching assumption is
requested from the user, and original P8 is not closed.
