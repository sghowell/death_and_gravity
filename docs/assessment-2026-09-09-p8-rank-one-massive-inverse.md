# P8: new rank-one massive scalar inverse

Original P8 remains open. The completed scoped objectives are unchanged,
and no user intervention is needed. This follows the
[prepared-response audit](assessment-2026-09-09-p8-prepared-ordinary-proca-response.md).

## New result

[S6.86](...md) derives the exact massive scalar range
factor for the new ordinary-Proca homogeneous reference response.
This is a flat physical reference, not an asserted healthy Minkowski
vacuum of the full scalar-tensor candidate.

The literal canonical current pairs and contacts give a rank-one
two-source block. The full massive scalar factor F=-H, including
its fixed finite local term, has Re(H)>=16/15 throughout its first
sheet. Consequently its inverse obeys |1/F|<=15/16 there. The
threshold, both cut banks and the large-circle limit are retained.
A pole suggested by retaining only the high-frequency logarithm is
excluded by the complete massive formula.

The scalar inverse has a positive cut density, with exact moments
1/4 and 9/(560 m^2), and no instantaneous or pole remainder.
Its retarded time kernel is an ordinary L1 function. The proof keeps
the conditionally convergent frequency integral and establishes
integrable small-time and large-time bounds before the Laplace limit.

This supplies a causal C0 scalar convolution inverse; it does not
invert the inactive source direction. No numerical full L1 norm or
generic one-derivative smoothing estimate is invented. Positivity
of this normalized scalar cut is not fundamental propagator
spectral positivity or a UV-completion test.

## Verification

The losslessly saved native report pins 16 source files and fully
rebuilds S6.85. It checks 50 named identities, 67 scalar entries,
21 proof gates and 78 rejected inputs. All 23 report fields have
mutation tests.

The native science suite passes 30 tests in 7.00 seconds. The
ordinary native suite passes 58 tests in 2161.71 seconds, and the
independent read-only CLI passes. Native scientific arithmetic is
unchanged; focused pytest uses the audited static namespace setup.

The complete P8 regression passes 6572 tests in 2481.11 seconds.
All 417 captured files are present and unchanged; path-list SHA-256:

    eb711285f614bfaa538a7218df285bc50b6f8f6ae56f9ff5c31d9a76395c15f5

The unchanged exact GCD adapter passes 128 original tuple comparisons
and records 30778 domain fallbacks, 7340 exact descents and four mixed
fallbacks. The snapshot predates S6.87's added tests.

Report SHA-256:

    0f04fd277040ad21525eb1d9102e6af45c4d695fac3f69d0d37710a7ab8a0db6

The analytic minimum-principle, contour and oscillatory-kernel arguments
are written proofs with exact algebraic anchors, not proof-assistant
formalization.

Ruff passes. The staged whitespace check reports only eleven extra blank
EOF lines in seven frozen Markdown files and four frozen Python files.
Their certified bytes are preserved; no other whitespace warnings occur.

## Continuing work

S6.87's coupled-inverse report generation is running from its frozen
sources; all 37 focused science tests pass. S6.88's nearby classical
solution draft now has an exact-jet enlargement to |u|<=10^-7,
and all 44 independent temporary science tests pass. It awaits the
parent/report pipeline. A general time-dependent scalar-principal
calculation is under development, not yet certified.

Quantum stability, spatial/state response, nonlinear/global control,
canonical profile interactions, omitted operators, mixed loops,
thresholds, finite Wilson matching and common-parent V/G/B remain open.
No universal exclusion, UV completion or original P8 closure is asserted.
