# P8 S6.320: relative-energy complex tubes and Cauchy estimates

Original V/G/B/P8 remain OPEN. Scoped P8(a) is unchanged.

## Result and strict scope

At every generic positive energy center w_i with total at most 1/8,
the unchanged complete finite-N tree is holomorphic on a neighborhood
of the closed polydisc |z_i-w_i|<=1e-12*w_i. The recoil map keeps
its original analytic branches and real Born normalization.

Throughout that tube,
|M_N(z)|/A0<=3*n^2*N!*(2e17)^N/
[kappa^(N/2)*product_i(w_i)].
The denominator energies are the positive real centers, not z_i.
For G_N=product_i(z_i)*sqrt(rho(z))*M_N(z)/A0, define
K_N=6*n^2*N!*(4e17)^N/kappa^(N/2). Multivariable Cauchy gives
|partial^alpha G_N|<=product_i(alpha_i!)*K_N/
product_i[(1e-12*w_i)^alpha_i].

Positive pair-invariant sums keep every pure-soft denominator
relatively separated from zero. The exact complex conserved
temporal inverse closes the real-center weighted current norm.
The Rosen/reflection grading, all-valence bounds and nonnegative
barriers remain valid for complex coefficients. The hard-core
ownership and recoil estimates have no multiplicity-dependent
loss in the relative perturbation of total momentum.

Exact complex current/inverse identities and a genuinely complex
original 5116-tree coefficient calibrate the proof. Baseline and
complete temporal coefficients agree. Gaussian-rational
normalization is exact; the original SymPy backend is unchanged.

A complete three-ray source has transverse residue 47089/2601
at an explicit signed-energy root pole. Adding a harder spectator
places the scaled pole within a naive global-total-energy disc.
That rules out this block-holomorphy shortcut, not a positive-real
physical divergence or every full hard-amplitude residue.

The valid radii shrink with each energy and do not reach its
zero-energy face. These Cauchy estimates do not yet furnish
integrable all-N subtractions, inclusive probabilities or a
convergent physical series. The original V/G/B/P8 gates remain
OPEN. Earlier private metadata and scope-word failures are
preserved in the validation history; no scientific assertion
was weakened before the successful cold/fresh passes.

## Immutable source and native evidence

Cold 10152 and separate fresh-root 82392 returned zero,
including all 557 science tests in each run. The fresh preflight
forced the complete immediate-parent packets. All 18 private,
fresh and repository source files matched byte for byte before freeze.

The 18 ASCII sources total 71,419 bytes, with 289 named
exact checks, 343 scalar entries, 108 proof gates, 8 controls and
73 rejected inputs. All 9 primitive records, 176 matching records
and 6 historical qualifications retain the explicitly scoped frontier.

One native specification protected 6,449 inputs. Its 209,276-byte
ASCII report arrived in 18 chunks with 17 complete-chunk ACKs.
SHA256:
18292735783403d7588808de71e7620238ffbbe2f70d82a5a1012ef431f03108.
Both raw/source/count/frontier validators and the 20-field REPORT
AST passed. No frozen predecessor source was rewritten.

## External acceptance

The original-SymPy sequential acceptance batch used two independent
processes: ordinary 43516 and CLI 20898, each traversing S316--S320.
For S320, all 582 ordinary tests passed in 779.58s;
the unchanged CLI verifier returned zero (wrapper 790.627186s).
Both complete batches returned zero: 3,199 ordinary tests in total
and all five CLI verifiers. Scientific ancestor caches were reused
within each process; this is not a claim of an isolated fresh-process
replay for every checkpoint. The separate cold and fresh-root
scientific runs above supply that independent reconstruction evidence.

All 95 frozen source/raw files were hash-checked before and after
each checkpoint, and the original SymPy GCD method was asserted
unchanged. The captured 95-file map SHA256 is
e956a6423ec077bdae62796165e969675f599f387c904b066d020d378486cdbb.
The ordinary and CLI checkpoint completions were recorded at
2026-09-17T07:20:13.536592+00:00 and
2026-09-17T07:25:41.021983+00:00, respectively. Both complete batch
successes were collected after the interruption on 2026-09-17.

The complete FULL 28981 passed 89,430 tests in 7857.95s (2:10:57),
with all 891 captured files unchanged. Test-file-list snapshot SHA256:
3a17c6e477ac83f7e38b52961a2ccba291831d0bfb3301eff4493b57918d5757.
It captured 713 namespace ancestors, 5 helpers and 128 contracts.
Counters: 68,757/7,388/94. The successful completion was collected at
2026-09-17 16:42 UTC after the interruption. This snapshot includes
S315 through S320.

Native/direct/ordinary/CLI use original SymPy; the exact-GCD
adapter is FULL-only. Publication requires the exact 22-file scope,
unchanged source/raw hashes and matching staged bytes, preserving
unrelated P4/P9 work. Original V/G/B/P8 remain OPEN.
