# P8: an actual null-line state-cap obstruction

Original P8 remains open; no user intervention is needed.
This follows the [nonminimal scalar cosmology audit](assessment-2026-09-09-p8-nonminimal-scalar-cosmology.md).

## New certified result

[A.21](../problems/P8/a/fields/nonminimal/null/FORMULATION.md)
constructs actual normalized one-mode squeezed Hadamard states of
a real massless scalar, with the full physical nonminimal stress
for every coupling 0<=xi<=1/4.

On the entire fixed null sampling segment, the relative Wick square
is negative while the null stress is uniformly negative with an
amplitude growing quadratically in a transverse momentum scale.
Every fixed nonzero compact null average therefore tends to minus
infinity, even though every state satisfies the same one-sided
nonnegative Wick-square upper cap on that segment.

The states have genuine normalized smooth compact mass-shell modes,
positive pure oscillator covariances and finite energy at each finite
scale. The family does not have a common total-energy, momentum or
two-sided Wick-square bound. Its complete-line ANEC is strictly
positive at each finite scale. No limiting state or interchange
of divergent limits is asserted.

This rules out the direct null-line version of the timelike
one-sided-state-cap strategy. It is not a null SEE counterexample,
a negative-ANEC construction, a general null no-go, or a claim that
smearing over both null directions fails.

## Verification

The losslessly saved report pins 14 sources and fully rebuilds A.20
and its ancestry. There are 29 named identities and scalar entries,
17 proof gates, 21 rejected inputs and mutation controls for all
20 report fields.

Native science passes 66 tests in .41 seconds.
Independent ordinary native replay passes 110 tests in 9.19
seconds; the separate read-only CLI also passes.
Scientific SymPy is unchanged.

The complete regression passes 8355 tests in 2561.41 seconds,
with all 451 captured files present and unchanged. Path-list SHA-256:

    e48005a65ed57bb7b6360895aa6861e55baa28f2a53239a63c9ea4381274cdd4

The unchanged exact GCD adapter passes 128 original tuple comparisons,
with 32010 domain fallbacks, 7340 exact descents and 4 mixed fallbacks.
This snapshot predates A.22's added tests.

Report SHA-256:

    ada5315159f41bf96c455987f4779dffae2fa6bfef3fca0921e60761fccc4e88

The finite-energy/Fock, Hadamard and continuous-profile arguments
are written and source-pinned, not proof-assistant formalized or
independently peer reviewed. Exact 18-file staging verifies every
staged source and report hash plus the audit/ledger/pointer updates.

## Continuing work

A.22's physical double-null inequality has 17 frozen sources,
95 native science tests, 139 ordinary tests and a separate CLI
passing. Its complete 453-file regression is running. It retains
the finite second width, nonminimal improvement, physical affine
normalization and full scalar reference.

A.23 is private work on a conditional FLRW null incompleteness
theorem using that finite-plane QEI and the actual SEE. Its 75 native
science tests pass, including an exact index margin greater than
one half, a complete geometry satisfying all geometric caps, and
actual scalar thermal past compatibility. It does not assume a
homogeneous quantum state. Its written proof, frozen report and
full replays are not yet complete.

These named results do not automatically assign original P8(a)
closure. P8(b)'s full physical stress, backreaction, interactions,
omitted-operator control and actual V/G/B UV matching remain open.
