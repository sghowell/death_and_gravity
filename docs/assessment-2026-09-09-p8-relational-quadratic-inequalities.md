# P8: actual relational quadratic inequalities

Original P8 remains open; no user intervention is needed.
This follows the [global Hadamard-state audit](assessment-2026-09-09-p8-global-two-cone-hadamard-state.md).
The previously completed scoped objectives are unchanged.

## New certified result

[S6.100](...md)
derives finite reference-state difference bounds for real differential
squares of the actual global reduced relational scalar. Every compact
sample on a worldline slower than the clock mode is covered; the
all-time physical-speed class |v|<=.99 is explicit.

The actual normalized clock residue is nonzero. Consequently, the
relational commutator cannot be replaced by that of a free field
with only the physical-matter null cone. A named timelike
interluminal control fails the sufficient pullback criterion;
that failure is not declared a general nonexistence theorem.

At the comoving bounce, the leading short-sampling test-energy
coefficient is hbar/(16pi^2 kappa) times

    1+10863 sqrt(17985)/1723683599,

strictly between 1.00084 and 1.00085 times the unmixed coefficient.
This is a leading asymptotic coefficient, not an optimal inequality
or a finite-width error estimate.

The fixed-background test stress has an explicit nonzero coupled
clock source in its divergence. It is not the full covariant
renormalized metric/clock stress and cannot yet be inserted into
a semiclassical source equation as that full stress.

## Verification

The losslessly saved report pins 18 sources and fully rebuilds S6.99.
It checks 31 named identities, 56 scalar entries, 17 proof gates
and 66 rejected inputs. All 20 report fields have mutation tests.

Native science passes 67 tests in 6.60 seconds.
Independent ordinary native replay passes 92 tests in 1845.33
seconds. The independent read-only CLI also passes.
Scientific SymPy is unchanged.

The complete regression passes 7975 tests in 2546.03 seconds,
with all 445 captured files present and unchanged. Path-list SHA-256:

    794c4a642d5e93074b7bed62da64fc94a699a0f7b444e0939852ff6627d1d77d

The unchanged exact GCD adapter passes 128 original tuple comparisons,
with 31640 domain fallbacks, 7340 exact descents and 4 mixed fallbacks.
This snapshot predates the added S6.101 tests.

Report SHA-256:

    e9ae3ec1449071bddb04cb3106fc1f82194d8b885f6357a39405849d5a5df5d0

The positive-type, wavefront, distributional and short-sampling
arguments are written and source-pinned, not proof-assistant
formalized. Exact staging verifies the 18-source manifest, report
and three audit/ledger files, including every staged source/report hash.

## Continuing work

S6.101 has a losslessly saved report and all 20 source hashes
verified. Its 70 native science tests pass; separate ordinary,
CLI and complete 447-file snapshot replays are now running.
It gives a concrete nonoptimal finite-width majorant with a
quantitatively prepared all-order state, zero-momentum coverage,
whole-interval Riccati errors and complete mode-error control.

A separate private P8(a) nonminimal conformal-scalar branch has
57 exact identities passing, including 72 scalar entries, and
an independent Fraction cost reconstruction. Its actual scalar
thermal SEE example fits the required past history and the
small field-strength budget. The future field cap is an explicit
conditional hypothesis, and it fails near that example's earlier
high-curvature endpoint. This work is not yet published or certified.

Sharp cosmological calibration of the coupled test observable,
the full renormalized physical stress and Ward identities,
self-consistent backreaction, tensor/vector state completion,
interactions/omitted operators/loops/thresholds and actual
vacuum/dispersion/common-parent V/G/B matching remain open.
The separate scalar theorem does not close all realistic/null/
nonminimal P8(a), and original UV classification P8(b) remains open.
