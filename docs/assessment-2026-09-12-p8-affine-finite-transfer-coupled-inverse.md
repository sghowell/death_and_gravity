# P8 continuation: actual coupled inverse on smooth Fourier balls

S6.229 proves an actual prepared linear inverse for the full scalar, clock and matter system on each fixed external-momentum ball. It includes the unchanged conditional Gaussian response, full retuned classical action, original contacts and force-dependent constraints. It is not only a reference-factor inverse. Original P8 remains OPEN.

## Complete mixed coordinates and constraints

The prepared coordinates are
(n,zeta,b,sigma)=(eta',w+H eta,c'+q eta,r+ell eta),
with q=P²/a² and b the shift divergence. The full original weighted Euler rows have orders(4,4,4,2) and highest matrix diag(-6delta²,0,0,-1). The matter invariant is essential: omitting ell eta leaves an uncancelled eta'' term. The eta pivot stays invertible through the bounce, with reciprocal bound15625/6144; no division by H, Theta or E is made.

Both ordered Ward legs reduce the nonlocal response to the synchronous pair(w,c), retaining the distinct nonlinear clock contact
3a³ pressure(4delta²-3delta)etaD' etaG'.
All fourteen local monomials have at most one derivative on either leg. Full cancellation removes inverse external momentum, not the original infinite internal loop.

The weighted residual adjoint retains the boundary
a³[(En-delta Ev)eta+Eb c].
The original zero germ, rather than final data, recovers the lapse and shift equations from the adapted equations. An explicit unprepared residual with Eb=a^-3 shows why preparation cannot be dropped.

## Actual strong normal form

Full-dimensional contractions give
C(d)=(2d²+d-8)/[2d(d+2)] and
B(d)=(d³-4d²-3d+16)/[4d(d+2)].
Only after this reconstruction are the physical contractions fixed. Their leading matrix is
[[4,-4/3],[-4/3,8/5]], with determinant208/45 and diagonal form diag(4,52/45). The fixed-physical dimension jet differs from the derivative of a dimension-varying invariant basis. Original radial, Abel and finite fourth-contact coefficients are all retained.

At each fixed external ball, the complete massive mode comparison retains the order-zero residual phase exp(-i n.P Delta_sigma), the unchanged all-order prepared state, original dimensional regulator, finite local terms and full retuning. The proper-variable change works in the full nearby dimension, not just at d3. A sufficiently deep finite comparison for each derivative order bounds the actual remainder; it does not reset the state.

With I=diag(I4,I4,I4,I2), the ACTUAL adapted operator satisfies

I Tadapt=Fref+V,

where Fref contains the nonzero classical eta and matter pivots and the original trace/shear factors. Every fixed simultaneous derivative of the complete remainder has a weak-log row-sum majorant Cj(Pmax). The constants are finite but unevaluated and retain the kappa-dependent terms. Variable-coefficient primitives keep both the upper multiplication and every integration-by-parts coefficient derivative.

## Ordered inverse and exact feedback recovery

Let Kmax be the larger original finite-window trace/shear inverse norm, with the shear factor3/8 retained, and let

b0=max(15625/6144,16Kmax/(9gamma),1).

The weight lambda=(4b0 C0+1)² gives a strict norm below1/2 for B0 V. The ordered inverse is

(I+B0 V)^-1 B0 I,

and its unweighted adapted-source bound is2 exp(lambda)b0 times the row-primitive source norm. Finite weighted existence is not numerical smallness. Prepared time shifts prove smoothness while retaining both kernel derivatives and derivatives of the variable eta pivot.

The complete source map includes (-W en+H ezeta+q eb,ezeta,-W eb,0). Recovery retains output multiplication by1/(kappa a³), both contacts and the rank-two direct auxiliary term. It gives both actual S222 feedback identities, including a general mathematical phase drive, on smooth prepared bounded-Fourier histories.

The Sobolev norm here is for scalar amplitudes, including b, not the inverse-gradient shift vector. In three dimensions its1/P reconstruction is locally integrable as a distribution; it need not be in the same H^r space. Literal P0 remains a separate homogeneous sector. Taking a union of external balls does not prove an inverse on the unrestricted original completed X/Y graph.

## Independent checks and completed validation

Independent tests check the complete mixed classical top terms, matter invariant, both weighted adjoints, the unprepared counterexample, the fixed-physical dimension jet, residual phase and all variable-coefficient primitives. A noncommuting8x8 model checks both inverse products and density order. A second finite system uses the actual S222 block formulas and nonzero phase driving; it detects deleting direct auxiliary terms or reversing feedback order. These are algebraic diagnostics, not continuum convergence proofs.

Formatting and lint passed before freezing. Private science passed280 tests in10.14 seconds; final private science passed280 in10.52 seconds and repository science passed280 in10.43 seconds. Preflight checked17 inputs and20 fields in9.25 seconds. The inverse-gradient shift qualification was added before freezing; no frozen scientific bytes were subsequently edited.

Fresh original-SymPy ordinary replay passed305 tests in2480.98 seconds. Standalone CLI replay passed. Full P8 regression passed46910 tests in4491.87 seconds, exit code0. The711-file snapshot SHA is

`cd019223e86ee1e33baf78e70601c54176f5d8d8148097f8f9377de7385766b4`.

Only full regression used the audited exact-GCD adapter, with the frozen S219 helper-directory allowance. All128 original tuple comparisons passed; final counters were36145 domain fallbacks,7340 exact descents and94 mixed fallbacks. Native, direct, ordinary and CLI retained original SymPy.

The43785-character native report was transferred in four checked chunks. Its20 fields contain40 named identities,164 scalar entries,30 proof gates,nine controls and191 rejected inputs, retaining85 matching rows and nine original primitive rows. Report SHA:

`076648eee2112f5b86302a072e11265350e155a92877f5b68b5007ab0cf09432`.

All17 source hashes and the report hash are checked before exact21-file publication. The separately frozen S230 successor and unrelated P4/P9 changes are excluded. Analytic matching, Volterra regularity and constraint recovery are written proofs, not FORMALIZED.

## Remaining frontier

The full flat-vacuum tensor successor has completed native and direct checks and is undergoing fresh replay. A separate scalar investigation quantifies conditional near-tree matching, without assuming a physical cutoff. Uniform all-momentum curved estimates, the unrestricted completed graph, quantitative physical stability, nonlinear/finite-coupling common-parent control and original V/G/B remain open. No user intervention is currently required.
