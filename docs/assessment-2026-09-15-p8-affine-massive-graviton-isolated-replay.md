# P8 S6.280: isolated complete massive graviton-cut replay

All independent validation gates passed. Original V/G/B/P8 remain OPEN.

## Outcome and preserved rejected evidence

This successor retains the full original-source leading two-graviton
production calculation, exact complete nonforward angular sewing,
all-even-spin representation and uniform physical-window bound.
It corrects a reproducibility defect without changing frozen old sources.

S6.279 is REJECTED, not certified. Its cold and fresh runs contained
130 named checks and391 scalar entries, but its full-parent-first native
report contained139 and409. S278's cached full_vacuum dictionary is mutable:
its data builder adds nine checks. S279 copied every entry and therefore
inherited a call-order-dependent report/test contract. All displayed
scientific residuals vanished, but the validation counts did not agree.

The exact18 frozen S279 source files (70620ASCII bytes) and132764-byte
native raw report are preserved in research/p8-rejected-packets/s6_279.
The report SHA256 is
a45d6d297e8ecea8f6049983b69c8914125ef2fde89cea537fe578d34064d256.
The rejected directory was never committed or included in an accepted
complete-regression snapshot. Its exact-byte recoverable move out of
the accepted P8 test tree and subsequent hash verification are recorded.
Private originals remain unchanged. Its old native helper was retired
because it protects the former paths. A new untouched native helper
successfully rebuilt the accepted S278 ancestry from scratch, exactly
reproducing its raw report before accepting S280. No helper was patched.

S280 selects the original ten vacuum-check labels explicitly, returning
a new dictionary. Tests verify independence, no alias and whole packet
equality after parent warmup. S278 remains the scientific parent.
S279's failure evidence is hash-pinned without calling its failed verifier.
No corrected raw report or substituted counts are fabricated.

## Scientific scope

The original full R/F functions, all three vacuum constants and explicit
formal loop bookkeeping remain. Literal Einstein-Hilbert Gamma-Gamma
cubic, matter contact and both scalar exchanges give all four physical
TT helicity amplitudes. Two separate Ward checks and a covariant
factorized amplitude agree; a rank-two physical projector excludes
unwanted scalar polarization sums.

Writing mu=m^2, e^2=s/4, p^2=(s-4mu)/4 and D=e^2-p^2 x^2:
A_same=mu^2/(kappa D), A_opposite=p^4(1-x^2)^2/(kappa D).
Full nonforward sewing retains the helicity phase and is not a product
of absolute-valued forward amplitudes. The identical-particle1/2! and
optical1/2 are included.

The exact forward cut is s^2 F(beta)/(256pi kappa^2), with
F=4-31beta^2/3+118beta^4/15-beta^6
 +(beta^7-6beta^3+8beta-3/beta)atanh(beta).
F(0)=1 andF(1)=8/15. The whole angular integral, all-even-spin
Legendre-Q/associated-spin expansion and convergence proof are retained,
not inferred from finitely many checked spins.

A uniform physical-window bound follows from the amplitude inequality
and Cauchy-Schwarz. At the unchanged mass1 and formal upper boundary
10^196 the positive forward moment is below591/(128kappa^2),
less than10^-990 of4lambda. Lambda is only a comparison reference;
the canceled bare quartic-derivative operator is not reinserted.
This is not b20, a full low-cut subtraction, or a physical EFT cutoff.
Unphysical continuation, remaining loops, local matching, infrared/Regge
data, state preparation and global constraint-domain requirements remain.

## Accepted source validation

Before freeze, formatting changed4 Python files and left5 unchanged;
Ruff passed. The exact original-SymPy cold private preflight passed in
88.89137025002856s, then328 science tests passed in1.08s. A separate
forced-parent-warm replay passed in88.75973041693214s, then the same
328 tests passed in0.94s.

Both actual preflights contained18 ASCII sources,20 AST-derived report
fields,7 export modules,130 named checks,391 scalar entries,39 proof
gates,8 controls with70 actual rejections,9 primitive and136 matching rows.
All six historical physical qualifications and134 prior accepted matching
rows remain; H8A443 records the rejected packet, H8A444 the corrected scope.

Exact formatted bodies and SHA256 values were captured. The repository
copy matched all18 sources and all76771 bytes. Sources were frozen at
2026-09-15 06:48:30 UTC and must never be edited in place.

The fresh repository forced-parent-warm original-SymPy preflight passed
in85.90976554201916s, then328 science tests passed in1.06s, exit0.
All18 hashes equal both accepted private manifests.

## Accepted fresh native rebuild

A new untouched original-SymPy process rebuilt S278's complete ancestry.
Its84449-byte report exactly reproduced the accepted S278 raw bytes and
SHA256, not only selected fields. The baseline protected5671 inputs.
After READY the S280 specification was sent once,18 sources and20 fields;
its before-batch snapshot protected5689 unchanged scientific inputs.

S280 returned exactly130 named checks and391 scalar entries, matching
both cold and parent-warm preflights. Its133169-byte native ASCII report
was captured without reserialization in12 chunks with11 acknowledgments;
the last chunk starts at132000 and contains1169 bytes. Raw SHA256:
db850b820bd006cc1400e9e72667ff64ec5d5ea45f1de86c4f1f9683428b9b05.
Independent private-raw and repository-raw validators passed all18 source
byte/hash comparisons,76771 source bytes,20 AST-derived fields,130/391/39
counts,8 controls/70 rejections,9 primitive and136 matching rows, the
explicit S279 rejection and all6 unchanged physical qualifications.
The raw report is frozen; the helper returned READY and was not sent
research probes or DONE. There are5690 protected inputs including it.


## Completed independent replays and full regression

The independent ordinary and CLI lanes used original SymPy, with the
original polynomial GCD implementation unchanged. Neither used the FULL
adapter. Both captured ordinary test files were present and unchanged.
S280's ordinary replay passed353 tests in2921.04s (0:48:41), exit0.
Its separate CLI returned the original scoped-cut success line, exit0.

The combined S280/S281 FULL replay completed with exit0 on2026-09-15.
It actually collected and passed71,032 tests in5560.98s (1:32:40), covering
all813 captured test files, not a projected count. Snapshot SHA256:
4a2f5c58d12446cd99b0bace6434233dc591137ebd9b02023c22163f97fc835b.

That snapshot used635 audited static namespace ancestors and5 helper
directories. The FULL-only exact-GCD adapter passed128 contract checks.
Final counters were68110 domain fallbacks,7388 exact descents and94 mixed
fallbacks. The rejected S279 archive was outside the accepted test tree.
This completed snapshot covers S280 andS281, not later S282/S283 work.

Private/fresh sources, independent native raw bytes, ordinary tests, CLI
and the complete regression all passed their separate gates. This certifies
only the stated exact algebra and written proofs, not a Lean formalization,
an exact quantum vacuum, all-loop positivity or original V/G/B/P8 closure.
All frozen source and native-report bytes remain unchanged.
