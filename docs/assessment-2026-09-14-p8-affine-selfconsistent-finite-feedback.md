# P8 S6.273 external assessment -- 2026-09-14

## Physical qualification: archived model, not the original prepared state

The independently source-pinned [S275 canonical-boundary correction](assessment-2026-09-15-p8-affine-canonical-boundary-corrected-hybrid.md)
shows that the S269 raw scalar momentum dictionary omitted the EARLIER
S220 action boundary. At the bounce the missing shifts are
(3sigma/10,3v/10). The resulting configuration-dependent unitary changes
the full positive Gaussian covariance; it is not a global scalar phase.

The same-original-PHYSICAL-state identification is REFUTED. The frozen
source, report and explicit abstract finite model are retained without
rewriting history. Every result below refers to that archived model's
defined coordinates, seed and readout, not a renewed identification with
the original prepared physical state. Replay C0 does not repair this
physical bridge or refute every abstract finite-model bound.

S275 gives NEW correctly transported finite solutions and separately
revalidates their bounds. They are not the archived trajectories under
different names; nor do they retroactively validate S272's longer
physical interval. Original V/G/B/P8 stay OPEN.

## Scope and result

This is a separately named finite classical-homogeneous/quantum-nonzero-mode
hybrid model, not a completion of the original quantum problem. The original
source functions, fixed profiles,48 nonzero-mode pairs, pure state, finite
shell and torus, R=1e20, and both c=1,2 phase cutoffs are unchanged.
Both complete calibrated-coherent and Weyl orderings are covered.

The live homogeneous coordinates are alpha,p,pm,eta=1e100 h,ph, with cyclic
M1 reconstructed by its full quadrature. All three homogeneous spatial-vector
and FIVE traceless-shape canonical pairs satisfy their zero equations by the
actual radial seed's cubic symmetry. The heavy scalar remains live; the
temporal normal vector is reconstructed from the full original source.

For |u|<=1e-180, the complete coupled path map is a strict contraction
in a weighted MAXIMUM metric, with both row bounds<1e-50. The unique
solution in the declared domain differs from the reference homogeneous
path by<1e-390. The physical-volume relative error is<1e-380; both endpoints
exceed its central value by>5e-360, and all minima lie in |u|<1e-188.
No unique or strict positive-acceleration minimum is asserted.

This resolves external prescription of the background for this particular
finite HYBRID continuation only. It does not quantize homogeneous variables,
control coherent-core support, remove a regulator, or establish original
physical matching, omitted loops, Regge/UV remainders or global completion.
Original V/G/B/P8 remain OPEN. Scoped P8(a), S261's physical refutation
and S265's individual-coefficient integrability boundary are unchanged.

## What the proof adds

The full source is bounded on a wider complex lapse ball1e-115 using63
componentwise RFj jets,27 mixed total-order-six entries excluding time6,
and all500 original derivative expressions. Pure-time primitive contacts
are bounded by the complete NEW lapse integral. No extra real-time
regularity is attributed to the fixed C5 profiles.

The absolute homogeneous canonical chain retains every dilution factor,
the heavy1e100 conversion and the full scalar-center force. Exact
zero-mean input one-form splitting is distinguished from generated
nonlinear means. The complete nondiagonal trace generator is
Pi_v grad(v)-grad(Pi_v)/3; its fluctuation source is not deleted.
All twelve invariant images and average remainders retain the full
constraint response, shape/cotangent inverse, Gauss contact and curvature.
The full lapse self-map residual includes phase as well as homogeneous
deviations.

The original full scalar Gaussian preparation, both tensor polarizations,
heavy SLE and original Proca T/T/L prescription are bound to the24 proper
cubic actions. All576 group compositions and full96-phase covariance
identities are checked. Translation invariance alone is not substituted
for this stronger actual-state argument.

All196 phase derivative orders and first/second homogeneous Cauchy
derivatives enter complete operator bounds. The coupled-state comparison
uses unitary Duhamel, not exponential growth from a generator norm.
The scalar phase is factored only for that metric and is restored in
the physical state; its classical force remains throughout.
The volume estimate keeps the scale/operator cross product.

## Source and private validation

The21 ASCII source files total124112 bytes and were exactly copied from
the successful private preflight, with byte-for-byte equality and SHA256
comparison against its captured manifest. Source freeze:
2026-09-14 23:51:03 UTC. Frozen sources and later native certificate are
never edited in place.

Ruff formatting completed on the11 Python files; Ruff check and all9
sibling-module export contracts passed. The complete unfrozen six-module
probe passed with54 named exact checks,1331 scalar entries,66 gates,
11 controls including396 rejected inputs,9 unchanged primitive rows
and129 matching rows.

The original-SymPy private source/field/hash preflight passed in
100.55639512499329s, followed by1279 science tests in4.04s.
It checks the whole immediate predecessor's source hashes, all21 current
sources, and the20-field AST contract. It is not a complete ancestral
rebuild.

The fresh repository copy passed the same original-SymPy preflight in
100.28697504196316s, then all1279 science tests in4.15s. Its complete
source hash map equals the private preflight exactly. This is a new
process, not reuse of the private module caches.

Native certification completed with original SymPy and5574 protected P8
inputs unchanged. Its41 raw chunks and40 explicit nonfinal acknowledgments
were captured without reserialization or newline conversion. The484513-byte
ASCII report has SHA256
1ef7c3a7165db3688c4141313ed8fb7ee5a2c43ee0613ddd787144a8b201cdce.
Private and repository raw-byte SHA,21-source/20-field AST contract,
54 named/1331 scalar/66 gate counts,11 controls/396 rejected inputs,
and full previous primitive/matching frontier equality all passed at
2026-09-14 23:56:20 UTC. The report is now frozen.
Native certification rebuilds the unchanged report chain; it does not
replace the separate ordinary, CLI or full regression verdicts below.

The independent ordinary replay collected the unchanged two test files and
passed all1304 tests in3041.22s (0:50:41), exit0. The complete collection,
progress and final verdict were captured without truncation. This process
used original SymPy throughout.

The separate original-SymPy CLI replay completed with exit0 and printed
P8 S6.273 complete finite self-consistent HYBRID feedback replay passed;
original P8 remains OPEN. Both standalone verdicts have been captured; the separate full regression
verdict is recorded below.

The separate FULL regression passed all66953 tests in5376.05s (1:29:36),
exit0, with its complete final verdict captured at2026-09-15 02:02:14 UTC.
The original snapshot contains all799 default-pattern test files with SHA256
f19e3beaa7f667592fdcc810bfd703255c8a9d41dc186e47b3a7322dcf0eddd8,
621 static namespace ancestors,5 guarded sibling helpers and128 successful
original-versus-adapter contract checks. Actual collection confirmed66953
tests and every captured file present and unchanged. Adapter counters:
68106 domain fallbacks,7388 exact descents,94 mixed fallbacks.
One intermediate progress-output chunk was truncated; the collection,
manifest and complete final verdict were captured. This is not described
as an untruncated full raw log. The snapshot stops atS273 and does not
validate S274 or the later canonical-boundary correction. A successful
replay does not restore the refuted physical identification above.

## Unfrozen research and diagnostics

Earlier exploratory failures are retained as development history:
a matrix-valued substitution in the cubic fixture was replaced by an
explicit entry map; a generic canonical-force fixture had conflated
partial-alpha differentiation with the total density chain and was
corrected using an independent alpha argument; the first assembled
quantum packet had a NameError from h/g naming and was corrected.
The entire six-module replay subsequently passed. The first listing
of the componentwise source probe was truncated only in diagnostic
output and was repeated with complete capture.

Written proof review caught a SUM/MAXIMUM metric mismatch before freeze.
The computed row-sum bounds require the weighted maximum metric; that
is now the stated and independently tested metric. The source self-map
was also strengthened to explicitly include its nonzero phase image,
and the volume error keeps the scale/operator cross term.
Ruff flagged three eagerly evaluated lambda closures; their loop values
were explicitly bound before the accepted preflight. No frozen source
or historical certificate was modified by any of these corrections.

The independent science suite includes actual signed-wave reconstruction,
nontrivial whitening fixtures, original radial scalar forms, mixed
canonical energy derivatives, nonlinear implicit/density spatial means,
noncommuting unitary Duhamel fixtures, the MAX-versus-SUM counterexample,
scalar-phase-force retention and every unsupported-input rejection.
Numerical fixtures are diagnostics, not substitutes for written analytic
bounds. This packet does not claim formal proof verification.
