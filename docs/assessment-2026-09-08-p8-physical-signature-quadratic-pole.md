# P8: physical-signature quadratic mass-insertion pole

Original P8 remains open. This follows the
[dimensional-continuation audit](assessment-2026-09-08-p8-quadratic-dimensional-continuation.md).
The original physical clock, selected vector state and fixed scalar
profiles remain unchanged.

## New result

[S6.63](../problems/P8/s6/matching/affine/kinetic/aligned/nonlinear/elimination/quantum/quadratic/continuation/lorentzian/FORMULATION.md)
derives the physical-signature local quadratic pole and its specified
evanescent bare-counterterm variation. Independent Lorentzian frame
and negative-lapse coordinate calculations agree with local formal-jet
continuation in arbitrary symbolic spatial dimension.

The physical curvature invariants and all three flat Feynman sectors
are independently recovered. The actual time-dependent mass profiles
are inserted after local signature continuation, with all their time
derivatives and the differentiated dimensional measure retained.
No global complex-time history or Euclidean state is introduced.

The compact action and normalized Euler operators obey the exact
time/spatial derivative grading identity through the first dimensional
jet. The bare-action counterterm has the opposite sign to the loop
pole. Its finite evanescent part is distinguished from the same-sign
subtraction functional used at earlier checkpoints.

A direct substitution u -> i*u in the selected scale factor would
instead change (1+u^2)^2 into (1-u^2)^2. The nonzero history control
explicitly excludes that operation.

## Remaining work

The constrained selected-state bare retarded mass-response checkpoint
is now in its separate replay and regression. A further homogeneous
adiabatic finite-component calculation is in progress. Neither is
imported into this frozen claim.

The combined finite bare/contact/local response, compatible varied-state
data and integrated feedback bounds remain open, together with full
metric response, coupled quantum stability/cones, interactions, cutoff
and V/G/B. There is no user-intervention blocker.

## Verification

The report pins 15 sources and fully rebuilds S6.62. It verifies
54 named identities containing 144 scalar entries, 11 algebraic audit
checks and 204 rejected inputs. The focused science suite passes
15 tests in 58.54 seconds.

A terminal interruption invalidated unfinished validation sessions;
their results were not assumed. The fresh ordinary suite passes
45 tests in 601.76 seconds without the broad GCD adapter, and the
independent read-only CLI passes. The full regression through S6.63
passes 5338 tests in 1186.49 seconds, with no frozen checkpoint excluded.
The adapter passes 128 original tuple comparisons and records 6832
domain fallbacks and 6509 exact descents. This run collected before
the S6.64 tests were added.

Report SHA-256:
13944f63c00a27e4ef8d7d4f7d9e596fbdc8906e3b3014e42281e3a75a35da10.
The evidence is exact symbolic/rational computation and written
local-signature arguments, not proof-assistant formalization.
